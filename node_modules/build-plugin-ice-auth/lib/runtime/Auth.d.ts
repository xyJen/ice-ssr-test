/// <reference types="rax" />
import * as React from 'react';
import { FC } from 'react';
import { ContextType, AuthType, IAuth } from './types';
interface ProviderProps {
    value: AuthType;
}
interface InjectProps {
    auth: ContextType[0];
    setAuth: ContextType[1];
}
declare const Provider: FC<ProviderProps>;
declare const useAuth: () => ContextType;
declare function withAuth<Props extends InjectProps>(Component: React.ComponentType<Props>): (props: Omit<Props, keyof InjectProps>) => JSX.Element;
export { useAuth, withAuth, Provider };
export type { IAuth };
