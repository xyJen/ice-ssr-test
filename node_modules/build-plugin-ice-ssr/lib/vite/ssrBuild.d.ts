import type { InlineConfig } from 'vite';
interface Options {
    ssrEntry: string;
    ssgEntry: string;
    ssr: string;
}
declare const ssrBuild: (prodConfig: InlineConfig, buildOptions: Options) => Promise<void>;
export default ssrBuild;
