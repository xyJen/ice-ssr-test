declare type CheckMap = Record<string, boolean>;
declare type WebpackAlias = Record<string, string>;
declare type ViteAlias = {
    find: string | RegExp;
    replacement: string;
}[];
interface Options {
    rootDir: string;
    parallel?: number;
    analyzeRelativeImport?: boolean;
    mode?: 'webpack' | 'vite';
    alias?: WebpackAlias | ViteAlias;
    customRuntimeRules?: Record<string, string[]>;
}
export declare function globSourceFiles(sourceDir: string): Promise<string[]>;
export declare function getImportPath(importSpecifier: string, importer: string, options: Pick<Options, 'alias' | 'rootDir' | 'mode'>): string;
export default function analyzeRuntime(files: string[], options: Options): Promise<CheckMap>;
export {};
