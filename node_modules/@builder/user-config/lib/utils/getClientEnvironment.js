"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
var ICE_PREFIX = /^ICE_/i;
function getClientEnvironment(customEnv) {
    // Grab NODE_ENV and ICE_* environment variables and prepare them to be
    // injected into the application via DefinePlugin in webpack configuration.
    var raw = Object.keys(__assign(__assign({}, process.env), customEnv)).filter(function (key) { return ICE_PREFIX.test(key); })
        .reduce(function (env, key) {
        env[key] = process.env[key] || customEnv[key];
        return env;
    }, {});
    var stringified = Object.keys(raw).reduce(function (env, key) {
        var stringifiedKey = key.startsWith('process.env.') ? key : "process.env.".concat(key);
        env[stringifiedKey] = JSON.stringify(raw[key]);
        return env;
    }, {});
    return { raw: raw, stringified: stringified };
}
exports.default = getClientEnvironment;
//# sourceMappingURL=getClientEnvironment.js.map