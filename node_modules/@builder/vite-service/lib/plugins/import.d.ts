import type { Plugin } from 'vite';
interface SourceData {
    filename: string;
    value: string;
    type?: 'normal' | 'default';
}
export interface ImportDeclarations {
    name: string;
    value: string;
    type?: 'normal' | 'default';
    alias?: string;
    multipleSource?: SourceData[];
}
interface Option {
    source: string;
    redirectImports: ImportDeclarations[];
}
export declare const importPlugin: ({ source, redirectImports }: Option) => Plugin;
export {};
