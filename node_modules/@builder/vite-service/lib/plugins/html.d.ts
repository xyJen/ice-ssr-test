import type { Plugin } from 'vite';
interface Option {
    filename: string;
    template: string;
    entry: string;
    rootDir: string;
    templateParameters?: object;
    ssr?: boolean;
    command?: string;
}
export declare const htmlPlugin: ({ filename, template, entry, rootDir, templateParameters, ssr, command }: Option) => Plugin;
export {};
