import { Path } from '@ali/4ever-cangjie';
export function sortNodeKeys(keys, document) {
  return [].concat(keys).sort(function (keyA, keyB) {
    var pathA = document.getPath(keyA);
    var pathB = document.getPath(keyB);

    if (!pathA || !pathB) {
      return 0;
    }

    return Path.compare(pathA, pathB) || 0;
  });
}
//# sourceMappingURL=sortNodeKeys.js.map