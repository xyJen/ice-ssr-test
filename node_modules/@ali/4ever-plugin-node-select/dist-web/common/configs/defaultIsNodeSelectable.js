import { Block, Document } from '@ali/4ever-cangjie';
export function defaultIsNodeSelectable(node, controller) {
  var isSubject = controller.query('isSubject', node);
  if (isSubject) return false;
  var isColumnsParent = controller.query('isColumnsParent', node);
  var document = controller.value.document;
  var parent = document.getParent(node.key);
  var isBlock = Block.isBlock(node);
  if (!parent || !isBlock) return false;
  if (Document.isDocument(parent) && !isColumnsParent) return true;
  var grandParent = document.getParent(parent.key);
  return Document.isDocument(grandParent) && controller.query('isSubject', parent) && !isColumnsParent;
}
//# sourceMappingURL=defaultIsNodeSelectable.js.map