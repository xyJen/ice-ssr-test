export function createOnCangjieSelect(manager) {
  return function (event, controller, next) {
    var isSelecting = manager.isSelecting;

    if (isSelecting) {
      return controller;
    }

    return next();
  };
}
//# sourceMappingURL=index.js.map