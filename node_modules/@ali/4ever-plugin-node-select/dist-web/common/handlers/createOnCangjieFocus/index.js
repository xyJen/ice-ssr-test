export function createOnCangjieFocus(manager) {
  return function (controller, next) {
    var isSelecting = manager.isSelecting;

    if (isSelecting) {
      return controller;
    }

    return next();
  };
}
//# sourceMappingURL=index.js.map