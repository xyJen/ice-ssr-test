import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { Block } from '@ali/4ever-cangjie';
import { BlockSelectBox } from "./components/BlockSelectBox";
import { defaultIsNodeSelectable } from "./configs/defaultIsNodeSelectable";
export default function createRenderNode(config, manager) {
  var isNodeSelectable = config.isNodeSelectable;
  return function (props, controller, next) {
    var node = props.node;
    var isSelectableNode = isNodeSelectable ? isNodeSelectable(node) : defaultIsNodeSelectable(node, controller);

    if (isSelectableNode && Block.isBlock(node)) {
      return /*#__PURE__*/_createElement(BlockSelectBox, {
        node: node,
        manager: manager,
        controller: controller
      }, next());
    }

    return next();
  };
}
//# sourceMappingURL=createRenderNode.js.map