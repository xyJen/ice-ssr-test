import React from 'react';
interface Handlers {
    onPress: (touch: React.Touch) => void;
    onSwipeEnd: () => void;
    onClick?: () => void;
}
export declare class Swiper {
    constructor(getDomRef: () => HTMLElement | null, handlers: Handlers);
    private readonly getDomRef;
    private status;
    private isPressingTimer;
    private readonly handlers;
    private startTouches;
    private enableClick;
    private canScrollingHorizontally;
    private initZIndex;
    setEnableClick: (flag: boolean) => void;
    private readonly onPress;
    private readonly onSwipeEnd;
    onTouchStart: (e: TouchEvent) => void;
    private readonly onTouchMove;
    private readonly reset;
    private readonly onEnd;
    private readonly onTouchEnd;
    private readonly onTouchCancel;
}
export {};
