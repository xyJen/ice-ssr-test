"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.defaultIsNodeSelectable = defaultIsNodeSelectable;

var _everCangjie = require("@ali/4ever-cangjie");

function defaultIsNodeSelectable(node, controller) {
  const isSubject = controller.query('isSubject', node);
  if (isSubject) return false;
  const isColumnsParent = controller.query('isColumnsParent', node);
  const {
    document
  } = controller.value;
  const parent = document.getParent(node.key);

  const isBlock = _everCangjie.Block.isBlock(node);

  if (!parent || !isBlock) return false;
  if (_everCangjie.Document.isDocument(parent) && !isColumnsParent) return true;
  const grandParent = document.getParent(parent.key);
  return _everCangjie.Document.isDocument(grandParent) && controller.query('isSubject', parent) && !isColumnsParent;
}
//# sourceMappingURL=defaultIsNodeSelectable.js.map