/**
 * 判断两个数组是否相交
 * @param array1 
 * @param array2 
 * @returns 
 */
export var areArraysIntersected = function areArraysIntersected(array1, array2) {
  return array1.some(function (item) {
    return array2.includes(item);
  });
};
/**
 * 判断两个数组包含内容是否相同
 * @param array1 
 * @param array2 
 * @returns 
 */

export var areArraysContentEqual = function areArraysContentEqual(array1, array2) {
  if (array1.length !== array2.length) {
    return false;
  }

  for (var index = 0; index < array1.length; index++) {
    var element1 = array1[index];
    var element2 = array2[index];

    if (element1 !== element2) {
      return false;
    }
  }

  return true;
};
//# sourceMappingURL=utils.js.map