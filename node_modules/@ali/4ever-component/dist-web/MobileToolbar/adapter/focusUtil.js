import { constants } from '@ali/4ever-cangjie'; // 获取 text area dom 节点

function getTextArea(controller) {
  var key = controller.value.document.key;
  return window.document.querySelector("[" + constants.Selector.dockey + "=\"" + key + "\"]");
} // 进入底部 panel 模式，此时是聚焦状态、但是键盘没有弹出


export function enterFocusMode(controller) {
  var textArea = getTextArea(controller);
  textArea == null ? void 0 : textArea.blur();
  textArea == null ? void 0 : textArea.setAttribute('disabled', 'true');
  controller.run('onCangjieFocus');
} // 退出底部 panel 模式

export function exitFocusMode(controller, autoFocus) {
  var textArea = getTextArea(controller);
  textArea == null ? void 0 : textArea.removeAttribute('disabled');

  if (autoFocus) {
    textArea == null ? void 0 : textArea.focus();
  }
}
//# sourceMappingURL=focusUtil.js.map