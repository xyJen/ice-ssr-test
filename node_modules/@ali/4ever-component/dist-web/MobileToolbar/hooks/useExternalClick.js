import React from 'react';
export var KEEP_TOOLBAR = 'data-keep-toolbar';
export function useExternalClick(ref, callback) {
  React.useEffect(function () {
    var handleClick = function handleClick(event) {
      var _ref$current;

      var target = event.target;

      if (!(target instanceof HTMLElement)) {
        return;
      }

      var isSelfClick = ((_ref$current = ref.current) == null ? void 0 : _ref$current.contains(target)) || !!target.closest("[" + KEEP_TOOLBAR + "], ." + KEEP_TOOLBAR);
      !isSelfClick && callback();
    };

    window.document.addEventListener('click', handleClick, {
      capture: true
    });
    return function () {
      window.document.removeEventListener('click', handleClick, {
        capture: true
      });
    };
  }, [ref, callback]);
}
//# sourceMappingURL=useExternalClick.js.map