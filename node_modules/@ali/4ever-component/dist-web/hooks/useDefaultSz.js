import { useStyle } from '@ali/4ever-cangjie';
import { SzUtil } from '@ali/4ever-utils';
export function getDocDefaultSz(style) {
  var _style$docDefaults;

  var defaultSz;

  var _ref = (style == null ? void 0 : (_style$docDefaults = style.docDefaults) == null ? void 0 : _style$docDefaults.data) || {},
      rPr = _ref.rPr;

  if (rPr != null && rPr.sz) {
    defaultSz = SzUtil.toPt(rPr.sz, rPr.szUnit || 'px');
  }

  return defaultSz;
}
export default function useDefaultSz() {
  var docStyle = useStyle();
  return getDocDefaultSz(docStyle);
}
//# sourceMappingURL=useDefaultSz.js.map