import { judgeShouldShow } from "../Toolbar/util";
export var renderToolArray = function renderToolArray(props, key, target, renderFunc, option) {
  var showTargets = props.showTargets,
      hideTargets = props.hideTargets;
  var shouldShow = judgeShouldShow(target, showTargets, hideTargets);
  if (!shouldShow) return null;
  var contents = (props[key] || []).map(function (tb, idx) {
    return renderFunc(tb, idx, option);
  }).filter(function (item) {
    return item;
  });
  if (!contents.length) return null;
  return contents;
};
//# sourceMappingURL=util.js.map