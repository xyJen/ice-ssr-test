"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.enterFocusMode = enterFocusMode;
exports.exitFocusMode = exitFocusMode;

var _everCangjie = require("@ali/4ever-cangjie");

// 获取 text area dom 节点
function getTextArea(controller) {
  const {
    key
  } = controller.value.document;
  return window.document.querySelector(`[${_everCangjie.constants.Selector.dockey}="${key}"]`);
} // 进入底部 panel 模式，此时是聚焦状态、但是键盘没有弹出


function enterFocusMode(controller) {
  const textArea = getTextArea(controller);
  textArea?.blur();
  textArea?.setAttribute('disabled', 'true');
  controller.run('onCangjieFocus');
} // 退出底部 panel 模式


function exitFocusMode(controller, autoFocus) {
  const textArea = getTextArea(controller);
  textArea?.removeAttribute('disabled');

  if (autoFocus) {
    textArea?.focus();
  }
}
//# sourceMappingURL=focusUtil.js.map