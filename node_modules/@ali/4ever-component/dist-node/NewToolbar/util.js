"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.renderToolArray = void 0;

var _util = require("../Toolbar/util");

const renderToolArray = (props, key, target, renderFunc, option) => {
  const {
    showTargets,
    hideTargets
  } = props;
  const shouldShow = (0, _util.judgeShouldShow)(target, showTargets, hideTargets);
  if (!shouldShow) return null;
  const contents = (props[key] || []).map((tb, idx) => renderFunc(tb, idx, option)).filter(item => item);
  if (!contents.length) return null;
  return contents;
};

exports.renderToolArray = renderToolArray;
//# sourceMappingURL=util.js.map