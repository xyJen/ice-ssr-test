import * as React from 'react';
interface HeaderProps {
    title: string | React.ComponentType;
    onDragOffsetChange: (yOffset: number) => void;
    onDragStatusChange: (isDragging: boolean) => void;
    Icon?: React.ComponentType;
}
export default function Header({ onDragOffsetChange, onDragStatusChange, title: Title, Icon, }: HeaderProps): JSX.Element;
export {};
