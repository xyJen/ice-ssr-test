import { Controller } from '@ali/4ever-cangjie';
import { MobileToolbarItem, Plugins } from '../types';
interface CommonProps {
    controller: Controller;
    plugins: Plugins;
    isStart?: boolean;
    isEnd?: boolean;
}
interface ItemViewProps extends CommonProps {
    item: MobileToolbarItem;
}
/**
 * toolbar 按钮渲染逻辑，这里会处理分割线、新行等逻辑
 */
export default function ItemView(props: ItemViewProps): JSX.Element | null;
export {};
