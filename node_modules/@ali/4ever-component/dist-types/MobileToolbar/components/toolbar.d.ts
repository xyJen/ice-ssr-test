import { Controller } from '@ali/4ever-cangjie';
import { MobileToolbarProps, Plugins } from '../types';
export interface IProps extends MobileToolbarProps {
    controller: Controller;
    plugins: Plugins;
}
export default function ToolbarRender(props: IProps): JSX.Element | null;
