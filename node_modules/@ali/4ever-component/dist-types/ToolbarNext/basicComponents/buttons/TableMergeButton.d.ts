import React from 'react';
import { SplitButton } from './Basics';
export declare type CellMergeStatus = 'unmerged' | 'merged';
export interface ITableMergeButtonProps extends Pick<Partial<React.ComponentProps<typeof SplitButton>>, 'onVisibleChange' | 'title' | 'tooltip' | 'icon' | 'disabled' | 'testid' | 'role' | 'active'> {
    onChange?: (item: CellMergeStatus) => void;
    onClick?: (item: CellMergeStatus) => void;
    value: CellMergeStatus;
    options: Record<CellMergeStatus, {
        title: string;
        icon?: React.ReactElement;
    }>;
}
export declare const TableMergeButton: React.FC<ITableMergeButtonProps>;
