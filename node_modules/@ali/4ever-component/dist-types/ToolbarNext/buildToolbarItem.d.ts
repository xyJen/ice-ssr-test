import type { Controller } from '@ali/4ever-cangjie';
import { IToolbarButtonConfig, IToolBar } from './interface';
import type { Plugins } from '../PluginsContext';
export declare function buildToolbarItem(plugins: Plugins | null, controller: Controller, buttonName: string, customButtons: IToolBar['customButtons'], extraProps: {
    disabled?: true;
    key: string;
} & IToolbarButtonConfig): JSX.Element | null;
