import { MoConfig } from './type';
import type { MoSerializationPluginFactory as PluginFactory, MoSerializationPlugin as Plugin } from '@ali/4ever-cangjie';
/**
 * 创建自定义插件。传入自定义的插件 Map，返回一个插件创建函数（createMoPlugins）。
 * 传入内容格式：{ PluginName: PluginFactory }
 */
declare function createCustomizedMoPlugins(plugins: Record<string, PluginFactory>): (configs?: MoConfig) => Plugin[];
export default createCustomizedMoPlugins;
