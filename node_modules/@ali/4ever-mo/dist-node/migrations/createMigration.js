"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = createMigration;

function createMigration(prevVersion, nextVersion, algs) {
  return {
    prevVersion,
    nextVersion,
    upgrade: document => {
      return algs.reduce((doc, alg) => alg.upgrade(doc), document);
    },
    downgrade: document => {
      return algs.reduce((doc, alg) => alg.downgrade(doc), document);
    }
  };
}
//# sourceMappingURL=createMigration.js.map