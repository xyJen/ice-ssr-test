/**
 * @packageDocumentation
 * @module Serializer
 */
import { sortPlugins } from '@ali/4ever-utils';

/**
 * 创建自定义插件。传入自定义的插件 Map，返回一个插件创建函数（createMoPlugins）。
 * 传入内容格式：{ PluginName: PluginFactory }
 */
function createCustomizedMoPlugins(plugins) {
  return function createMoPlugins(configs) {
    if (configs === void 0) {
      configs = {};
    }

    var result = [];
    sortPlugins(plugins).forEach(function (_ref) {
      var factory = _ref.factory;
      var plugin = factory(configs);
      result.push(plugin);
    });
    return result;
  };
}

export default createCustomizedMoPlugins;
//# sourceMappingURL=createCustomizedMoPlugins.js.map