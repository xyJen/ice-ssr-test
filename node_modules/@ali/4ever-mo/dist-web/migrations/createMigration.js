export default function createMigration(prevVersion, nextVersion, algs) {
  return {
    prevVersion: prevVersion,
    nextVersion: nextVersion,
    upgrade: function upgrade(document) {
      return algs.reduce(function (doc, alg) {
        return alg.upgrade(doc);
      }, document);
    },
    downgrade: function downgrade(document) {
      return algs.reduce(function (doc, alg) {
        return alg.downgrade(doc);
      }, document);
    }
  };
}
//# sourceMappingURL=createMigration.js.map