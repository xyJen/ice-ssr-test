"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _everUtils = require("@ali/4ever-utils");

var _everCangjie = require("@ali/4ever-cangjie");

var _models = _interopRequireDefault(require("../models"));

var _default = {
  name: 'sticker',

  onOpenTag(state, name, attrs) {
    // 解析来自 IM 的表情
    if (!(0, _everUtils.isEmojiClsName)(attrs?.class)) return false;
    const sticker = ['span', {
      code: attrs.title.replace(/\[|\]/g, ''),
      'data-type': _models.default.TYPE
    }, (0, _everCangjie.createEmptyText)()];
    state.push(sticker);
    return true;
  },

  onCloseTag(state, name, attrs) {
    if (!(0, _everUtils.isEmojiClsName)(attrs?.class)) return false;
    const sticker = state.pop();
    const parent = state.peek();

    if (_everUtils.paragraphTags[(0, _everUtils.getTagName)(parent)]) {
      state.append(sticker);
      return true;
    }

    const prevSibling = (0, _everUtils.getLastChild)(parent);

    if (prevSibling && prevSibling.isPhantom) {
      prevSibling.push(sticker, (0, _everCangjie.createEmptyText)());
    } else {
      const phantom = ['p', (0, _everCangjie.createEmptyText)(), sticker, (0, _everCangjie.createEmptyText)()];
      phantom.isPhantom = true;
      state.append(phantom);
    }

    return true;
  }

};
exports.default = _default;
//# sourceMappingURL=htmlToJsonML.js.map