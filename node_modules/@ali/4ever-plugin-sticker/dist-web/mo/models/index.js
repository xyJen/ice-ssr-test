import _extends from "@babel/runtime/helpers/extends";
import _inheritsLoose from "@babel/runtime/helpers/inheritsLoose";
import { Inline } from '@ali/4ever-cangjie';
import { ELEMENT_TYPE } from "../../utils/meta";

var Sticker = /*#__PURE__*/function (_Inline) {
  _inheritsLoose(Sticker, _Inline);

  function Sticker() {
    return _Inline.apply(this, arguments) || this;
  }

  // 使用 emoji 而非 sticker ，是为兼容富文本输入框、评论等业务
  Sticker.create = function create(properties) {
    if (typeof properties === 'string') {
      throw new Error('`Sticker.create` does not accept string');
    }

    if (Sticker.isSticker(properties)) {
      return properties;
    }

    return _Inline.create.call(this, _extends({}, properties, {
      type: Sticker.TYPE
    }));
  };

  Sticker.isSticker = function isSticker(maybeSticker) {
    return Inline.isInline(maybeSticker) && maybeSticker.type === Sticker.TYPE;
  };

  return Sticker;
}(Inline);

Sticker.TYPE = ELEMENT_TYPE;
export { Sticker as default };
//# sourceMappingURL=index.js.map