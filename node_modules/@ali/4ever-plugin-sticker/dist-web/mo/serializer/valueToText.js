import Sticker from "../models";
var valueToText = {
  name: 'sticker',
  match: function match(node) {
    return Sticker.isSticker(node);
  },
  convert: function convert(node) {
    var _node$data;

    // @ts-ignore
    return "[" + ((node == null ? void 0 : (_node$data = node.data) == null ? void 0 : _node$data.code) || '表情') + "]";
  }
};
export default valueToText;
//# sourceMappingURL=valueToText.js.map