import _extends from "@babel/runtime/helpers/extends";
import { getTagName, getAttributes } from '@ali/4ever-utils';
import Sticker from "../models";
export default {
  name: 'sticker',
  match: function match(node) {
    return getTagName(node) === 'span' && getAttributes(node)['data-type'] === Sticker.TYPE;
  },
  convert: function convert(node) {
    var data = _extends({}, getAttributes(node));

    delete data['data-type'];
    return Sticker.create({
      data: data
    });
  }
};
//# sourceMappingURL=jsonMLToValue.js.map