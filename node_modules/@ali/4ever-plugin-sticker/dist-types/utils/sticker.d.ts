import { ICustomSticker, SingleSticker } from './types';
export interface IStickerProps {
    lang?: string;
    customSticker: ICustomSticker;
}
/**
 * @description 对 SingleSticker、SpriteSticker 加工后的通用类型
 */
interface CommonSticker extends SingleSticker {
    spriteWidth: number;
    spriteHeight: number;
}
declare class Sticker {
    private stickerMap;
    private stickerLang;
    stickerList: CommonSticker[];
    constructor({ customSticker, lang }: IStickerProps);
    private getStickerList;
    private parseStickers;
    getInfoByCode(code: any): CommonSticker | undefined;
    getStickerSource(code: any): string | undefined;
    getStickerLocale(code: any, lang?: any): any;
}
export default Sticker;
