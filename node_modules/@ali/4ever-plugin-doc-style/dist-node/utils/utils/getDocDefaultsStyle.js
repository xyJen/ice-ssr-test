"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getTextStyle;

var _everCangjie = require("@ali/4ever-cangjie");

var _everPluginMarks = require("@ali/4ever-plugin-marks");

var _everUtils = require("@ali/4ever-utils");

function getTextStyle(docStyle) {
  const style = {};
  const isMobile = _everCangjie.environment.IS_MOBILE;
  if (!docStyle?.docDefaults && !isMobile) return style;
  const {
    rPr
  } = docStyle?.docDefaults?.data || {};

  if (rPr?.sz) {
    let sz = _everUtils.SzUtil.toPt(rPr.sz, rPr.szUnit || 'px');

    if (isMobile) {
      // 移动端统一放大
      sz *= _everUtils.SzUtil.FONTSIZE_BIGGER;
    }

    style.fontSize = `${sz}pt`;
  } else if (isMobile) {
    style.fontSize = `${_everUtils.fontSize.DEFAULT * _everUtils.SzUtil.FONTSIZE_BIGGER}pt`;
  }

  if (rPr?.fonts) {
    style.fontFamily = _everUtils.fontRenderFallback.getFontFallback(_everPluginMarks.Fonts.create(rPr.fonts));
  }

  return style;
}
//# sourceMappingURL=getDocDefaultsStyle.js.map