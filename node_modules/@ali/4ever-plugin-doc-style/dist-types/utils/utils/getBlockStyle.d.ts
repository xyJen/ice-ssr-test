import { CSSProperties } from 'react';
import { DocStyle, Node } from '@ali/4ever-cangjie';
export default function getBlockStyle(node: Node, docStyle?: DocStyle, defaultStyle?: Record<string, any>): CSSProperties;
