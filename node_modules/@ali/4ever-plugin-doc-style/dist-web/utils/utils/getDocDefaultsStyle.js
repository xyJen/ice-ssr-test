import { environment } from '@ali/4ever-cangjie';
import { Fonts as FontsMark } from '@ali/4ever-plugin-marks';
import { fontSize, SzUtil, fontRenderFallback } from '@ali/4ever-utils';
import '@ali/4ever-utils';
export default function getTextStyle(docStyle) {
  var _docStyle$docDefaults;

  var style = {};
  var isMobile = environment.IS_MOBILE;
  if (!(docStyle != null && docStyle.docDefaults) && !isMobile) return style;

  var _ref = (docStyle == null ? void 0 : (_docStyle$docDefaults = docStyle.docDefaults) == null ? void 0 : _docStyle$docDefaults.data) || {},
      rPr = _ref.rPr;

  if (rPr != null && rPr.sz) {
    var sz = SzUtil.toPt(rPr.sz, rPr.szUnit || 'px');

    if (isMobile) {
      // 移动端统一放大
      sz *= SzUtil.FONTSIZE_BIGGER;
    }

    style.fontSize = sz + "pt";
  } else if (isMobile) {
    style.fontSize = fontSize.DEFAULT * SzUtil.FONTSIZE_BIGGER + "pt";
  }

  if (rPr != null && rPr.fonts) {
    style.fontFamily = fontRenderFallback.getFontFallback(FontsMark.create(rPr.fonts));
  }

  return style;
}
//# sourceMappingURL=getDocDefaultsStyle.js.map