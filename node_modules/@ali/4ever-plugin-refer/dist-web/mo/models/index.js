import _extends from "@babel/runtime/helpers/extends";
import _inheritsLoose from "@babel/runtime/helpers/inheritsLoose";
import { Inline } from '@ali/4ever-cangjie';
import { ELEMENT_TYPE } from "../../utils/meta";

var Refer = /*#__PURE__*/function (_Inline) {
  _inheritsLoose(Refer, _Inline);

  function Refer() {
    return _Inline.apply(this, arguments) || this;
  }

  Refer.create = function create(properties) {
    if (typeof properties === 'string') {
      throw new Error('`Refer.create` does not accept string');
    }

    if (Refer.isRefer(properties)) {
      return properties;
    }

    return _Inline.create.call(this, _extends({}, properties, {
      type: Refer.TYPE
    }));
  };

  Refer.isRefer = function isRefer(maybeRefer) {
    return Inline.isInline(maybeRefer) && maybeRefer.type === Refer.TYPE;
  };

  return Refer;
}(Inline);

Refer.TYPE = ELEMENT_TYPE;
export { Refer as default };
//# sourceMappingURL=index.js.map