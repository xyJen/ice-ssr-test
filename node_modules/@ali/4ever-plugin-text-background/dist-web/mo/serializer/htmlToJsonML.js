export default {
  name: 'highlight',
  onOpenTag: function onOpenTag(state, name, _attrs, id) {
    if (name !== 'mark') return false;
    state.addMarks({
      id: id,
      value: {
        highlight: '#FFC000'
      }
    });
    return true;
  },
  onCloseTag: function onCloseTag(state, name, _attrs, id) {
    if (name !== 'mark') return false;
    state.removeMarks({
      id: id
    });
    return true;
  }
};
//# sourceMappingURL=htmlToJsonML.js.map