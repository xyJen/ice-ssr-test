import React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { ProbeProvider } from "./probe";

var createRenderContent = function createRenderContent(config) {
  // 不支持动态
  var _ref = config || {},
      disableAntiAliasing = _ref.disableAntiAliasing;

  return function (props, controller, next) {
    // TODO: 出于性能考虑，这里应该是默认关闭比较好 @多鱼
    if (disableAntiAliasing && disableAntiAliasing()) return next();
    return /*#__PURE__*/_createElement(ProbeProvider, {
      controller: controller
    }, next());
  };
};

export default createRenderContent;
//# sourceMappingURL=renderContent.js.map