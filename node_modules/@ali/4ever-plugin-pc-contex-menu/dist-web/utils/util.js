import { environment } from '@ali/4ever-cangjie';
import { capitalize } from 'lodash-es';
var HOT_KEY_MOD = 'Mod';
var HOT_KEY_MOD_REPLACE = environment.IS_MAC ? '⌘' : 'Ctrl';
var HOT_KEY_DIVIDER = '+';
export function hotKeyToReadableString(hotKey) {
  var resultStr = hotKey.split(HOT_KEY_DIVIDER).map(capitalize).join(HOT_KEY_DIVIDER);
  return resultStr.replace(HOT_KEY_MOD, HOT_KEY_MOD_REPLACE);
}
export function toPx(px) {
  return px + "px";
}
//# sourceMappingURL=util.js.map