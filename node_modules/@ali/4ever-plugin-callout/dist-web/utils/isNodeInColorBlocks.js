import { ColorBlocks } from '@ali/4ever-plugin-color-blocks';
/**
 * 判断节点是否有 parent 属于高亮块
 */

export default function isNodeInColorBlocks(controller, node) {
  if (!node) return false;
  var document = controller.value.document;
  var parent = document.getParent(node.key);

  while (parent && !ColorBlocks.isColorBlocks(parent)) {
    parent = document.getParent(parent.key);
  }

  return Boolean(parent);
}
//# sourceMappingURL=isNodeInColorBlocks.js.map