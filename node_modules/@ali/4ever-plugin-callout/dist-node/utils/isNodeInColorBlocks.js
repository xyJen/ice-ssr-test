"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isNodeInColorBlocks;

var _everPluginColorBlocks = require("@ali/4ever-plugin-color-blocks");

/**
 * 判断节点是否有 parent 属于高亮块
 */
function isNodeInColorBlocks(controller, node) {
  if (!node) return false;
  const {
    document
  } = controller.value;
  let parent = document.getParent(node.key);

  while (parent && !_everPluginColorBlocks.ColorBlocks.isColorBlocks(parent)) {
    parent = document.getParent(parent.key);
  }

  return Boolean(parent);
}
//# sourceMappingURL=isNodeInColorBlocks.js.map