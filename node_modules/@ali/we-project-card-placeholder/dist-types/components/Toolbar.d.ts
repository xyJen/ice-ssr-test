import { PropsWithChildren } from 'react';
export interface IToolbarProps {
    url: string;
    name: string;
}
export default function Toolbar(props: PropsWithChildren<IToolbarProps>): JSX.Element;
