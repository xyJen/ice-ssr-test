import _extends from "@babel/runtime/helpers/extends";
import _inheritsLoose from "@babel/runtime/helpers/inheritsLoose";
import { Inline } from '@ali/4ever-cangjie';

var Tag = /*#__PURE__*/function (_Inline) {
  _inheritsLoose(Tag, _Inline);

  function Tag() {
    return _Inline.apply(this, arguments) || this;
  }

  Tag.create = function create(properties) {
    if (typeof properties === 'string') {
      throw new Error('`Tag.create` does not accept string');
    }

    if (Tag.isTag(properties)) {
      return properties;
    }

    return _Inline.create.call(this, _extends({}, properties, {
      type: Tag.TYPE
    }));
  };

  Tag.isTag = function isTag(maybeTag) {
    return Inline.isInline(maybeTag) && maybeTag.type === Tag.TYPE;
  };

  return Tag;
}(Inline);

Tag.TYPE = 'tag';
export { Tag as default };
//# sourceMappingURL=index.js.map