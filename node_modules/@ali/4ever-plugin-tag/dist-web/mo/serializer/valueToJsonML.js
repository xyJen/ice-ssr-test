import { omit } from 'lodash-es';
import Tag from "../models";
export default {
  match: function match(node) {
    return Tag.isTag(node);
  },
  convert: function convert(node) {
    // @ts-ignore
    // 河图插件兼容排版引擎 1.0 加的 hack
    var data = omit(node.data, '__HACK');
    return ['tag', data];
  }
};
//# sourceMappingURL=valueToJsonML.js.map