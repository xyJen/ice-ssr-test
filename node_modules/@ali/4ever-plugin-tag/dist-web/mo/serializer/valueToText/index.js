import Tag from "../../models";
import im from "./im";
import group from "./group";
import formula from "./formula";
var convertPlugins = {
  im: im,
  group: group,
  formula: formula
};
var valueToText = {
  name: 'tag',
  match: function match(node) {
    return Tag.isTag(node);
  },
  convert: function convert(node) {
    var _ref = node,
        data = _ref.data;
    var tagType = data.tagType,
        metadata = data.metadata;

    var _ref2 = metadata || {},
        imTitle = _ref2.imTitle;

    var targetConvert = convertPlugins[tagType];
    if (targetConvert) return targetConvert(data);
    return imTitle || '';
  }
};
export default valueToText;
//# sourceMappingURL=index.js.map