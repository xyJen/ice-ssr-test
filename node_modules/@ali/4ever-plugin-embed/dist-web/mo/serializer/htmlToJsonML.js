import { createTextWithString } from '@ali/4ever-cangjie';
export default {
  name: 'embed',
  onOpenTag: function onOpenTag(state, name, attrs) {
    if (name !== 'video') return false;
    var rawAttrs = {
      src: attrs.src || '',
      name: attrs.title ? attrs.title : '',
      type: 'video',
      mimeType: '',
      size: Number(attrs['data-size']) || 0,
      viewType: 'preview',
      previewSize: {
        height: 300
      }
    };
    var embed = ['embed', rawAttrs, createTextWithString('')];
    state.push(embed);
    return true;
  },
  onCloseTag: function onCloseTag(state, name) {
    if (name !== 'video') return false;
    var video = state.pop();
    state.append(video);
    return true;
  }
};
//# sourceMappingURL=htmlToJsonML.js.map