import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { getTagName, getAttributes, sanitizeUrl } from '@ali/4ever-utils';
export default {
  name: 'embed',
  match: function match(node) {
    return getTagName(node) === 'embed';
  },
  convert: function convert(node) {
    // @ts-ignore
    var _getAttributes = getAttributes(node),
        src = _getAttributes.src,
        name = _getAttributes.name,
        type = _getAttributes.type,
        size = _getAttributes.size;

    if (type === 'video') {
      if (!src) return null;
      return /*#__PURE__*/_createElement("video", {
        controls: true,
        src: sanitizeUrl(src),
        title: name,
        "data-size": size
      });
    }

    return null;
  }
};
//# sourceMappingURL=jsonMLToHTML.js.map