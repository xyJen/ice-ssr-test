import React from 'react';
import { EmbedNode } from '../../../types';
interface Props {
    isSelected: boolean;
    isUploadFailed?: boolean;
    node: EmbedNode;
    children: React.ReactElement;
    onRef?: Function;
    enableBorderHighlight?: boolean;
}
export default function EmbedCardWrapper(props: Props): JSX.Element;
export {};
