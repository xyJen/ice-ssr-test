import { EmbedNode } from '../../../types';
interface Props {
    node: EmbedNode;
    isSelected: boolean;
    enableBorderHighlight?: boolean;
}
export default function EmbedUploadProgressCard(props: Props): JSX.Element;
export {};
