import { EmbedNode } from '../../../types';
interface Props {
    node: EmbedNode;
    locale: Record<string, string>;
    isSelected?: boolean;
    enableBorderHighlight?: boolean;
}
export default function EmbedFailedCard(props: Props): JSX.Element;
export {};
