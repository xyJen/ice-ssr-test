import equal from 'fast-deep-equal';
import { marksToDecorations } from "../utils/marksToDecorations";
export default (function (configs) {
  return function onChange(controller, next) {
    if (configs != null && configs.isDisabled && configs.isDisabled()) {
      return next();
    }

    var decorations = marksToDecorations(controller);

    if (!equal(decorations, controller.value.decorations)) {
      controller.setDecorations(decorations);
    }

    return next();
  };
});
//# sourceMappingURL=onChange.js.map