import React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { MarkView } from "../components/markView";
import { ViewMark } from "../models/marks";
export default (function (configs) {
  return function renderMark(props, controller, next) {
    if (configs != null && configs.isDisabled && configs.isDisabled()) {
      return next();
    }

    var mark = props.mark;

    if (ViewMark.isViewMark(mark)) {
      return /*#__PURE__*/_createElement(MarkView, {
        controller: controller,
        mark: mark,
        showInline: configs == null ? void 0 : configs.showInline,
        customClick: configs == null ? void 0 : configs.onBubbleClick
      }, next());
    }

    return next();
  };
});
//# sourceMappingURL=renderMark.js.map