import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { SimpleLazyLoad } from '@ali/4ever-component';
export default function createRenderContent(configs) {
  var LazyLoad = (configs == null ? void 0 : configs.lazyload) || SimpleLazyLoad;
  var BubblesNew = LazyLoad(function () {
    return import("../components/BubblesNew");
  });
  return function renderContent(_, controller, next) {
    if (configs != null && configs.isGrayed && !configs.isGrayed()) {
      return next();
    }

    var isDisabled = Boolean((configs == null ? void 0 : configs.isDisabled) && configs.isDisabled());

    if (isDisabled) {
      return next();
    }

    return /*#__PURE__*/_createElement(React.Fragment, null, next(), /*#__PURE__*/_createElement(BubblesNew, {
      configs: configs,
      controller: controller
    }));
  };
}
//# sourceMappingURL=renderEditable.js.map