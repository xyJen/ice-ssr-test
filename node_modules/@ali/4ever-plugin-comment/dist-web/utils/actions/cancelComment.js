import { ViewMark } from "../models/marks";
export default function cancelComment(controller) {
  var decorations = controller.value.decorations;
  var activeDecoration = decorations.find(function (d) {
    return ViewMark.isViewMark(d.mark) && d.mark.data.isNew;
  });

  if (activeDecoration) {
    controller.setDecorations(decorations.filter(function (d) {
      return d !== activeDecoration;
    }));
  }
}
//# sourceMappingURL=cancelComment.js.map