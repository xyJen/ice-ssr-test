import _extends from "@babel/runtime/helpers/extends";
import isSidebarVisible from "../queries/isSidebarVisible"; // eslint-disable-next-line import/no-cycle

import { setSidebarVisible } from "../actions";
export default function setActiveId(controller, contentId, isAutoFocus) {
  if (isAutoFocus === void 0) {
    isAutoFocus = true;
  }

  var isSidebarShowing = isSidebarVisible(controller);
  controller.setData(_extends({}, controller.value.data, {
    _commentActiveMarkId: contentId,
    _commentIsAutoFocus: isAutoFocus
  }));

  if (contentId && !isSidebarShowing) {
    controller.run('onAction', setSidebarVisible(true));
  }

  return controller;
}
//# sourceMappingURL=setActiveId.js.map