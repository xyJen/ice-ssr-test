import { Mark } from '@ali/4ever-cangjie';
import { MARK_TYPE_DATA, MARK_TYPE_VIEW } from "../types";
export var DataMark = {
  TYPE: MARK_TYPE_DATA,
  create: function create(data) {
    return Mark.create({
      type: DataMark.TYPE,
      data: data
    });
  },
  isDataMark: function isDataMark(maybeMark) {
    return Mark.isMark(maybeMark) && maybeMark.type === DataMark.TYPE;
  }
};
export var ViewMark = {
  TYPE: MARK_TYPE_VIEW,
  create: function create(data) {
    return Mark.create({
      type: ViewMark.TYPE,
      data: data
    });
  },
  isViewMark: function isViewMark(maybeMark) {
    return Mark.isMark(maybeMark) && maybeMark.type === ViewMark.TYPE;
  }
};
//# sourceMappingURL=marks.js.map