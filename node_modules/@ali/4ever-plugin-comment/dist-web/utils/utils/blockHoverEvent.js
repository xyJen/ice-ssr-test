var callbacks = [];

function on(c) {
  callbacks.push(c);
}

function off(c) {
  callbacks = callbacks.filter(function (cb) {
    return cb !== c;
  });
}

function trigger(msg) {
  callbacks.forEach(function (c) {
    return c(msg);
  });
}

export default {
  on: on,
  off: off,
  trigger: trigger
};
//# sourceMappingURL=blockHoverEvent.js.map