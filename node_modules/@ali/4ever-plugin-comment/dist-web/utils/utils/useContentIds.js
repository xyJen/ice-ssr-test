import * as React from 'react';
import { ViewMark } from "../models/marks";
export default function useContentIds(controller, isLoaded, callback) {
  var decorations = controller.value.decorations;
  React.useEffect(function () {
    if (!callback) {
      return;
    }

    var ids = decorations.map(function (d) {
      return ViewMark.isViewMark(d.mark) ? d.mark.data.contentId : undefined;
    }).filter(Boolean);
    callback(ids, isLoaded);
  }, [decorations, isLoaded]);
}
//# sourceMappingURL=useContentIds.js.map