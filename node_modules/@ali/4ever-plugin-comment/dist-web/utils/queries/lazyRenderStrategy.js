import { ViewMark } from "../models/marks";
export default function lazyRenderStrategy(controller, node) {
  if (!node.isElement()) {
    return undefined;
  }

  var decorations = controller.value.decorations;
  var hasCommentMark = decorations.some(function (d) {
    return ViewMark.isViewMark(d.mark) && node.hasNode(d.start.key);
  });
  return hasCommentMark ? {} : undefined;
}
//# sourceMappingURL=lazyRenderStrategy.js.map