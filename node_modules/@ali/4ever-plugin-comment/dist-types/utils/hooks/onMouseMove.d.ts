import { Controller } from '@ali/4ever-cangjie';
import { CommentPluginConfigs } from '../types';
export default function createOnMoseMove(configs: CommentPluginConfigs | undefined): (event: React.MouseEvent, controller: Controller, next: any) => any;
