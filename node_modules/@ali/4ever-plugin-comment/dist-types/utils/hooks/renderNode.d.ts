import { Node, RenderNodeProps, Controller } from '@ali/4ever-cangjie';
import { CommentPluginConfigs } from '../types';
export default function createRenderNode(configs: CommentPluginConfigs | undefined): (props: RenderNodeProps<Node>, controller: Controller, next: any) => any;
