import { Controller, Selection, Decoration } from '@ali/4ever-cangjie';
import { PostionRowItem } from '../types';
declare type FindPositionParams = {
    controller: Controller;
    decorations: Decoration[];
    selection?: Selection;
    container?: Window | HTMLElement;
    isFromPopup?: boolean;
};
/**
 * 获取评论框的位置。返回相对于当前 block 的位置，和屏幕绝对位置
 */
export declare function findCommentPostion({ controller, decorations, selection, container, isFromPopup, }: FindPositionParams): PostionRowItem[];
export {};
