import * as React from 'react';
interface Params {
    ref: React.RefObject<Element>;
    updator: Function;
    popHeights: Record<string, number>;
    contentId: string;
    isVisible: boolean;
    zoom: number;
}
/**
 * 在 ref 尺寸变动时，调用 updator
 */
export default function useResizer(params: Params): void;
export {};
