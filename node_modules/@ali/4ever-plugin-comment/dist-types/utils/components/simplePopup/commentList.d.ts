import { BasicCommentItem } from '../../types';
declare type CommentListProps = {
    contentId: string;
    getCommentList?: (contentId: string) => Promise<BasicCommentItem[]>;
};
export default function CommentList(props: CommentListProps): JSX.Element;
export {};
