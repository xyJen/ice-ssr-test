import { EdgePoint, TextPoint, Controller, domUtils } from '@ali/4ever-cangjie';
interface MemoPoint {
    key: string;
    caretTop: number;
    row: ReturnType<typeof domUtils.splitRows>[number];
}
interface CachedRowsItem {
    top: number;
    height: number;
    points: MemoPoint[];
    rows: ReturnType<typeof domUtils.splitRows>;
}
interface IParams {
    controller: Controller;
    isFromPopup?: boolean;
    docTop: number;
    container: HTMLElement | Window;
    allRows: Record<string, CachedRowsItem>;
    paramPoint: EdgePoint | TextPoint | undefined;
}
/**
 * 根据一个 point，获取对应的分行（row）、相对于 document 的 offsetTop
 */
export declare function getRowTopFromPoint(params: IParams): number[] | undefined;
export {};
