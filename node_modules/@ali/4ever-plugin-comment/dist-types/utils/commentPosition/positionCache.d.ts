import { Controller, domUtils } from '@ali/4ever-cangjie';
interface MemoPoint {
    key: string;
    caretTop: number;
    row: ReturnType<typeof domUtils.splitRows>[number];
}
interface CachedRowsItem {
    top: number;
    height: number;
    points: MemoPoint[];
    rows: ReturnType<typeof domUtils.splitRows>;
}
declare type AllRows = Record<string, CachedRowsItem>;
interface IParams {
    controller: Controller;
    docWidth: number;
    docHeight: number;
    docTop: number;
    container: HTMLElement | Window;
}
/**
 * 每次获取缓存之前，先清理一遍失效的缓存，针对的场景：
 * 1. document dom 的宽度变了
 * 2. 某一个节点变化了
 */
export declare function validateCaches(params: IParams): AllRows;
export {};
