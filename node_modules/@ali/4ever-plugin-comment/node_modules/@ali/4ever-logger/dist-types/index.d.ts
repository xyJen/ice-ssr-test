import WeLogger from '@ali/we-logger-main';
import type ArmsAppender from '@ali/we-logger-appender-arms';
import DebugAppender from '@ali/we-logger-appender-debug';
export declare enum EnvEnum {
    DAILY = "daily",
    PRE = "pre",
    PROD = "prod",
    LOCAL = "local",
    UNKNOWN = "unknown"
}
export declare const getEnvironment: () => EnvEnum;
/**
 * 默认需要一个 debug appender, 外层业务也可以主动移除
 */
export declare const debugAppender: DebugAppender;
/**
 * 默认配置好一个 arms appender, 需要外层业务手动注入才能够生效
 * @see https://arms.console.aliyun.com/retcode?pid=en91t6prdd%4081db28b43ff68d6#/index
 */
export declare const getArmsAppender: () => ArmsAppender | null;
export interface ILogger {
    setEnabled: (boolean: any) => void;
    setBizName: (name: string) => Logger;
}
declare class Logger extends WeLogger implements ILogger {
    private armsAppender;
    private sentPV;
    private bizName;
    setEnabled(flag?: boolean): void;
    setBizName(name: any): this;
}
declare const logger: Logger;
export default logger;
