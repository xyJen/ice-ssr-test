"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = createOnMoseMove;

var _lodash = require("lodash");

var _everUtils = require("@ali/4ever-utils");

var _hoverCapture = _interopRequireDefault(require("../utils/hoverCapture"));

const hoverBlockHandler = (0, _lodash.debounce)((event, controller) => {
  const result = _everUtils.HoverBlock.getBlock(event, controller.value.document);

  _hoverCapture.default.trigger(result);
}, 50);

function createOnMoseMove(configs) {
  return function (event, controller, next) {
    if (configs?.isDisabled && configs.isDisabled()) {
      return next();
    }

    hoverBlockHandler(event.nativeEvent, controller);
    return next();
  };
}
//# sourceMappingURL=onMouseMove.js.map