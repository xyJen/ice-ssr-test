"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = cancelComment;

var _marks = require("../models/marks");

function cancelComment(controller) {
  const {
    decorations
  } = controller.value;
  const activeDecoration = decorations.find(d => _marks.ViewMark.isViewMark(d.mark) && d.mark.data.isNew);

  if (activeDecoration) {
    controller.setDecorations(decorations.filter(d => d !== activeDecoration));
  }
}
//# sourceMappingURL=cancelComment.js.map