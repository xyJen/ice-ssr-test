"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = lazyRenderStrategy;

var _marks = require("../models/marks");

function lazyRenderStrategy(controller, node) {
  if (!node.isElement()) {
    return undefined;
  }

  const {
    decorations
  } = controller.value;
  const hasCommentMark = decorations.some(d => _marks.ViewMark.isViewMark(d.mark) && node.hasNode(d.start.key));
  return hasCommentMark ? {} : undefined;
}
//# sourceMappingURL=lazyRenderStrategy.js.map