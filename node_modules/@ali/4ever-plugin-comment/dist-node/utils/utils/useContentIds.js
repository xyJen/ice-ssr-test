"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = useContentIds;

var React = _interopRequireWildcard(require("react"));

var _marks = require("../models/marks");

function useContentIds(controller, isLoaded, callback) {
  const {
    decorations
  } = controller.value;
  React.useEffect(() => {
    if (!callback) {
      return;
    }

    const ids = decorations.map(d => _marks.ViewMark.isViewMark(d.mark) ? d.mark.data.contentId : undefined).filter(Boolean);
    callback(ids, isLoaded);
  }, [decorations, isLoaded]);
}
//# sourceMappingURL=useContentIds.js.map