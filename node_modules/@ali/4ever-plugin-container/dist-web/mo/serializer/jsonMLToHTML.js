import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { getTagName, getAttributes } from '@ali/4ever-utils';

var _ref = /*#__PURE__*/_createElement("div", null, null);

export default {
  name: 'container',
  match: function match(node) {
    var tag = getTagName(node);
    var attrs = getAttributes(node);
    return tag === 'container' && (attrs == null ? void 0 : attrs.subType);
  },
  convert: function convert(node, path, _convert) {
    // 兜底显示空 div
    return _ref;
  }
};
//# sourceMappingURL=jsonMLToHTML.js.map