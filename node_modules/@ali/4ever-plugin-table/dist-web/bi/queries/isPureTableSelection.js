import TableCell from "../../mo/models/tableCell";
;
export var isPureTableSelection = function isPureTableSelection(controller) {
  var _controller$value$sel, _controller$value$sel2;

  var isByTable = (_controller$value$sel = controller.value.selection) == null ? void 0 : (_controller$value$sel2 = _controller$value$sel.data) == null ? void 0 : _controller$value$sel2.isByTable;
  var selection = controller.query('getTableSelection');
  var _controller$value = controller.value,
      document = _controller$value.document,
      _selection = _controller$value.selection;
  var anchor = _selection.anchor,
      focus = _selection.focus;

  if (!anchor || !focus) {
    return false;
  }

  var anchorCell = document.getClosest(anchor.key, TableCell.isTableCell);
  var focusCell = document.getClosest(focus.key, TableCell.isTableCell);

  if (selection) {
    if (anchorCell === focusCell && !isByTable) {
      return false;
    }

    return true;
  }

  return false;
};
//# sourceMappingURL=isPureTableSelection.js.map