import { domUtils } from '@ali/4ever-cangjie';
export default function getAllRowsDomRect(_, _ref) {
  var node = _ref.node;
  var table = domUtils.findDOMNodeSafely(node.key);
  if (!table) return [];
  return Array.from(table.querySelectorAll('tr')).map(function (tableRow) {
    return tableRow.getBoundingClientRect();
  });
}
//# sourceMappingURL=getAllRowsDomRect.js.map