import { domUtils } from '@ali/4ever-cangjie';
export default function getAllRowsHeight(_, _ref) {
  var node = _ref.node;
  return node.nodes.map(function (tableRow) {
    // eslint-disable-next-line react/no-find-dom-node
    var tr = domUtils.findDOMNode(tableRow.key); // 这里就应该取实际的，不应该取设置的。内容太高撑开的时候，取设置的加delta就是有问题的

    var h = tr.offsetHeight;
    return h;
  });
}
//# sourceMappingURL=getAllRowsHeight.js.map