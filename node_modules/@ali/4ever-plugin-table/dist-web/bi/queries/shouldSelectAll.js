import { isTable } from "../types";
export default function shouldSelectAll(controller, container) {
  var _controller$value = controller.value,
      document = _controller$value.document,
      selection = _controller$value.selection;

  if (!selection) {
    return false;
  }

  var start = selection.getStart(document);
  var table = document.getClosest(start.key, function (node) {
    return isTable(node) && node !== container;
  });
  return table ? true : undefined;
}
//# sourceMappingURL=shouldSelectAll.js.map