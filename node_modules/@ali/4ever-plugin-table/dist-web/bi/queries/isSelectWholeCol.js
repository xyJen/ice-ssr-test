export default function isSelectWholeCol(controller, _ref) {
  var node = _ref.node;
  var tableSelection = controller.query('tableSelection');

  if (!tableSelection || tableSelection.key !== node.key) {
    return false;
  }

  var isSelectWholeTable = controller.query('isSelectWholeTable', {
    node: node
  });
  return !isSelectWholeTable && tableSelection.startRowIndex === 0 && tableSelection.endRowIndex === node.nodes.length - 1;
}
//# sourceMappingURL=isSelectWholeCol.js.map