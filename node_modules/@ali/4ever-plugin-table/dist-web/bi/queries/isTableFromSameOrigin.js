import Table from "../../mo/models";
export default function isTableFromSameOrigin(_, _ref) {
  var node = _ref.node,
      tableKey = _ref.tableKey;

  // 其他情况pi才可能返回true
  if (Table.isTable(node) && node.key === tableKey) {
    return true;
  }

  return false;
}
//# sourceMappingURL=isTableFromSameOrigin.js.map