import getAllRowsDomRect from "./getAllRowsDomRect";
export default function getRowIndexByClientY(controller, _ref) {
  var node = _ref.node,
      clientY = _ref.clientY;
  var rowsRect = getAllRowsDomRect(controller, {
    node: node
  });
  var lastRowRect = rowsRect[rowsRect.length - 1];

  if (clientY < rowsRect[0].y + rowsRect[0].height) {
    return 0;
  }

  if (lastRowRect.y < clientY) {
    return rowsRect.length - 1;
  }

  return rowsRect.findIndex(function (rect) {
    return rect.y > clientY;
  }) - 1;
}
//# sourceMappingURL=getRowIndexByClientY.js.map