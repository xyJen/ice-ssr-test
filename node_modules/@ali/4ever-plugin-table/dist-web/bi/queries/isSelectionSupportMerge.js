import isMoreThanNCell from "../utils/isMoreThanNCell";
import createTableSelectionForFocusedCell from "../utils/createTableSelectionForFocusedCell";
import getDataTableSelectionByTable from "../utils/getDataTableSelectionByTable";
export default function isSelectionSupportMerge(controller, _ref) {
  var table = _ref.node,
      visible = _ref.visible;

  if (!table) {
    return false;
  }

  var tableSelection = getDataTableSelectionByTable(controller, table) || createTableSelectionForFocusedCell(controller.value, table);

  if (!tableSelection) {
    return false;
  }

  var has2VisibleCell = isMoreThanNCell(2, 'visible', {
    visible: visible,
    table: table,
    selection: tableSelection
  });

  if (!has2VisibleCell) {
    return false;
  }

  var canMerge = controller.query('isAllSelectedCellsComplete', {
    node: table,
    tableSelection: tableSelection
  });
  return canMerge;
}
//# sourceMappingURL=isSelectionSupportMerge.js.map