/**
 * 是否选中整行（不包括全选）
 */
export default function isSelectWholeRow(controller, _ref) {
  var node = _ref.node;
  var tableSelection = controller.query('tableSelection');

  if (!tableSelection || tableSelection.key !== node.key) {
    return false;
  }

  var isSelectWholeTable = controller.query('isSelectWholeTable', {
    node: node
  });

  if (isSelectWholeTable) {
    return false;
  }

  var tableNodes = node.nodes[0].nodes;
  var tableRow = node.nodes[tableSelection.startRowIndex];
  var endColTableCell = tableRow.nodes[tableSelection.endColIndex];

  if (!endColTableCell) {
    return true;
  }

  var _ref2 = (endColTableCell == null ? void 0 : endColTableCell.data) || {},
      _ref2$colSpan = _ref2.colSpan,
      colSpan = _ref2$colSpan === void 0 ? 1 : _ref2$colSpan;

  var isSelecedAllColTableCells = Number(tableSelection.endColIndex) + Number(colSpan) === tableNodes.length;
  return tableSelection.startColIndex === 0 && isSelecedAllColTableCells;
}
//# sourceMappingURL=isSelectWholeRow.js.map