export var TABLE_RESIZE_COL_WIDTH = Symbol('TABLE_RESIZE_COL_WIDTH');
export function resizeTableColWidth(payload) {
  var node = payload.node,
      colIndex = payload.colIndex,
      delta = payload.delta,
      containerWidth = payload.containerWidth;
  return {
    type: TABLE_RESIZE_COL_WIDTH,
    payload: {
      node: node,
      colIndex: colIndex,
      delta: delta,
      containerWidth: containerWidth
    }
  };
}
export function isTableResizeTableColWidthAction(action) {
  return action.type === TABLE_RESIZE_COL_WIDTH;
}
//# sourceMappingURL=resizeTableColWidth.js.map