import { handleCangjiePaste } from "../utils";
import { PasteMode } from '@ali/4ever-plugin-clipboard';
export default function createOnCangjiePaste(clipboard, tableConfig) {
  return function onCangjiePaste(event, controller, next) {
    var clipboardData = controller.query('getClipboardData', event);
    var fragment = clipboardData == null ? void 0 : clipboardData.getFragment();
    if (!fragment || PasteMode.text === clipboard.mode) return next();
    return handleCangjiePaste(controller, {
      fragment: fragment,
      tableConfig: tableConfig
    }, next);
  };
}
//# sourceMappingURL=onCangjiePaste.js.map