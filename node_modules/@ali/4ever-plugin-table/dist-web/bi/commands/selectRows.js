import selectTableSelection from "./selectTableSelection";
export default function selectRows(controller, table, startIndex, endIndex) {
  var min = Math.min(startIndex, endIndex);
  var max = Math.max(startIndex, endIndex);
  var MAX_COLS_INDEX = (table.data.colsWidth || []).length - 1;
  var tableSelection = {
    startRowIndex: min,
    startColIndex: 0,
    endRowIndex: max,
    endColIndex: MAX_COLS_INDEX,
    key: table.key,
    type: 'row'
  };
  return selectTableSelection(controller, table, tableSelection, false);
}
//# sourceMappingURL=selectRows.js.map