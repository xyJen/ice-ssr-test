import selectTableSelection from "./selectTableSelection";
export default function selectCols(controller, table, startIndex, endIndex) {
  var min = Math.min(startIndex, endIndex);
  var max = Math.max(startIndex, endIndex);
  var rows = table.nodes;
  var tableSelection = {
    startRowIndex: 0,
    startColIndex: min,
    endRowIndex: rows.length - 1,
    endColIndex: max,
    key: table.key,
    type: 'col'
  };
  return selectTableSelection(controller, table, tableSelection, false);
}
//# sourceMappingURL=selectCols.js.map