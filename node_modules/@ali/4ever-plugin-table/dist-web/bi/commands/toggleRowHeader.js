import _extends from "@babel/runtime/helpers/extends";
import { Commands } from '@ali/4ever-cangjie';
import updateTableStyle from "./updateTableStyle";
export default function toggleRowHeader(controller, table) {
  var tablePath = controller.value.document.getPath(table.key);
  if (!tablePath) return controller;
  var firstRow = table.nodes[0];
  if (!firstRow) return controller;
  var rowData = firstRow.data || {};
  controller.command(Commands.setNodeByPath, [].concat(tablePath, [0]), {
    data: _extends({}, rowData, {
      isTblHeader: !rowData.isTblHeader
    })
  });
  var newTable = controller.value.document.getNode(table.key);
  return updateTableStyle(controller, newTable);
}
//# sourceMappingURL=toggleRowHeader.js.map