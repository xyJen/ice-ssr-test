import _extends from "@babel/runtime/helpers/extends";
import { Commands } from '@ali/4ever-cangjie';
import { MIN_ROW_HEIGHT } from "../constants";
export default function resizeRowsHeight(controller, tableRow, newHeight) {
  return controller.command(Commands.setNodeByKey, tableRow.key, {
    data: _extends({}, tableRow.data, {
      h: Math.max(newHeight, MIN_ROW_HEIGHT)
    })
  });
}
//# sourceMappingURL=resizeRowsHeight.js.map