import { isTable } from "../types";
export default function isSelectionInTable(value, configs) {
  var document = value.document,
      selection = value.selection;

  if (!selection) {
    return false;
  }

  var start = selection.getStart(document);
  var table = document.getClosest(start.key, isTable);

  if (!table) {
    return false;
  }

  if (table && configs != null && configs.excludeColumns && table.data.sr) {
    return false;
  }

  return true;
}
//# sourceMappingURL=isSelectionInTable.js.map