export default function getVisibleCellOfCol(table, colIndex) {
  for (var i = 0; i < table.nodes.length; i += 1) {
    var row = table.nodes[i];
    var cell = row.nodes[colIndex];

    if (!cell) {
      return null;
    }

    if (cell && !cell.data.hidden) {
      return cell;
    }
  }

  return null;
}
//# sourceMappingURL=getVisibleCellOfCol.js.map