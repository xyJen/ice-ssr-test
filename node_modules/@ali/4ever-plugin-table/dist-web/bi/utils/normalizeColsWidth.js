import isNumber from "./isNumber";
import { MIN_COL_WIDTH } from "../../utils/constants";
export default function normalizeColsWidth(table) {
  var colsWidth = table.data.colsWidth;

  if (!Array.isArray(colsWidth)) {
    return Array(table.nodes[0].nodes.length).fill(MIN_COL_WIDTH);
  }

  return colsWidth.map(function (c) {
    return isNumber(c) ? c : MIN_COL_WIDTH;
  });
}
//# sourceMappingURL=normalizeColsWidth.js.map