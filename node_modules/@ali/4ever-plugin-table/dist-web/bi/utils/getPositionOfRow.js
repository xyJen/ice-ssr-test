export default function getPositionOfRow(rowKey, table) {
  var rowSize = table.nodes.length;

  for (var rowIndex = 0; rowIndex < rowSize; rowIndex += 1) {
    if (table.nodes[rowIndex].key === rowKey) {
      return rowIndex;
    }
  }

  return null;
}
//# sourceMappingURL=getPositionOfRow.js.map