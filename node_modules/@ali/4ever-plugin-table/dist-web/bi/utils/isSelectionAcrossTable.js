import { isTable } from "../types";
export default function isSelectionAcrossTable(value) {
  var document = value.document,
      selection = value.selection;

  if (!(selection != null && selection.isExpanded)) {
    return false;
  }

  var anchor = selection.anchor,
      focus = selection.focus;
  var anchorTable = document.getClosest(anchor.key, isTable);
  var focusTable = document.getClosest(focus.key, isTable);

  if (!anchorTable && !focusTable) {
    return false;
  }

  return anchorTable !== focusTable;
}
//# sourceMappingURL=isSelectionAcrossTable.js.map