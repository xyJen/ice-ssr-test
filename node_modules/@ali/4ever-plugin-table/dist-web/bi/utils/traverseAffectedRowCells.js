export default function traverseAffectedRowCells(table, targetRowIndex, callback) {
  for (var _rowIndex = 0; _rowIndex < targetRowIndex; _rowIndex += 1) {
    var row = table.nodes[_rowIndex];

    for (var _colIndex = 0; _colIndex < row.nodes.length; _colIndex += 1) {
      var _cell = row.nodes[_colIndex];

      if (_cell) {
        var hidden = _cell.data.hidden || false;
        var rowSpan = _cell.data.rowSpan || 1;

        if (!hidden && _rowIndex + rowSpan - 1 >= targetRowIndex) {
          callback(_rowIndex, _colIndex, _cell);
        }
      }
    }
  }
}
//# sourceMappingURL=traverseAffectedRowCells.js.map