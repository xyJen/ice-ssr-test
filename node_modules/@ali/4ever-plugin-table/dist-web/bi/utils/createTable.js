import { Block } from '@ali/4ever-cangjie';
import createTableRow from "./createTableRow";
export default function createTable(params) {
  var rowSize = params.rowSize,
      colSize = params.colSize,
      data = params.data;
  var nodes = [];

  for (var i = 0; i < rowSize; i += 1) {
    nodes.push(createTableRow(colSize));
  }

  return Block.create({
    type: 'table',
    data: data,
    nodes: nodes
  });
}
//# sourceMappingURL=createTable.js.map