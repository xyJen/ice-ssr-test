import createTableSelectionForFocusedCell from "./createTableSelectionForFocusedCell";
export var getToolbarTableSelection = function getToolbarTableSelection(controller, table) {
  var tblSelections = controller.query('viewTableSelections');
  var tbSel;

  if (tblSelections) {
    tbSel = tblSelections.find(function (ts) {
      return ts.key === table.key;
    });
  }

  if (!tbSel) {
    tbSel = createTableSelectionForFocusedCell(controller.value, table);
  }

  return tbSel;
};
//# sourceMappingURL=getToolbarTableSelection.js.map