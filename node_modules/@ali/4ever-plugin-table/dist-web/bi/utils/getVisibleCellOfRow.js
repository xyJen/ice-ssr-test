export default function getVisibleCellOfRow(table, rowIndex) {
  var row = table.nodes[rowIndex];

  if (!row) {
    return null;
  }

  for (var i = 0; i < row.nodes.length; i += 1) {
    var cell = row.nodes[i];

    if (cell && !cell.data.hidden) {
      return cell;
    }
  }

  return null;
}
//# sourceMappingURL=getVisibleCellOfRow.js.map