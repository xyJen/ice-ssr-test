export default function traverseAffectedColCells(table, targetColIndex, callback) {
  for (var _rowIndex = 0; _rowIndex < table.nodes.length; _rowIndex += 1) {
    var row = table.nodes[_rowIndex];

    for (var _colIndex = 0; _colIndex < targetColIndex; _colIndex += 1) {
      var _cell = row.nodes[_colIndex];

      if (_cell) {
        // 判断cell是否存在
        var hidden = _cell.data.hidden || false;
        var colSpan = _cell.data.colSpan || 1;

        if (!hidden && _colIndex + colSpan - 1 >= targetColIndex) {
          callback(_rowIndex, _colIndex, _cell);
        }
      }
    }
  }
}
//# sourceMappingURL=traverseAffectedColCells.js.map