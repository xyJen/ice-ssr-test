import { logNPEInfo } from "./logger";
export default function traverseSingleRowCells(table, targetRowIndex, triggerRowIndex, callback) {
  var targetRow = table.nodes[targetRowIndex];
  var triggerRow = table.nodes[triggerRowIndex]; // triggerRow 可能被协作者删除

  if (!triggerRow) {
    logNPEInfo(table, {
      type: 'traverseSingleRowCells',
      rowIndex: triggerRowIndex
    });
    return;
  }

  for (var _colIndex = 0; _colIndex < triggerRow.nodes.length; _colIndex += 1) {
    var _triggerCell = triggerRow.nodes[_colIndex];

    var _originalCell = targetRow ? targetRow.nodes[_colIndex] : null;

    callback(targetRowIndex, _colIndex, _triggerCell, _originalCell);
  }
}
//# sourceMappingURL=traverseSingleRowCells.js.map