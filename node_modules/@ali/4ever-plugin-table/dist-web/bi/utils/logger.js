import logger from '@ali/4ever-logger';
export function logNPEInfo(table, info) {
  try {
    var tableRowColData = table.nodes.map(function (row) {
      var rowData = row.nodes.map(function (cell) {
        var _cell$data, _cell$data2, _cell$data3;

        if ((_cell$data = cell.data) != null && _cell$data.hidden) {
          return 'hidden';
        }

        return (((_cell$data2 = cell.data) == null ? void 0 : _cell$data2.rowSpan) || 1) + "-" + (((_cell$data3 = cell.data) == null ? void 0 : _cell$data3.colSpan) || 1);
      });
      return rowData;
    });
    logger.info({
      type: 'table_npe',
      info: JSON.stringify(info),
      tableRowColData: JSON.stringify(tableRowColData)
    }); // eslint-disable-next-line no-empty
  } catch (e) {}
}
//# sourceMappingURL=logger.js.map