var getDataTableSelectionByTable = function getDataTableSelectionByTable(controller, table) {
  var tableSelection = controller.query('tableSelection');

  if (!tableSelection) {
    return null;
  }

  var sameOrigin = controller.query('isTableFromSameOrigin', {
    node: table,
    tableKey: tableSelection.key
  });

  if (sameOrigin) {
    return tableSelection;
  }

  return null;
};

export default getDataTableSelectionByTable;
//# sourceMappingURL=getDataTableSelectionByTable.js.map