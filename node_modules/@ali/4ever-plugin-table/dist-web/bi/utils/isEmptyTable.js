export default function isEmptyTable(table) {
  var rowSize = table.nodes.length;

  for (var i = 0; i < rowSize; i += 1) {
    var row = table.nodes[i];

    if (row.type !== 'table-row') {
      continue; // eslint-disable-line no-continue
    }

    var cells = row.nodes.filter(function (_ref) {
      var type = _ref.type;
      return type === 'table-cell';
    });

    if (cells.length !== 0) {
      return false;
    }
  }

  return true;
}
//# sourceMappingURL=isEmptyTable.js.map