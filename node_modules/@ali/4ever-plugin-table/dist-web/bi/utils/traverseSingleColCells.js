export default function traverseSingleColCells(table, targetColIndex, triggerColIndex, callback) {
  for (var _rowIndex = 0; _rowIndex < table.nodes.length; _rowIndex += 1) {
    var _finalIndex = Math.min(targetColIndex, table.nodes[_rowIndex].nodes.length);

    var row = table.nodes[_rowIndex];

    if (!row) {
      return;
    }

    var _originalCell = row.nodes[_finalIndex];
    var triggerCell = row.nodes[triggerColIndex];
    callback(_rowIndex, _finalIndex, triggerCell, _originalCell);
  }
}
//# sourceMappingURL=traverseSingleColCells.js.map