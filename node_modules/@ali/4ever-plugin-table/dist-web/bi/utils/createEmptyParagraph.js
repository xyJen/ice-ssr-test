import { Block, Text } from '@ali/4ever-cangjie';
export default function getEmptyParagraph(text) {
  if (text === void 0) {
    text = '';
  }

  return Block.create({
    type: 'paragraph',
    nodes: [Text.create(text)]
  });
}
//# sourceMappingURL=createEmptyParagraph.js.map