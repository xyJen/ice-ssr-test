import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { insertTable } from "../../actions";
export function MobileTableButton(props) {
  var controller = props.controller,
      item = props.item,
      Wrapper = props.Wrapper,
      locale = props.locale;
  var isDisabled = !controller.query('canInsertTable');
  var handleClick = React.useCallback(function () {
    return controller.run('onAction', insertTable(3, 3));
  }, [controller]);
  return /*#__PURE__*/_createElement(Wrapper, {
    item: item,
    type: "line",
    icon: "table",
    lineText: locale == null ? void 0 : locale.quickInsertTitle,
    isDisabled: isDisabled,
    onClick: handleClick
  });
}
//# sourceMappingURL=tableButtonMobile.js.map