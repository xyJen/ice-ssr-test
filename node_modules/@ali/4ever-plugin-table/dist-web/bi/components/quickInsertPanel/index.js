import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import styled from 'styled-components';
import { TablePanel } from '@ali/we-design';
import { getClosestContainerBlock, getInnerWidth } from '@ali/4ever-utils';
import { insertTable } from "../../actions";
var Wrapper = /*#__PURE__*/styled.div(["display:flex;align-items:center;justify-content:center;height:100%;td{width:", ";height:", ";}"], function (props) {
  return props.cellSize + "px";
}, function (props) {
  return props.cellSize + "px";
});

var QuickInsertPanel = function QuickInsertPanel(props) {
  var controller = props.controller,
      close = props.close,
      from = props.from;
  var handleChange = React.useCallback(function (row, col) {
    var containerBlock = getClosestContainerBlock(controller.value);
    var containerWidth = getInnerWidth(containerBlock);
    close();
    return controller.run('onAction', insertTable(row, col, containerWidth));
  }, [controller, close]);
  return /*#__PURE__*/_createElement(Wrapper, {
    cellSize: from === 'toolbar' ? 10 : 25
  }, /*#__PURE__*/_createElement(TablePanel, {
    row: 10,
    col: 10,
    onPick: handleChange
  }));
};

export default QuickInsertPanel;
//# sourceMappingURL=index.js.map