import { getClosestContainerBlockFromViewModel, getInnerFullWidth } from '@ali/4ever-utils';
export default function getTableMaxWidthByContainer(controller, table) {
  var containerBlock = getClosestContainerBlockFromViewModel(controller.view, table);
  var width = getInnerFullWidth(containerBlock, controller.value.document);
  return width ? width - 2 : 0;
}
//# sourceMappingURL=getTableMaxWidthByContainer.js.map