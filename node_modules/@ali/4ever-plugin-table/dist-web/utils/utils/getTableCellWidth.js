/**
 * 获取 TableCell 数据模型对应的 width（百分比或绝对宽度）
 */
export default function getTableCellWidth(cell, table) {
  var _table$data, _cell$data;

  if (!table || !Array.isArray(table == null ? void 0 : (_table$data = table.data) == null ? void 0 : _table$data.colsWidth)) {
    return null;
  }

  var row = table.getParent(cell.key);
  if (!row) return null;
  var colIndex = row.nodes.findIndex(function (c) {
    return c.key === cell.key;
  });
  if (colIndex === -1) return null;
  var colNum = ((_cell$data = cell.data) == null ? void 0 : _cell$data.colSpan) || 1;
  var colSpan = (typeof colNum === 'number' ? colNum : parseInt(colNum, 10)) || 1;
  return table.data.colsWidth.slice(colIndex, colIndex + colSpan).reduce(function (acc, v) {
    return acc + v;
  }, 0);
}
//# sourceMappingURL=getTableCellWidth.js.map