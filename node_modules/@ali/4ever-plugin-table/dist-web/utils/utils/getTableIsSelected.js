export default function getTableIsSelected(table, controller) {
  var _controller$value = controller.value,
      selection = _controller$value.selection,
      document = _controller$value.document;

  if (selection.anchor.key === table.key || selection.focus.key === table.key) {
    return false;
  }

  var _selection$convertToT = selection.convertToTextPoints(document),
      anchor = _selection$convertToT.anchor,
      focus = _selection$convertToT.focus;

  var isSelectionInsideTable = table.getNode(anchor.key) && table.getNode(focus.key);
  return Boolean(isSelectionInsideTable);
}
//# sourceMappingURL=getTableIsSelected.js.map