import _extends from "@babel/runtime/helpers/extends";
import { Injection } from '@ali/4ever-cangjie';
export default function setColsWidthInjection(controller, node, colsWidth) {
  var injections = controller.value.injections;
  var curInjection = injections.find(function (inj) {
    return inj.key === node.key;
  }) || {};
  var injection = Injection.create({
    key: node.key,
    data: _extends({}, curInjection.data || {}, {
      colsWidth: colsWidth
    })
  });
  return controller.setInjections([].concat(injections.filter(function (inj) {
    return inj.key !== node.key;
  }), [injection]));
}
;
//# sourceMappingURL=setColsWidthInjection.js.map