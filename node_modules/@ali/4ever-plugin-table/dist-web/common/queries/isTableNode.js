import { isTableNode as isTableNodeUtil } from "../../utils/utils/isTableNode";
/**
 * 判断节点是否是表格
 */

export function isTableNode(_, node, excludeColumns) {
  if (excludeColumns === void 0) {
    excludeColumns = false;
  }

  return isTableNodeUtil(node, excludeColumns);
}
//# sourceMappingURL=isTableNode.js.map