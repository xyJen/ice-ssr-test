import { TOOLBAR_ITEM_SIZE, SPACING } from "../../utils/constants";
/**
 * 判断节点是否有 parent 属于表格/分栏（不包含节点本身）
 */

export function getTableConstants() {
  return {
    TOOLBAR_ITEM_SIZE: TOOLBAR_ITEM_SIZE,
    SPACING: SPACING
  };
}
//# sourceMappingURL=getTableConstants.js.map