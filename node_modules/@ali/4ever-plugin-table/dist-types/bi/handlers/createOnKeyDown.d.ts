import { Controller } from '@ali/4ever-cangjie';
import { TableConfig } from '../types';
export default function createOnKeyDown(config: TableConfig): (event: React.KeyboardEvent, controller: Controller, next: any) => any;
