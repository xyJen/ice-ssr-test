import { Controller, Action } from '@ali/4ever-cangjie';
import { TableConfig } from '../types';
export default function createOnAction(config: TableConfig): (action: Action, controller: Controller, next: any) => any;
