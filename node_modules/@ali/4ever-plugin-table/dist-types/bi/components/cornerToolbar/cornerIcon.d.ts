import * as React from 'react';
interface Props {
    size?: number;
    active: boolean;
    onClick: (e: React.MouseEvent) => void;
}
declare const CornerIcon: (props: Props) => JSX.Element;
export default CornerIcon;
