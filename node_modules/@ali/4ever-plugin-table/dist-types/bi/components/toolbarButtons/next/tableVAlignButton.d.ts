import * as React from 'react';
import { Controller } from '@ali/4ever-cangjie';
import { IToolbarButtonConfig, VerticalAlign } from '@ali/4ever-component';
export declare const DEFAULT_TABLE_ALIGNMENT = "middle";
export interface TableVAlignButtonProps extends IToolbarButtonConfig {
    controller: Controller;
    locale: Record<string, string>;
    options?: Partial<Record<VerticalAlign, {
        title?: string;
        icon?: React.ReactElement;
    }>>;
}
export default function TableVAlignButton(props: TableVAlignButtonProps): JSX.Element;
