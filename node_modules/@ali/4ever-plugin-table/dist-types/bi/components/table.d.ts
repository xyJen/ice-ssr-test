import * as React from 'react';
import { Controller, BiPluginConfig } from '@ali/4ever-cangjie';
import MoTable from '../../mo/models';
import { ITableSelection, TableConfig } from '../types';
export interface Selection {
    startRowIndex: number;
    startColIndex: number;
    endRowIndex: number;
    endColIndex: number;
}
export interface TableProps {
    controller: Controller;
    isSelected: boolean;
    node: MoTable;
    colsWidth: number[];
    scale: number;
    zoomContainer: HTMLElement;
    scrollableContainer?: Window | HTMLElement | null;
    tableSelection?: Omit<ITableSelection, 'key'> | null;
    children: () => React.ReactNode;
    tableConfig?: TableConfig;
    draggableConfig?: BiPluginConfig['draggable'];
    locale: any;
    visible?: boolean;
}
declare const _default: (props: Omit<TableProps, 'colsWidth' | 'scale' | 'zoomContainer'>) => JSX.Element;
export default _default;
