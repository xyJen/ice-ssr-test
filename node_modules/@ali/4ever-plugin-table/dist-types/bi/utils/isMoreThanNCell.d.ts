import Table from '../../mo/models';
import { ITableSelection } from '../types';
interface tableProps {
    visible: boolean;
    table: Table | null;
    selection: ITableSelection;
}
export default function isMoreThanNCell(n: number, status: string, tableProps: tableProps): boolean;
export {};
