import { Document, Controller } from '@ali/4ever-cangjie';
import { TableConfig } from '../types';
interface Payload {
    fragment: Document;
    tableConfig?: TableConfig;
}
export declare function handleCangjiePaste(controller: Controller, payload: Payload, next?: (...args: any[]) => void): void | Controller;
export {};
