import Table from '../../mo/models';
import TableCell from '../../mo/models/tableCell';
export default function traverseSingleRowCells(table: Table, targetRowIndex: number, triggerRowIndex: number, callback: (targetRowIndex: number, colIndex: number, triggerCell: TableCell, originalCell: TableCell | null) => void): void;
