import { Controller } from '@ali/4ever-cangjie';
import Table from '../../mo/models';
import { ITableSelection } from '../types';
export declare const getSelectedRowRangeFromTableSelection: ({ controller, table, tableSelection, includeTable, }: {
    controller: Controller;
    table: Table;
    tableSelection: ITableSelection | null;
    includeTable?: boolean | undefined;
}) => {
    start: number;
    end: number;
} | null;
export declare const getSelectedColRangeFromTableSelection: ({ controller, table, tableSelection, includeTable, }: {
    controller: Controller;
    table: Table;
    tableSelection: ITableSelection | null;
    includeTable?: boolean | undefined;
}) => {
    start: number;
    end: number;
} | null;
