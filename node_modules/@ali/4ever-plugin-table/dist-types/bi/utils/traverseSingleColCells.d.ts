import Table from '../../mo/models';
import TableCell from '../../mo/models/tableCell';
export default function traverseSingleColCells(table: Table, targetColIndex: number, triggerColIndex: number, callback: (rowIndex: number, finalIndex: number, trggerCell: TableCell, originalCell: TableCell) => void): void;
