import { Controller } from '@ali/4ever-cangjie';
import { LayoutConfig } from '../basic';
import { ITableSelection, TableConfig } from '../../../../types';
import Table from '../../../../../mo/models';
declare enum KeyEnum {
    copy = "copy",
    cut = "cut",
    paste = "paste",
    clear = "clear",
    insertRowAbove = "insertRowAbove",
    insertRowBelow = "insertRowBelow",
    deleteRow = "deleteRow",
    insertColLeft = "insertColLeft",
    insertColRight = "insertColRight",
    deleteCol = "deleteCol"
}
declare type TableLayoutConfig = Omit<LayoutConfig, 'key'> & {
    key: KeyEnum;
};
declare type LayoutFactory = (params: {
    controller: Controller;
    table: Table;
    type: 'row' | 'col';
    hideToolbar: Function;
    tableSelection: ITableSelection;
    locale: TableConfig['locale'];
}) => TableLayoutConfig[];
export declare const createToolbarLayout: LayoutFactory;
export {};
