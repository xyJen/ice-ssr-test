import { MobileTableButton } from '../components/toolbarButtons/tableButtonMobile';
export default function toolbar(tableConfig: any): {
    buttons: {
        mobile: typeof MobileTableButton;
    };
    locale: any;
};
