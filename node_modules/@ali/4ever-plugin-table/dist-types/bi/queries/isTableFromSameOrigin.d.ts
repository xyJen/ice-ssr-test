import type { Node } from '@ali/4ever-cangjie';
interface IParam {
    node: Node;
    tableKey: string;
}
export default function isTableFromSameOrigin(_: any, { node, tableKey }: IParam): boolean;
export {};
