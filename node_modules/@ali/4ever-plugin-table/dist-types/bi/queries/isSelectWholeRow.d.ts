/**
 * 是否选中整行（不包括全选）
 */
import { Controller } from '@ali/4ever-cangjie';
import Table from '../../mo/models';
export default function isSelectWholeRow(controller: Controller, { node }: {
    node: Table;
}): boolean;
