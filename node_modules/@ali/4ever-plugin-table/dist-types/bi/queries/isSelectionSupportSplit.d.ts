import { Controller } from '@ali/4ever-cangjie';
import Table from '../../mo/models';
export default function isSelectionSupportSplit(controller: Controller, { node: table, visible }: {
    node: Table;
    visible: boolean;
}): boolean | null;
