import Table from '../../mo/models';
import { ITableSelection } from '../types';
export default function isAllSelectedCellsComplete(controller: any, { node: table, tableSelection }: {
    node: Table;
    tableSelection: Omit<ITableSelection, 'key'>;
}): boolean;
