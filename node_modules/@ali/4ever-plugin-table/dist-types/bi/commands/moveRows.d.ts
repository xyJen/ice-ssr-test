import { Controller } from '@ali/4ever-cangjie';
import Table from '../../mo/models';
export default function moveRows(controller: Controller, table: Table, originIndexRange: {
    start: number;
    end: number;
}, targetIndex: number): Controller;
