import { Controller } from '@ali/4ever-cangjie';
interface Payload {
    rowSize: number;
    colSize: number;
    containerWidth?: number;
    defaultAutofit?: boolean;
    defaultHeader?: boolean;
    setTableHeaderStyle?: () => void;
}
export default function insertTable(controller: Controller, payload: Payload): Controller;
export {};
