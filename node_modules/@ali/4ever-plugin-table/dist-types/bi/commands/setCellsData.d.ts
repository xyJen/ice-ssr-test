import { Controller } from '@ali/4ever-cangjie';
import Table from '../../mo/models';
import { ITableSelection } from '../types';
declare enum CellDataKey {
    fill = "fill",
    vAlign = "vAlign"
}
export declare const DEFAULT_CELL_DATA: {
    fill: string;
    vAlign: string;
};
export default function setCellsData(controller: Controller, table: Table, selection: ITableSelection, key: keyof typeof CellDataKey, data: Record<string, any>): Controller;
export {};
