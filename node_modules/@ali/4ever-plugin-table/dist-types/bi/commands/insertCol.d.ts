import { Controller } from '@ali/4ever-cangjie';
import Table from '../../mo/models';
export default function insertCol(controller: Controller, table: Table, targetColIndex: number, triggerColIndex: number, colWidth?: number, containerWidth?: number): Controller;
