import * as React from 'react';
interface Props {
    children: React.ReactElement;
    visible?: boolean;
    isMobile: boolean;
}
declare const TableWrapper: (props: Props) => JSX.Element;
export default TableWrapper;
