import { ZhiPlugin } from '@ali/4ever-cangjie';
import MoTable from '../mo/models';
interface Padding {
    top?: number;
    bottom?: number;
}
export interface TableConfig {
    padding?: Padding;
    overflow?: 'auto' | 'visible';
    onScroll?: (scrollContainer: HTMLDivElement | null, table: MoTable) => void;
    isMobile?: boolean;
    enableHeaderSticky?: () => boolean;
    forceRenderPercentColWidthForAutoFit?: () => boolean;
}
export declare type CreateRenderNode = (config: TableConfig, isMobile?: boolean) => ZhiPlugin['renderNode'];
declare module '@ali/4ever-cangjie' {
    /** 插件配置 */
    interface ZhiPluginConfig {
        table?: TableConfig;
    }
    interface MobileZhiPluginConfig {
        table?: TableConfig;
    }
}
export {};
