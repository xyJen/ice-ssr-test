/**
 * 是否属于表格元素（可配置是否包括分栏）
 */
export declare function isTableNode(maybeTable: any, excludeColumns?: boolean): boolean;
/**
 * 是否属于表格元素 (不包括分栏)
 */
export declare function isPureTableNode(maybeTable: any): boolean;
