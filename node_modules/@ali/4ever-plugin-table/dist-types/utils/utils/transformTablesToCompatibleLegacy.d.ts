import { Document } from '@ali/4ever-cangjie';
interface TransformResult {
    shouldTransfrom: boolean;
    fragment: Document;
}
export default function transformTablesToCompatibleLegacy(document: Document, fragment: Document, isMobile?: boolean): TransformResult;
export {};
