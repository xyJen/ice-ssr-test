import { Block } from '@ali/4ever-cangjie';
interface Position {
    colIndex: number;
    rowIndex: number;
}
export default function getPositionOfCell(cellKey: string, table: Block | null): Position | null;
export {};
