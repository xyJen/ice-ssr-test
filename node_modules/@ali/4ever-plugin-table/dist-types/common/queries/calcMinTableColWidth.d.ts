import { Controller, Node } from '@ali/4ever-cangjie';
interface IPayload {
    node: Node;
    colIndex: number;
}
interface IResult {
    key?: string;
    width?: number;
}
export default function calcMinTableColWidth(controller: Controller, payload: IPayload): IResult;
export {};
