import { Node, Controller } from '@ali/4ever-cangjie';
/**
 * 判断节点是否有 parent 属于表格/分栏（不包含节点本身）
 */
export default function isNodeInTable(controller: Controller, node: Node | null, excludeColumns?: boolean): boolean;
