/// <reference types="lodash" />
import { Node, Controller, Selection, Document, SelectionWrapperMap } from '@ali/4ever-cangjie';
export declare const getAllTables: ((root: Document) => Node[]) & import("lodash").MemoizedFunction;
interface SelectionContainersProps {
    selection: Selection;
    defaultWrapper: HTMLDivElement;
    document?: Document;
}
export default function getSelectionContainers(controller: Controller, { selection: originSelection, defaultWrapper, document: doc }: SelectionContainersProps): SelectionWrapperMap;
export {};
