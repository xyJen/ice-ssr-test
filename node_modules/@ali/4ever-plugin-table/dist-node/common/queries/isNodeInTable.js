"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isNodeInTable;

var _isTableNode = require("../../utils//utils/isTableNode");

/**
 * 判断节点是否有 parent 属于表格/分栏（不包含节点本身）
 */
function isNodeInTable(controller, node, excludeColumns = false) {
  if (!node) return false;
  const {
    document
  } = controller.value;
  let parent = document.getParent(node.key);

  while (parent && !(0, _isTableNode.isTableNode)(parent, excludeColumns)) {
    parent = document.getParent(parent.key);
  }

  return Boolean(parent);
}
//# sourceMappingURL=isNodeInTable.js.map