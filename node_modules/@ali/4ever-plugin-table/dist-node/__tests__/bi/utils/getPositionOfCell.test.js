"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _getPositionOfCell = _interopRequireDefault(require("../../../utils/utils/getPositionOfCell"));

var _utils = require("../../../bi/utils");

var _helpers = require("../helpers");

const table = (0, _helpers.createTableInstance)();
describe('bi/table/utils/getPositionOfCell', () => {
  it('cell is child of table', () => {
    const colIndex = 1;
    const rowIndex = 1;
    const cell = table.nodes[rowIndex].nodes[colIndex];
    expect((0, _getPositionOfCell.default)(cell.key, table)).toEqual({
      colIndex,
      rowIndex
    });
  });
  it('cell is not child of table', () => {
    const cell = (0, _utils.createEmptyTableCell)();
    expect((0, _getPositionOfCell.default)(cell.key, table)).toBe(null);
  });
});
//# sourceMappingURL=getPositionOfCell.test.js.map