"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _helpers = require("../helpers");

var _normalizeColsWidth = _interopRequireDefault(require("../../../bi/utils/normalizeColsWidth"));

var _constants = require("../../../utils/constants");

describe('bi/table/utils/normalizeColsWidth', () => {
  it('invalid colsWidth', () => {
    const table = (0, _helpers.createTableInstance)({
      table: [['A', 'B']],
      colsWidth: null
    });
    const colsWidth = (0, _normalizeColsWidth.default)(table);
    expect(colsWidth).toEqual([_constants.MIN_COL_WIDTH, _constants.MIN_COL_WIDTH]);
  });
  it('col width is not number', () => {
    const colsWidth = [null, 50];
    const table = (0, _helpers.createTableInstance)({
      colsWidth,
      isAutofit: true
    });
    expect((0, _normalizeColsWidth.default)(table)).toEqual([_constants.MIN_COL_WIDTH, 50]);
  });
});
//# sourceMappingURL=normalizeColsWidth.test.js.map