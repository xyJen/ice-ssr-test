"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getTableIsSelected;

function getTableIsSelected(table, controller) {
  const {
    selection,
    document
  } = controller.value;

  if (selection.anchor.key === table.key || selection.focus.key === table.key) {
    return false;
  }

  const {
    anchor,
    focus
  } = selection.convertToTextPoints(document);
  const isSelectionInsideTable = table.getNode(anchor.key) && table.getNode(focus.key);
  return Boolean(isSelectionInsideTable);
}
//# sourceMappingURL=getTableIsSelected.js.map