"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isTableNode = isTableNode;
exports.isPureTableNode = isPureTableNode;

var _models = _interopRequireDefault(require("../../mo/models"));

/**
 * 是否属于表格元素（可配置是否包括分栏）
 */
function isTableNode(maybeTable, excludeColumns = false) {
  if (_models.default.isTable(maybeTable)) {
    if (excludeColumns) {
      return !maybeTable.data.sr;
    }

    return true;
  }

  return false;
}
/**
 * 是否属于表格元素 (不包括分栏)
 */


function isPureTableNode(maybeTable) {
  return isTableNode(maybeTable) && !maybeTable?.data?.sr;
}
//# sourceMappingURL=isTableNode.js.map