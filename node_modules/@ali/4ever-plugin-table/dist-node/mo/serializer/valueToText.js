"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _everCangjie = require("@ali/4ever-cangjie");

var _models = _interopRequireDefault(require("../models"));

var _tableRow = _interopRequireDefault(require("../models/tableRow"));

var _tableCell = _interopRequireDefault(require("../models/tableCell"));

const BREAK = '\n';
const valueToText = {
  name: 'table',

  match(node) {
    return _models.default.isTable(node);
  },

  convert(node, convert) {
    if (!_everCangjie.Block.isBlock(node)) {
      return node.text;
    }

    const tableText = node.nodes.map(tableRow => {
      // 不是 tableRow 节点，坏表格
      if (!_tableRow.default.isTableRow(tableRow)) {
        return tableRow.text + BREAK;
      }

      const rowText = tableRow.nodes.map(tableCell => {
        // 不是 tableCell 节点，坏表格
        if (!_tableCell.default.isTableCell(tableCell)) {
          return tableCell.text + BREAK;
        }

        const cellText = tableCell.nodes.map(child => convert(child)).join('');
        return cellText;
      }).join('');
      return rowText;
    }).join('');
    return tableText;
  }

};
var _default = valueToText;
exports.default = _default;
//# sourceMappingURL=valueToText.js.map