"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _everCangjie = require("@ali/4ever-cangjie");

var _meta = require("../../utils/meta");

class TableRow extends _everCangjie.Block {
  static create(properties) {
    if (typeof properties === 'string') {
      throw new Error('`TableRow.create` does not accept string');
    }

    if (TableRow.isTableRow(properties)) {
      return properties;
    }

    return super.create({ ...properties,
      type: TableRow.TYPE
    });
  }

  static isTableRow(maybeTableRow) {
    return _everCangjie.Block.isBlock(maybeTableRow) && maybeTableRow.type === TableRow.TYPE;
  }

  static isHeader(row) {
    return row.data.isTblHeader === true;
  }

  static shouldSticky(row) {
    return this.isTableRow(row) && this.isHeader(row) && row.nodes.every(cell => (cell.data?.rowSpan || 1) === 1);
  }

}

exports.default = TableRow;
TableRow.TYPE = _meta.ELEMENT_TYPE_TABLE_ROW;
//# sourceMappingURL=tableRow.js.map