"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isPureTableSelection = void 0;

var _tableCell = _interopRequireDefault(require("../../mo/models/tableCell"));

;

const isPureTableSelection = controller => {
  const isByTable = controller.value.selection?.data?.isByTable;
  const selection = controller.query('getTableSelection');
  const {
    document,
    selection: _selection
  } = controller.value;
  const {
    anchor,
    focus
  } = _selection;

  if (!anchor || !focus) {
    return false;
  }

  const anchorCell = document.getClosest(anchor.key, _tableCell.default.isTableCell);
  const focusCell = document.getClosest(focus.key, _tableCell.default.isTableCell);

  if (selection) {
    if (anchorCell === focusCell && !isByTable) {
      return false;
    }

    return true;
  }

  return false;
};

exports.isPureTableSelection = isPureTableSelection;
//# sourceMappingURL=isPureTableSelection.js.map