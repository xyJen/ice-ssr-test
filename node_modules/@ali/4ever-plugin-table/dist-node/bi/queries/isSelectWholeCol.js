"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isSelectWholeCol;

function isSelectWholeCol(controller, {
  node
}) {
  const tableSelection = controller.query('tableSelection');

  if (!tableSelection || tableSelection.key !== node.key) {
    return false;
  }

  const isSelectWholeTable = controller.query('isSelectWholeTable', {
    node
  });
  return !isSelectWholeTable && tableSelection.startRowIndex === 0 && tableSelection.endRowIndex === node.nodes.length - 1;
}
//# sourceMappingURL=isSelectWholeCol.js.map