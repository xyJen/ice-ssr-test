"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = shouldSelectAll;

var _types = require("../types");

function shouldSelectAll(controller, container) {
  const {
    document,
    selection
  } = controller.value;

  if (!selection) {
    return false;
  }

  const start = selection.getStart(document);
  const table = document.getClosest(start.key, node => (0, _types.isTable)(node) && node !== container);
  return table ? true : undefined;
}
//# sourceMappingURL=shouldSelectAll.js.map