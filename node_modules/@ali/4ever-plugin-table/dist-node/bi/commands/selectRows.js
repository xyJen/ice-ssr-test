"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = selectRows;

var _selectTableSelection = _interopRequireDefault(require("./selectTableSelection"));

function selectRows(controller, table, startIndex, endIndex) {
  const min = Math.min(startIndex, endIndex);
  const max = Math.max(startIndex, endIndex);
  const MAX_COLS_INDEX = (table.data.colsWidth || []).length - 1;
  const tableSelection = {
    startRowIndex: min,
    startColIndex: 0,
    endRowIndex: max,
    endColIndex: MAX_COLS_INDEX,
    key: table.key,
    type: 'row'
  };
  return (0, _selectTableSelection.default)(controller, table, tableSelection, false);
}
//# sourceMappingURL=selectRows.js.map