"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = selectCols;

var _selectTableSelection = _interopRequireDefault(require("./selectTableSelection"));

function selectCols(controller, table, startIndex, endIndex) {
  const min = Math.min(startIndex, endIndex);
  const max = Math.max(startIndex, endIndex);
  const rows = table.nodes;
  const tableSelection = {
    startRowIndex: 0,
    startColIndex: min,
    endRowIndex: rows.length - 1,
    endColIndex: max,
    key: table.key,
    type: 'col'
  };
  return (0, _selectTableSelection.default)(controller, table, tableSelection, false);
}
//# sourceMappingURL=selectCols.js.map