"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = toggleRowHeader;

var _everCangjie = require("@ali/4ever-cangjie");

var _updateTableStyle = _interopRequireDefault(require("./updateTableStyle"));

function toggleRowHeader(controller, table) {
  const tablePath = controller.value.document.getPath(table.key);
  if (!tablePath) return controller;
  const firstRow = table.nodes[0];
  if (!firstRow) return controller;
  const rowData = firstRow.data || {};
  controller.command(_everCangjie.Commands.setNodeByPath, [...tablePath, 0], {
    data: { ...rowData,
      isTblHeader: !rowData.isTblHeader
    }
  });
  const newTable = controller.value.document.getNode(table.key);
  return (0, _updateTableStyle.default)(controller, newTable);
}
//# sourceMappingURL=toggleRowHeader.js.map