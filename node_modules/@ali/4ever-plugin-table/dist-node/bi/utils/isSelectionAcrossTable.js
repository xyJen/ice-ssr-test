"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isSelectionAcrossTable;

var _types = require("../types");

function isSelectionAcrossTable(value) {
  const {
    document,
    selection
  } = value;

  if (!selection?.isExpanded) {
    return false;
  }

  const {
    anchor,
    focus
  } = selection;
  const anchorTable = document.getClosest(anchor.key, _types.isTable);
  const focusTable = document.getClosest(focus.key, _types.isTable);

  if (!anchorTable && !focusTable) {
    return false;
  }

  return anchorTable !== focusTable;
}
//# sourceMappingURL=isSelectionAcrossTable.js.map