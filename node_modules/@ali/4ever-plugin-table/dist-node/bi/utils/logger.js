"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.logNPEInfo = logNPEInfo;

var _everLogger = _interopRequireDefault(require("@ali/4ever-logger"));

function logNPEInfo(table, info) {
  try {
    const tableRowColData = table.nodes.map(row => {
      const rowData = row.nodes.map(cell => {
        if (cell.data?.hidden) {
          return 'hidden';
        }

        return `${cell.data?.rowSpan || 1}-${cell.data?.colSpan || 1}`;
      });
      return rowData;
    });

    _everLogger.default.info({
      type: 'table_npe',
      info: JSON.stringify(info),
      tableRowColData: JSON.stringify(tableRowColData)
    }); // eslint-disable-next-line no-empty

  } catch (e) {}
}
//# sourceMappingURL=logger.js.map