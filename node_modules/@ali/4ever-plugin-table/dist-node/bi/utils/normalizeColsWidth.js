"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = normalizeColsWidth;

var _isNumber = _interopRequireDefault(require("./isNumber"));

var _constants = require("../../utils/constants");

function normalizeColsWidth(table) {
  const {
    colsWidth
  } = table.data;

  if (!Array.isArray(colsWidth)) {
    return Array(table.nodes[0].nodes.length).fill(_constants.MIN_COL_WIDTH);
  }

  return colsWidth.map(c => (0, _isNumber.default)(c) ? c : _constants.MIN_COL_WIDTH);
}
//# sourceMappingURL=normalizeColsWidth.js.map