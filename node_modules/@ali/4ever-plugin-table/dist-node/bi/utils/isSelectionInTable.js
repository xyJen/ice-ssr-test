"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isSelectionInTable;

var _types = require("../types");

function isSelectionInTable(value, configs) {
  const {
    document,
    selection
  } = value;

  if (!selection) {
    return false;
  }

  const start = selection.getStart(document);
  const table = document.getClosest(start.key, _types.isTable);

  if (!table) {
    return false;
  }

  if (table && configs?.excludeColumns && table.data.sr) {
    return false;
  }

  return true;
}
//# sourceMappingURL=isSelectionInTable.js.map