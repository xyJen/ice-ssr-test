"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = createTableRow;

var _everCangjie = require("@ali/4ever-cangjie");

var _createEmptyTableCell = _interopRequireDefault(require("./createEmptyTableCell"));

function createTableRow(colSize, originalTableRow) {
  const nodes = [];

  for (let i = 0; i < colSize; i += 1) {
    const data = {};

    if (originalTableRow) {
      const originalCell = originalTableRow.nodes[i];

      if (originalCell) {
        const originalCellData = originalCell.data;

        if (originalCellData && originalCellData.bdr) {
          data.bdr = originalCellData.bdr;
        }

        if (originalCellData && originalCellData.fill) {
          data.fill = originalCellData.fill;
        }
      }
    }

    nodes.push((0, _createEmptyTableCell.default)(data));
  }

  return _everCangjie.Block.create({
    type: 'table-row',
    nodes
  });
}
//# sourceMappingURL=createTableRow.js.map