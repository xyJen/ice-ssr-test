"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getVisibleCellOfRow;

function getVisibleCellOfRow(table, rowIndex) {
  const row = table.nodes[rowIndex];

  if (!row) {
    return null;
  }

  for (let i = 0; i < row.nodes.length; i += 1) {
    const cell = row.nodes[i];

    if (cell && !cell.data.hidden) {
      return cell;
    }
  }

  return null;
}
//# sourceMappingURL=getVisibleCellOfRow.js.map