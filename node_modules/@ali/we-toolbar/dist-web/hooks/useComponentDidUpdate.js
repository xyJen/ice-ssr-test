/* eslint-disable @typescript-eslint/no-explicit-any */
import { useRef, useEffect } from 'react';
export default (function (fn, conditions) {
  var isMounted = useRef(false);
  useEffect(function () {
    if (isMounted.current) {
      fn();
    } else {
      isMounted.current = true;
    }
  }, conditions);
});