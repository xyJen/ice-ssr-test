import { TOptionTitle } from '@ali/we-design';
import React from 'react';
import { SplitButtonOmit } from '../../../common/interface';
export declare type CellMergeStatus = 'unmerged' | 'merged';
export declare type IMergeCellButton = SplitButtonOmit & {
    /**
     * 合并状态（受控）
     */
    status?: CellMergeStatus;
    /**
     * 默认的合并状态（非受控）
     */
    defaultStatus?: CellMergeStatus;
    /**
     * 点击选项的回调函数，回调的入参为选择的选项对应状态
     */
    onSelect?: (e: CellMergeStatus) => void;
    /**
     * 点击左侧的回调函数，回调的入参为单元格 **当前** 的状态
     */
    onClick?: (e: CellMergeStatus) => void;
    /**
     * tooltip提示
     */
    title?: string | React.ReactNode;
    /**
     * 选项的tooltip提示
     */
    titles?: Record<string, TOptionTitle>;
    /**
     * 国际化语言
     */
    locale?: string;
    /**
     * 是否显示字
     */
    showTitle?: boolean;
};
export declare const MergeCellButton: React.FC<IMergeCellButton>;
