import React from 'react';
import { TOptionTitle } from '@ali/we-design';
import { IconSelectPropsOmit, Omit } from '../../../common/interface';
export declare type HorizontalAlign = 'left' | 'center' | 'right' | 'justify';
export interface IHorizontalAlignSelectProps extends IconSelectPropsOmit<HorizontalAlign> {
    /**
     * 默认的对齐方式，当设置了value，该项忽略
     */
    defaultValue?: HorizontalAlign;
    /**
     * 对齐方式
     */
    value?: HorizontalAlign;
    /**
     * 对齐方式修改时的回调函数
     */
    onChange?: (align: HorizontalAlign) => void;
    /**
     * 禁用的选项列表
     */
    disabledKeys?: HorizontalAlign[];
    /**
     * 选项的tooltip提示
     */
    titles?: Record<string, TOptionTitle>;
}
export declare const HorizontalAlignSelect: React.FC<Omit<IHorizontalAlignSelectProps, 'bordered'>>;
