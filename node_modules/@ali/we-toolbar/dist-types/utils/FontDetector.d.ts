/**
 * 20200126 引入 uuid，保证多实例 hook，避免多个hook同时对dom操作造成的判断失效
 * 保证每次调用能拿到同一个值
 */
declare class FontDetector {
    src: string;
    css: string;
    timer: number;
    private readonly uuid;
    private readonly styleUuid;
    private readonly iframeUuid;
    private detection;
    constructor();
    inject: () => void;
    remove: () => any;
    /**
     * 增加500ms的delay避免hooks快速触发导致的回掉失效
     * @param callback
     */
    ready: (callback: any) => any;
    destroy(): void;
    getResult: (fonts: string[], callback: any) => any;
    detect: (fonts: string[], callback: any) => any;
}
export declare const getSharedFontDetector: () => FontDetector;
export {};
