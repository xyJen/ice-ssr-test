import _extends from "@babel/runtime/helpers/extends";
import _inheritsLoose from "@babel/runtime/helpers/inheritsLoose";
import { Inline } from '@ali/4ever-cangjie';
import { ELEMENT_TYPE } from "../../utils/meta";

var InlinePlaceholder = /*#__PURE__*/function (_Inline) {
  _inheritsLoose(InlinePlaceholder, _Inline);

  function InlinePlaceholder() {
    return _Inline.apply(this, arguments) || this;
  }

  InlinePlaceholder.create = function create(properties) {
    if (typeof properties === 'string') {
      throw new Error('`InlinePlaceholder.create` does not accept string');
    }

    if (InlinePlaceholder.isInlinePlaceholder(properties)) {
      return properties;
    }

    return _Inline.create.call(this, _extends({}, properties, {
      type: InlinePlaceholder.TYPE
    }));
  };

  InlinePlaceholder.isInlinePlaceholder = function isInlinePlaceholder(maybeInlinePlaceholder) {
    return Inline.isInline(maybeInlinePlaceholder) && maybeInlinePlaceholder.type === InlinePlaceholder.TYPE;
  };

  return InlinePlaceholder;
}(Inline);

InlinePlaceholder.TYPE = ELEMENT_TYPE;
export { InlinePlaceholder as default };
//# sourceMappingURL=index.js.map