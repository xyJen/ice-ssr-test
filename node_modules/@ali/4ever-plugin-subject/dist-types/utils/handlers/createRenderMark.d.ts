import { Plugin } from '@ali/4ever-cangjie';
export interface RenderMarkOptions {
    /**
     * 是否注入 Injection
     * @default false
     */
    inject?: boolean;
}
declare function createRenderMark(options?: RenderMarkOptions): Plugin['renderMark'];
export default createRenderMark;
