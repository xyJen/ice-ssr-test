import { Value } from '@ali/4ever-cangjie';
/**
 * 首先判断Text节点的左侧/右侧是否有Inline Code
 * 如果有，则计算光标处于Inline Code左右侧Text节点的偏移量
 * @param value
 * @param side
 * @return
 */
export default function getTextOffsetOutsideInlineCode(value: Value, side: 'left' | 'right'): number | null;
