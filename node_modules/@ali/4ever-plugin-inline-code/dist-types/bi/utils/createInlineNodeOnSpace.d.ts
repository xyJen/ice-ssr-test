import { Controller } from '@ali/4ever-cangjie';
/**
 * 通过分隔符创建一个Inline Node
 * @param delimiter
 * @param size
 * @param insideWhitespace
 */
export default function createInlineNodeOnSpace(delimiter: string, size: number, insideWhitespace: boolean): (event: any, controller: Controller, next: any) => any;
