import { isKeyHotkey } from 'is-hotkey';
import toggleInlineCode from "../commands/toggleInlineCodeMark";
var isInlineCodeHotKey = isKeyHotkey('mod+e');
export default function onKeyDown(event, controller, next) {
  if (isInlineCodeHotKey(event)) {
    return toggleInlineCode(controller);
  }

  return next();
}
//# sourceMappingURL=onKeyDown.js.map