import _extends from "@babel/runtime/helpers/extends";
import { ItemKey } from '@ali/4ever-component';
import createInlineCodeButton from "./components/next/createInlineCodeToolbarButton";
export default function createRenderToolbar(locale) {
  return function (props, controller) {
    var buttonName = props.buttonName,
        extraProps = props.extraProps;

    if (buttonName === ItemKey.inlineCode) {
      return createInlineCodeButton(_extends({
        controller: controller,
        locale: locale
      }, extraProps));
    }

    return null;
  };
}
//# sourceMappingURL=createRenderToolbar.js.map