import InlineCode from "../../mo/models";
export default function isCursorInInlineCode(value) {
  var selection = value.selection;
  if (!selection || selection.isExpanded) return false; // 选区闭合，获取anchor即可

  var inline = value.anchorInline;
  return !!InlineCode.isInline(inline);
}
//# sourceMappingURL=isCursorInInlineCode.js.map