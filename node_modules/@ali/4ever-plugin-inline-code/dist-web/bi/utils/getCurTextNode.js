// 获取在选区闭合状态下光标所处于的TextNode
export default function getCurTextNode(value) {
  var selection = value.selection;

  if (selection.isCollapsed && selection.anchor.isTextPoint()) {
    return value.startText;
  }

  return null;
}
//# sourceMappingURL=getCurTextNode.js.map