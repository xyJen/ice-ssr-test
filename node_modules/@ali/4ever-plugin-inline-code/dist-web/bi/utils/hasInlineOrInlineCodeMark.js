import hasInlineCodeMark from "./hasInlineCodeMark";
export default function hasInlineOrInlineCodeMark(controller) {
  var _controller$value = controller.value,
      selection = _controller$value.selection,
      document = _controller$value.document,
      inlines = _controller$value.inlines;

  if (selection.isCollapsed) {
    return false;
  }

  var inlineCodeMark = hasInlineCodeMark(document, selection);
  return inlines.length > 0 || inlineCodeMark;
}
//# sourceMappingURL=hasInlineOrInlineCodeMark.js.map