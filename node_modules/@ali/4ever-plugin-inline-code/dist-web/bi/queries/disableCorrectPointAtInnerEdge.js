import InlineCode from "../../mo/models";
export function disableCorrectPointAtInnerEdge(controller, point) {
  var inline = controller.value.document.getClosestInline(point.key);

  if ((inline == null ? void 0 : inline.type) === InlineCode.TYPE) {
    return point.isAtStartOfNode(inline) || point.isAtEndOfNode(inline);
  }

  return false;
}
//# sourceMappingURL=disableCorrectPointAtInnerEdge.js.map