import { getAttributes, getTagName } from '@ali/4ever-utils';
import InlineCode from "../models";
export default {
  name: 'inlineCode',
  match: function match(node) {
    return getTagName(node) === InlineCode.TYPE;
  },
  convert: function convert(node) {
    return InlineCode.create({
      data: getAttributes(node) || {}
    });
  }
};
//# sourceMappingURL=jsonMLToValue.js.map