import _extends from "@babel/runtime/helpers/extends";
import _inheritsLoose from "@babel/runtime/helpers/inheritsLoose";
import { Inline } from '@ali/4ever-cangjie';

var InlineCode = /*#__PURE__*/function (_Inline) {
  _inheritsLoose(InlineCode, _Inline);

  function InlineCode() {
    return _Inline.apply(this, arguments) || this;
  }

  InlineCode.create = function create(properties) {
    if (typeof properties === 'string') {
      throw new Error('`InlineCode.create` does not accept string');
    }

    if (InlineCode.isInlineCode(properties)) {
      return properties;
    }

    return _Inline.create.call(this, _extends({}, properties, {
      type: InlineCode.TYPE
    }));
  };

  InlineCode.isInlineCode = function isInlineCode(maybeInlineCode) {
    return Inline.isInline(maybeInlineCode) && maybeInlineCode.type === InlineCode.TYPE;
  };

  return InlineCode;
}(Inline);

InlineCode.TYPE = 'inlineCode';
export { InlineCode as default };
//# sourceMappingURL=index.js.map