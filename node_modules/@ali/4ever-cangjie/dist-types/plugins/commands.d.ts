/**
 * @packageDocumentation
 * @ignore
 */
import Controller from '../controller/index';
import Command from '../interfaces/command';
import CommandEvent from '../interfaces/commandEvent';
export default function createCommandsPlugin(cmds?: Record<string, Command>): {
    models: never[];
    onCommand: (cmd: CommandEvent, controller: Controller, next: any) => any;
};
