export declare type UUID = string;
export declare type NodeKey = string;
/** UUID 映射表，key 为 uuid，value 为 node.key */
export declare type UUIDMap = Record<UUID, NodeKey>;
export interface UUIDConfig {
    /** 是否开启 UUID */
    enable: boolean;
    /** UUID 生成函数 */
    gen: () => string;
    /** UUID Key, 默认为 `__uuid__` */
    uuidKey?: string;
}
export interface UUIDSDiff {
    adds: Array<{
        uuid: UUID;
        key: NodeKey;
    }>;
    removes: UUID[];
    updates: Array<{
        oldUUID: UUID;
        newUUID: UUID;
    }>;
}
