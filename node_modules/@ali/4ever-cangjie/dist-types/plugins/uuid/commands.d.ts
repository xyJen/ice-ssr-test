import Controller from '../../controller';
import { ConvertToPluginCommands } from '../../interfaces/command';
import Node from '../../models/node';
import Element from '../../models/element';
export declare function setNodeUUID(controller: Controller, node: Element, uuid: string, uuidKey: string): Controller;
export declare function createGenerateUUID(gen: () => string, uuidKey: string): (controller: Controller, node: Node) => Controller;
declare module '@ali/4ever-cangjie' {
    /** type combination for commands、queries、actions */
    interface PluginCommands extends ConvertToPluginCommands<{
        generateUUID: ReturnType<typeof createGenerateUUID>;
    }> {
    }
}
