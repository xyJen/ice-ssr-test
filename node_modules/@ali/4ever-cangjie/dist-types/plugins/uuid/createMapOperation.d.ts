import Document from '../../models/document';
import Operation from '../../models/operation';
import { UUIDConfig, UUIDMap, UUIDSDiff } from './types';
declare const createMapOperations: (gen: UUIDConfig['gen'], uuidKey: string, getUUIDs: () => UUIDMap) => (operation: Operation, document: Document) => {
    op: Operation;
    diff: UUIDSDiff;
};
export default createMapOperations;
