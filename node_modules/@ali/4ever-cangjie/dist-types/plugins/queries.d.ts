/**
 * @packageDocumentation
 * @ignore
 */
import Controller from '../controller/index';
import Query from '../interfaces/query';
import QueryEvent from '../interfaces/queryEvent';
export default function createQueriesPlugin(qrys?: Record<string, Query>): {
    models: never[];
    onQuery: (qry: QueryEvent, controller: Controller, next: any) => any;
};
