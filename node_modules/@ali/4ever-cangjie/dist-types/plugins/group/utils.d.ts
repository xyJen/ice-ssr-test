import Controller from '../../controller';
import { Group, GroupWithKey } from '../../interfaces/group';
/**
 * 是否分组被折叠
 * @param group
 * @returns
 */
export declare function isGroupCollapsed(group: Group): boolean;
/**
 * 是否分组为空
 * @param controller
 * @param group
 * @returns
 */
export declare function isGroupEmpty(controller: Controller, group: GroupWithKey): boolean;
