import { Controller, Node, Document } from '..';
/**
 * 开启热区时，使用热区视图模型
 * @param controller
 * @param viewNode
 * @param viewDocument
 * @returns
 */
declare function useView<T extends Node>(controller: Controller, viewNode: T, viewDocument: Document, isSelected: any): {
    node: Node;
    document: Document;
    isSelected: any;
};
export default useView;
