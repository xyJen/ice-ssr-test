import { GroupWithKey } from '../interfaces/group';
import Node from '../models/node';
export declare type GroupDataSelector<T = any> = (groups: GroupWithKey[]) => T;
/**
 * 获得所在分组的数据
 * @param node
 * @param selector
 * @param defaultValue
 * @returns
 *
 * @example
 * ```tsx
 * const Element = props => {
 *   const hidden = useGroupData(groups => groups.some(group => group.hidden));
 *
 *   // ...
 * }
 * ```
 */
export default function useGroupData<T>(node: Node, selector: GroupDataSelector<T>, defaultValue: T): T;
