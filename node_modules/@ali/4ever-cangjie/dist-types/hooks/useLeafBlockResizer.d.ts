import * as React from 'react';
/**
 * 在 ref 上层对应的 leaf block 尺寸变动时，调用 updator。内置了 throttle。
 */
export default function useLeafBlockResizer(ref: React.RefObject<Element>, updator: Function, deps?: any[]): void;
