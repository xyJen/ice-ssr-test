import { Observable, Observer } from '../models/observable';
import { Terminal } from '../interfaces/controller';
export declare function usePendingTerminal(terminal$: Observable<Terminal | null>, onTerminate: Observer<Terminal | null>): void;
