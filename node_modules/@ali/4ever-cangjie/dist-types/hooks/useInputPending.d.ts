import { Observable } from '../models/observable';
import Path from '../models/path';
import Node from '../models/node';
import Document from '../models/document';
import { InputData } from '../interfaces/input';
import Controller from '../controller';
export interface PendingElements {
    document: Document;
    node: Node;
}
export declare function useInputPending<T extends InputData>(elements: PendingElements, nodePath: Path, controller: Controller, enablePending: boolean | undefined, pending$: Observable<T>, updateElements: (elements: PendingElements) => void): void;
