import Selection from '../../models/selection';
import Controller from '../../controller/index';
export declare type PopupPosition = {
    x: number;
    y: number;
    isTop: boolean;
    arrowOffset: number;
};
export declare const getPopupPosition: (selection: Selection, controller: Controller, popupWidth: number, popupHeight: number, defaultTop?: boolean) => PopupPosition | null;
