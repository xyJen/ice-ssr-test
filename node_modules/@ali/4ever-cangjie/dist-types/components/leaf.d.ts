import * as React from 'react';
export interface LeafComponentProps {
    leafKey: string;
    style?: React.CSSProperties;
}
declare const LeafComponent: React.FC<LeafComponentProps>;
export default LeafComponent;
