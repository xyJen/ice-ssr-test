/**
 * @packageDocumentation
 * @ignore
 */
import * as React from 'react';
import Selection from '../../../models/selection';
import Controller from '../../../controller';
declare type CaretMode = 'placeholder' | 'caret';
interface LocalCaretProps {
    controller: Controller;
    selection: Selection;
    color: string;
    position: {
        top: number;
        left: number;
        height: number;
    };
    style?: React.CSSProperties;
    mode?: CaretMode;
}
export default function LocalCaret(props: LocalCaretProps): JSX.Element;
export {};
