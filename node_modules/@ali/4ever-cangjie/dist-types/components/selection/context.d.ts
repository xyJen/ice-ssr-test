import React from 'react';
import Decoration from '../../models/decoration';
import Document from '../../models/document';
import Injection from '../../models/injection';
export interface RelativeRect extends Pick<DOMRect, 'top' | 'left' | 'width' | 'height'> {
    scrollLeft: number;
}
/**
 * 用于选区定位的依赖项
 *
 * @export
 * @interface SelectionContext
 */
export interface SelectionContext {
    contentRect?: DOMRect;
    relativeRect?: RelativeRect;
    zoom: number;
    resizeFlag: number;
    document: Document;
    injections: Injection[];
    decorations: Decoration[];
}
export declare const Context: React.Context<SelectionContext | null>;
/**
 * @deprecated
 */
export declare function useSelectionContext(): SelectionContext;
