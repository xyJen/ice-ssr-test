/**
 * @packageDocumentation
 * @module Commands
 */
import Controller from '../controller/index';
import Path from '../models/path';
/**
 * 删除指定位置的Node，并添加到parentPath的第index处
 * 1. 如果传入无效的节点，会抛异常。
 */
export declare function moveNodeByKey(controller: Controller, key: string, parentKey: string, index: number): Controller;
/**
 * 删除指定位置的Node，并添加到parentPath的第index处
 * 1. 如果传入无效的节点，会抛异常。
 */
export declare function moveNodeByPath(controller: Controller, path: Path, parentPath: Path, index: number): Controller;
