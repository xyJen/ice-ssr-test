/**
 * @packageDocumentation
 * @module Commands
 */
import Controller from '../controller/index';
import Path from '../models/path';
/**
 * 把目标 path 的 Node，合并到其上一个 Sibling 上。
 * 1. 必须满足 Node 和上一个 Sibling 是同一种类型。
 * 2. 如果没有上一个 Sibling，直接抛异常。
 */
export declare function mergeNodeByKey(controller: Controller, key: string): Controller;
/**
 * 把目标 path 的 Node，合并到其上一个 Sibling 上。
 * 1. 必须满足 Node 和上一个 Sibling 是同一种类型。
 * 2. 如果没有上一个 Sibling，直接抛异常。
 */
export declare function mergeNodeByPath(controller: Controller, path: Path): Controller;
