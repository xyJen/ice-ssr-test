import Controller from '../controller/index';
import Path from '../models/path';
/**
 * 删除指定位置的节点。
 * 1. 如果传入无效的节点，会抛异常。
 */
export declare function removeNodeByKey(controller: Controller, key: string): Controller;
/**
 * 删除指定位置的节点。
 * 1. 如果传入无效的节点，会抛异常。
 */
export declare function removeNodeByPath(controller: Controller, path: Path): Controller;
/**
 * 删除指定位置的文字。
 * 1. 如果传入无效的节点，会抛异常。
 */
export declare function removeTextByKey(controller: Controller, key: string, offset: number, text: string): Controller;
/**
 * 删除指定位置的文字。
 * 1. 如果传入无效的节点，会抛异常。
 * @text 删除的文字；如果长度大于可用长度，按照可用长度计算
 */
export declare function removeTextByPath(controller: Controller, path: Path, offset: number, text: string): Controller;
