import Controller from '../controller/index';
import Selection from '../models/selection';
import Block from '../models/block';
import { MoveUnit } from '../queries/selection';
/**
 * 删除 Selection 内的所有内容
 */
export declare function deleteAtRange(controller: Controller, range: Selection): Controller;
/**
 * 删除当前Selection内的所有内容
 * 1. 将选区开头的样式设置到 selection.marks 中
 */
export declare function del(controller: Controller): Controller;
/**
 * 在指定selection上，进行往后删除。
 * @range 如果是expand选区，会删除选区内的内容，并忽略掉后续参数；否则从anchor点往后计算n个unit进行删除
 */
export declare function deleteBackwardAtRange(controller: Controller, range: Selection, n?: number, unit?: MoveUnit): Controller;
/**
 * 在指定selection上，进行往前删除。
 * @range 如果是expand选区，会删除选区内的内容，并忽略掉后续参数；否则从anchor点往前计算n个unit进行删除
 */
export declare function deleteForwardAtRange(controller: Controller, range: Selection, n?: number, unit?: MoveUnit): Controller;
/**
 * 从所有当前选区的位置，往后删除n个offset
 */
export declare function deleteBackward(controller: Controller, n?: number): Controller;
/**
 * 从所有当前选区的位置，往前删除n个offset
 */
export declare function deleteForward(controller: Controller, n?: number): Controller;
/**
 * @ignore
 */
export declare const deleteBoundrayBackward: (controller: Controller, getBoundaryOrIteratePositionsFromPoint: number | ((block: Block, offset: number) => number), unit?: MoveUnit) => Controller;
/**
 * @ignore
 */
export declare const deleteBoundrayForward: (controller: Controller, getBoundaryOrIteratePositionsFromPoint: number | ((block: Block, offset: number) => number), unit?: MoveUnit) => Controller;
/**
 * 从所有当前选区的位置，往后删除1个文字（大多情况下，1个文字等于1个offset，但是1个emoji可能对应多个offset）
 */
export declare function deleteCharBackward(controller: Controller): Controller;
/**
 * 从所有当前选区的位置，往前删除1个文字（大多情况下，1个文字等于1个offset，但是1个emoji可能对应多个offset）
 */
export declare function deleteCharForward(controller: Controller): Controller;
/**
 * 从所有当前选区的位置，往后删除1个词
 */
export declare function deleteWordBackward(controller: Controller): Controller;
/**
 * 从所有当前选区的位置，往前删除1个词
 */
export declare function deleteWordForward(controller: Controller): Controller;
/**
 * 从所有当前选区的位置，往后删除1行文字
 */
export declare function deleteLineBackward(controller: Controller): Controller;
/**
 * 从所有当前选区的位置，往前删除1行文字
 */
export declare function deleteLineForward(controller: Controller): Controller;
