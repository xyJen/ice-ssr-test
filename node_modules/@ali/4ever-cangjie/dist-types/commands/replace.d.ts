/**
 * @packageDocumentation
 * @module Commands
 */
import Controller from '../controller/index';
import Mark from '../models/mark';
import Path from '../models/path';
import Node from '../models/node';
/**
 * 在指定的位置上，替换node
 */
export declare function replaceNodeByKey(controller: Controller, key: string, newNode: Node): Controller;
/**
 * 在指定的位置上，替换node
 */
export declare function replaceNodeByPath(controller: Controller, path: Path, newNode: Node): Controller;
/**
 * 在指定的位置上，替换文本，并附加marks
 * 1. 提过oldText长度大于可用长度，按照可用长度计算
 */
export declare function replaceTextByKey(controller: Controller, key: string, offset: number, oldText: string, text: string, marks?: Mark[]): Controller;
/**
 * 在指定的位置上，替换文本，并附加marks
 * 1. 提过oldText长度大于可用长度，按照可用长度计算
 */
export declare function replaceTextByPath(controller: Controller, path: Path, offset: number, oldText: string, text: string, marks?: Mark[]): Controller;
