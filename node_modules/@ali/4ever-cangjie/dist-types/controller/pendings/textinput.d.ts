import { ShouldAppend, PendingPattern } from '../../interfaces/pending';
import Path from '../../models/path';
export interface TextInputContext {
    /** 输入作用的 text node key */
    key: string;
    /** 输入开始的 path */
    startPath: Path;
    /** 输入开始的偏移 */
    startOffset: number;
}
export declare const shouldAppend: ShouldAppend;
declare const pattern: PendingPattern<TextInputContext>;
export default pattern;
