export default abstract class ImmutableRecord {
    protected freeze(): void;
    abstract merge(props: Partial<object>): ImmutableRecord;
    abstract set<K extends keyof object>(key: K, value: object[K]): ImmutableRecord;
}
