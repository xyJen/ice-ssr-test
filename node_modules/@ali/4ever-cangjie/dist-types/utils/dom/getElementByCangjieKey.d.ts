import Controller from '../../controller';
export declare const ROOT_TO_CONTROLLER: WeakMap<Document | Element, Controller>;
export declare const CONTROLLER_TO_KEY_DOM: WeakMap<Controller, Map<string, HTMLElement>>;
/**
 * 这里原先作用应该是提供缓存命中率，由于内存泄露问题，暂时中断GLOBAL_KEY_DOM的缓存
 * 不确定什么场景下CONTROLLER_TO_KEY_DOM会失效，需要用到GLOBAL_KEY_DOM
 * 如果需要继续维持的话，这里也可以通过documentKey的维度进行缓存，类似：
 * const GLOBAL_KEY_DOM = new Map<string, Map<string, HTMLElement>>();
 */
export declare const GLOBAL_KEY_DOM: Map<string, HTMLElement>;
declare function getElementByCangjieKey(key: string, root: Element | Document): HTMLElement | null;
export default getElementByCangjieKey;
