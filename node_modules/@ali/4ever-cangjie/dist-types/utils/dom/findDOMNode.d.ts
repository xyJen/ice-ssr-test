/**
 * 根据节点的 key 查找对应的 DOM 节点。如果找不到，就返回 null
 */
export declare function findDOMNodeSafely(key: string, root?: Window | Element): HTMLElement | undefined;
/**
 * 根据节点的 key 查找对应的 DOM 节点。如果找不到，就 throw error。
 * 类似的 API：domUtils.findDOMNodeSafely
 */
declare function findDOMNode(key: string, root?: Window | Element): HTMLElement;
export default findDOMNode;
