/**
 * @packageDocumentation
 * @module domUtils
 */
import { TextPoint } from '../../models/point';
/**
 * 根据 Point 来获取浏览器的 HTML 节点、offset。
 */
declare function findDOMPoint(point: TextPoint, root?: Window | Element): {
    node: ChildNode | null;
    offset: number;
} | null;
export default findDOMPoint;
