/**
 * @packageDocumentation
 * @module domUtils
 */
/**
 * 获取 DOM 的真实选区
 * @param root
 * @description 由于编辑器层没有浏览器的选区，所以使用 mark 的方式自行构造选区
 */
export default function findCurrentDOMRange(root?: HTMLElement | Document): Range | null;
