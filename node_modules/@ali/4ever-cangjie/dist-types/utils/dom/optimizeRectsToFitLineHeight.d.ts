export declare type Rect = {
    left: number;
    top: number;
    width: number;
    height: number;
};
declare type RectRow = {
    top: number;
    height: number;
};
/**
 * 获取分行。`rows` 构建了分行的结构，每一个 row 表示一行
 */
export declare function splitRows(blockDOMNode: Element, extendToFill?: boolean): RectRow[];
/**
 * 选区、色块抗锯齿优化，调整 rect 的高度，使属于同一行的 rect 高度相同。
 */
export declare function fitRows(rows: RectRow[], rects: Rect[], offsetRect: Rect): Rect[];
/**
 * 相邻的 rect 进行合并，避免碎片化的色块
 */
export declare function mergeRects(rects: Rect[]): Rect[];
export {};
