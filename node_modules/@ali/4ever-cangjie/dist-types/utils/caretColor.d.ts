/**
 * @packageDocumentation
 * @ignore
 */
import { EdgePoint, TextPoint } from '..';
declare type RGBATuple = [number, number, number, number];
/**
 * @deprecated
 * 获取当前元素的显示背景色。
 * 从此元素的 parent 依次向上，知道找到一个设置背景色的 element 为止。
 * 如果一直没找到，则返回白色。
 */
export declare function getBackgroundColor(at: Element, caretContainer: Element | null): RGBATuple;
export declare function getBgColor(targetBlock: HTMLElement, at: {
    top: number;
    left: number;
    height: number;
}, point: TextPoint | EdgePoint): RGBATuple;
/**
 * 根据背景色，获取光标的颜色。
 */
export declare function getCaretColor(backgroundColor: RGBATuple): "black" | "white";
export {};
