/**
 * 插件 action 创建快捷方法，解决 action 的 typing 问题
 */
import type { default as Action } from '../interfaces/action';
export declare const createActionHandlers: <P extends {
    [key: string]: any;
} = {}>(type: symbol) => [(payload?: P | undefined) => Action<P>, (action: Action<any>) => action is Action<P>];
