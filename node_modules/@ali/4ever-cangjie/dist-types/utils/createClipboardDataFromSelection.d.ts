import CangjieDataTransfer from '../models/dataTransfer';
import Document from '../models/document';
import Controller from '../controller';
import { UserData } from '../controller/userData';
export default function createClipboardDataFromSelection(controller: Controller): CangjieDataTransfer;
/**
 * 将节点的用户数据复制到 fragment 中
 * @param fragment
 * @param userData
 * @param uid
 * @returns
 */
export declare function withUserData(fragment: Document, userData?: UserData, uid?: string): Document;
