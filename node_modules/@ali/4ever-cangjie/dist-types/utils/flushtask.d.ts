/**
 * @packageDocumentation
 * @ignore
 */
export declare enum Status {
    Idle = "Idle",
    Pending = "Pending",
    Canceled = "Canceled",
    Done = "Done"
}
/**
 * WARN: Sometimes request idle callback in Safari will never be called!
 */
export declare type FlushType = 'microtask' | 'frametask' | 'idletask';
export interface FlushTaskOptions {
    flushType?: FlushType;
}
declare class FlushTask {
    static create(task: Function, options?: FlushTaskOptions): FlushTask;
    /**
     * 等待执行的微任务
     */
    private task;
    /**
     * 当前任务的状态
     */
    private status;
    /**
     * 是否已经 resolved，保障一个任务只执行一次函数
     */
    private resolved;
    /**
     * 原始的 flushType
     */
    private flushType;
    /**
     * 当前支持的 flushType
     */
    private supportedFlushType;
    private taskId;
    private constructor();
    start(): FlushTask;
    cancel(): FlushTask;
    getStatus(): Status;
    private requestTask;
    private cancelTask;
    private run;
}
export default FlushTask;
