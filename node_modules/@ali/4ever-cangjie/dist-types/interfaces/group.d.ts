import { GROUP_COLLAPSED_KEY } from "../constants";
export interface Group<T = Record<string, any>> {
    type: string;
    data: T & {
        [GROUP_COLLAPSED_KEY]: boolean;
    };
}
export declare type GroupWithKey = Group & {
    key: string;
};
