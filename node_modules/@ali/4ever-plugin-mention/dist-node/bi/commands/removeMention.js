"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.cancelPlaceholder = exports.default = void 0;

var _default = controller => {
  const {
    value
  } = controller;
  const {
    decorations
  } = value;
  const newDecorations = decorations.filter(item => item.mark.type !== 'mention' && item.mark.type !== 'quick-insert');
  return controller.setDecorations(newDecorations);
};

exports.default = _default;

const cancelPlaceholder = (controller, key) => {
  const {
    value: {
      injections
    }
  } = controller;
  const newInjections = injections.filter(inj => inj.key !== key);
  return controller.setInjections(newInjections);
};

exports.cancelPlaceholder = cancelPlaceholder;
//# sourceMappingURL=removeMention.js.map