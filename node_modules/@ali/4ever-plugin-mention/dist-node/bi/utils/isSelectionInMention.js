"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _default = value => {
  const {
    document,
    selection,
    decorations
  } = value;

  if (!selection) {
    return false;
  }

  const start = selection.getStart(document);

  if (!start.key) {
    return false;
  }

  const closeInline = document.getClosestInline(start.key);
  if (closeInline && closeInline.type === 'mention') return true;
  return value.inlines.some(inline => inline.type === 'mention') || decorations.some(decoration => decoration.mark.type === 'mention');
};

exports.default = _default;
//# sourceMappingURL=isSelectionInMention.js.map