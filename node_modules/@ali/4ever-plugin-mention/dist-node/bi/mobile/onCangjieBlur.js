"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = onCangjieBlur;

function onCangjieBlur(controller, next) {
  const {
    value
  } = controller;
  const {
    decorations
  } = value;
  const mentionDecorations = decorations.filter(item => item.mark.type === 'mention');

  if (mentionDecorations.length > 0) {
    controller.dispatch('cancelMentionSelect');
  }

  return next();
}
//# sourceMappingURL=onCangjieBlur.js.map