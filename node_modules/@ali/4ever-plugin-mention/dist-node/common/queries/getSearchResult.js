"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getSearchResult = void 0;

const getSearchResult = (controller, findText, node) => {
  // TODO: 这里为什么可能为空？
  if (!node) return undefined;
  const {
    type,
    key,
    data: {
      name
    }
  } = node;

  if (type === 'mention' && `@${name}`.includes(findText)) {
    return {
      type,
      key,
      readonly: true
    };
  }

  return undefined;
};

exports.getSearchResult = getSearchResult;
//# sourceMappingURL=getSearchResult.js.map