import { Controller } from '@ali/4ever-cangjie';
export default function getPanelPosition(_: any, { controller, scrollableContainer, portalDom, align }: {
    controller: Controller;
    scrollableContainer: HTMLElement;
    portalDom: HTMLElement;
    align: string;
}): {
    left: number;
    top: number;
};
