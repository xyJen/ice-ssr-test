import { Controller, Action } from '@ali/4ever-cangjie';
import { MentionPluginConfig } from '../types';
export default function createOnAction({ onSuggestionSelected, }: MentionPluginConfig): (action: Action, controller: Controller, next: () => void) => void | Controller;
