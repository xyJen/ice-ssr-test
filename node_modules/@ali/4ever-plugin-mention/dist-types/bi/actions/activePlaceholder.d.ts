import { Inline, Action } from '@ali/4ever-cangjie';
export interface ActivePlaceholderAction {
    type: typeof MENTION_PLACEHOLDER_ACTIVE;
    payload: {
        node: Inline;
    };
}
export declare const MENTION_PLACEHOLDER_ACTIVE: unique symbol;
export declare function activePlaceholder(node: Inline): ActivePlaceholderAction;
export declare function isActivePlaceholder(action: Action): action is ActivePlaceholderAction;
