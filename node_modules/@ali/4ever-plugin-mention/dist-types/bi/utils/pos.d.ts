import { Controller } from '@ali/4ever-cangjie';
export default function getPos(controller: Controller, scrollableContainer: HTMLElement, portalDom: HTMLElement, align?: string): {
    left: number;
    top: number;
};
