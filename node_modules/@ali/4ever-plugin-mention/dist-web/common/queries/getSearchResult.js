export var getSearchResult = function getSearchResult(controller, findText, node) {
  // TODO: 这里为什么可能为空？
  if (!node) return undefined;
  var type = node.type,
      key = node.key,
      name = node.data.name;

  if (type === 'mention' && ("@" + name).includes(findText)) {
    return {
      type: type,
      key: key,
      readonly: true
    };
  }

  return undefined;
};
//# sourceMappingURL=getSearchResult.js.map