import _extends from "@babel/runtime/helpers/extends";
import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import Mention from "../mention";
export function createRenderNode(mentionConfig) {
  var onMentionClick = mentionConfig.onMentionClick,
      onMentionHover = mentionConfig.onMentionHover,
      getCurrentUser = mentionConfig.getCurrentUser,
      _mentionConfig$locale = mentionConfig.locale,
      locale = _mentionConfig$locale === void 0 ? {} : _mentionConfig$locale;

  var renderNode = function renderNode(props, _) {
    var node = props.node;
    var attributes = props.attributes;
    return /*#__PURE__*/_createElement(Mention, _extends({}, attributes, {
      node: node,
      onMentionClick: onMentionClick,
      onMentionHover: onMentionHover,
      getCurrentUser: getCurrentUser,
      locale: locale
    }));
  };

  return {
    mention: renderNode
  };
}
//# sourceMappingURL=createRenderNode.js.map