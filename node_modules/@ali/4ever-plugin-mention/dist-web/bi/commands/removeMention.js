export default (function (controller) {
  var value = controller.value;
  var decorations = value.decorations;
  var newDecorations = decorations.filter(function (item) {
    return item.mark.type !== 'mention' && item.mark.type !== 'quick-insert';
  });
  return controller.setDecorations(newDecorations);
});
export var cancelPlaceholder = function cancelPlaceholder(controller, key) {
  var injections = controller.value.injections;
  var newInjections = injections.filter(function (inj) {
    return inj.key !== key;
  });
  return controller.setInjections(newInjections);
};
//# sourceMappingURL=removeMention.js.map