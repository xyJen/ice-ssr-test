import { default as insertMention } from "./insertMention";
import { default as insertMentions } from "./insertMentions";
import { default as removeMention } from "./removeMention";
import { default as setMention } from "./setMention";
export var commands = {
  insertMention: insertMention,
  insertMentions: insertMentions,
  removeMention: removeMention,
  setMention: setMention
};
//# sourceMappingURL=index.js.map