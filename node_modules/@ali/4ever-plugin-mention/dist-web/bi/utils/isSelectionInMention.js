export default (function (value) {
  var document = value.document,
      selection = value.selection,
      decorations = value.decorations;

  if (!selection) {
    return false;
  }

  var start = selection.getStart(document);

  if (!start.key) {
    return false;
  }

  var closeInline = document.getClosestInline(start.key);
  if (closeInline && closeInline.type === 'mention') return true;
  return value.inlines.some(function (inline) {
    return inline.type === 'mention';
  }) || decorations.some(function (decoration) {
    return decoration.mark.type === 'mention';
  });
});
//# sourceMappingURL=isSelectionInMention.js.map