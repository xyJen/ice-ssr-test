export default (function (value) {
  var decorations = value.decorations;
  var mentionSuggestion = decorations.find(function (decoration) {
    return decoration.mark.type === 'mention';
  });

  if (!mentionSuggestion) {
    return false;
  } // TODO: 等新版 slate
  //   selection.isCollapsed && selection.start.isInRange(mentionSuggestion);


  return true;
});
//# sourceMappingURL=isSelectionInMentionSuggestion.js.map