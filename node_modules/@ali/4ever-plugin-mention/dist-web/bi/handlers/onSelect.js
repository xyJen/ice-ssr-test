import removeMention from "../commands/removeMention";
export default function onSelect(event, controller, next) {
  var value = controller.value;
  var selection = value.selection,
      decorations = value.decorations;
  var mentionDecorations = decorations.find(function (item) {
    return item.mark.type === 'mention';
  });
  var isCollapsed = selection.isCollapsed;
  var focus = selection.focus;

  if (!isCollapsed) {
    removeMention(controller);
  }

  if (isCollapsed && mentionDecorations) {
    if (focus.offset > mentionDecorations.start.offset || focus.offset < mentionDecorations.end.offset) {
      removeMention(controller);
    }
  }

  return next();
}
//# sourceMappingURL=onSelect.js.map