import _extends from "@babel/runtime/helpers/extends";
import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import MentionTrigger from "./components/mentionTrigger";

function createRenderEditable(config) {
  // @ts-ignore
  return function renderEditable(_, controller, next) {
    var isNecessary = controller.query('isSelectionInMentionSuggestion');

    if (!isNecessary) {
      return next();
    }

    var text = controller.query('getMentionKeyword');
    return /*#__PURE__*/_createElement(React.Fragment, null, next(), /*#__PURE__*/_createElement(MentionTrigger, _extends({
      text: text
    }, config, {
      isComposing: !!controller.value.composing,
      controller: controller
    })));
  };
}

export default createRenderEditable;
//# sourceMappingURL=createRenderEditable.js.map