export default function onCangjieBlur(controller, next) {
  var value = controller.value;
  var decorations = value.decorations;
  var mentionDecorations = decorations.filter(function (item) {
    return item.mark.type === 'mention';
  });

  if (mentionDecorations.length > 0) {
    controller.dispatch('cancelMentionSelect');
  }

  return next();
}
//# sourceMappingURL=onCangjieBlur.js.map