import { Commands } from '@ali/4ever-cangjie';
/**
 * 移动端取消时默认插入一个
 */

export default function removeMentionMark(controller) {
  var value = controller.value;
  var decorations = value.decorations;
  var newDecorations = decorations.filter(function (item) {
    return item.mark.type !== 'mention';
  });
  return controller.setDecorations(newDecorations).command(Commands.focus);
}
//# sourceMappingURL=removeMention.js.map