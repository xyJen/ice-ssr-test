import getPos from "../utils/pos";
export function getMentionPos(controller, scrollableContainer, portalDom, align) {
  if (align === void 0) {
    align = 'follow';
  }

  return getPos(controller, scrollableContainer, portalDom, align);
}
//# sourceMappingURL=getMentionPos.js.map