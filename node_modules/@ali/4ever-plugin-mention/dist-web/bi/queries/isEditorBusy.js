export default function isEditorBusy(controller) {
  var hasMention = controller.value.decorations.some(function (_ref) {
    var mark = _ref.mark;
    return mark.type === 'mention';
  });

  if (hasMention) {
    return true;
  }

  return undefined;
}
//# sourceMappingURL=isEditorBusy.js.map