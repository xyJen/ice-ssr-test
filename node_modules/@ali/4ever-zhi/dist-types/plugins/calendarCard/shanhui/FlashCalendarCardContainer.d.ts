import { CalendarCardPlugin } from '@ali/4ever-bamboo';
import { JumpToCalendarEventType } from '../type';
interface IProps {
    locale?: {
        refresh: string;
        refreshTips: string;
        receiverCount: string;
        viewCalendardetails: string;
        noresponse: string;
        received: string;
        refused: string;
        pending: string;
        organizer: string;
    };
    metadata: CalendarCardPlugin.CalendarCardMetadataType;
    jumpToCalendar?: (link?: string, eventType?: JumpToCalendarEventType) => void;
    openProfile?: (uid: number) => void;
}
export default function FlashCalendarCardContainer(props: IProps): JSX.Element;
export {};
