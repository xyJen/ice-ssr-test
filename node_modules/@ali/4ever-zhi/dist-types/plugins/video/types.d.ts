/// <reference types="react" />
import { EmbedCommonPluginConfig } from '../embed/types';
/**
 * @packageDocumentation
 * @module Plugin-Video
 */
export declare type VideoPluginConfig = EmbedCommonPluginConfig & {
    /**
     * 音视频 URL 转换。如果是音视频类型，这里可以获取可播放URL
     */
    transformVideoURL?: (url: string) => string;
    /**
     * 音视频文件，加载成功
     */
    onMediaLoadSuccess?: (url: string) => void;
    /**
     * 音视频文件，加载失败
     */
    onMediaLoadError?: (url: string) => void;
    /**
     * 视频是否允许全屏
     */
    allowFullscreen?: boolean;
    /**
     * 渲染 videoMask，用于在视频上传渲染自定义的一些内容，如转码过程
     */
    renderVideoMask?: ({ node, canPlay, reload }: {
        node: any;
        canPlay: any;
        reload: any;
    }) => React.ReactElement | null;
    /**
     * 动态设置视频 poster ，调用此方法设置的 poster 地址不落库
     */
    setTempPosterUrl?: (data: any) => Promise<string>;
    /**
     * 视频是否允许预览，默认为 true，长图等场景目前设置为 false
     */
    enableVideoPreview?: boolean;
};
