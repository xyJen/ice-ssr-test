import { Plugin } from '@ali/4ever-cangjie';
import { SubjectConfig } from '../types';
declare function createRenderNode(config: SubjectConfig): Plugin['renderNode'];
export default createRenderNode;
