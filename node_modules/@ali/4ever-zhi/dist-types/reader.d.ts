/**
 * @packageDocumentation
 * @module Reader
 */
import * as React from 'react';
import { Controller, Plugin, ContentOwnProps, Value, Change, ControllerOptions, ControllerConfigs, FlushType, DocStyle } from '@ali/4ever-cangjie';
/**
 * 编辑器只读组件的选项。
 */
export interface ZhiOwnProps extends ContentOwnProps {
    /**
     * 展示相应 uid 下的用户数据
     */
    dataUid?: string | number | undefined;
    /**
     * 插件列表。
     */
    plugins: Plugin[];
    /**
     * 编辑器数据。
     */
    content: Value;
    /**
     * 是否允许拷贝
     */
    canCopy?: boolean;
    /**
     * 编辑器参数
     */
    options?: ControllerOptions;
    /**
     * 编辑器的调度模式
     */
    flushType?: FlushType;
    /**
     * 产生变更回调，主要的变更内容来源于选区操作，如果不注入回调，则组件内部会自行更新视图数据
     */
    onChange?: (change: Change) => void;
    /**
     * 编辑器无内容时，render 回调。
     */
    renderEmpty?: () => React.ReactNode;
    /**
     * 获取编辑器的 controller 对象。
     */
    controllerRef?: (controller: Controller) => void;
    /**
     * 给编辑器提供滚动的 dom 节点，用户后续使用 useScrollableContrainer 来获取。默认是 window 对象。
     */
    getScrollableContainer?: () => HTMLElement | Window;
    getScrollableContent?: () => HTMLElement;
    /**
     * 编辑器配置
     *
     * 目前提供的配置：移动端「复制粘贴」气泡的国际化文案配置
     */
    configs?: ControllerConfigs;
    /**
     * 文档样式表
     */
    docStyle?: DocStyle;
}
export declare type ZhiProps = ZhiOwnProps & Record<string, any>;
/**
 * 编辑器只读组件
 */
declare function Zhi(props: ZhiProps): JSX.Element;
export default Zhi;
