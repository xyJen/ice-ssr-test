import _extends from "@babel/runtime/helpers/extends";
import { zhiCardFactory as cardFactory } from '@ali/4ever-factory';
import locale from "../../locales/zh_CN";
import { VoteCardPlugin } from '@ali/4ever-bamboo';
import createRenderNode from "./createRenderNode";
var createOnAction = VoteCardPlugin.createOnAction,
    createQueries = VoteCardPlugin.createQueries;

var createPlugin = function createPlugin(bizConfig) {
  var voteCardConfig = _extends({}, locale.voteCard, bizConfig.voteCard);

  var renderNode = createRenderNode(voteCardConfig);
  var onAction = createOnAction(voteCardConfig);
  return {
    cardType: 'vote',
    onAction: onAction,
    renderNode: renderNode,
    queries: createQueries()
  };
};

var createBiVoteCardPlugin = cardFactory(createPlugin);
export default createBiVoteCardPlugin;
//# sourceMappingURL=index.js.map