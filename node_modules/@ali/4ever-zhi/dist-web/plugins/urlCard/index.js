import { zhiCardFactory as cardFactory } from '@ali/4ever-factory';
import { NoSupportPlaceholder } from '@ali/we-biz-url-card';
import React from 'react';
var _createElement = /*#__PURE__*/React.createElement;

var _ref = /*#__PURE__*/_createElement(NoSupportPlaceholder, null);

var URLCard = function URLCard(config) {
  var _config$urlCard;

  var isMobile = (_config$urlCard = config.urlCard) == null ? void 0 : _config$urlCard.isMobile;
  return {
    cardType: 'url',
    isMobile: isMobile,
    renderNode: function renderNode() {
      // 导出长图时统一返回占位符
      return _ref;
    }
  };
};

export default cardFactory(URLCard);
//# sourceMappingURL=index.js.map