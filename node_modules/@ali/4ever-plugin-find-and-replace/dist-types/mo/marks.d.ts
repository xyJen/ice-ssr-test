import { Mark, Data } from '@ali/4ever-cangjie';
interface MatchMarkData extends Data {
    selected: boolean;
}
export declare type IMatchMark = Mark<MatchMarkData>;
export declare const MatchMark: {
    TYPE: string;
    create(selected: boolean): Mark<MatchMarkData>;
    isMatchMark(maybeMark: any): maybeMark is IMatchMark;
};
export {};
