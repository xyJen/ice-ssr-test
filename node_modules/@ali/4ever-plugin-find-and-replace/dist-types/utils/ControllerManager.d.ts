import { Controller } from '@ali/4ever-cangjie';
export default class ControllerManager {
    ctls: Controller[];
    mainController: Controller | null;
    constructor(controllers?: []);
    push(c: Controller, role?: string): void;
    remove(c: Controller): void;
    isMainController(c: Controller): boolean;
    run(fn: (c: Controller) => void): void;
    runWithSchedule(fn: (c: Controller) => void, mainController?: Controller | null): void;
}
