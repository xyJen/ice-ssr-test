import { Controller, RenderContentProps } from '@ali/4ever-cangjie';
import { FindAndReplaceConfig } from '../utils/types';
import type { ControllerManager } from '../utils';
export default function createRenderContent(config: FindAndReplaceConfig, ctlManager: ControllerManager): (props: RenderContentProps, controller: Controller, next: any) => any;
