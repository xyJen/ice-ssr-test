import _extends from "@babel/runtime/helpers/extends";

var mergeData = function mergeData(controller, data) {
  var value = controller.value;
  var newData = Object.keys(data).reduce(function (tgt, key) {
    var _extends2;

    var v = data[key];
    return _extends({}, tgt, (_extends2 = {}, _extends2[key] = v, _extends2));
  }, value.data);
  return controller.setData(newData);
};

export default mergeData;
//# sourceMappingURL=mergeData.js.map