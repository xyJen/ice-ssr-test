var isReadonlyMatch = function isReadonlyMatch(match) {
  return match == null ? void 0 : match.type.includes('readonly');
};

export default isReadonlyMatch;
//# sourceMappingURL=isReadonlyMatch.js.map