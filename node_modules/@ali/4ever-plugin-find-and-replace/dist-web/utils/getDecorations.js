import { MatchMark } from "../mo/marks";

var getMatchedDecorations = function getMatchedDecorations(decorations) {
  return decorations.filter(function (d) {
    return MatchMark.isMatchMark(d.mark);
  });
};

var getNoMatchedDecorations = function getNoMatchedDecorations(decorations) {
  return decorations.filter(function (d) {
    return !MatchMark.isMatchMark(d.mark);
  });
};

export { getMatchedDecorations, getNoMatchedDecorations };
//# sourceMappingURL=getDecorations.js.map