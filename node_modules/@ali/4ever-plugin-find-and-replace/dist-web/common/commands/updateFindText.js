import { mergeData } from "../../utils";
export default function updateFindText(controller, findText) {
  var data = controller.value.data;
  var prevFindText = data.findText;
  return mergeData(controller, {
    findText: findText,
    prevFindText: prevFindText
  });
}
//# sourceMappingURL=updateFindText.js.map