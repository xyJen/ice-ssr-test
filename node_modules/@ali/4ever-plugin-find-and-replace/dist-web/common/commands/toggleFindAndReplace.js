import { Commands } from '@ali/4ever-cangjie';
import { mergeData } from "../../utils";
export default function toggleFindAndReplace(controller, forceVisible) {
  var data = controller.value.data;
  var visible = data.findAndReplaceVisible || false;
  var newVisible = forceVisible !== undefined ? forceVisible : !visible;
  mergeData(controller, {
    findAndReplaceVisible: newVisible
  });
  controller.command(newVisible ? Commands.blur : Commands.focus);
  return controller;
}
//# sourceMappingURL=toggleFindAndReplace.js.map