import _extends from "@babel/runtime/helpers/extends";
import { mergeData } from "../../utils";
export default function updateSearchResult(controller, focus) {
  var data = controller.value.data;
  var _data$searchResult = data.searchResult,
      searchResult = _data$searchResult === void 0 ? [] : _data$searchResult;
  var focusIndex = searchResult.indexOf(focus);
  var matches = searchResult.map(function (match, idx) {
    return _extends({}, match, {
      selected: idx === focusIndex
    });
  });
  return mergeData(controller, {
    searchResult: matches
  });
}
//# sourceMappingURL=updateSearchResult.js.map