"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = updateSearchResult;

var _utils = require("../../utils");

function updateSearchResult(controller, focus) {
  const {
    data
  } = controller.value;
  const {
    searchResult = []
  } = data;
  const focusIndex = searchResult.indexOf(focus);
  const matches = searchResult.map((match, idx) => ({ ...match,
    selected: idx === focusIndex
  }));
  return (0, _utils.mergeData)(controller, {
    searchResult: matches
  });
}
//# sourceMappingURL=updateSearchResult.js.map