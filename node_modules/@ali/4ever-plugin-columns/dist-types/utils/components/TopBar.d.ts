import { CellItem } from '../utils/ColumnsProvider';
import { ColumnsConfigs } from '../types';
interface IProps {
    items: CellItem[];
    configs?: ColumnsConfigs;
}
export default function TopBar({ items, configs }: IProps): JSX.Element;
export {};
