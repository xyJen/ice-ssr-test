import * as React from 'react';
import { Controller } from '@ali/4ever-cangjie';
import { Table } from '@ali/4ever-plugin-table';
import { OnDragChange, ColumnsConfigs } from '../types';
interface IProps {
    table: Table;
    onDragChange?: OnDragChange;
    controller: Controller;
    configs?: ColumnsConfigs;
    TopBar?: React.ComponentType<any>;
    attributes?: Record<string, any>;
}
export default function TableView({ table, controller, TopBar, configs, onDragChange, children, attributes, }: React.PropsWithChildren<IProps>): JSX.Element | null;
export {};
