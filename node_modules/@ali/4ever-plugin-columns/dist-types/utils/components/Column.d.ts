import * as React from 'react';
import { Controller } from '@ali/4ever-cangjie';
import { TableCell } from '@ali/4ever-plugin-table';
import { ColumnsConfigs } from '../types';
interface IProps {
    cell: TableCell;
    configs?: ColumnsConfigs;
    controller: Controller;
    ColorButton?: React.ComponentType<any>;
}
export default function Column({ cell, controller, ColorButton, children, configs, }: React.PropsWithChildren<IProps>): JSX.Element;
export {};
