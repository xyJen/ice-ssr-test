import { Controller, Block } from '@ali/4ever-cangjie';
export default function createGetLeftToolbarOffset(isDisabled?: Function): (controller: Controller, data: {
    node: Block;
}) => {
    x: number;
    y: number;
} | undefined;
