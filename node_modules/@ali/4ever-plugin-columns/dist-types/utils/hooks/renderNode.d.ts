import { Controller, RenderNodeProps } from '@ali/4ever-cangjie';
import { ColumnsConfigs } from '../types';
export default function createRenderNode(configs?: ColumnsConfigs): {
    [x: string]: ({ node, children, attributes }: RenderNodeProps, controller: Controller, next: any) => any;
};
