import { Node, Controller } from '@ali/4ever-cangjie';
import { ColumnsConfigs } from '../types';
export default function placeholder(configs?: ColumnsConfigs): {
    text: (controller: Controller, node: Node) => string;
    when: (controller: Controller, block: Node) => boolean;
}[];
