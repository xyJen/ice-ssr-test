import { Table } from '@ali/4ever-plugin-table';
export function isColumnsTable(node) {
  return Boolean(Table.isTable(node) && node.data.sr);
}
export function isColumnsRow(document, key) {
  if (key === void 0) {
    key = '';
  }

  var parent = document.getParent(key);
  return Boolean(Table.isTable(parent) && parent.data.sr);
}
export function isColumnsCell(document, key) {
  var parent = document.getParent(key);
  return isColumnsRow(document, parent == null ? void 0 : parent.key);
}
//# sourceMappingURL=isColumns.js.map