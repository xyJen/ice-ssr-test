import { Table } from '@ali/4ever-plugin-table';
export default function isSelectionInColumns(controller) {
  var _controller$value = controller.value,
      document = _controller$value.document,
      selection = _controller$value.selection;
  var anchor = selection.anchor,
      focus = selection.focus;
  var anchorParent = document.getFurthsest(anchor.key, Table.isTable);
  var focusParent = document.getFurthsest(focus.key, Table.isTable);

  if (anchorParent && anchorParent === focusParent && anchorParent.data.sr) {
    return true;
  }

  return false;
}
//# sourceMappingURL=isSelectionInColumns.js.map