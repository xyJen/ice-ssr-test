"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isSelectionInColumns;

var _everPluginTable = require("@ali/4ever-plugin-table");

function isSelectionInColumns(controller) {
  const {
    document,
    selection
  } = controller.value;
  const {
    anchor,
    focus
  } = selection;
  const anchorParent = document.getFurthsest(anchor.key, _everPluginTable.Table.isTable);
  const focusParent = document.getFurthsest(focus.key, _everPluginTable.Table.isTable);

  if (anchorParent && anchorParent === focusParent && anchorParent.data.sr) {
    return true;
  }

  return false;
}
//# sourceMappingURL=isSelectionInColumns.js.map