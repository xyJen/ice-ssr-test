"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isColumnsParent = isColumnsParent;
exports.isColumnsRow = isColumnsRow;
exports.isColumnsChild = isColumnsChild;

var _everPluginTable = require("@ali/4ever-plugin-table");

function isColumnsParent(controller, node) {
  return _everPluginTable.Table.isTable(node) && node.data.sr;
}

function isColumnsRow(controller, node) {
  const parent = controller.value.document.getParent(node.key);
  return parent && isColumnsParent(controller, parent) && _everPluginTable.TableRow.isTableRow(node);
}

function isColumnsChild(controller, node) {
  if (node && _everPluginTable.TableCell.isTableCell(node)) {
    const parent = controller.value.document.getParent(node.key);
    return parent && isColumnsRow(controller, parent);
  }

  return false;
}
//# sourceMappingURL=isColumns.js.map