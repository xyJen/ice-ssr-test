import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";
import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { MenuItem } from '@ali/we-design';
import { TemplateButtonRole } from "../utils/role";
;
export default function ImageMenu(props) {
  var menuItem = props.menuItem,
      controller = props.controller;

  var title = menuItem.title,
      rest = _objectWithoutPropertiesLoose(menuItem, ["title"]);

  var isCollapsedAtEdge = controller.value.selection.isCollapsedAtEdge;
  var isDisabled = isCollapsedAtEdge || controller.query('isSelectionInImage') || controller.query('isSelectionInLink') || controller.query('isSelectionInMention') || controller.query('isSelectionInLinkCard');
  var handleClick = React.useCallback(function () {
    return controller.dispatch('insertTemplateButton');
  }, [controller]);
  return /*#__PURE__*/_createElement(MenuItem, _extends({}, rest, {
    disabled: isDisabled,
    onClick: handleClick,
    role: TemplateButtonRole.templateButton
  }), title || '');
}
//# sourceMappingURL=menu.js.map