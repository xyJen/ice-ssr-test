import { TemplateButton } from "../../mo";
export default function getLeftToolbarOffset(controller, data) {
  var node = data.node;
  if (!node) return false;
  var parent = controller.value.document.getParent(node.key);

  while (parent && !TemplateButton.isTemplateButton(parent)) {
    parent = controller.value.document.getParent(parent.key);
  }

  if (parent) {
    return {
      x: 11,
      y: 0
    };
  }

  return false;
}
//# sourceMappingURL=getLeftToolbarOffset.js.map