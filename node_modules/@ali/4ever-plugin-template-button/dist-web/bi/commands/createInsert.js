import _extends from "@babel/runtime/helpers/extends";
import { TemplateButton } from "../../mo";
import { toggleOpen } from "./toggleOpen";
export default function createInsert(config) {
  return function insert(controller, payload) {
    var _config$locale;

    var defaultTitle = (config == null ? void 0 : (_config$locale = config.locale) == null ? void 0 : _config$locale.nameTitleDefault) || 'Add a new to-do';
    var block = TemplateButton.createTemplateButton(_extends({}, payload || {}, {
      title: (payload == null ? void 0 : payload.title) || defaultTitle
    }), process.env.NODE_ENV === 'test' ? 'test' : undefined);
    controller.dispatch("createInsertBlockWithoutExtraBlankAction", {
      node: block
    });
    return toggleOpen(controller, block.key);
  };
}
//# sourceMappingURL=createInsert.js.map