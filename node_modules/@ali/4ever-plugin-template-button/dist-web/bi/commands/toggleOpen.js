import _extends from "@babel/runtime/helpers/extends";
import { Injection } from '@ali/4ever-cangjie';
import { TemplateButton } from "../../mo";
export function toggleOpen(controller, key) {
  var injections = controller.value.injections;
  var isOpen = TemplateButton.isOpen(controller, key);
  var injection = Injection.create({
    key: key,
    data: {
      metadata: {
        isOpen: !isOpen
      }
    },
    mergeData: function mergeData(a, b) {
      return _extends({}, a, b, {
        metadata: _extends({}, a.metadata, b.metadata)
      });
    }
  });
  var excludeInjections = injections.filter(function (inj) {
    return inj.key !== key;
  });
  return controller.setInjections(excludeInjections.concat(injection));
}
//# sourceMappingURL=toggleOpen.js.map