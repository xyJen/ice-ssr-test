"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = createInsert;

var _mo = require("../../mo");

var _toggleOpen = require("./toggleOpen");

function createInsert(config) {
  return function insert(controller, payload) {
    const defaultTitle = config?.locale?.nameTitleDefault || 'Add a new to-do';

    const block = _mo.TemplateButton.createTemplateButton({ ...(payload || {}),
      title: payload?.title || defaultTitle
    }, process.env.NODE_ENV === 'test' ? 'test' : undefined);

    controller.dispatch("createInsertBlockWithoutExtraBlankAction", {
      node: block
    });
    return (0, _toggleOpen.toggleOpen)(controller, block.key);
  };
}
//# sourceMappingURL=createInsert.js.map