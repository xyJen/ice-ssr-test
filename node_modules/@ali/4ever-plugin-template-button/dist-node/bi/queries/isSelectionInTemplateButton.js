"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = _default;

var _mo = require("../../mo");

function _default(controller) {
  const {
    document,
    selection
  } = controller.value;

  if (!selection) {
    return undefined;
  }

  const {
    start,
    end
  } = selection.sort(document);

  if (start.isEdgePoint() || end.isEdgePoint()) {
    return undefined;
  }

  const startBlock = document.getClosest(start.key, _mo.TemplateButton.isTemplateButton);
  const endBlcok = document.getClosest(end.key, _mo.TemplateButton.isTemplateButton);

  if (startBlock && startBlock === endBlcok) {
    return 'all';
  } else if (startBlock) {
    return 'start';
  } else if (endBlcok) {
    return 'end';
  }

  return undefined;
}
//# sourceMappingURL=isSelectionInTemplateButton.js.map