"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isInTemplateButton;

var _mo = require("../../mo");

// 此处不能缓存父子关系， 拖拽会改变父子关系
function isInTemplateButton(controller, data) {
  const {
    node
  } = data;
  if (!node) return false;
  let parent = controller.value.document.getParent(node.key);

  while (parent && !_mo.TemplateButton.isTemplateButton(parent)) {
    parent = controller.value.document.getParent(parent.key);
  }

  return !!parent;
}
//# sourceMappingURL=isInTemplateButton.js.map