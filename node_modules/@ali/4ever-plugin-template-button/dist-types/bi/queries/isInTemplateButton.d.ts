import { Controller, Node, Block } from '@ali/4ever-cangjie';
interface QueryData {
    node: Node | Block | null;
}
export default function isInTemplateButton(controller: Controller, data: QueryData): boolean;
export {};
