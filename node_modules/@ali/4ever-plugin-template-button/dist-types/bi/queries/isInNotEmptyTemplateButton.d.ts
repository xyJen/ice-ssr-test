import { Controller, Block } from '@ali/4ever-cangjie';
interface QueryData {
    node: Block;
}
/**
 * 是否在非空的模板按钮内（打开状态包含内容 || 非打开）
 * @param controller
 * @param data
 * @returns
 */
export default function isInNotEmptyTemplateButton(controller: Controller, data: QueryData): boolean;
export {};
