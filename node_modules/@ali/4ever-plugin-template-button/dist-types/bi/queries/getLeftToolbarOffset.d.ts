import { Controller, Node, Block } from '@ali/4ever-cangjie';
interface QueryData {
    node: Node | Block | null;
}
export default function getLeftToolbarOffset(controller: Controller, data: QueryData): false | {
    x: number;
    y: number;
};
export {};
