import { Block, Document, Controller } from '@ali/4ever-cangjie';
import { Paragraph } from '@ali/4ever-plugin-paragraph';
import { Variables } from '../type';
/**
 * 判断节点是否是空节点
 * @param node
 * @returns
 */
export declare const isEmptyNode: (controller: Controller, node: Block) => boolean | undefined;
/**
 * 判断一个 node 是否是 list
 * @param node
 * @returns
 */
export declare const isList: (node: Block) => node is Paragraph;
/**
 * 判断当前光标是否在 inline code 中
 * @param controller
 * @returns
 */
export declare const isInlineCode: (controller: Controller) => boolean;
export declare function buildReplacer(replaceFns: Variables): (str: string) => string;
export declare const replaceTemplate: (fragment: Document, replacer: (str: string) => string) => any;
