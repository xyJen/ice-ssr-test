import React from 'react';
import { Controller, Block } from '@ali/4ever-cangjie';
import { TemplateButtonConfig } from '../type';
interface Props {
    direction: 'top' | 'bottom';
    controller: Controller;
    isOpen?: boolean;
    isOnce: boolean;
    title: string;
    node: Block<any, any>;
    placeholder: string;
    isSelected: boolean;
    editable: boolean;
    config?: TemplateButtonConfig;
}
declare const CloseView: React.FC<Props>;
export default CloseView;
