import { Document } from '@ali/4ever-cangjie';
export interface Locale {
    title: string;
    nameTitle: string;
    nameTitleDefault: string;
    namePlaceholder: string;
    contentTile: string;
    direction: string;
    insertTop: string;
    insertBottom: string;
    insertOnce: string;
    save: string;
    editTooltip: string;
    weeks: string;
}
export interface TemplateButtonConfig {
    variables?: Variables;
    enable?: {
        direction?: boolean;
        isOnce?: boolean;
    };
    locale?: Locale;
    serializer?: any;
    /**
     * 应用层在插入模板之前可能需要对模板进行预处理
     */
    beforeInsert?: (fragment: Document, replacers: (str: string) => string) => Promise<Document>;
}
export declare type Variables = Record<string, () => string | number>;
