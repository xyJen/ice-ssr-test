import React from 'react';
import { Block, Controller } from '@ali/4ever-cangjie';
import type { VotePluginConfig, IVoteData } from '../types';
interface IProps {
    node: Block;
    locale: VotePluginConfig['locale'];
    controller: Controller;
    isSelected?: boolean;
    data: IVoteData;
    setData: React.Dispatch<React.SetStateAction<IVoteData>>;
}
declare const Edit: React.FC<IProps>;
export default Edit;
