import { Controller, Node } from '@ali/4ever-cangjie';
import { VotePluginConfig } from '../types';
export declare const removeVoteId: (node: Node) => Node;
declare const createOnAction: (votePluginConfig: VotePluginConfig, copyNodes?: any) => (action: any, controller: Controller, next: any) => any;
export default createOnAction;
