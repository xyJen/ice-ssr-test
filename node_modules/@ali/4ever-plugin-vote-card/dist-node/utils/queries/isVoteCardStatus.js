"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isVoteCardEditStatus;
exports.isVoteCardVotingStatus = isVoteCardVotingStatus;

function isVoteCardEditStatus(controller, {
  node
}) {
  const {
    injections
  } = controller.value;
  const findNode = injections.find(n => n.key === node.key && n.data.editing);
  return Boolean(findNode);
}

function isVoteCardVotingStatus(controller, {
  node
}) {
  const {
    injections
  } = controller.value;
  const findNode = injections.find(n => n.key === node.key && n.data.voting);
  return Boolean(findNode);
}
//# sourceMappingURL=isVoteCardStatus.js.map