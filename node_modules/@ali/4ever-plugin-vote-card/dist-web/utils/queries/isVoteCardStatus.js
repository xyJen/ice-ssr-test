export default function isVoteCardEditStatus(controller, _ref) {
  var node = _ref.node;
  var injections = controller.value.injections;
  var findNode = injections.find(function (n) {
    return n.key === node.key && n.data.editing;
  });
  return Boolean(findNode);
}
export function isVoteCardVotingStatus(controller, _ref2) {
  var node = _ref2.node;
  var injections = controller.value.injections;
  var findNode = injections.find(function (n) {
    return n.key === node.key && n.data.voting;
  });
  return Boolean(findNode);
}
//# sourceMappingURL=isVoteCardStatus.js.map