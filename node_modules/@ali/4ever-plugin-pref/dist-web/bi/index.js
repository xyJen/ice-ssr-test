import { key } from "../utils/meta";
export var biFactory = function biFactory(configs) {
  var _ref = configs.pref || {},
      _getColorPanelMode = _ref.getColorPanelMode;

  return {
    key: key,
    queries: {
      getColorPanelMode: function getColorPanelMode() {
        return typeof _getColorPanelMode === 'function' ? _getColorPanelMode() : 'normal';
      }
    }
  };
};
//# sourceMappingURL=index.js.map