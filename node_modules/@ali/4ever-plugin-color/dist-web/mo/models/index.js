import _extends from "@babel/runtime/helpers/extends";
import _inheritsLoose from "@babel/runtime/helpers/inheritsLoose";
import { Block } from '@ali/4ever-cangjie';
import { ELEMENT_TYPE } from "../../utils/meta"; // 定义元素的 data 类型

var Color = /*#__PURE__*/function (_Block) {
  _inheritsLoose(Color, _Block);

  function Color() {
    return _Block.apply(this, arguments) || this;
  }

  Color.create = function create(properties) {
    if (typeof properties === 'string') {
      throw new Error('`create` does not accept string');
    }

    if (this.isColor(properties)) {
      return properties;
    }

    return _Block.create.call(this, _extends({}, properties, {
      type: Color.TYPE
    }));
  };

  Color.isColor = function isColor(maybeCode) {
    return Block.isBlock(maybeCode) && maybeCode.type === Color.TYPE;
  };

  return Color;
}(Block);

Color.TYPE = ELEMENT_TYPE;
export { Color as default };
//# sourceMappingURL=index.js.map