import { Color } from '@ali/4ever-plugin-marks';
import { DEFAULT_COLOR } from "../constants";
export default function getColor(controller) {
  var symbolColor = controller.query('getSymbolStyleByKey', 'color');
  if (symbolColor) return symbolColor;
  var colorMark = controller.query('getActiveMark', Color.TYPE);
  var color = colorMark && colorMark.data.value;
  if (color === '') return color;
  return color || DEFAULT_COLOR;
}
//# sourceMappingURL=getColor.js.map