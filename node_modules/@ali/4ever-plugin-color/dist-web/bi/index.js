import _extends from "@babel/runtime/helpers/extends";
import { moFactory } from "../mo/index";
import toolbar from "./toolbar";
import { commands } from "./commands";
import { queries } from "./queries";
import onAction from "./handlers/onAction";
import { actions, isActions } from "./actions";
import createRenderToolbar from "./createRenderToolbar";
export var biFactory = function biFactory(configs) {
  var colorConfig = configs.color || {};
  return _extends({}, moFactory({}), {
    actions: actions,
    isActions: isActions,
    toolbar: toolbar(colorConfig),
    renderToolbar: createRenderToolbar(colorConfig.locale),
    commands: commands,
    queries: queries,
    onAction: onAction
  });
};
//# sourceMappingURL=index.js.map