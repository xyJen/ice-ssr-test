"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _everPluginContainer = require("@ali/4ever-plugin-container");

var _everPluginCallout = require("@ali/4ever-plugin-callout");

var _default = {
  name: 'container',
  onOpenTag: (state, name, attrs) => {
    if (name !== 'div' || !(attrs.class || '').includes('colorBlocks')) return false;
    const type = attrs.class.split(' ')[1];
    const metadata = {
      showstk: false,
      bgcolor: type ? _everPluginCallout.COLOR_MAP[type] : undefined
    };
    const colorBlocks = [_everPluginContainer.Container.TYPE, {
      subType: 'colorBlocks',
      metadata: metadata
    }];
    state.push(colorBlocks);
    return true;
  },
  onCloseTag: (state, name, attrs) => {
    if (name !== 'div' || !(attrs.class || '').includes('colorBlocks')) return false;
    const colorBlock = state.pop();
    state.append(colorBlock);
    return true;
  }
};
exports.default = _default;
//# sourceMappingURL=htmlToJsonML.js.map