export default {
  name: 'container',
  filter: function filter(node) {
    return node.getAttribute('data-type') === 'container';
  },
  replacement: function replacement(content) {
    if (content === void 0) {
      content = '';
    }

    return ":::\n" + content.replace(/(^\n+)|(\n+$)/g, '') + "\n:::\n";
  }
};
//# sourceMappingURL=htmlToMarkdown.js.map