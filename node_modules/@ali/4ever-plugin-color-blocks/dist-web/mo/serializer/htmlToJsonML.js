import { Container } from '@ali/4ever-plugin-container';
import { COLOR_MAP } from '@ali/4ever-plugin-callout';
export default {
  name: 'container',
  onOpenTag: function onOpenTag(state, name, attrs) {
    if (name !== 'div' || !(attrs["class"] || '').includes('colorBlocks')) return false;
    var type = attrs["class"].split(' ')[1];
    var metadata = {
      showstk: false,
      bgcolor: type ? COLOR_MAP[type] : undefined
    };
    var colorBlocks = [Container.TYPE, {
      subType: 'colorBlocks',
      metadata: metadata
    }];
    state.push(colorBlocks);
    return true;
  },
  onCloseTag: function onCloseTag(state, name, attrs) {
    if (name !== 'div' || !(attrs["class"] || '').includes('colorBlocks')) return false;
    var colorBlock = state.pop();
    state.append(colorBlock);
    return true;
  }
};
//# sourceMappingURL=htmlToJsonML.js.map