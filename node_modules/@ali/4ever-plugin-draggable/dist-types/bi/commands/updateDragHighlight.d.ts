import { Controller } from '@ali/4ever-cangjie';
/**
 * 给可拖拽元素加高亮（dom操作）
 * 不会引起 controller.value 变更
 */
export default function updateDragHighlight(controller: Controller, payload: any): Controller;
