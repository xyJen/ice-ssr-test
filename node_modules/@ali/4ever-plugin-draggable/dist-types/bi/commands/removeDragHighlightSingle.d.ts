import { Controller } from '@ali/4ever-cangjie';
/**
 * 给已添加高亮可拖拽元素移除高亮（dom操作）
 * 不会引起 controller.value 变更
 */
export default function removeDragHighlightSingle(controller: Controller, payload: any): Controller;
