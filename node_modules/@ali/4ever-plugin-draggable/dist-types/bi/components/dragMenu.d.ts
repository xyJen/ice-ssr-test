import React from 'react';
import { Controller, Block, Data, BlockChildren } from '@ali/4ever-cangjie';
import { utils as refblockUtils } from '@ali/4ever-plugin-refblock';
import { HetuConfig } from '../utils/hetu';
import { IMenu } from '../types';
export declare const sizeStyle: React.CSSProperties;
export declare const commonHeadStyle: React.CSSProperties;
export declare const menuItemSizeStyle: React.CSSProperties;
interface DragMenuRenderProps {
    controller: Controller;
    currentBlock: Block;
    locale?: Record<string, string>;
    getCopyURL?: (id: string) => string;
    shareParagraph?: (type: string, uuid: string, block: Block<Data, BlockChildren>) => void;
    hetu?: HetuConfig;
    refblock?: refblockUtils.RefBlockDraggableConfig;
    handleClickMenu?: any;
    extraTransformMenu: IMenu[];
    customizeMenus?: IMenu[];
}
declare const DragMenu: React.FC<DragMenuRenderProps>;
export default DragMenu;
