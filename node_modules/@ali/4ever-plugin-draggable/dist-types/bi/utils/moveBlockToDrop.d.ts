import { Node, Controller } from '@ali/4ever-cangjie';
import { DROP_POSITION } from '../types';
/**
 * 移动节点到 drop 位置
 * @param controller
 * @param dragKey
 * @param dropBlock
 * @param dropPosition
 * @param isCopy
 * @returns 返回移动后的节点
 */
export default function moveBlockToDrop(controller: Controller, dragKey: string, dropBlock: Node, dropPosition?: DROP_POSITION, isCopy?: boolean): Node | null | undefined;
