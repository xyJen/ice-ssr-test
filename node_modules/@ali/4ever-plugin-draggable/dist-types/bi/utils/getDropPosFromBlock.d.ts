import { Controller, Block } from '@ali/4ever-cangjie';
import { DropHolderProps } from '../types';
export default function getDropPosFromBlock(controller: Controller, block: Block, x: number, y: number, onlyHorizon?: boolean): DropHolderProps | null;
