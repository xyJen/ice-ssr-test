import { Controller, Node } from '@ali/4ever-cangjie';
/**
 * 判断 node 是否可拖拽
 * @param node
 * @param container
 */
export default function isNodeDraggable(controller: Controller, node: Node): boolean;
