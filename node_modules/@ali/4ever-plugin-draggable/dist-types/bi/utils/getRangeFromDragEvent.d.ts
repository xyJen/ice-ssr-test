import { Controller, Node } from '@ali/4ever-cangjie';
/**
   * 从 drag 事件中计算命中的块级元素
   * 水平位置矫正策略（I 表示 drag 事件的位置）：
   * |   [---I---  ]   |
   * |   [------ I ]   |
   * | I [-------  ]   |
   * |   [-------  ] I |
   * 当事件命中 content 的 padding 区域时，如果取原始的水平位置拿不到 block
   * 因此矫正为 dragEvent.target 的中点
   */
export declare function getBlockFromDragEvent(controller: Controller, event: DragEvent, zoom?: number, onlyEdge?: boolean): Node | null;
export default function getRangeFromDragEvent(controller: Controller, event: DragEvent): import("@ali/4ever-cangjie").Selection | null;
