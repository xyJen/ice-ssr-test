import { Controller } from '@ali/4ever-cangjie';
import { DROP_POSITION } from '../types';
export default function getDropHolderDataFromViewKeys(controller: Controller, event: DragEvent, viewKeys: string[]): {
    rect: {
        top: number;
        left: number;
        width: number;
        height: number;
    };
    pagePosition: DROP_POSITION;
} | undefined;
