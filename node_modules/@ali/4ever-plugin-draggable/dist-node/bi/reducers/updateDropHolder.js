"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = updateDropHolder;

function updateDropHolder(draggableData, payload) {
  const {
    dropHolder,
    ...rest
  } = draggableData;

  if (!payload && !dropHolder) {
    // 都为空不更新
    return draggableData;
  }

  if (dropHolder?.key === payload?.key && dropHolder?.dropPosition === payload?.dropPosition) {
    // 同样位置不更新
    return draggableData;
  }

  if (!payload) {
    return rest;
  }

  return { ...rest,
    dropHolder: payload
  };
}
//# sourceMappingURL=updateDropHolder.js.map