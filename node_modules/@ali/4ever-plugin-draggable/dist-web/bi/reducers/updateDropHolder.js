import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";
export default function updateDropHolder(draggableData, payload) {
  var dropHolder = draggableData.dropHolder,
      rest = _objectWithoutPropertiesLoose(draggableData, ["dropHolder"]);

  if (!payload && !dropHolder) {
    // 都为空不更新
    return draggableData;
  }

  if ((dropHolder == null ? void 0 : dropHolder.key) === (payload == null ? void 0 : payload.key) && (dropHolder == null ? void 0 : dropHolder.dropPosition) === (payload == null ? void 0 : payload.dropPosition)) {
    // 同样位置不更新
    return draggableData;
  }

  if (!payload) {
    return rest;
  }

  return _extends({}, rest, {
    dropHolder: payload
  });
}
//# sourceMappingURL=updateDropHolder.js.map