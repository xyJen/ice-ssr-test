import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";
export default function updateDragHander(draggableData, payload) {
  var dragHander = draggableData.dragHander,
      rest = _objectWithoutPropertiesLoose(draggableData, ["dragHander"]); // 避免频繁更新


  if (!payload && !dragHander) return draggableData;

  if (!payload) {
    return rest;
  }

  if (payload.viewKey === (dragHander == null ? void 0 : dragHander.viewKey)) return draggableData;
  return _extends({}, rest, {
    dragHander: payload
  });
}
//# sourceMappingURL=updateDragHander.js.map