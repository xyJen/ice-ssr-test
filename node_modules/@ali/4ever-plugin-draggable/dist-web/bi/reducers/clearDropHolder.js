import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";

/**
 * 清除左边抓手和占位数据
 * @param draggableData
 * @param delayTime 延时清除高亮
 * @returns
 */
export default function clearDropHolder(draggableData) {
  var dropHolder = draggableData.dropHolder,
      dragHander = draggableData.dragHander,
      rest = _objectWithoutPropertiesLoose(draggableData, ["dropHolder", "dragHander"]);

  return rest;
}
//# sourceMappingURL=clearDropHolder.js.map