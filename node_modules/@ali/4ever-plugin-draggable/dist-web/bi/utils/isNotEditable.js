import { constants } from '@ali/4ever-cangjie';
var notEditable = constants.Selector.notEditable;
export default function isNotEditable(event) {
  var target = event.target;

  while (target && target !== window.document.body) {
    var _target;

    if ((_target = target) != null && _target.getAttribute(notEditable)) return true;
    target = target.parentElement;
  }

  return false;
}
//# sourceMappingURL=isNotEditable.js.map