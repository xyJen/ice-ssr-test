import { DRAG_TYPE } from "../types";
/**
 * 判断 fragment 是否是图片
 * @param fragment
 * @returns
 */

function isImageFragment(fragment) {
  if (fragment.type === DRAG_TYPE.inline) {
    var _ref = fragment.fragment,
        type = _ref.type;
    return type === 'image';
  }

  return false;
}

export default isImageFragment;
//# sourceMappingURL=isImageFragment.js.map