import { DRAG_TYPE } from "../types";
/**
 * 判断 fragment 是否的议题区块
 * @param fragment
 * @returns
 */

function isSubjectFragment(fragment) {
  if (fragment.type === DRAG_TYPE.blocks) {
    var _first$nodeJson$data;

    var first = fragment.fragment[0];
    return !!first && first.type === 'heading-1' && !!((_first$nodeJson$data = first.nodeJson.data) != null && _first$nodeJson$data.subjectPr);
  }

  return false;
}

export default isSubjectFragment;
//# sourceMappingURL=isSubjectFragment.js.map