import { Commands } from '@ali/4ever-cangjie';
import { DRAG_TYPE } from "../types";
export default function deleteDragFragment(controller, payload) {
  var dragFragment = payload.dragFragment;
  if (!dragFragment) return controller;
  var type = dragFragment.type;

  if (type === DRAG_TYPE.blocks && Array.isArray(dragFragment.fragment)) {
    var fragment = [].concat(dragFragment.fragment);
    fragment.forEach(function (block) {
      controller.command(Commands.removeNodeByKey, block.key);
    });
  } else if (type === DRAG_TYPE.inline && dragFragment.fragment.key) {
    controller.command(Commands.removeNodeByKey, dragFragment.fragment.key);
  }

  return controller;
}
//# sourceMappingURL=deleteDragFragment.js.map