/* eslint-disable @typescript-eslint/ban-ts-comment */
import { debounce } from 'lodash-es';
import { Block } from '@ali/4ever-cangjie';
import { HoverBlock } from '@ali/4ever-utils';
import { updateDragHander } from "../actions";
var mouseMoveHandler = debounce(function (event, controller, zoomContainer, dispatch, zoom) {
  var _HoverBlock$getBlock = HoverBlock.getBlock(event, controller.value.document, {
    zoomContainer: zoomContainer,
    zoom: zoom,
    filterColumn: true
  }),
      block = _HoverBlock$getBlock.block;

  if (Block.isBlock(block)) {
    dispatch(updateDragHander({
      viewKey: block.key
    }));
  } else {
    dispatch(updateDragHander());
  }
}, 50);
export default mouseMoveHandler;
//# sourceMappingURL=mouseMoveHandler.js.map