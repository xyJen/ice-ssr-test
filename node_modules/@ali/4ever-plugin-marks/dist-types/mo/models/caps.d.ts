import { Mark } from '@ali/4ever-cangjie';
declare type CapsMark = Mark;
export interface CapData {
    value: boolean;
}
declare const Caps: {
    TYPE: string;
    create(capData?: CapData | null): CapsMark;
    isCaps(maybeCaps: any): maybeCaps is CapsMark;
};
export default Caps;
