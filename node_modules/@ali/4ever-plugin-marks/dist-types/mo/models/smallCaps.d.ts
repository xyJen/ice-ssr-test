import { Mark } from '@ali/4ever-cangjie';
interface SmallCaps extends Mark {
}
declare const SmallCaps: {
    TYPE: string;
    create(): SmallCaps;
    isSmallCaps(maybeSmallCaps: any): maybeSmallCaps is SmallCaps;
};
export default SmallCaps;
