import { Mark } from '@ali/4ever-cangjie';
interface InlineCode extends Mark {
    type: 'inlineCode';
}
declare const InlineCode: {
    TYPE: string;
    create(): InlineCode;
    isInlineCode(maybeInlineCode: any): maybeInlineCode is InlineCode;
};
export default InlineCode;
