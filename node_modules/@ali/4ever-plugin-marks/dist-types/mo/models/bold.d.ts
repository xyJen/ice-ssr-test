import { Mark } from '@ali/4ever-cangjie';
interface Bold extends Mark {
    type: 'bold';
}
declare const Bold: {
    TYPE: string;
    create(bold?: boolean): Bold;
    isBold(maybeBold: any): maybeBold is Bold;
};
export default Bold;
