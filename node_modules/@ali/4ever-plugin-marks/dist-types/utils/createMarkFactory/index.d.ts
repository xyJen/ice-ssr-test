/// <reference types="react" />
import { Command, Query, MoFactory, BiPlugin, BiPluginConfig } from '@ali/4ever-cangjie';
/** depreacted 只为兼容，请勿使用 */
import * as oldActions from './actions';
import type { Delimiter } from '@ali/4ever-utils';
export interface ICreateMarkFactoryPayload {
    moFactory?: MoFactory;
    type: string;
    hotKey: string;
    Button: React.ComponentType;
    delimiter: Delimiter | Delimiter[];
    insideWhitespace?: boolean;
    Icon: React.ComponentType;
    toolbarIcon?: React.ReactElement;
    localeKeys?: Record<string, string>;
}
export declare function createMarkFactory({ type, hotKey, Button, delimiter, insideWhitespace, Icon, toolbarIcon, localeKeys, moFactory }: ICreateMarkFactoryPayload): {
    (config: BiPluginConfig): BiPlugin;
    commands: Record<string, Command>;
    queries: Record<string, Query>;
    actions: typeof oldActions;
};
