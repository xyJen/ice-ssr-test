"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _everCangjie = require("@ali/4ever-cangjie");

const Caps = {
  TYPE: 'caps',

  create(capData) {
    // 这里capData可能传入null是因为在线编辑场景下服务端转换的数据可能为null
    // 所以这里需要兼容一下这种情况
    const cap = capData ? capData.value : false;
    return _everCangjie.Mark.create({
      type: Caps.TYPE,
      data: {
        value: !!cap
      }
    });
  },

  isCaps(maybeCaps) {
    return _everCangjie.Mark.isMark(maybeCaps) && maybeCaps.type === Caps.TYPE;
  }

};
var _default = Caps;
exports.default = _default;
//# sourceMappingURL=caps.js.map