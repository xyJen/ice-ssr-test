"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getMarkdownFromDelimiter = getMarkdownFromDelimiter;

function getMarkdownFromDelimiter(delimiter, locale) {
  if (!delimiter) return undefined;
  const delimiters = !Array.isArray(delimiter) ? [delimiter] : delimiter;
  const {
    character,
    size
  } = delimiters[0];
  return character.repeat(size) + (locale?.text || 'Text') + character.repeat(size) + ' ' + (locale?.space || 'Space');
}
//# sourceMappingURL=utils.js.map