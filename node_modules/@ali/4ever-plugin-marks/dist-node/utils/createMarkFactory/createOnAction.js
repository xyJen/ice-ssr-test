"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = createOnAction;

var _lodash = require("lodash");

var _actions = require("./actions");

function createOnAction(markType) {
  return function onAction(action, controller, next) {
    const {
      type,
      payload
    } = action;
    if (type !== _actions.MARK_TOGGLE || payload?.mark !== markType) return next();
    const {
      mark
    } = payload;
    controller.command(`toggle${(0, _lodash.capitalize)(mark)}`); // 列表等插件还依赖此 action

    return next();
  };
}
//# sourceMappingURL=createOnAction.js.map