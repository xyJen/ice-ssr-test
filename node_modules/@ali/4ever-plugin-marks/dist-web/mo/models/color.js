import { Mark } from '@ali/4ever-cangjie';
var Color = {
  TYPE: 'color',
  create: function create(color) {
    return Mark.create({
      type: Color.TYPE,
      data: {
        value: color
      }
    });
  },
  isColor: function isColor(maybeColor) {
    return Mark.isMark(maybeColor) && maybeColor.type === Color.TYPE;
  }
};
export default Color;
//# sourceMappingURL=color.js.map