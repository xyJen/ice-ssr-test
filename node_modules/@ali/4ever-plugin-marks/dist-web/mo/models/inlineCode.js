import { Mark } from '@ali/4ever-cangjie';
;
var InlineCode = {
  TYPE: 'inlineCode',
  create: function create() {
    return Mark.create({
      type: InlineCode.TYPE
    });
  },
  isInlineCode: function isInlineCode(maybeInlineCode) {
    return Mark.isMark(maybeInlineCode) && maybeInlineCode.type === InlineCode.TYPE;
  }
};
export default InlineCode;
//# sourceMappingURL=inlineCode.js.map