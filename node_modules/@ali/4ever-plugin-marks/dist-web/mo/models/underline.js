import { Mark } from '@ali/4ever-cangjie';
var Underline = {
  TYPE: 'underline',
  create: function create(underline, color) {
    if (underline === void 0) {
      underline = 'single';
    }

    var data = {
      value: underline
    };

    if (color) {
      // @ts-ignore
      data.color = color;
    }

    return Mark.create({
      type: Underline.TYPE,
      data: data
    });
  },
  isUnderline: function isUnderline(maybeUnderline) {
    return Mark.isMark(maybeUnderline) && maybeUnderline.type === Underline.TYPE;
  }
};
export default Underline;
//# sourceMappingURL=underline.js.map