import { Mark } from '@ali/4ever-cangjie';
var Caps = {
  TYPE: 'caps',
  create: function create(capData) {
    // 这里capData可能传入null是因为在线编辑场景下服务端转换的数据可能为null
    // 所以这里需要兼容一下这种情况
    var cap = capData ? capData.value : false;
    return Mark.create({
      type: Caps.TYPE,
      data: {
        value: !!cap
      }
    });
  },
  isCaps: function isCaps(maybeCaps) {
    return Mark.isMark(maybeCaps) && maybeCaps.type === Caps.TYPE;
  }
};
export default Caps;
//# sourceMappingURL=caps.js.map