import { Mark } from '@ali/4ever-cangjie';
;
var Bold = {
  TYPE: 'bold',
  create: function create(bold) {
    if (bold === false) {
      return Mark.create({
        type: Bold.TYPE,
        data: {
          value: false
        }
      });
    } else {
      return Mark.create({
        type: Bold.TYPE
      });
    }
  },
  isBold: function isBold(maybeBold) {
    return Mark.isMark(maybeBold) && maybeBold.type === Bold.TYPE;
  }
};
export default Bold;
//# sourceMappingURL=bold.js.map