import _extends from "@babel/runtime/helpers/extends";
import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { MarksComponent } from "../components/marks";
import isBuiltInMark from "../utils/isBuiltInMark";
export default function renderMark(props, _, next) {
  var node = props.node,
      offset = props.offset,
      mark = props.mark,
      marks = props.marks;

  if (marks[0] === mark) {
    // buildInMarks 优先级更高
    // 在第一次调用 renderMark hook 时就消费所有的 buildInMarks，后续不会再消费
    var builtInMarks = marks.filter(isBuiltInMark);

    if (builtInMarks.length) {
      return /*#__PURE__*/_createElement(MarksComponent, _extends({}, props, {
        key: node.key + ":" + offset
      }));
    }
  }

  return next();
}
//# sourceMappingURL=renderMark.js.map