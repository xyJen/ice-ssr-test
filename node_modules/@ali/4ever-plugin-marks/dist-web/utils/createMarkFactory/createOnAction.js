import { capitalize } from 'lodash-es';
import { MARK_TOGGLE } from "./actions";
export default function createOnAction(markType) {
  return function onAction(action, controller, next) {
    var type = action.type,
        payload = action.payload;
    if (type !== MARK_TOGGLE || (payload == null ? void 0 : payload.mark) !== markType) return next();
    var mark = payload.mark;
    controller.command("toggle" + capitalize(mark)); // 列表等插件还依赖此 action

    return next();
  };
}
//# sourceMappingURL=createOnAction.js.map