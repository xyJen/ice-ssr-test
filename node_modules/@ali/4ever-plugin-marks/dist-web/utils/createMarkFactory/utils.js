export function getMarkdownFromDelimiter(delimiter, locale) {
  if (!delimiter) return undefined;
  var delimiters = !Array.isArray(delimiter) ? [delimiter] : delimiter;
  var _delimiters$ = delimiters[0],
      character = _delimiters$.character,
      size = _delimiters$.size;
  return character.repeat(size) + ((locale == null ? void 0 : locale.text) || 'Text') + character.repeat(size) + ' ' + ((locale == null ? void 0 : locale.space) || 'Space');
}
//# sourceMappingURL=utils.js.map