export default function canToggleMark(editor) {
  var value = editor.value;
  var isCollapsedAtEdge = value.selection.isCollapsedAtEdge;

  if (isCollapsedAtEdge) {
    return false;
  }

  return !editor.query('isSelectionInImage') && !editor.query('isSelectionInHr') && !editor.query('isSelectionInLinkCard');
}
//# sourceMappingURL=canToggleMark.js.map