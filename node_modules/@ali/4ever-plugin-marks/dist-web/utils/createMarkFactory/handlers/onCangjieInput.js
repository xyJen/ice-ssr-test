import { createSpaceHandler } from '@ali/4ever-utils';
export default function createOnCangjieInput(mark, delimiter, insideWhitespace, alwaysTrailSpace) {
  if (alwaysTrailSpace === void 0) {
    alwaysTrailSpace = false;
  }

  var onSpace;

  if (delimiter) {
    onSpace = createSpaceHandler({
      mark: mark,
      delimiter: delimiter,
      insideWhitespace: insideWhitespace,
      alwaysTrailSpace: alwaysTrailSpace
    });
  }

  return function onCangjieInput(event, controller, next) {
    if (!onSpace || event.detail.data !== ' ') return next();
    return onSpace(event, controller, next);
  };
}
//# sourceMappingURL=onCangjieInput.js.map