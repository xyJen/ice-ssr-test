import { createActionHandlers } from '@ali/4ever-cangjie'; // undoAction

export var HISTORY_UNDO = Symbol('undoAction');

var _createActionHandlers = createActionHandlers(HISTORY_UNDO),
    undo = _createActionHandlers[0],
    isUndo = _createActionHandlers[1]; // redoAction


export { undo, isUndo };
export var HISTORY_REDO = Symbol('redoAction');

var _createActionHandlers2 = createActionHandlers(HISTORY_REDO),
    redo = _createActionHandlers2[0],
    isRedo = _createActionHandlers2[1];

export { redo, isRedo };
export var actions = {
  undo: undo,
  redo: redo
};
export var isActions = {
  isUndo: isUndo,
  isRedo: isRedo
};
/** @deprecated */

export var DEPRECATED_ACTIONS = {
  HISTORY_REDO: HISTORY_REDO,
  HISTORY_UNDO: HISTORY_UNDO,
  undo: undo,
  redo: redo
};
//# sourceMappingURL=actions.js.map