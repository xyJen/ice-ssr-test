export function canUndo(controller) {
  var history = controller.value.data.history;

  if (history && history.undos.length > 0) {
    return true;
  }

  return false;
}
//# sourceMappingURL=canUndo.js.map