export function canRedo(controller) {
  var history = controller.value.data.history;

  if (history && history.redos.length > 0) {
    return true;
  }

  return false;
}
//# sourceMappingURL=canRedo.js.map