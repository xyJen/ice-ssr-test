import _extends from "@babel/runtime/helpers/extends";
import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import DefaultUndoButton from "./components/next/undoButton";
import DefaultRedoButton from "./components/next/redoButton";
import { ItemSlot, SlotType } from '@ali/4ever-component';
export default function createRenderToolbar(historyConfig) {
  var locale = historyConfig.locale || {};
  var UndoButton = historyConfig.UndoButton || DefaultUndoButton;
  var RedoButton = historyConfig.RedoButton || DefaultRedoButton;
  var buttons = {
    // @ts-ignore to fix
    'history.redo': RedoButton,
    // @ts-ignore to fix
    'history.undo': UndoButton
  };
  return function (props, controller) {
    var buttonName = props.buttonName,
        extraProps = props.extraProps;
    var Component = buttons[buttonName];

    if (Component) {
      return /*#__PURE__*/_createElement(ItemSlot, {
        display: SlotType.iconButton
      }, /*#__PURE__*/_createElement(Component, _extends({
        controller: controller,
        locale: locale
      }, extraProps)));
    }

    return null;
  };
}
//# sourceMappingURL=createRenderToolbar.js.map