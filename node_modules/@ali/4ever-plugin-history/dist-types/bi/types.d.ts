/**
 * @packageDocumentation
 * @module Plugin-History
 */
/// <reference types="react" />
import { Controller, ControllerConfigs } from '@ali/4ever-cangjie';
/**
 * @ignore
 */
export interface HistoryLocale {
    undo: string;
    redo: string;
}
export interface UndoRedoButtonProps {
    controller: Controller;
    locale: Record<string, string>;
}
export declare type HistoryPluginConfig = ControllerConfigs['history'] & {
    locale?: HistoryLocale;
    /**
     * 是否可以进行 undo。控制工具条按钮是否是禁用状态
     */
    canUndo?: (controller: Controller) => boolean;
    /**
     * 是否可以进行 redo。控制工具条按钮是否是禁用状态。
     */
    canRedo?: (controller: Controller) => boolean;
    UndoButton?: React.ComponentType<UndoRedoButtonProps>;
    RedoButton?: React.ComponentType<UndoRedoButtonProps>;
};
