var SnippetManager = /*#__PURE__*/function () {
  /**
   * 当前 active 的 snippet 类型和 triggerText
   */
  function SnippetManager(cfg) {
    if (cfg === void 0) {
      cfg = {};
    }

    this.configs = void 0;
    this.current = {};
    this.isShowing = false;
    this.configs = cfg;
  }

  var _proto = SnippetManager.prototype;

  _proto.setConfigs = function setConfigs(cfg) {
    this.configs = cfg;
  };

  _proto.setCurrentSnippet = function setCurrentSnippet(trigger, text) {
    this.current.trigger = trigger;
    this.current.text = text;
  };

  _proto.getSnippet = function getSnippet() {
    return this.current;
  };

  _proto.isShowingPanel = function isShowingPanel() {
    return this.isShowing;
  };

  _proto.setIsShowing = function setIsShowing(value) {
    this.isShowing = value;
  };

  return SnippetManager;
}();

export { SnippetManager as default };
//# sourceMappingURL=snippetManager.js.map