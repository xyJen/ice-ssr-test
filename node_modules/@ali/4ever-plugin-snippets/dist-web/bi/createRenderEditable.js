import React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { SnippetsPanel } from "./components/SnippetsPanel";
export function createRenderEditable(config, manager) {
  if (config === void 0) {
    config = {};
  }

  // TODO: FIXME 引入 SimpleLazyLoad 构建时 ts 检查有问题
  // const LazyLoad = config.lazyload || SimpleLazyLoad;
  // const LazySnippetsPanel = LazyLoad(() =>
  //   import('./components/SnippetsPanel').then(({ SnippetsPanel }) => SnippetsPanel),
  // );
  return function (props, controller, next) {
    var _manager$getSnippet = manager.getSnippet(),
        trigger = _manager$getSnippet.trigger,
        text = _manager$getSnippet.text;

    return /*#__PURE__*/_createElement(React.Fragment, null, next(), /*#__PURE__*/_createElement(SnippetsPanel, {
      controller: controller,
      manager: manager,
      currentTrigger: trigger,
      currentText: text
    }));
  };
}
//# sourceMappingURL=createRenderEditable.js.map