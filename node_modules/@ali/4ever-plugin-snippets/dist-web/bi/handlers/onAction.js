import { Commands } from '@ali/4ever-cangjie';
import { isReplaceWithSnippet } from "../actions/replaceWithSnippet";
export var onAction = function onAction(action, controller, next) {
  if (isReplaceWithSnippet(action)) {
    var _ref = action.payload,
        sourceRange = _ref.sourceRange,
        insertTarget = _ref.insertTarget;
    controller.command(Commands.deleteAtRange, sourceRange);
    insertTarget();
    return;
  }

  return next();
};
//# sourceMappingURL=onAction.js.map