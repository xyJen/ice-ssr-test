import { Controller } from '@ali/4ever-cangjie';
import type SnippetManger from '../snippetManager';
export declare function createOnKeyUp(manager: SnippetManger): (event: React.KeyboardEvent, controller: Controller, next: Function) => any;
