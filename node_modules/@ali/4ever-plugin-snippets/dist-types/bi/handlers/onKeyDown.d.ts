import React from 'react';
import { Controller } from '@ali/4ever-cangjie';
import type SnippetManager from '../snippetManager';
export declare function createOnKeyDown(manager: SnippetManager): (event: React.KeyboardEvent, _controller: Controller, next: Function) => any;
