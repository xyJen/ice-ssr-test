import type SnippetManager from '../snippetManager';
export declare type Queries = ReturnType<typeof createQueries>;
export declare const createQueries: (manager: SnippetManager) => {
    isTriggerringSnippets: () => string | boolean;
};
