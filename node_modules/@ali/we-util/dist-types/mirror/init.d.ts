declare type APICollection = Record<string, any>;
export declare const isThisTopWindow: boolean;
export declare const isDingTalk: () => boolean;
/**
 * 初始化jsapi集合，通过包装能做到
 * - 如果是在iframe中，就从顶层窗口去取api
 * - 保留api原来的类型
 */
export default function initJsApi<M extends APICollection>(collection: M): <K extends keyof M>(name: K) => M[K] | (() => any);
export {};
