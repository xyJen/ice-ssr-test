import { Block, Node } from '@ali/4ever-cangjie';
import { JsonML } from '@ali/4ever-utils';
declare const _default: {
    match(node: Node): boolean;
    convert(node: Block): JsonML;
};
export default _default;
