import { unitsConverter } from '@ali/4ever-utils';
export function parseBorder(border) {
  var measureDom = document.createElement('div');

  if (border) {
    measureDom.style.border = border;
  }

  var _measureDom$style = measureDom.style,
      borderWidth = _measureDom$style.borderWidth,
      borderStyle = _measureDom$style.borderStyle,
      borderColor = _measureDom$style.borderColor;
  return {
    sz: unitsConverter.toPT(borderWidth),
    type: borderStyle,
    color: borderColor
  };
}
//# sourceMappingURL=utils.js.map