/**
 * 默认高度
 */
export var IMTAG_DEFAULT_HEIGHT = 24;
/**
 * 初始化宽度，模板中绑定的默认值是 65（65 为文档来源前缀宽度，只要群名不为空，宽度即会大于该值）
 */

export var IMTAG_INIT_WIDTH = 65;
/**
 * 最大宽度
 */

export var IMTAG_MAX_WIDTH = 318;
/**
 * 默认参与计算字符
 * 作为生成长图时国际化兜底方案
 */

export var IMTAG_MEASURE_STRING = '来至会话';
/**
 * 最小容器宽度(左右padding+图标)
 */

export var IMTAG_MIN_CONTAINER_WIDTH = 28;
//# sourceMappingURL=constants.js.map