import React from 'react';
import { ButtonSize, ButtonType } from './Button';
interface IWaveProps {
    children: React.ReactElement;
    type?: ButtonType;
    size?: ButtonSize;
    testid?: string;
}
export declare const IWave: (props: IWaveProps) => JSX.Element;
export {};
