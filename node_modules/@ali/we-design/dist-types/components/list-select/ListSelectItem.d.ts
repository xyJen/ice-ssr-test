import React from 'react';
import { ICheckboxOption } from '../checkbox';
export interface IListItem extends ICheckboxOption {
    count?: number;
}
export declare type IListSelectItemProps = {
    /**
     * 传入的数据列表
     */
    data: {
        dataList: IListItem[];
        itemStyle?: React.CSSProperties;
    };
    /**
     * 数组索引
     */
    index: number;
    /**
     * 传入的样式
     */
    style: React.CSSProperties;
    /**
     * 是否正在滚动
     */
    isScrolling?: boolean;
};
export declare const ListSelectItem: React.FC<IListSelectItemProps>;
