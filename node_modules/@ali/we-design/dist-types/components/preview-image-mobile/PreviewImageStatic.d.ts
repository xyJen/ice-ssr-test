import { IPreviewImageProps, IPreviewImageStatus } from './PreviewImage';
import '../../../locale/zh-CN';
import '../../../locale/en-US';
export interface IPreviewImageConfig extends IPreviewImageProps {
    /**
     * 预览组件 Key
     */
    previewImageKey?: string;
}
export declare const previewImageMobile: {
    open(config: IPreviewImageConfig): void;
    show(config: IPreviewImageConfig): void;
    close(previewImageKey?: string | undefined, callback?: any): void;
    setStatus(status: IPreviewImageStatus): void;
};
