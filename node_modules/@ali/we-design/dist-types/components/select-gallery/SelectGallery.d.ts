import React, { FC } from 'react';
export interface SelectGroupConfig {
    /**
     * 标题
     */
    title?: string;
    /**
     * 是否显示分割线，如果有标题，使用标题分隔 Group，没有
     * 标题的情况下，如果配置了 showDivider，那么使用分割线
     * 如果既没有标题又关闭分割线，那么 Group 和上个 Group 显示在一起
     * 视觉上无法区分
     */
    showDivider?: boolean;
    /**
     * 具体的 SelectItem 配置
     */
    items: {
        id: any;
        node: React.ReactNode;
        testID?: string;
    }[];
}
export interface SelectGalleryProps {
    /**
     * 样式覆盖
     */
    style?: React.CSSProperties;
    /**
     * SelectGroup 配置
     */
    groups: SelectGroupConfig[];
    /**
     * SelectItem 的宽度
     */
    width?: number;
    /**
     * SelectItem 的高度
     */
    height?: number;
    /**
     * 每行显示几个 SelectItem，从左到右排列
     */
    itemsPerRow?: number;
    /**
     * active 的 SelectItem id
     */
    value?: any;
    /**
     * 通知外界选项发生变化
     */
    onChange?: (newID: any) => void;
}
export declare const SelectGallery: FC<SelectGalleryProps>;
