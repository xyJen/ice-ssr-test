import React from 'react';
import { useForm } from 'rc-field-form';
import type { FormInstance } from 'rc-field-form';
export interface FormProps {
    /**
     * 控制表单的整体布局方式
     */
    layout?: 'horizontal' | 'vertical';
    /**
     * label 占据的 span 数，总 span 为 24
     * Form.Item 可以覆盖这个设置
     */
    labelSpan?: number;
    /**
     * form 实例
     */
    form?: FormInstance;
    /**
     * 初始值
     */
    initialValues?: object;
    /**
     * 提交表单并且数据验证成功以后的回调
     */
    onFinish?: (values: object) => void;
    /**
     * 提交表单，数据验证失败以后的回调
     */
    onFinishFailed?: (errorInfo: any) => void;
    /**
     * 字段值更新时的回调
     */
    onValuesChange?: (values: object) => void;
}
export declare type FormContext = Pick<FormProps, 'layout' | 'labelSpan'>;
export declare const FormContext: React.Context<FormContext>;
declare const Main: React.ForwardRefRenderFunction<FormInstance, FormProps>;
export default Main;
export { useForm };
export type { FormInstance };
