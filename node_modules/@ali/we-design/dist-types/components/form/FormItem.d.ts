import { CSSProperties, FC } from 'react';
import type { Rule } from 'rc-field-form/es/interface';
/**
 * 支持 渲染函数
 */
export interface FormItemProps {
    label?: string;
    /**
     * 当前控件的 name，用于值绑定
     * 如果没有 name，将 **不会** 进行值绑定
     */
    name?: string;
    /**
     * 覆盖 Form 的 lbaelSpan 设置
     */
    labelSpan?: number;
    /**
     * 定制顶层 FormItem 元素的样式
     */
    style?: CSSProperties;
    /**
     * 校验规则
     */
    validateTrigger?: string | string[];
    /**
     * 校验规则
     */
    rules?: Rule[];
}
declare const FormItem: FC<FormItemProps>;
export default FormItem;
export type { Rule };
