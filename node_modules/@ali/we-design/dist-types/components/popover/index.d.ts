/// <reference types="react" />
import { ITriggerProps } from '../trigger';
export interface IPopoverProps extends Omit<ITriggerProps, 'pageX' | 'pageY' | 'type' | 'placement' | 'action'> {
    placement?: 'bottom' | 'right' | 'top' | 'left';
    action?: ITriggerProps['action'];
}
export declare const Popover: {
    (props: IPopoverProps): JSX.Element;
    defaultProps: {
        placement: string;
        action: string[];
    };
};
