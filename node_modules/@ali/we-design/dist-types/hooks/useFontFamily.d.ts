export interface fontFamily {
    title: string;
    value: string;
    installed: boolean;
}
/**
 * 获取fontFamily, 字体探测结果, 语言转换函数
 * @param locale 本地语言类型
 * @returns {[options, installedRecord, t]}
 */
export declare const useFontFamily: (locale?: string | undefined) => [fontFamily[], Record<string, boolean>, any];
