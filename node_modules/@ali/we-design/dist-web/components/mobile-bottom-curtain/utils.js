export var getStartTouch = function getStartTouch(touchList, startTouch) {
  if (startTouch === undefined) {
    return null;
  }

  var length = touchList.length;

  for (var i = 0; i < length; i++) {
    if (touchList[i].identifier === startTouch) {
      return touchList[i];
    }
  }

  return null;
};