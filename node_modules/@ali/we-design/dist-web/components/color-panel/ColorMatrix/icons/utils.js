import isDark from "../../utils/isDark";
var DARK_COLOR = '#4B4B4B';
var LIGHT_COLOR = '#FFFFFF';
export var getContrastingColor = function getContrastingColor(color) {
  if (!color) return DARK_COLOR;
  return isDark(color) ? LIGHT_COLOR : DARK_COLOR;
};