import tinycolor from 'tinycolor2';

var isDark = function isDark(color) {
  if (!color) return false;
  var colorRgbObj = tinycolor(color).toRgb();
  var yiq = (colorRgbObj.r * 299 + colorRgbObj.g * 587 + colorRgbObj.b * 114) / 1000;
  return yiq < 210;
};

export default isDark;