export var GRADIENT_FLAG = '__';

var isGradientColor = function isGradientColor(color) {
  // 判断是否为渐变色，渐变色格式为 ‘#color1__#color2’
  return color && color.includes(GRADIENT_FLAG);
};

export default isGradientColor;