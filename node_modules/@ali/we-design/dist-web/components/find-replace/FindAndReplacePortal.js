import { FindAndReplace } from "./FindAndReplace";
import { dragAndDropHOC } from "../../utils/dragAndDropHOC";
export var FindAndReplacePortal = dragAndDropHOC(FindAndReplace, {
  shouldDragElement: function shouldDragElement(event) {
    var tagName = event.target.tagName.toLowerCase(); // 鼠标点击在输入框上时，不可拖拽

    return tagName !== 'input';
  }
});