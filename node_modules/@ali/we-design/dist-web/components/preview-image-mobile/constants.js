export var SCALING_MAX_SCALE = 3;
export var SCALING_MIN_SCALE = 0.5;
export var STOP_MAX_SCALE = 2;
export var STOP_MIN_SCALE = 1;
export var PRELOAD_COUNT = 2; // 双击的两次单击间的间隔

export var DOUBLE_CLICK_INTERVAL = 300; // touch start 和 touch end 判定为单击的最大间隔

export var CLICK_INTERVAL = 100; // touch start 和 touch end 判定为快速滑动的最大间隔

export var QUICK_SLIDE_TIME = 300; // 判定为快速滑动的最小距离

export var QUICK_SLIDE_DISTANCE = 25;