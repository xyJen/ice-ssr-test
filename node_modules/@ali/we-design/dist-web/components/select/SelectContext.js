import React from 'react';
var executed = false;
var context; // 为了传人VT，需要写成函数的形式，但是应该是单例

export var createSelectContext = function createSelectContext() {
  if (!executed) {
    // eslint-disable-next-line @typescript-eslint/consistent-type-assertions
    context = /*#__PURE__*/React.createContext({});
    executed = true;
  }

  return context;
};
export var createMultiSelectContext = function createMultiSelectContext() {
  if (!executed) {
    // eslint-disable-next-line @typescript-eslint/consistent-type-assertions
    context = /*#__PURE__*/React.createContext({});
    executed = true;
  }

  return context;
};