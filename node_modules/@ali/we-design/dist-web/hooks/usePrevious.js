import { useRef, useEffect } from 'react'; // eslint-disable-next-line @typescript-eslint/no-explicit-any

export default (function (value) {
  var valueRef = useRef();
  useEffect(function () {
    valueRef.current = value;
  });
  return valueRef.current;
});