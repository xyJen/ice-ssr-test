var rule = {
  ruleName: 'code',
  rewrite: [{
    name: 'fence',
    createRenderer: function createRenderer(md) {
      var defaultRule = md.renderer.rules.fence;
      return function () {
        if (!defaultRule) {
          return '';
        }

        var defaultOutput = defaultRule.apply(void 0, arguments);
        var matches = defaultOutput.match(/^<pre><code( class=".+")?>([\s\S]*)<\/code><\/pre>\n$/);

        if (!matches) {
          return defaultOutput;
        }

        var _ = matches[0],
            classNames = matches[1],
            code = matches[2];
        return "<pre><code" + (classNames || '') + ">" + code.replace(/\n$/, '') + "</code></pre>\n";
      };
    }
  }]
};
export default rule;
//# sourceMappingURL=markdownToHTML.js.map