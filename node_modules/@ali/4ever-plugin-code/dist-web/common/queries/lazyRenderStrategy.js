import React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import getCodeHeight from "../../components/Codemirror/utils";
import { DEFAULT_THEME } from "../../utils/constants/themes";
import Placeholder from "../../components/Placeholder";
import Code from "../../mo/models";
export default function lazyRenderStrategy(_, node) {
  if (Code.isCode(node)) {
    var height = getCodeHeight(node);
    var _node$data$theme = node.data.theme,
        theme = _node$data$theme === void 0 ? DEFAULT_THEME : _node$data$theme;
    return {
      placeholder: /*#__PURE__*/_createElement(Placeholder, {
        height: height,
        dark: theme !== DEFAULT_THEME
      })
    };
  }

  return undefined;
}
//# sourceMappingURL=lazyRenderStrategy.js.map