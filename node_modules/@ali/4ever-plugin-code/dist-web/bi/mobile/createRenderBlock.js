import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { Block } from '@ali/4ever-cangjie';
import { MobileCode } from "./MobileCode";
export default function createRenderBlock(config) {
  return function renderBlock(props, controller, next) {
    var attributes = props.attributes,
        node = props.node,
        isSelected = props.isSelected;
    var _ref = node,
        type = _ref.type;

    if (Block.isBlock(node) && type === 'code') {
      return /*#__PURE__*/_createElement(MobileCode, {
        controller: controller,
        attributes: attributes,
        node: node,
        locale: config.locale,
        isSelected: isSelected
      });
    }

    return next();
  };
}
//# sourceMappingURL=createRenderBlock.js.map