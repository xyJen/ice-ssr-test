import { FOCUSED_CODE } from "../constants/value";
export default function (value, focusedCodeKey) {
  var data = value.data,
      document = value.document;
  var key = data[FOCUSED_CODE] || focusedCodeKey;
  return document.getNode(key);
}
//# sourceMappingURL=getCurrentCode.js.map