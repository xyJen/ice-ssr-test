export default function isUnderCode(controller) {
  var _controller$value = controller.value,
      selection = _controller$value.selection,
      document = _controller$value.document;

  if (!selection) {
    return false;
  }

  var isCollapsed = selection.isCollapsed;
  var start = selection.getStart(document);
  var previousBlock = document.getPreviousBlock(start.key);

  if (!isCollapsed || !previousBlock) {
    return false;
  }

  return previousBlock.type === 'code';
}
//# sourceMappingURL=isUnderCode.js.map