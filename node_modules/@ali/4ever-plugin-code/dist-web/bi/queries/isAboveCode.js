export default function isAboveCode(controller) {
  var _controller$value = controller.value,
      selection = _controller$value.selection,
      document = _controller$value.document;

  if (!selection) {
    return false;
  }

  var isCollapsed = selection.isCollapsed;
  var start = selection.getStart(document);
  var nextBlock = document.getNextBlock(start.key);

  if (!isCollapsed || !nextBlock) {
    return false;
  }

  return nextBlock.type === 'code';
}
//# sourceMappingURL=isAboveCode.js.map