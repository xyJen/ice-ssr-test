export default function isAtEndOfBlock(controller) {
  var _controller$value = controller.value,
      selection = _controller$value.selection,
      startBlock = _controller$value.startBlock,
      document = _controller$value.document;

  if (!selection || !startBlock) {
    return false;
  }

  var start = selection.getStart(document);
  return selection.isCollapsed && start.isAtEndOfNode(startBlock);
}
//# sourceMappingURL=isAtEndOfBlock.js.map