import { Controller, Action } from '@ali/4ever-cangjie';
import { CodePluginConfig } from '../types';
export default function createOnAction(config: CodePluginConfig): (action: Action, controller: Controller, next: () => void) => void | Controller;
