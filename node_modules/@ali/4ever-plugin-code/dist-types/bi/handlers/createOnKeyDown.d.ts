import React from 'react';
import { Controller } from '@ali/4ever-cangjie';
import { CodePluginConfig } from '../types';
export default function createOnKeyDown(config: CodePluginConfig, enableInsert?: boolean): (event: React.KeyboardEvent, controller: Controller, next: () => void) => void | Controller;
