import { Controller } from '@ali/4ever-cangjie';
import { CodePluginConfig } from '../types';
export default function createInsertCode(config?: CodePluginConfig): (controller: Controller, id: string, code?: string, syntax?: string, theme?: string) => Controller;
