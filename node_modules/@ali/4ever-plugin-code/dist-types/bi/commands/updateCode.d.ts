import { Controller, Block } from '@ali/4ever-cangjie';
import { CodeData } from '../types';
export default function updateCode(controller: Controller, node: Block, data: Partial<CodeData>): Controller;
