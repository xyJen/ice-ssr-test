import { Controller, RenderNodeProps } from '@ali/4ever-cangjie';
import { BiConfig } from '../types';
export default function createRenderBlock(config: BiConfig): (props: RenderNodeProps, controller: Controller, next: () => JSX.Element) => JSX.Element;
