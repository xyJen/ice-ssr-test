import * as React from 'react';
import { CodePluginConfig } from '../types';
export interface SelectOptions {
    name: string;
    createList: (locale: CodePluginConfig['locale']) => Array<{
        title: React.ReactNode;
        key: string;
        show?: boolean;
    }>;
    search?: boolean;
}
export interface SelectProps {
    value: string;
    tip: string;
    onChange: (value: string) => void;
    locale: CodePluginConfig['locale'];
    renderTrigger?: (value: any) => JSX.Element;
    showArrow?: boolean;
    style?: React.CSSProperties;
    filterOption?: boolean | ((search: string, option: React.ReactNode) => boolean);
    inputTriggerText?: (value: string) => string;
    visible?: boolean;
    onVisibleChange?: (v: boolean) => void;
}
export default function createSelect(options: SelectOptions): React.NamedExoticComponent<SelectProps>;
