import { Controller } from '@ali/4ever-cangjie';
import { CodeLocale } from '../types';
export declare type MoreClickEvent = (type: 'copy' | 'cut' | 'delete') => void;
interface ToolbarMoreProps {
    controller: Controller;
    locale: CodeLocale;
    getContainer: Function;
    nodeKey: string;
}
export default function ToolbarMore(props: ToolbarMoreProps): JSX.Element;
export {};
