import { RenderNodeProps } from '@ali/4ever-cangjie';
import { CodePluginConfig } from './types';
export default function createRenderBlock(config: CodePluginConfig): {
    code: (props: RenderNodeProps, controller: any) => JSX.Element;
};
