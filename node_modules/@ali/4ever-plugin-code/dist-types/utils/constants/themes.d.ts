export declare const DEFAULT_THEME = "default";
interface Locale {
    lightTheme: string;
    darkTheme: string;
}
export declare const CodeWrapper: import("styled-components").StyledComponent<"div", any, {}, never>;
export declare const createThemeMenuItems: (locale: Locale, key: string) => {
    key: string;
    prefix: JSX.Element;
    title: string;
    selected: boolean;
    testid: string;
}[];
export declare const createCodeThemes: (locale: Locale) => {
    title: JSX.Element;
    key: string;
}[];
export {};
