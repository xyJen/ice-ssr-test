"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isAboveCode;

function isAboveCode(controller) {
  const {
    selection,
    document
  } = controller.value;

  if (!selection) {
    return false;
  }

  const {
    isCollapsed
  } = selection;
  const start = selection.getStart(document);
  const nextBlock = document.getNextBlock(start.key);

  if (!isCollapsed || !nextBlock) {
    return false;
  }

  return nextBlock.type === 'code';
}
//# sourceMappingURL=isAboveCode.js.map