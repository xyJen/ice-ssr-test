"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isAtEndOfBlock;

function isAtEndOfBlock(controller) {
  const {
    selection,
    startBlock,
    document
  } = controller.value;

  if (!selection || !startBlock) {
    return false;
  }

  const start = selection.getStart(document);
  return selection.isCollapsed && start.isAtEndOfNode(startBlock);
}
//# sourceMappingURL=isAtEndOfBlock.js.map