"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isUnderCode;

function isUnderCode(controller) {
  const {
    selection,
    document
  } = controller.value;

  if (!selection) {
    return false;
  }

  const {
    isCollapsed
  } = selection;
  const start = selection.getStart(document);
  const previousBlock = document.getPreviousBlock(start.key);

  if (!isCollapsed || !previousBlock) {
    return false;
  }

  return previousBlock.type === 'code';
}
//# sourceMappingURL=isUnderCode.js.map