"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isAtStartOfBlock;

function isAtStartOfBlock(controller) {
  const {
    selection,
    startBlock,
    document
  } = controller.value;

  if (!selection || !startBlock) {
    return false;
  }

  const start = selection.getStart(document);
  return selection.isCollapsed && start.isAtStartOfNode(startBlock);
}
//# sourceMappingURL=isAtStartOfBlock.js.map