import Appender from './interface/Appender';
import APIParams from './interface/APIParams';
import Level from './interface/Level';
export default class NoopAppender implements Appender {
    ignore(action: string, params?: Record<string, any>): boolean;
    setBaseInfo(params: Record<string, any>): void;
    sum(key: string): void;
    avg(key: string, value?: number): void;
    log(level: Level, category: string, content: string | Record<string, any> | Error): void;
    api(params: APIParams): void;
}
