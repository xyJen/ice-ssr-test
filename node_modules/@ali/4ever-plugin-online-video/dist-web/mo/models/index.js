import _extends from "@babel/runtime/helpers/extends";
import _inheritsLoose from "@babel/runtime/helpers/inheritsLoose";
import { Block } from '@ali/4ever-cangjie';
import { ELEMENT_TYPE } from "../../utils/meta";

var OnlineVideo = /*#__PURE__*/function (_Block) {
  _inheritsLoose(OnlineVideo, _Block);

  function OnlineVideo() {
    return _Block.apply(this, arguments) || this;
  }

  OnlineVideo.create = function create(properties) {
    if (typeof properties === 'string') {
      throw new Error('`OnlineVideo.create` does not accept string');
    }

    if (OnlineVideo.isOnlineVideo(properties)) {
      return properties;
    }

    return _Block.create.call(this, _extends({}, properties, {
      type: OnlineVideo.TYPE
    }));
  };

  OnlineVideo.isOnlineVideo = function isOnlineVideo(maybeOnlineVideo) {
    return Block.isBlock(maybeOnlineVideo) && maybeOnlineVideo.type === OnlineVideo.TYPE;
  };

  return OnlineVideo;
}(Block);

OnlineVideo.TYPE = ELEMENT_TYPE;
export { OnlineVideo as default };
//# sourceMappingURL=index.js.map