import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import P from "../components/EditParagraph";
export default function createRenderNode() {
  return {
    paragraph: function paragraph(props) {
      var children = props.children,
          controller = props.controller,
          node = props.node,
          attributes = props.attributes,
          visible = props.visible;
      /**
       * list 与 paragraph 的 type 一致，render 通过 query 解耦。
       *
       * 注：通过中间件方式解耦更合理，但不如 k/v 方式快，需要评估对性能的影响
       */

      var content = props.controller.query('renderParagraph', props);

      if (content) {
        return content;
      }

      return /*#__PURE__*/_createElement(P, {
        controller: controller,
        node: node,
        attributes: attributes,
        visible: visible
      }, children());
    }
  };
}
//# sourceMappingURL=createRenderNode.js.map