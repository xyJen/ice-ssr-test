import _extends from "@babel/runtime/helpers/extends";
import { paragraphTags } from '@ali/4ever-utils';
export default function getParagraphTagStyle(name, rawAttrs) {
  if (['MsoTitle', 'MsoSubtitle'].includes(rawAttrs == null ? void 0 : rawAttrs["class"])) {
    return {
      p: [paragraphTags.h2],
      rawAttrs: _extends({
        style: "textAlign:center;" + (rawAttrs.style || '')
      }, rawAttrs, {
        styleObj: _extends({}, !rawAttrs.style ? {
          textAlign: 'center'
        } : {}, rawAttrs.styleObj)
      })
    };
  }

  return {
    p: [paragraphTags[name]],
    rawAttrs: rawAttrs
  };
}
//# sourceMappingURL=getParagraphTagStyle.js.map