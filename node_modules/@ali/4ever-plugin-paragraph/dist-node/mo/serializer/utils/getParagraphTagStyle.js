"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getParagraphTagStyle;

var _everUtils = require("@ali/4ever-utils");

function getParagraphTagStyle(name, rawAttrs) {
  if (['MsoTitle', 'MsoSubtitle'].includes(rawAttrs?.class)) {
    return {
      p: [_everUtils.paragraphTags.h2],
      rawAttrs: {
        style: `textAlign:center;${rawAttrs.style || ''}`,
        ...rawAttrs,
        styleObj: { ...(!rawAttrs.style ? {
            textAlign: 'center'
          } : {}),
          ...rawAttrs.styleObj
        }
      }
    };
  }

  return {
    p: [_everUtils.paragraphTags[name]],
    rawAttrs
  };
}
//# sourceMappingURL=getParagraphTagStyle.js.map