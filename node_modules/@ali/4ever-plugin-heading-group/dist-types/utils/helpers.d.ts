import { Controller, Document, Node } from '@ali/4ever-cangjie';
/**
 * 节点是否在折叠的 Group 之内
 * @param document
 * @param node
 * @returns
 */
export declare function isFoldInGroup(document: Document, node: Node, controller: Controller): boolean;
/**
 * 节点是否在未折叠的 Group 内
 * @param document
 * @param node
 * @returns
 * @description 通常此方法需要与 `isFoldInGroup` 配合使用
 */
export declare function isUnfoldInGroup(document: Document, node: Node, controller: Controller): boolean;
