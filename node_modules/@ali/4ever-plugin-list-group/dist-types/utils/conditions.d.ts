import { Block } from '@ali/4ever-cangjie';
export declare const listGroupConditions: Array<{
    /**
     * 聚合列表
     */
    when: (node: any, nextNode: any) => boolean;
    /**
     * 聚合截止
     */
    until: (node: any, lastFoldNode: Block) => boolean;
}>;
