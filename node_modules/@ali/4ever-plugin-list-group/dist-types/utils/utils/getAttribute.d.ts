import { Block, Controller, Element } from '@ali/4ever-cangjie';
export declare function isInContainer(node: Element | null): boolean;
/**
 * 查询节点是否在非 listGroup 的容器节点中
 * 容器节点，如 表格、高亮块等
 */
export declare function isInUnlistGroupContainer(node: Block, controller: Controller): boolean;
export declare function getAttributes(attributes: {
    [key: string]: any;
}, node: Block, controller: Controller): {
    style: any;
};
