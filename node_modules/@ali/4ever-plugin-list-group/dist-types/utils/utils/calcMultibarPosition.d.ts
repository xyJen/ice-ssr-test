import { Block } from '@ali/4ever-cangjie';
/**
 * 计算列表连接线位置
 * @param node 折叠节点
 * @returns left 列表与左侧距离, symbolWidth 符号宽度
 */
export declare function calcMultibarPosition(node: Block): {
    left: number;
    listSymbol: string;
    symbolWidth: number;
};
/**
 * 计算折叠按钮位置
 * @param
 * @param
 * @returns
 */
export declare function calcFoldBoxPosition(node: Block): number;
/**
 * 计算 list group 背景颜色宽度
 * @param
 * @param
 * @returns
 */
export declare function calcBackgroundWidth(node: Block, left: number, symbolWidth: number): string;
