"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = arabic2Chinese;
exports.CHINESE_UNIT_LIST = exports.CHINESE_NUMBER_LIST = void 0;
const CHINESE_NUMBER_LIST = ['零', '一', '二', '三', '四', '五', '六', '七', '八', '九'];
exports.CHINESE_NUMBER_LIST = CHINESE_NUMBER_LIST;
const CHINESE_UNIT_LIST = ['', '十', '百', '千', '万', '十', '百', '千', '亿', '十', '百', '千', '万'];
exports.CHINESE_UNIT_LIST = CHINESE_UNIT_LIST;

function arabic2Chinese(num) {
  let unitPos = 0;
  let zero = true;
  let strIns = '';
  let chnStr = '';
  let arabic = num;

  while (arabic > 0) {
    const v = arabic % 10;

    if (v === 0) {
      if (!zero) {
        zero = true;
        chnStr = CHINESE_NUMBER_LIST[v] + chnStr;
      }
    } else {
      zero = false;
      strIns = CHINESE_NUMBER_LIST[v];
      strIns += CHINESE_UNIT_LIST[unitPos];
      chnStr = strIns + chnStr;
    }

    unitPos += 1;
    arabic = Math.floor(arabic / 10);
  }

  if (num > 9 && num < 20) {
    return chnStr.slice(1);
  }

  return chnStr;
}
//# sourceMappingURL=arabic2Chinese.js.map