"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = arabic2Letter;
exports.LETTER_LIST = void 0;
const LETTER_LIST = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';
exports.LETTER_LIST = LETTER_LIST;

function arabic2Letter(number, type) {
  let num = number;
  const map = type === 'upper' ? LETTER_LIST : LETTER_LIST.toLowerCase();
  let result = '';

  while (num > 0) {
    num -= 1;
    result = map[num % 26] + result;
    num = Math.floor(num / 26);
  }

  return result;
}
//# sourceMappingURL=arabic2Letter.js.map