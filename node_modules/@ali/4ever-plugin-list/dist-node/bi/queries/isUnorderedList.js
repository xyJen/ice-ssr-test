"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isUnorderedList;

function isUnorderedList(editor) {
  const {
    value
  } = editor;
  const {
    selection,
    document
  } = value;
  const block = selection && document.getClosest(selection.focus.key, ({
    type
  }) => type === 'paragraph' || String(type).indexOf('heading-') >= 0);
  return Boolean(block && block.data.list && !block.data.list.isOrdered);
}
//# sourceMappingURL=isUnorderedList.js.map