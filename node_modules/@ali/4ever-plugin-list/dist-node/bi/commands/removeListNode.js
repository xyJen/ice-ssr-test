"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = removeListNode;

var _everCangjie = require("@ali/4ever-cangjie");

var _utils = require("../utils");

function removeListNode(editor) {
  const {
    value
  } = editor;
  const currentList = (0, _utils.getCurrentItem)(editor);
  const data = currentList[0]?.data;
  if (!data || !data.list) return editor;
  const newData = { ...data
  };
  delete newData.list;
  currentList.forEach(element => {
    element && editor.command(_everCangjie.Commands.setNodeByKey, element.key, {
      data: newData
    });
  });
  return editor;
}
//# sourceMappingURL=removeListNode.js.map