"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isFirstInCurrentLevel;

var _isList = _interopRequireDefault(require("./isList"));

function isFirstInCurrentLevel(value, currentItem) {
  const {
    document
  } = value;
  const curList = currentItem.data?.list;
  let prev = document.getPreviousBlock(currentItem.key);

  while (prev) {
    const prevList = prev.data?.list;

    if ((0, _isList.default)(prev) && prevList.listId === curList.listId) {
      return prevList.level < curList.level;
    }

    prev = document.getPreviousBlock(prev.key);
  }

  return true;
}
//# sourceMappingURL=isFirstInCurrentLevel.js.map