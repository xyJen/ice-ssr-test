"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getListStyleType;

function getListStyleType(controller) {
  const {
    value
  } = controller;
  const {
    selection,
    document
  } = value;
  const paragraph = selection && document // @ts-ignore
  .getClosest(selection.anchor.key, ({
    type
  }) => type === 'paragraph');
  if (!paragraph) return '';
  const list = paragraph.data.list;
  return list?.listStyleType || '';
}
//# sourceMappingURL=getListStyleType.js.map