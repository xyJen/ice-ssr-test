"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

const getListItemsBySelection = controller => {
  const {
    document
  } = controller.value;
  const {
    listSelections
  } = controller.value.data;

  if (listSelections.length) {
    return listSelections.reduce((acc, cur) => {
      const n = document.getNode(cur);

      if (n) {
        acc.push(n);
      }

      return acc;
    }, []);
  }

  return [];
};

var _default = getListItemsBySelection;
exports.default = _default;
//# sourceMappingURL=getListItemsBySelection.js.map