"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _isInSameList = require("../../utils/isInSameList");

var _default = (value, currrentItem) => {
  const result = [];
  if (!currrentItem) return result;
  const {
    key
  } = currrentItem;
  const currentList = currrentItem.data.list;
  if (!key || !currentList) return [];
  const {
    listId
  } = currentList;
  const {
    document
  } = value;
  const root = document.getParent(key) || document;
  const nodesLength = root?.nodes.length || 0;

  for (let i = 0; i < nodesLength; i++) {
    const node = root?.nodes[i];
    const listConfig = node.data.list;
    if (!(0, _isInSameList.isInSameList)(listConfig, listId)) continue;
    result.push(node.key);
  }

  return result;
};

exports.default = _default;
//# sourceMappingURL=getSameGroupListItems.js.map