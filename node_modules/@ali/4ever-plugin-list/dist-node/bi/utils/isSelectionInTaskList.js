"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isSelectionInTaskList;

function isSelectionInTaskList(value) {
  const {
    selection,
    document
  } = value;
  const paragraph = document // @ts-ignore
  .getClosest(selection.getStart(document).key, node => node.type === 'paragraph');
  if (!paragraph || !paragraph.data) return false;
  const {
    list
  } = paragraph.data;
  if (!list) return false;
  const {
    isTaskList
  } = list;
  return isTaskList;
}
//# sourceMappingURL=isSelectionInTaskList.js.map