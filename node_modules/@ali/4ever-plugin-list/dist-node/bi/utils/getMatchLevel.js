"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getMatchLevel;

var _everUtils = require("@ali/4ever-utils");

/**
 * @description 根据普通段落的缩进，获取列表中匹配的 level 值
 * */
function getMatchLevel(block) {
  let level = 0;

  if (block?.data?.ind) {
    const firstLine = block.data.ind.firstLine || 0;
    const left = block.data.ind.left || 0;
    level = Math.floor((firstLine + left) / _everUtils.INDENT_SIZE);
  }

  return level;
}
//# sourceMappingURL=getMatchLevel.js.map