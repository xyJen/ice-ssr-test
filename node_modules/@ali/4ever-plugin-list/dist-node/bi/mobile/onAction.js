"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = onAction;

var _actions = require("../actions");

var _commands = require("../commands");

function onAction(action, controller, next) {
  const {
    type,
    payload
  } = action;

  if (type === _actions.LIST_TOGGLE_LIST) {
    const {
      listType,
      listStyleType
    } = payload;
    return (0, _commands.toggleWrapInList)(controller, listType, listStyleType);
  }

  if (type === _actions.LIST_TRANSFORM) {
    const {
      listType,
      listStyleType
    } = payload;
    return (0, _commands.transform)(controller, listType, listStyleType); // return controller.command(transform, listType, listStyleType);
  }

  if (type === _actions.LIST_REMOVE_LIST) {
    return (0, _commands.removeListNode)(controller);
  }

  if (type === _actions.LIST_SET_CHECK_STATUS) {
    const {
      node,
      checked
    } = payload;
    return (0, _commands.toggleCheckStatus)(controller, node.key, checked);
  }

  return next();
}
//# sourceMappingURL=onAction.js.map