"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = placeholder;

var _utils = require("./utils");

function placeholder(config) {
  const {
    locale
  } = config; // 可以通过先后顺序控制

  return [{
    text: locale?.taskListPlaceholder,
    when: (controller, node) => {
      return node?.data?.list?.isTaskList && (0, _utils.isEmpty)(node);
    }
  }, {
    text: locale?.listPlaceholder,
    needWidth: true,
    when: (controller, node) => {
      return node?.data?.list && !node?.data?.list?.isTaskList && (0, _utils.isEmpty)(node);
    }
  }];
}
//# sourceMappingURL=placeholder.js.map