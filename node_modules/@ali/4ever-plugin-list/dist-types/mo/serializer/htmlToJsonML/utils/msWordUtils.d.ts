import type { MoState as State } from '@ali/4ever-cangjie';
/**
 * @description word缩进单位与仓颉的缩进单位不同，此函数做单位转换
 * */
export declare function transfromIndLeft(marginLeft: string, level: number): number;
/**
 * @description 移除掉word 节点中项目符号相关的文本，只保留列表内容
 * */
export declare function removeSymbolText(state: State, preTxt: string): void;
/**
 * @description 通过style判断是否为word列表项
 * */
export declare function isMsWordList(style: string): boolean;
