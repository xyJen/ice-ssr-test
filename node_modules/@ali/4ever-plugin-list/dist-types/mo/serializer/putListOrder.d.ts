import { Block, Document, Injection } from '@ali/4ever-cangjie';
export interface ListCountContext {
    itemCount: Map<number, number>;
    prevLevel?: number;
}
export declare const createListCountContext: () => ListCountContext;
export declare const getListOrder: (level: number, orderCount: Map<number, number>, prevLevel?: number) => number;
export declare function getListOrderData(root: Document | Block, listId: string): Injection<{
    start: number;
    step: string;
    listId: string;
}>[];
