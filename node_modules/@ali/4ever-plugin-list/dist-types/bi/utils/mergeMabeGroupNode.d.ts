import { Block, Controller } from '@ali/4ever-cangjie';
/**
 * 若上一个可视节点是 GroupNode list 节点，做可视节点的合并
 * I -> 光标，在光标位置按 backspace 键
 *
 * 场景一：列表 ddd 合并到列表 ccc
 *
 *   1. aaa             1. aaa
 *   |  a. bbb          |  a. bbb
 *   |    i. ccc   ->   |    i. cccIddd
 *   2. Iddd
 *
 * 场景二：段落 ddd 合并到列表 bbb
 *
 *   1. aaa             1. aaa
 *   | >a. bbb     ->   |  > a.bbbIddd
 *   Iddd
 *
 * 场景三：段落 ddd 合并到列表 aaa
 *
 *  >1. aaa        ->   >1. aaaIddd
 *   Iddd
 */
export default function mergeMabeGroupNode(node: Block, controller: Controller): void;
