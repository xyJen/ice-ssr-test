import { Block, Controller } from '@ali/4ever-cangjie';
/**
 *
 * @param controller
 * @param newData
 * @param type 可选，要转的列表类型
 * @param blocks 可选，要转为列表的块节点
 * @returns
 */
export default function toggleList(controller: Controller, newData: any, type?: any, blocks?: Block[]): Controller;
