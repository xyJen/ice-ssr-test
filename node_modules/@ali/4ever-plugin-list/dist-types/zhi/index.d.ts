import { Document, Block, Plugin } from '@ali/4ever-cangjie';
export interface ListConfig {
    getListItemOrder?: (document: Document, node: Block) => number;
}
declare function createListPlugin(config: {
    list?: ListConfig;
}): Plugin;
export default createListPlugin;
