import React from 'react';
import { Block, Controller, Document } from '@ali/4ever-cangjie';
import { StyledComponent } from 'styled-components';
export interface IListItemProps {
    node: Block;
    controller: Controller;
    nodeRef?: React.MutableRefObject<HTMLElement | null> | ((instance: HTMLElement | null) => void) | null;
    step?: string;
    children?: React.ReactNode;
    Component?: StyledComponent<any, any>;
    document?: Document;
    getListItemOrder?: (document: Document, node: Block) => void;
    visible?: boolean;
}
export interface IProps {
    allowCustomCheck?: boolean;
}
export default function createListItem(Paragraph: any): React.ForwardRefExoticComponent<IListItemProps & React.RefAttributes<HTMLElement>>;
