import { ListStyleProperties } from './types';
export declare const RELATIVE_LEVEL: number[];
export declare function generateSymbol(level: number, start: number, step: string, isOrdered: boolean, listStyleType?: string, listStyle?: ListStyleProperties): string;
