/**
 * 无序命名规则
 * 例：SCIR_ECIR_SREC, 形状 + 形状 + 形状
 *
 * 首字母：
 * S -> solid (■)
 * E -> empty (❏)
 *
 * 形状：
 * ● -> CIR -> circle
 * ❏ -> REC -> rect
 * ❖ -> FDI -> four diomand
 * ◆ -> DIO -> diomand
 * ➔ -> ARR -> arrow
 * ➢ -> RIG -> right
 * */
export declare const symbol_text_microsoft: {
    Ø: string;
    l: string;
    u: string;
    n: string;
    '+': string;
    '-': string;
};
export declare const symbol_type_miscrosoft: {
    Ø: string;
    l: string;
    u: string;
    n: string;
    '+': string;
    '-': string;
};
export declare enum DEFAULT_BULLET_LIST_ARRAY {
    'SCIR_ECIR_SREC' = 0,
    'SFDI_SRIG_SREC' = 1,
    'EREC_EREC_EREC' = 2,
    'SARR_SDIO_SCIR' = 3,
    'SSTA_ECIR_SREC' = 4,
    'SRIG_ECIR_SREC' = 5,
    'MINUS_MINUS_MINUS' = 6,
    'PLUS_PLUS_PLUS' = 7,
    'VACATION_TEA_MILKTEA' = 8,
    'RIGHT_NAIL_LOVE' = 9,
    'FIRE_ROCKET_NAIL' = 10
}
/**
 * 有序命名规则
 * 例： DEC_LEN_LROM_P ，符号类型 + 符号类型 + 符号类型 + 首个符号格式
 *
 * 符号类型：
 * DEC -> decimal
 * DDEC -> decimalZero
 * LEN -> lowerLetter
 * UEN -> upperLetter
 * LROM -> lowerRoman
 * UROM -> upperRoman
 * ZH -> chineseCountingThousand
 *
 * 符号格式：
 * P（point) -> xxx.
 * H（half)  -> xxx)
 * A（all)   -> (xxx)
 * D (don)   -> xxx、
 * */
export declare enum DEFAULT_NUMBER_LIST_ARRAY {
    'DEC_LEN_LROM_P' = 0,
    'DEC_LEN_LROM_H' = 1,
    'DEC_DEC_DEC_P' = 2,
    'UEN_LEN_LROM_P' = 3,
    'UROM_UEN_DEC_P' = 4,
    'DDEC_LEN_LROM_P' = 5,
    'ZH_DEC_LEN_D' = 6,
    'ZH_DEC_LEN_A' = 7,
    'LEN_LROM_DEC_P' = 8,
    'DEC_LEN_LROM_A' = 9,
    'LEN_LROM_DEC_H' = 10,
    'DEC_LEN_LROM_D' = 11,
    'ZH_LEN_LROM_A' = 12,
    'ZH_LEN_LROM_D' = 13,
    'ZH_ZH_DEC_D' = 14
}
/**
 * unicode映射表
 * ● -> \u25cf
 * ○ -> \u25cb
 * ■ -> \u25a0
 * ❖ -> \u2756
 * ❏ -> \u274f
 * ➔ -> \u2794
 * ◆  -> \u25c6
 * ★ -> \u2605
 * ➢ -> \u27a2
 * -  -> \u2012
 * +  ->  +
 * */
export interface ListStyle {
    format: string;
    text: string;
    align: string;
}
export declare const mapping: {
    SCIR_ECIR_SREC: string[][];
    SFDI_SRIG_SREC: string[][];
    EREC_EREC_EREC: string[][];
    SARR_SDIO_SCIR: string[][];
    SSTA_ECIR_SREC: string[][];
    SRIG_ECIR_SREC: string[][];
    MINUS_MINUS_MINUS: string[][];
    PLUS_PLUS_PLUS: string[][];
    VACATION_TEA_MILKTEA: string[][];
    RIGHT_NAIL_LOVE: string[][];
    FIRE_ROCKET_NAIL: string[][];
    DEC_LEN_LROM_P: string[][];
    DEC_LEN_LROM_H: string[][];
    DEC_LEN_LROM_A: string[][];
    DEC_DEC_DEC_P: string[][];
    UEN_LEN_LROM_P: string[][];
    UROM_UEN_DEC_P: string[][];
    DDEC_LEN_LROM_P: string[][];
    ZH_LEN_LROM_D: string[][];
    ZH_LEN_LROM_A: string[][];
    ZH_DEC_LEN_D: string[][];
    ZH_DEC_LEN_A: string[][];
    ZH_ZH_DEC_D: string[][];
    LEN_LROM_DEC_P: string[][];
    LEN_LROM_DEC_H: string[][];
    DEC_LEN_LROM_D: string[][];
};
