export default function isTaskList(editor) {
  var value = editor.value;
  var selection = value.selection,
      document = value.document;
  var paragraph = selection && document.getClosest(selection.anchor.key, // @ts-ignore
  function (_ref) {
    var type = _ref.type;
    return typeof type === 'string' && (type === 'paragraph' || type.startsWith('heading-'));
  });

  if (!paragraph) {
    return false;
  }

  var list = paragraph.data.list;
  return !!(list && !list.isOrdered && list.isTaskList);
}
//# sourceMappingURL=isTaskList.js.map