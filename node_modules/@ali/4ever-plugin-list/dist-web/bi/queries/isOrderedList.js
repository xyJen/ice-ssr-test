export default function isOrderedList(editor) {
  var value = editor.value;
  var selection = value.selection,
      document = value.document;
  var block = selection && document.getClosest(selection.focus.key, function (_ref) {
    var type = _ref.type;
    return type === 'paragraph' || String(type).indexOf('heading-') >= 0;
  });
  return Boolean(block && block.data.list && block.data.list.isOrdered);
}
//# sourceMappingURL=isOrderedList.js.map