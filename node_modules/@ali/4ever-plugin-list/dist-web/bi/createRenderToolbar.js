import _extends from "@babel/runtime/helpers/extends";
import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { ItemKey, ItemSlot, SlotType } from '@ali/4ever-component';
import TaskListButton from "./components/next/taskListButton";
import UnorderedListButton from "./components/next/unorderedListButton";
import OrderedListButton from "./components/next/orderedListButton";
var buttons = {
  'list.ulist': UnorderedListButton,
  'list.olist': OrderedListButton
};
export default function createRenderToolbar(config) {
  return function (props, controller) {
    var buttonName = props.buttonName,
        extraProps = props.extraProps;
    var Component = buttons[buttonName];

    if (Component) {
      return /*#__PURE__*/_createElement(ItemSlot, {
        display: SlotType.splitButton
      }, /*#__PURE__*/_createElement(Component, _extends({
        controller: controller,
        locale: config.locale,
        enableSticker: config.enableSticker
      }, extraProps)));
    }

    if (buttonName === ItemKey['list.tlist']) {
      return /*#__PURE__*/_createElement(ItemSlot, {
        display: SlotType.iconButton
      }, /*#__PURE__*/_createElement(TaskListButton, _extends({
        controller: controller,
        locale: config.locale
      }, extraProps)));
    }

    return null;
  };
}
//# sourceMappingURL=createRenderToolbar.js.map