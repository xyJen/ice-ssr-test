import { Commands } from '@ali/4ever-cangjie';
import { getCurrentItem, mergeListData } from "../utils";
export default (function (editor) {
  var _currentList$;

  var value = editor.value;
  var currentList = getCurrentItem(editor);
  var data = (_currentList$ = currentList[0]) == null ? void 0 : _currentList$.data;
  if (!data || !data.list || !currentList[0]) return editor;
  return editor.command(Commands.setNodeByKey, currentList[0].key, mergeListData(data, {
    hideSymbol: true
  }));
});
//# sourceMappingURL=removeListSymbol.js.map