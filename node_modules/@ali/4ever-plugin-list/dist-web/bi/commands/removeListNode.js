import _extends from "@babel/runtime/helpers/extends";
import { Commands } from '@ali/4ever-cangjie';
import { getCurrentItem } from "../utils";
export default function removeListNode(editor) {
  var _currentList$;

  var value = editor.value;
  var currentList = getCurrentItem(editor);
  var data = (_currentList$ = currentList[0]) == null ? void 0 : _currentList$.data;
  if (!data || !data.list) return editor;

  var newData = _extends({}, data);

  delete newData.list;
  currentList.forEach(function (element) {
    element && editor.command(Commands.setNodeByKey, element.key, {
      data: newData
    });
  });
  return editor;
}
//# sourceMappingURL=removeListNode.js.map