import { Commands } from '@ali/4ever-cangjie';
import { mergeListData } from "../utils";
export default (function (editor, key, isChecked) {
  var value = editor.value;
  var document = value.document;
  var node = document.getNode(key);
  var data = node.data;
  if (!data.list) return editor;
  return editor.command(Commands.setNodeByKey, key, mergeListData(data, {
    isChecked: isChecked
  }));
});
//# sourceMappingURL=toggleCheckStatus.js.map