import { Text } from '@ali/4ever-cangjie';
export default function isEmpty(block) {
  return block.nodes.every(function (node) {
    return Text.isText(node) && !node.text;
  });
}
//# sourceMappingURL=isEmpty.js.map