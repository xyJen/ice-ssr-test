export default function isSelectionInTaskList(value) {
  var selection = value.selection,
      document = value.document;
  var paragraph = document // @ts-ignore
  .getClosest(selection.getStart(document).key, function (node) {
    return node.type === 'paragraph';
  });
  if (!paragraph || !paragraph.data) return false;
  var list = paragraph.data.list;
  if (!list) return false;
  var isTaskList = list.isTaskList;
  return isTaskList;
}
//# sourceMappingURL=isSelectionInTaskList.js.map