export default function isSelectionInList(value) {
  var selection = value.selection,
      document = value.document;
  var paragraph = document // @ts-ignore
  .getClosest(selection.anchor.key, function (_ref) {
    var type = _ref.type;
    return type === 'paragraph';
  });
  var list = paragraph.data.list;
  return list;
}
//# sourceMappingURL=isSelectionInList.js.map