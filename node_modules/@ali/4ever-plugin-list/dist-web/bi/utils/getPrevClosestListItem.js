import getMatchLevel from "./getMatchLevel";
export default (function (value, currentItem, config) {
  if (config === void 0) {
    config = {};
  }

  var document = value.document;
  var level = getMatchLevel(currentItem);
  var prevBlock = document.getPreviousNode(currentItem.key);

  while (prevBlock) {
    var _prevBlock, _prevBlock$data;

    var listConfig = (_prevBlock = prevBlock) == null ? void 0 : (_prevBlock$data = _prevBlock.data) == null ? void 0 : _prevBlock$data.list;

    if (listConfig && listConfig.level !== undefined) {
      if (config.unLimitLevel || (listConfig == null ? void 0 : listConfig.level) === level) {
        var isSameType = !listConfig.isTaskList && listConfig.isOrdered === config.isOrdered;

        if (config.isOrdered === undefined || isSameType) {
          return prevBlock;
        } else {
          prevBlock = document.getPreviousNode(prevBlock.key);
          continue;
        }
      }
    }

    prevBlock = document.getPreviousNode(prevBlock.key);
  }

  return null;
});
//# sourceMappingURL=getPrevClosestListItem.js.map