var getListItemsBySelection = function getListItemsBySelection(controller) {
  var document = controller.value.document;
  var listSelections = controller.value.data.listSelections;

  if (listSelections.length) {
    return listSelections.reduce(function (acc, cur) {
      var n = document.getNode(cur);

      if (n) {
        acc.push(n);
      }

      return acc;
    }, []);
  }

  return [];
};

export default getListItemsBySelection;
//# sourceMappingURL=getListItemsBySelection.js.map