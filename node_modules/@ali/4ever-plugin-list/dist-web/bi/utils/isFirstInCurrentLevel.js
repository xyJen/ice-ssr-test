import isList from "./isList";
export default function isFirstInCurrentLevel(value, currentItem) {
  var _currentItem$data;

  var document = value.document;
  var curList = (_currentItem$data = currentItem.data) == null ? void 0 : _currentItem$data.list;
  var prev = document.getPreviousBlock(currentItem.key);

  while (prev) {
    var _prev$data;

    var prevList = (_prev$data = prev.data) == null ? void 0 : _prev$data.list;

    if (isList(prev) && prevList.listId === curList.listId) {
      return prevList.level < curList.level;
    }

    prev = document.getPreviousBlock(prev.key);
  }

  return true;
}
//# sourceMappingURL=isFirstInCurrentLevel.js.map