export default function getListStyleType(controller) {
  var value = controller.value;
  var selection = value.selection,
      document = value.document;
  var paragraph = selection && document // @ts-ignore
  .getClosest(selection.anchor.key, function (_ref) {
    var type = _ref.type;
    return type === 'paragraph';
  });
  if (!paragraph) return '';
  var list = paragraph.data.list;
  return (list == null ? void 0 : list.listStyleType) || '';
}
//# sourceMappingURL=getListStyleType.js.map