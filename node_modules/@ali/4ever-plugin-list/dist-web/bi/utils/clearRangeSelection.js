import { Commands } from '@ali/4ever-cangjie';
export default (function (controller, block) {
  var selection = controller.value.selection;

  if (selection) {
    controller.command(Commands.moveAnchorToStartOfNode, block).command(Commands.moveFocusToStartOfNode, block);
  }

  controller.command(Commands.blur);
});
//# sourceMappingURL=clearRangeSelection.js.map