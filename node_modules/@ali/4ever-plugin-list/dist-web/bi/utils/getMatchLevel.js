import { INDENT_SIZE } from '@ali/4ever-utils';
/**
 * @description 根据普通段落的缩进，获取列表中匹配的 level 值
 * */

export default function getMatchLevel(block) {
  var _block$data;

  var level = 0;

  if (block != null && (_block$data = block.data) != null && _block$data.ind) {
    var firstLine = block.data.ind.firstLine || 0;
    var left = block.data.ind.left || 0;
    level = Math.floor((firstLine + left) / INDENT_SIZE);
  }

  return level;
}
//# sourceMappingURL=getMatchLevel.js.map