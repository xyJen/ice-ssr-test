import { Block } from '@ali/4ever-cangjie';
import isList from "./isList";
/**
 * 寻找 listId 中首个列表项
 */

export default function findFirstListItemById(listItem, document) {
  if (!isList(listItem)) {
    return undefined;
  }

  var parent = document.getParent(listItem.key);
  return parent == null ? void 0 : parent.nodes.find(function (node) {
    var _node$data, _node$data$list;

    return Block.isBlock(node) && isList(node) && (node == null ? void 0 : (_node$data = node.data) == null ? void 0 : (_node$data$list = _node$data.list) == null ? void 0 : _node$data$list.listId) === listItem.data.list.listId;
  });
}
//# sourceMappingURL=getFirstListItemById.js.map