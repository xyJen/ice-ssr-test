import { isEmpty } from "./utils";
export default function placeholder(config) {
  var locale = config.locale; // 可以通过先后顺序控制

  return [{
    text: locale == null ? void 0 : locale.taskListPlaceholder,
    when: function when(controller, node) {
      var _node$data, _node$data$list;

      return (node == null ? void 0 : (_node$data = node.data) == null ? void 0 : (_node$data$list = _node$data.list) == null ? void 0 : _node$data$list.isTaskList) && isEmpty(node);
    }
  }, {
    text: locale == null ? void 0 : locale.listPlaceholder,
    needWidth: true,
    when: function when(controller, node) {
      var _node$data2, _node$data3, _node$data3$list;

      return (node == null ? void 0 : (_node$data2 = node.data) == null ? void 0 : _node$data2.list) && !(node != null && (_node$data3 = node.data) != null && (_node$data3$list = _node$data3.list) != null && _node$data3$list.isTaskList) && isEmpty(node);
    }
  }];
}
//# sourceMappingURL=placeholder.js.map