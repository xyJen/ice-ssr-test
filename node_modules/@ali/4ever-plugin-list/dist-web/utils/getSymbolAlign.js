export function getSymbolAlign(listData) {
  var listStyle = listData.listStyle,
      isTaskList = listData.isTaskList;

  if (listStyle && ['upperRoman', 'lowerRoman'].includes(listStyle.format) || isTaskList) {
    return 'right';
  } else {
    return 'left';
  }
}
//# sourceMappingURL=getSymbolAlign.js.map