import { TextMeasure } from "./measure";
export var SYMBOL_DEFAULT_FONT = 'Arial';
export var SYMBOL_DEFAULT_SIZE = '11pt';
var SYMBOL_SPACE_OFFSET = 10;
var RegExpChinese = /.*[\u4e00-\u9fa5]+.*$/;
export var SYMBOL_UNIT_WIDTH = 24;
export var CHINESE_COUNTING_THOUSAND = 'chineseCountingThousand';
export var getSymbolRightSpace = function getSymbolRightSpace(align, listSymbol, symbolStyle) {
  var symbolSize = symbolStyle.fontSize || SYMBOL_DEFAULT_SIZE;
  var symbolWidth = TextMeasure.measureTextWidth(String(listSymbol), symbolStyle.fontFamily || SYMBOL_DEFAULT_FONT, symbolSize);
  var result;

  if (RegExpChinese.test(listSymbol) || align === 'right' || listSymbol.length === 1) {
    // 无序列表、中文有序、罗马有序保持8px距离
    result = 8;
  } else {
    result = SYMBOL_UNIT_WIDTH - symbolWidth % SYMBOL_UNIT_WIDTH;

    if (symbolSize === SYMBOL_DEFAULT_SIZE && result === SYMBOL_UNIT_WIDTH) {
      result -= SYMBOL_SPACE_OFFSET;
    }
  }

  return result;
};
//# sourceMappingURL=getSymbolRightSpace.js.map