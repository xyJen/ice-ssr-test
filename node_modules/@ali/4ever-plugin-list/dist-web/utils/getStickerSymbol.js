import React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { unitsConverter } from '@ali/4ever-utils';
import { DEFAULT_STICKERS, StickerItem, StickerManager } from '@ali/4ever-plugin-sticker';
var sticker;
export function getStickerSymbolWidth(size) {
  var sz = parseInt(size, 10);
  return unitsConverter.toPX((sz > 0 ? sz : 10) * 1.5 + "pt");
}
export default function getStickerSymbol(code, size) {
  if (size === void 0) {
    size = '10pt';
  }

  if (!sticker) {
    sticker = new StickerManager({
      customSticker: [DEFAULT_STICKERS]
    });
  }

  return sticker.getInfoByCode(code) ? /*#__PURE__*/_createElement(StickerItem, {
    code: code,
    sz: getStickerSymbolWidth(size),
    sticker: sticker
  }) : null;
}
//# sourceMappingURL=getStickerSymbol.js.map