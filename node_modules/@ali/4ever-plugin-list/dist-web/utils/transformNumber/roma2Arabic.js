export default function roman2Arabic(roman) {
  var map = {
    I: 1,
    IV: 4,
    V: 5,
    IX: 9,
    X: 10,
    XL: 40,
    L: 50,
    XC: 90,
    C: 100,
    CD: 400,
    D: 500,
    CM: 900,
    M: 1000
  };
  var ans = 0;
  roman = roman.toUpperCase();

  for (var i = 0; i < roman.length;) {
    if (i + 1 < roman.length && map[roman.substring(i, i + 2)]) {
      ans += map[roman.substring(i, i + 2)];
      i += 2;
    } else {
      ans += map[roman.substring(i, i + 1)];
      i++;
    }
  }

  return ans;
}
//# sourceMappingURL=roma2Arabic.js.map