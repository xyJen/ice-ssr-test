export var CHINESE_NUMBER_LIST = ['零', '一', '二', '三', '四', '五', '六', '七', '八', '九'];
export var CHINESE_UNIT_LIST = ['', '十', '百', '千', '万', '十', '百', '千', '亿', '十', '百', '千', '万'];
export default function arabic2Chinese(num) {
  var unitPos = 0;
  var zero = true;
  var strIns = '';
  var chnStr = '';
  var arabic = num;

  while (arabic > 0) {
    var v = arabic % 10;

    if (v === 0) {
      if (!zero) {
        zero = true;
        chnStr = CHINESE_NUMBER_LIST[v] + chnStr;
      }
    } else {
      zero = false;
      strIns = CHINESE_NUMBER_LIST[v];
      strIns += CHINESE_UNIT_LIST[unitPos];
      chnStr = strIns + chnStr;
    }

    unitPos += 1;
    arabic = Math.floor(arabic / 10);
  }

  if (num > 9 && num < 20) {
    return chnStr.slice(1);
  }

  return chnStr;
}
//# sourceMappingURL=arabic2Chinese.js.map