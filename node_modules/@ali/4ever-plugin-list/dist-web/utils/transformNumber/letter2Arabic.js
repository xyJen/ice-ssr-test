export default function letter2Arabic(letter) {
  var result = 0;
  letter = letter.toUpperCase();

  for (var i = 0; i < letter.length; i++) {
    var num = letter[i].charCodeAt(0) - 64;
    result += num * Math.pow(26, letter.length - 1 - i);
  }

  return result;
}
//# sourceMappingURL=letter2Arabic.js.map