export var LETTER_LIST = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';
export default function arabic2Letter(number, type) {
  var num = number;
  var map = type === 'upper' ? LETTER_LIST : LETTER_LIST.toLowerCase();
  var result = '';

  while (num > 0) {
    num -= 1;
    result = map[num % 26] + result;
    num = Math.floor(num / 26);
  }

  return result;
}
//# sourceMappingURL=arabic2Letter.js.map