export var ROMA_LIST = ['M', 'CM', 'D', 'CD', 'C', 'XC', 'L', 'XL', 'X', 'IX', 'V', 'IV', 'I'];
var ARABIC_LIST = [1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1];
export default function arabic2Roma(number, type) {
  var map = type === 'upper' ? ROMA_LIST : ROMA_LIST.map(function (item) {
    return item.toLowerCase();
  });
  var ret = '';
  var num = number;

  for (var i = 0; i < map.length; i++) {
    if (num === 0) break;
    var y = Math.floor(num / ARABIC_LIST[i]);
    num -= ARABIC_LIST[i] * y;

    for (var m = 0; m < y; m++) {
      ret += map[i];
    }
  }

  return ret;
}
//# sourceMappingURL=arabic2Roma.js.map