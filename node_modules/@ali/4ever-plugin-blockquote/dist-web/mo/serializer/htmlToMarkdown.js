export default {
  name: 'blockquote',
  filter: function filter(node) {
    return node.getAttribute('data-type') === 'blockquote';
  },
  replacement: function replacement(content) {
    if (content === void 0) {
      content = '';
    }

    return "> " + content + "\n\n";
  }
};
//# sourceMappingURL=htmlToMarkdown.js.map