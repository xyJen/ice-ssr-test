import { Plugin } from '@ali/4ever-cangjie';
declare type RunSequence<P> = {
    runAfter?: Array<Factory<P>>;
};
export declare type Factory<P> = ((...args: any) => P) & RunSequence<P>;
declare type PluginItem<P> = {
    key: string;
    factory: Factory<P>;
};
export declare function sortPlugins<P = Plugin>(pluginsFactoryMap: Record<string, Factory<P>>): PluginItem<P>[];
export {};
