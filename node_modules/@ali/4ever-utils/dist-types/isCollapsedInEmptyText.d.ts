import { Document, RangeSelection } from '@ali/4ever-cangjie';
/**
 * 选区是否闭合且选区内的text为空
 * @param range
 * @param document
 */
export default function isCollapsedInEmptyText(range: RangeSelection, document: Document): boolean;
