declare class SimpleMapCache<T> {
    private max;
    private cache;
    constructor(max?: number);
    get(key: string): T | undefined;
    add(key: string, value: T): void;
}
export default SimpleMapCache;
