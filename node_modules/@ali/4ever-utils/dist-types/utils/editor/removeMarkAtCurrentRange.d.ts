import { Controller, Selection } from '@ali/4ever-cangjie';
/**
 * 在给定选区内删除指定类型的 mark
 * 策略同 removeMarkAtCurrentRange
 * @param controller
 * @param selection
 * @param type
 */
export declare function removeMarkAtRange(controller: Controller, selection: Selection, type: string): Controller;
/**
 * 在当前选区内删除指定类型的 mark
 * 1. 未闭合时策略：清除选区内样式
 * 2. 闭合时策略：
 *  2.1 如果是 isCollapsedAtEmptyParagraph/Heading 时需要清除选区临时样式，也需要清除 text 节点的mark（操作可持久化），对应的场景有二：
 *    a. 表格选区中的空 cell；
 *    b. focus 在空段落（paragraph 和 heading）
 *  2.2 边缘闭合选区：仅设置选区的临时样式，对应的场景：focus 在分割线、卡片、表格等左右
 *  2.3 focus 在段落中以及其它情形：仅设置选区的临时样式
 * @param controller
 * @param type 类型
 */
export declare function removeMarkAtCurrentRange(controller: Controller, type: string): Controller;
