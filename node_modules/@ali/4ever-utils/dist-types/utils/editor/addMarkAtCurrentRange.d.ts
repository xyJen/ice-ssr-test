import { Controller, Mark, Selection } from '@ali/4ever-cangjie';
/**
 * 在给定选区内增加指定 mark
 * 策略同 addMarkAtCurrentRange
 * @param controller
 * @param selection
 * @param mark
 */
export declare function addMarkAtRange(controller: Controller, selection: Selection, mark: Mark): Controller;
/**
 * 在当前选区内增加指定 mark
 * 1. 未闭合时策略：在选区内增加样式
 * 2. 闭合时策略：
 *  2.1 如果是 isCollapsedAtEmptyParagraph/Heading 时需要给 text 节点加mark（操作可持久化），对应的场景有二：
 *    a. 表格选区中的空 cell；
 *    b. focus 在空段落（paragraph 和 heading）
 *  2.2 边缘闭合选区：仅设置选区的临时样式，对应的场景：focus 在分割线、卡片、表格等左右
 *  2.3 focus 在段落中以及其它情形：仅设置选区的临时样式
 * @param controller
 * @param mark 要求必须是 Mark，尽量使用 mo 中的 create 函数创建以确保兼容性（直接使用 Mark.create 不安全）
 */
export default function addMarkAtCurrentRange(controller: Controller, mark: Mark): Controller;
