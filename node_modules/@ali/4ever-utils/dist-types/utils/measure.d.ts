export declare const CJK_REG: RegExp;
interface MeasureInfo {
    width: number;
    height: number;
}
declare type FontSize = string;
/**
 * 供非 Web 端粗略计算字符宽高使用
 * 中文字符（宽 = fontSize, 高 = fontSize）
 * 非中文字符（宽= fontSize / 2, 高 = fontSize）
 */
export declare function getTextWidthOnServer(text: string, fontSize: number): {
    width: number;
    height: number;
};
export interface WarmupBatch {
    [key: string]: {
        [key: string]: Set<FontSize>;
    };
}
declare class Measure {
    private container;
    private measurer;
    private readonly measureCache;
    ensureMeasurer(): void;
    measureSingleText: (text: string, font: string, sz: string) => MeasureInfo;
    measureTextWidth: (chars: string, font: string, sz: string) => number;
    warmup: (batch: WarmupBatch) => void;
}
export default Measure;
