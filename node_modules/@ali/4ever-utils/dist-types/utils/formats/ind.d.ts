import { Block } from '@ali/4ever-cangjie';
import { IndentProperties } from '../modelData';
declare function charsToPx(char: number): number;
declare function pxToChars(px: number): number;
/**
 * 解析 block indent 数据
 * leftChars/rightChars/firstLineChars/hangingChars 将覆盖对应不带单位的字段，并被转换为 px 单位
 * 会处理悬挂和首行缩进冲突，并根据第一个字的字号动态计算缩进值
 * @param node BLock
 * @param inheritedData 继承数据
 * @returns IndentProperties
 */
declare function fromBlock(node: Block, inheritedData?: Record<string, any>): IndentProperties;
export declare const IndUtil: {
    fromBlock: typeof fromBlock;
    charsToPx: typeof charsToPx;
    pxToChars: typeof pxToChars;
};
export declare enum IndentTrigger {
    keyboard = 0,
    toolbar = 1
}
export declare enum IndentType {
    none = "noe",
    hang = "hang",
    firstLine = "firstLine"
}
export declare const DEFAULT_INDENT: Required<Pick<IndentProperties, 'firstLine' | 'firstLineChars' | 'left' | 'leftChars' | 'hanging' | 'hangingChars'>>;
export {};
