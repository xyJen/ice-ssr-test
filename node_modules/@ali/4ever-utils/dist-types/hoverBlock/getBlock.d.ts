import { Block, Document } from '@ali/4ever-cangjie';
declare type HoverResult = {
    block?: Block;
};
declare type Options = {
    zoomContainer?: HTMLElement | null;
    zoom?: number;
    filterColumn?: boolean;
};
export default function getBlock(event: MouseEvent, document: Document, options?: Options): HoverResult;
export {};
