/// <reference types="lodash" />
import { throttle } from 'lodash-es';
declare type ThrottleSettings = Parameters<typeof throttle>[2];
declare function Throttle(wait?: number, options?: ThrottleSettings): MethodDecorator;
export default Throttle;
