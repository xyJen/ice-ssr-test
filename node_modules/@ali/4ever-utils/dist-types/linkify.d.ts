/// <reference types="lodash" />
import Linkify from 'linkify-it';
declare function createLinkify(protocolAllowList?: string[]): Linkify.LinkifyIt;
declare const _default: typeof createLinkify & import("lodash").MemoizedFunction;
export default _default;
/**
 * 检查字符串是否包含链接（排除 URL 包含在 md 链接、图片语法内的情况）
 * @param linkify
 * @param text 待检查的字符串
 * @returns 若不包含链接，则返回 null，否则返回符合条件的 LinkifyIt.Match[]
 */
export declare function matchesLink(linkify: Linkify.LinkifyIt, text: string): Linkify.Match[] | null;
