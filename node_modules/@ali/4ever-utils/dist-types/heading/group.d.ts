import { Block, Document, Plugin } from "@ali/4ever-cangjie";
/**
 * 生成 group type
 * @param type
 * @returns
 */
export declare function generateHeadingGroupType(type: string): string;
/**
 * 生成 group key
 * @param key
 * @returns
 */
export declare function generateHeadingGroupKey(key: string): string;
/**
 * 聚合一组节点
 * @param nodes 节点组
 * @param type group type
 * @param until 停止 group 条件
 * @param pass 跳过节点
 * @returns
 */
export declare function groupHeading([start, end]: [number, number], parent: Document | Block, type: string, until: (node: Block, first: Block) => boolean, pass?: (node: Block) => boolean): ReturnType<Required<Plugin>['groupNodes']>;
