declare class PerfResizeObserver {
    private observer;
    private weakMap;
    private static _resizeObserver;
    constructor();
    /**
     *  消费同一个实例
     */
    static create: () => PerfResizeObserver;
    private resizeCallback;
    /**
     * @param {Element} target 要监控 Resize 的 Dom 节点
     * @param {(rect: DOMRect) = void} cb 触发 Resize 后的回调
     * @memberof PerfResizeObserver
     */
    observe: (target: Element, cb: (rect: DOMRect) => void, options?: {
        box: 'content-box' | 'border-box' | 'device-pixel-content-box';
    }) => void;
    /**
     * 从堆栈中移除已监控 Resize 的 Dom 节点
     */
    unobserve: (target: Element) => void;
}
export default PerfResizeObserver;
