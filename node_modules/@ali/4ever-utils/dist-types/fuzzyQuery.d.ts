import type { IFilterOptions } from 'fuzzaldrin-plus';
/**
 * 获取模糊搜索的正则表达式
 * @param text 模糊匹配的目标对象字符串
 * @returns 用于模糊匹配 text 的正则
 */
export declare const getFuzzyQuery: (text: string) => RegExp;
export declare function fuzzySearch<T extends (string | object)>(items: T[], query: string, options?: IFilterOptions): Promise<T[]>;
