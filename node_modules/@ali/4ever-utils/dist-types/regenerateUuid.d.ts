import { Document } from '@ali/4ever-cangjie';
/**
 * 重新生成 fragment 中的 uuid，并替换用户数据
 * @param fragment
 * @param generateUuid
 * @returns
 */
export declare function regenerateUuid(fragment: Document, generateUuid?: () => string): Document;
