export var isSelectedBlock = function isSelectedBlock(value, targetType) {
  var selection = value.selection,
      document = value.document,
      isFocused = value.isFocused;
  return !!(selection && isFocused && selection.isCollapsed // @ts-ignore
  && document.getClosest(selection.anchor.key, function (_ref) {
    var type = _ref.type;
    return type === targetType;
  }));
};
//# sourceMappingURL=isSelectedBlock.js.map