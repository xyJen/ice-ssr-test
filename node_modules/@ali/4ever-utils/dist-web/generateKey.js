/* eslint no-useless-escape: 0 */
var CHARS = 'abcdefghigklmnopqrstuvwxyz';
var NUMS = '0123456789';
var ALL = CHARS + NUMS;

var generateKey = function generateKey(node) {
  var repeatStr = '';

  if (node) {
    var texts = node.getTexts() || [];
    var string = texts.map(function (t) {
      return t.text;
    }).join('');

    if (string) {
      repeatStr = string.toLowerCase().replace(/[\!|\#|\s|\r|\n|\<|\>|\/|\"|\']+/g, '-') + "-";
    }
  }

  for (var i = 0; i < 4; i += 1) {
    repeatStr += 'z';
  }

  return (repeatStr + "xx").replace(/[xz]/g, function (c) {
    return c === 'x' ? CHARS[Math.random() * 26 | 0] : ALL[Math.random() * 36 | 0];
  });
};

export default generateKey;
//# sourceMappingURL=generateKey.js.map