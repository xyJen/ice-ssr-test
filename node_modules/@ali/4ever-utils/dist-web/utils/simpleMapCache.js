var SimpleMapCache = /*#__PURE__*/function () {
  function SimpleMapCache(max) {
    this.max = void 0;
    this.cache = new Map();
    this.max = max || 200;
  }

  var _proto = SimpleMapCache.prototype;

  _proto.get = function get(key) {
    return this.cache.get(key);
  };

  _proto.add = function add(key, value) {
    if (this.cache.has(key)) {
      // 更新
      this.cache["delete"](key);
    }

    ;

    if (this.cache.size > this.max) {
      // 强制删除第一个
      this.cache["delete"](this.cache.keys().next().value);
    }

    this.cache.set(key, value);
  };

  return SimpleMapCache;
}();

export default SimpleMapCache;
//# sourceMappingURL=simpleMapCache.js.map