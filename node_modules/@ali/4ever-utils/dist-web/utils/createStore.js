import _extends from "@babel/runtime/helpers/extends";
var GLOBAL_MAP = {};
export var createStore = function createStore(namespace) {
  var getValue = function getValue(key) {
    return (GLOBAL_MAP[namespace] || {})[key] || null;
  };

  var mergeValue = function mergeValue(key, mergeData) {
    var data = GLOBAL_MAP[namespace] || {};
    data[key] = _extends({}, data[key], mergeData);
    GLOBAL_MAP[namespace] = data;
  };

  return {
    getValue: getValue,
    mergeValue: mergeValue
  };
};
//# sourceMappingURL=createStore.js.map