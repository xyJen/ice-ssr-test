import _extends from "@babel/runtime/helpers/extends";
var attrsWithoutUnit = ['font-weight', 'opacity', 'line-height'];
export function addDefaultUnit(theme) {
  return Object.entries(theme).reduce(function (acc, cur) {
    var _extends3;

    var key = cur[0],
        value = cur[1];

    if (typeof value !== 'number' || attrsWithoutUnit.includes(key)) {
      var _extends2;

      return _extends({}, acc, (_extends2 = {}, _extends2[key] = value, _extends2));
    }

    return _extends({}, acc, (_extends3 = {}, _extends3[key] = value + "px", _extends3));
  }, {});
}
export function transformThemeObjToCssString(theme) {
  return Object.entries(addDefaultUnit(theme)).reduce(function (acc, cur) {
    var key = cur[0],
        value = cur[1];
    return "" + acc + key + ":" + value + ";";
  }, '');
}
//# sourceMappingURL=unit.js.map