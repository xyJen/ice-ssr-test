import { isNil } from 'lodash-es';

var isFirstPart = function isFirstPart(node) {
  var key = node.key;

  var _key$split = key.split('-'),
      _ = _key$split[0],
      partIndex = _key$split[1];

  return isNil(partIndex) || partIndex === '0';
};

export default isFirstPart;
//# sourceMappingURL=isFirstPart.js.map