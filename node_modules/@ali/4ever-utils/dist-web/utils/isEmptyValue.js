import isEmptyParagraph from "./isEmptyParagraph";
export var isEmptyValue = function isEmptyValue(value) {
  var document = value.document;
  if (document.nodes.length === 0) return true;
  var first = document.nodes[0];
  return document.nodes.length === 1 && isEmptyParagraph(first);
};
//# sourceMappingURL=isEmptyValue.js.map