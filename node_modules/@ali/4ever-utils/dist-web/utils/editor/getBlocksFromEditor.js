import { getRangesFromEditor } from "./getRangesFromEditor";
export var getBlocksFromEditor = function getBlocksFromEditor(controller) {
  var blocks = [];
  var document = controller.value.document;
  var selecttions = getRangesFromEditor(controller);
  selecttions.forEach(function (selecttion) {
    document.getLeafBlocksAtRange(selecttion).forEach(function (block) {
      blocks.push(block);
    });
  });
  return blocks;
};
//# sourceMappingURL=getBlocksFromEditor.js.map