import { Text } from '@ali/4ever-cangjie';
/**
 * 判断是否渲染 placeholder
 * @param controller
 * @param node
 */

export function isPlaceholderVisible(controller, node) {
  var value = controller.value;
  var document = value.document;
  var first = document.nodes[0];
  return document.nodes.length === 1 && node.key === first.key && first.type === 'paragraph' && !first.data.blockquote && (first.data.jc || 'left') === 'left' && !first.data.ind && !first.data.list && Text.isTextList(first.nodes) && first.text === '';
}
//# sourceMappingURL=placeholder.js.map