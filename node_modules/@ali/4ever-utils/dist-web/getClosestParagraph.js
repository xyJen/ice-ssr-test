var getClosestParagraph = function getClosestParagraph(value, alignBlock) {
  if (!value) return null;
  var startBlock = value.startBlock,
      document = value.document;
  var block = alignBlock || startBlock;
  if (!startBlock || !block) return null;

  if (block.type === 'paragraph' || block.type === 'heading-1' || block.type === 'heading-2' || block.type === 'heading-3' || block.type === 'heading-4' || block.type === 'heading-5' || block.type === 'heading-6') {
    return block;
  } // @ts-ignore


  return document.getClosest(block.key, function (_ref) {
    var type = _ref.type;
    return type === 'paragraph';
  });
};

export default getClosestParagraph;
//# sourceMappingURL=getClosestParagraph.js.map