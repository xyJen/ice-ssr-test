// 处理字段映射相关
export var MAPPING = [{
  jsonML: 'width',
  value: 'length'
}, {
  jsonML: 'type',
  value: 'lineType'
}, {
  jsonML: 'sz',
  value: 'size'
}, {
  jsonML: 'color',
  value: 'color'
}];

/**
 *
 * @param data slate value data
 * @param map 映射对象
 */
export var parseValueToJsonML = function parseValueToJsonML(data, map) {
  var newObj = {
    width: null,
    type: null,
    sz: null,
    color: null
  };
  if (!data) return newObj;
  map.forEach(function (item) {
    newObj[item.jsonML] = data[item.value];
  });
  return newObj;
};
/**
 *
 * @param data jsonML data
 * @param map  映射对象
 */

export var parseJsonMLToValue = function parseJsonMLToValue(data, map) {
  var newObj = {
    length: null,
    lineType: null,
    size: null,
    color: null
  };
  if (!data) return newObj;
  map.forEach(function (item) {
    newObj[item.value] = data[item.jsonML];
  });
  return newObj;
};
//# sourceMappingURL=mapping.js.map