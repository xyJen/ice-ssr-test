"use strict";

var _fuzzyQuery = require("../fuzzyQuery");

describe('getFuzzyQuery', () => {
  describe('普通字符', () => {
    const text = 'abc';
    const cases = [['abc', true], ['zzzaxxxxbyyyycwwww', true], ['axxxxbyyyyc', true], ['ac', false], ['bc', false], ['c', false], ['', false], ['d', false], ['azzzzzzc', false]];
    const regexp = (0, _fuzzyQuery.getFuzzyQuery)(text);
    cases.forEach(([input, output]) => {
      it(`search ${text} for ${input}`, () => {
        expect(regexp.test(input)).toBe(output);
      });
    });
  });
  describe('escaped 字符', () => {
    const text = 't*.';
    const cases = [['t*.', true], ['\tttwww*s .', true], ['tt***...', true], ['\t~~++[]', false]];
    const regexp = (0, _fuzzyQuery.getFuzzyQuery)(text);
    cases.forEach(([input, output]) => {
      it(`search ${text} for ${input}`, () => {
        expect(regexp.test(input)).toBe(output);
      });
    });
  });
});
//# sourceMappingURL=fuzzyQuery.test.js.map