"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isPlaceholderVisible = isPlaceholderVisible;

var _everCangjie = require("@ali/4ever-cangjie");

/**
 * 判断是否渲染 placeholder
 * @param controller
 * @param node
 */
function isPlaceholderVisible(controller, node) {
  const {
    value
  } = controller;
  const {
    document
  } = value;
  const first = document.nodes[0];
  return document.nodes.length === 1 && node.key === first.key && first.type === 'paragraph' && !first.data.blockquote && (first.data.jc || 'left') === 'left' && !first.data.ind && !first.data.list && _everCangjie.Text.isTextList(first.nodes) && first.text === '';
}
//# sourceMappingURL=placeholder.js.map