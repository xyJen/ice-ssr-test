"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createStore = void 0;
const GLOBAL_MAP = {};

const createStore = namespace => {
  const getValue = key => {
    return (GLOBAL_MAP[namespace] || {})[key] || null;
  };

  const mergeValue = (key, mergeData) => {
    const data = GLOBAL_MAP[namespace] || {};
    data[key] = { ...data[key],
      ...mergeData
    };
    GLOBAL_MAP[namespace] = data;
  };

  return {
    getValue,
    mergeValue
  };
};

exports.createStore = createStore;
//# sourceMappingURL=createStore.js.map