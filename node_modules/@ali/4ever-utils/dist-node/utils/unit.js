"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.addDefaultUnit = addDefaultUnit;
exports.transformThemeObjToCssString = transformThemeObjToCssString;
const attrsWithoutUnit = ['font-weight', 'opacity', 'line-height'];

function addDefaultUnit(theme) {
  return Object.entries(theme).reduce((acc, cur) => {
    const [key, value] = cur;

    if (typeof value !== 'number' || attrsWithoutUnit.includes(key)) {
      return { ...acc,
        [key]: value
      };
    }

    return { ...acc,
      [key]: `${value}px`
    };
  }, {});
}

function transformThemeObjToCssString(theme) {
  return Object.entries(addDefaultUnit(theme)).reduce((acc, cur) => {
    const [key, value] = cur;
    return `${acc}${key}:${value};`;
  }, '');
}
//# sourceMappingURL=unit.js.map