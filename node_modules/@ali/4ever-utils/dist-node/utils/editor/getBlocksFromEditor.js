"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getBlocksFromEditor = void 0;

var _getRangesFromEditor = require("./getRangesFromEditor");

const getBlocksFromEditor = controller => {
  const blocks = [];
  const {
    document
  } = controller.value;
  const selecttions = (0, _getRangesFromEditor.getRangesFromEditor)(controller);
  selecttions.forEach(selecttion => {
    document.getLeafBlocksAtRange(selecttion).forEach(block => {
      blocks.push(block);
    });
  });
  return blocks;
};

exports.getBlocksFromEditor = getBlocksFromEditor;
//# sourceMappingURL=getBlocksFromEditor.js.map