import { Action, Controller, Plugin } from '@ali/4ever-cangjie';
declare type ActionType = (action: Action, controller: Controller, next: () => void) => void;
declare function createOnAction(customizedAction?: ActionType): Plugin['onAction'];
export default createOnAction;
