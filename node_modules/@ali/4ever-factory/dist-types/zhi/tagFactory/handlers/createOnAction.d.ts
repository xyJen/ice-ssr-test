import { Plugin, Action, Controller } from '@ali/4ever-cangjie';
declare const createOnAction: (customizedAction?: Plugin['onAction']) => (action: Action, controller: Controller, next: () => void) => void;
export default createOnAction;
