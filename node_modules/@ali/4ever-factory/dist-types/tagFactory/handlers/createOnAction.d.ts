import { Action, Controller } from '@ali/4ever-cangjie';
declare type ActionType = (action: Action, controller: Controller, next: () => void) => void;
export default function createOnAction(customizedAction?: ActionType): (action: Action, controller: Controller, next: () => void) => void | Controller;
export {};
