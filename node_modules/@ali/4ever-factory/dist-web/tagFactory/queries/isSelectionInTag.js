export default (function (value) {
  var document = value.document,
      selection = value.selection;

  if (!selection) {
    return false;
  }

  var start = selection.getStart(document);

  if (!start.key) {
    return false;
  }

  var closeInline = document.getClosestInline(start.key);
  if (closeInline && closeInline.type === 'tag') return true;
  return value.inlines.some(function (inline) {
    return inline.type === 'tag';
  });
});
//# sourceMappingURL=isSelectionInTag.js.map