import { Card } from '@ali/4ever-plugin-card';
import { createStore } from '@ali/4ever-utils';
var store = createStore(Card.TYPE);
export var isOwnerAndFirstCreated = function isOwnerAndFirstCreated(key) {
  var _store$getValue;

  return !!((_store$getValue = store.getValue(key)) != null && _store$getValue.isOwnerAndFirstCreated);
};
export var setOwnerAndFirstCreated = function setOwnerAndFirstCreated(key, bo) {
  store.mergeValue(key, {
    isOwnerAndFirstCreated: bo
  });
};
//# sourceMappingURL=tagStore.js.map