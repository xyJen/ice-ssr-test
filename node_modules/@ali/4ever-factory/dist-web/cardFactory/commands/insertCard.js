import _extends from "@babel/runtime/helpers/extends";
import { Block } from '@ali/4ever-cangjie';
import { Card } from '@ali/4ever-plugin-card';
import { biActions as basicActions } from '@ali/4ever-plugin-basic';
import { setOwnerAndFirstCreated } from "../utils/cardStore";
export default (function (controller, payload) {
  var finalNode = Block.isBlock(payload) ? payload : Block.create({
    type: Card.TYPE,
    data: _extends({}, payload)
  });
  controller.run('onAction', basicActions.createInsertBlockWithoutExtraBlankAction({
    node: finalNode
  }));
  setOwnerAndFirstCreated(finalNode.key, true);
  return controller;
});
//# sourceMappingURL=insertCard.js.map