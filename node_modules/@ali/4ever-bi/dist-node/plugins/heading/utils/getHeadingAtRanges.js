"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getHeadingAtRanges;

function filterSameHeading(headings) {
  const keyMap = {};
  return headings.filter(heading => {
    if (keyMap[heading]) {
      return false;
    }

    keyMap[heading] = true;
    return true;
  });
}

function getHeadingAtRanges(controller, ranges) {
  const {
    document
  } = controller.value;
  let headings = [];
  ranges.forEach(range => {
    headings = headings.concat(document.getLeafBlocksAtRange(range).map(block => block.type));
  });
  return filterSameHeading(headings);
}
//# sourceMappingURL=getHeadingAtRanges.js.map