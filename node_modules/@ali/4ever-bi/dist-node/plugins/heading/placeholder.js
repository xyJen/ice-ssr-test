"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = placeholder;

var _everMo = require("@ali/4ever-mo");

const {
  Heading1,
  Heading2,
  Heading3,
  Heading4
} = _everMo.Heading;

function placeholder(config) {
  const {
    locale
  } = config;
  return [{
    text: locale?.heading1Placeholder,
    when: (controller, node) => {
      return Heading1.isHeading1(node) && _everMo.Heading.isEmpty(node);
    }
  }, {
    text: locale?.heading2Placeholder,
    when: (controller, node) => {
      return Heading2.isHeading2(node) && _everMo.Heading.isEmpty(node);
    }
  }, {
    text: locale?.heading3Placeholder,
    when: (controller, node) => {
      return Heading3.isHeading3(node) && _everMo.Heading.isEmpty(node);
    }
  }, {
    text: locale?.heading4Placeholder,
    when: (controller, node) => {
      return Heading4.isHeading4(node) && _everMo.Heading.isEmpty(node);
    }
  }];
}
//# sourceMappingURL=placeholder.js.map