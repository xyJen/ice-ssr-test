"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isCollapsedAtEmptyHeading;

var _everCangjie = require("@ali/4ever-cangjie");

var _everMo = require("@ali/4ever-mo");

function isCollapsedAtEmptyHeading(controller, range) {
  const {
    isCollapsed,
    isCollapsedAtEdge
  } = range; // 边缘不算

  if (!isCollapsed || isCollapsedAtEdge) return false;
  const {
    document
  } = controller.value;
  const block = document.getClosestBlock(range.focus.key); // 段落和标题以外的块级元素不算

  if (!_everMo.Heading.isHeading(block)) return false;
  return block?.text === '' && _everCangjie.Text.isTextList(block.nodes);
}
//# sourceMappingURL=isCollapsedAtEmptyHeading.js.map