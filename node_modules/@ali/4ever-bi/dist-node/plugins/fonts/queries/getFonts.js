"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getFonts;

var _getActiveFontsMark = _interopRequireWildcard(require("../utils/getActiveFontsMark"));

function getFonts(controller, inheritedFonts) {
  const symbolFont = controller.query('getSymbolStyleByKey', 'fonts');
  if (symbolFont) return symbolFont;
  const activeFontsMark = (0, _getActiveFontsMark.default)(controller, inheritedFonts);

  if (activeFontsMark) {
    const {
      eastAsia,
      ascii
    } = activeFontsMark.data;

    if (eastAsia && eastAsia !== _getActiveFontsMark.defaultFonts) {
      // 优先显示中文字体
      return eastAsia;
    } else if (ascii && ascii !== _getActiveFontsMark.defaultFonts) {
      return ascii;
    }

    return _getActiveFontsMark.defaultFonts;
  } // 没有公共字体则返回空，以便区别默认


  return '';
}
//# sourceMappingURL=getFonts.js.map