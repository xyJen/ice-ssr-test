"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.setCalloutBackground = setCalloutBackground;

var _everCangjie = require("@ali/4ever-cangjie");

var _everMo = require("@ali/4ever-mo");

function setCalloutBackground(controller, key, background) {
  const node = controller.value.document.getNode(key);

  if (!_everMo.ColorBlocks.isColorBlocks(node)) {
    return controller;
  }

  const metadata = { ...node.data.metadata
  };

  if (background) {
    metadata.bgcolor = background;
  } else {
    delete metadata.bgcolor;
  }

  return controller.command(_everCangjie.Commands.setNodeByKey, key, {
    data: { ...node.data,
      metadata
    }
  });
}
//# sourceMappingURL=setCalloutBackground.js.map