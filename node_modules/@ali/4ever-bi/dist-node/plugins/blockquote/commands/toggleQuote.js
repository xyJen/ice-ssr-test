"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = toggleQuote;

var _getBlockquoteState = _interopRequireDefault(require("../utils/getBlockquoteState"));

var _isBlockquoteDisabled = _interopRequireDefault(require("../utils/isBlockquoteDisabled"));

var _disableQuote = _interopRequireDefault(require("./disableQuote"));

var _enableQuote = _interopRequireDefault(require("./enableQuote"));

function toggleQuote(editor, next) {
  if (!(0, _isBlockquoteDisabled.default)(editor)) {
    const isBlockquoteExist = (0, _getBlockquoteState.default)(editor);

    if (isBlockquoteExist) {
      (0, _disableQuote.default)(editor, next);
    } else {
      (0, _enableQuote.default)(editor, next);
    }
  }

  if (next) {
    next();
  }

  return editor;
}
//# sourceMappingURL=toggleQuote.js.map