"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getStartBlock;

/**
 * 根据 startBlock 获取光标所在的 block（兼容排版模式）
 * line 是排版中分割的行，需要再向上一层
 * @param controller
 */
function getStartBlock(controller) {
  const {
    startBlock,
    document
  } = controller.value;
  if (!startBlock) return startBlock;
  let node = startBlock;

  if (node.type === 'line') {
    node = document.getClosestBlock(node.key);
  }

  return node;
}
//# sourceMappingURL=getStartBlock.js.map