"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = onKeyDown;

var _isHotkey = require("is-hotkey");

var _actions = require("../actions");

// 比较event.which 而不是 event.key, 否则中文输入下不生效
const isSzUpHotKey = (0, _isHotkey.isHotkey)('mod+shift+.');
const isSzDownHotKey = (0, _isHotkey.isHotkey)('mod+shift+,');

function onKeyDown(event, controller, next) {
  if (isSzUpHotKey(event)) {
    event.preventDefault();
    return controller.run('onAction', (0, _actions.adjustSz)(true));
  }

  if (isSzDownHotKey(event)) {
    event.preventDefault();
    return controller.run('onAction', (0, _actions.adjustSz)(false));
  }

  return next();
}
//# sourceMappingURL=onKeyDown.js.map