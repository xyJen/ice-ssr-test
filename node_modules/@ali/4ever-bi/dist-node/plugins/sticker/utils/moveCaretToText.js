"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = moveCaretToText;

var _actions = require("../actions");

var _constants = require("../constants");

/**
 * @description sticker 为选中态时，根据实际场景移动光标至节点左侧或右侧
 * @param offset 光标移动偏移量
 * */
function moveCaretToText({
  controller,
  offset,
  cb
}) {
  const {
    document,
    selection
  } = controller.value;
  const {
    anchor
  } = selection;
  const parent = document.getParent(anchor.key);
  if (parent?.type !== _constants.STICKER_NODE_TYPE) return;

  if (cb) {
    cb();
  } else {
    controller.run('onAction', (0, _actions.moveCaret)({
      offset
    }));
  }
}
//# sourceMappingURL=moveCaretToText.js.map