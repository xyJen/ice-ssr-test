"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.handleDragChange = exports.leftToolbarHandlers = void 0;
const leftToolbarHandlers = [];
exports.leftToolbarHandlers = leftToolbarHandlers;

const getLeftToolbarHandler = controller => {
  for (const handlerGetter of leftToolbarHandlers) {
    const res = handlerGetter(controller);

    if (res) {
      return res;
    }
  }

  return null;
};

const handleDragChange = (isDragging, controller) => {
  const leftToolbar = getLeftToolbarHandler(controller);

  if (!leftToolbar) {
    return;
  }

  if (isDragging) {
    // 开始拖拽分栏时，隐藏 draggable bar、暂时禁用 draggable
    leftToolbar.disable();
  } else {
    // 结束拖拽分栏时，恢复 draggable
    leftToolbar.enable();
  }
};

exports.handleDragChange = handleDragChange;
//# sourceMappingURL=leftToolbarHandlers.js.map