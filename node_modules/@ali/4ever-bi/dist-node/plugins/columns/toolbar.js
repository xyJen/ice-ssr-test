"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = toolbar;

var _everMo = require("@ali/4ever-mo");

function toolbar() {
  return {
    buttons: {},
    locale: {},

    getTarget(controller) {
      const {
        document,
        selection
      } = controller.value;
      const {
        anchor,
        focus
      } = selection;
      const anchorParent = document.getFurthsest(anchor.key, _everMo.Table.isTable);
      const focusParent = document.getFurthsest(focus.key, _everMo.Table.isTable);

      if (anchorParent && anchorParent === focusParent && anchorParent.data.sr) {
        return 'columns';
      }

      return '';
    }

  };
}
//# sourceMappingURL=toolbar.js.map