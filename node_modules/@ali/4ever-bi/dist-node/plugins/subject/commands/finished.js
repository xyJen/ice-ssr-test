"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.toggleFinished = toggleFinished;
exports.toggleFinishedByKey = toggleFinishedByKey;

var _everCangjie = require("@ali/4ever-cangjie");

function toggleFinished(controller, node, finished) {
  const subjectPr = { ...node.data.subjectPr,
    finished
  };

  if (!finished) {
    delete subjectPr.finished;
  }

  controller.command(_everCangjie.Commands.setNodeByKey, node.key, {
    data: { ...node.data,
      subjectPr
    }
  });
  return controller;
}

function toggleFinishedByKey(controller, key, finished) {
  const node = controller.value.document.assertElement(key);
  return toggleFinished(controller, node, finished);
}
//# sourceMappingURL=finished.js.map