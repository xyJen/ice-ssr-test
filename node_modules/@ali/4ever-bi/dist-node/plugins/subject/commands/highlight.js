"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.highlightSubject = highlightSubject;

var _constants = require("../constants");

function highlightSubject(controller, subjectId, highlight) {
  const {
    data
  } = controller.value;

  if (highlight) {
    return controller.setData({ ...data,
      [_constants.HIGHLIGHT_SUBJECT_KEY]: subjectId
    });
  }

  if (data[_constants.HIGHLIGHT_SUBJECT_KEY]) {
    const newData = { ...data
    };
    delete newData[_constants.HIGHLIGHT_SUBJECT_KEY];
    return controller.setData(newData);
  }

  return controller;
}
//# sourceMappingURL=highlight.js.map