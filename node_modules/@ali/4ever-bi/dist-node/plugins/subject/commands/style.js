"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.clearSubjectStyle = clearSubjectStyle;

var _everCangjie = require("@ali/4ever-cangjie");

/**
 * 清楚议题格式
 * @param controller
 */
function clearSubjectStyle(controller) {
  const {
    selection,
    document
  } = controller.value;
  const blocks = document.getRootBlocksAtRange(selection);
  blocks.forEach(block => {
    if (block.data.subjectPr) {
      const {
        subjectPr,
        ...rest
      } = block.data;
      controller.command(_everCangjie.Commands.setNodeByKey, block.key, {
        data: rest
      });
    }
  });
  return controller;
}
//# sourceMappingURL=style.js.map