"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.hasSubMark = hasSubMark;
exports.hasSupMark = hasSupMark;
exports.getHotKeyTips = getHotKeyTips;
exports.SHORTCUT = void 0;

var _everCangjie = require("@ali/4ever-cangjie");

function hasVertAlign(value, type) {
  return value.activeMarks.some(m => m.type === 'vertAlign' && m.data.get('value') === type);
}

function hasSubMark(value) {
  return hasVertAlign(value, 'subscript');
}

function hasSupMark(value) {
  return hasVertAlign(value, 'superscript');
}

const MOD = _everCangjie.environment.IS_MAC ? '⌘' : 'Ctrl';
const supKey = _everCangjie.environment.IS_WINDOWS ? "'" : '.';
const subKey = _everCangjie.environment.IS_WINDOWS ? ';' : ',';
const SHORTCUT = {
  sub: `${MOD}+${subKey}`,
  sup: `${MOD}+${supKey}`
};
exports.SHORTCUT = SHORTCUT;

function getHotKeyTips(type, location) {
  const key = type.toLowerCase().concat('ButtonTitle');
  return `${location[key]} ${SHORTCUT[type]}`;
}
//# sourceMappingURL=utils.js.map