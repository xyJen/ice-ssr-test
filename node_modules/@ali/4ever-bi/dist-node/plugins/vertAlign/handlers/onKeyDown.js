"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = onKeyDown;

var _everCangjie = require("@ali/4ever-cangjie");

var _isHotkey = require("is-hotkey");

var _actions = require("../actions");

const isSupHotKey = _everCangjie.environment.IS_WINDOWS ? (0, _isHotkey.isKeyHotkey)("mod+'") : (0, _isHotkey.isKeyHotkey)('mod+.');
const isSubHotKey = _everCangjie.environment.IS_WINDOWS ? (0, _isHotkey.isKeyHotkey)('mod+;') : (0, _isHotkey.isKeyHotkey)('mod+,');

function onKeyDown(event, editor, next) {
  if (isSupHotKey(event)) {
    event.preventDefault();
    return editor.run('onAction', (0, _actions.toggleSup)());
  }

  if (isSubHotKey(event)) {
    event.preventDefault();
    return editor.run('onAction', (0, _actions.toggleSub)());
  }

  return next();
}
//# sourceMappingURL=onKeyDown.js.map