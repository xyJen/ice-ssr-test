"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _everUtils = require("@ali/4ever-utils");

var _default = (width, height, controller) => {
  const container = (0, _everUtils.getClosestContainerBlock)(controller.value);
  const natural = {
    width,
    height
  };

  if (!container) {
    return natural;
  }

  const {
    document
  } = controller.value;
  const maxWidth = Math.max((0, _everUtils.getInnerFullWidth)(container, document) || 0, 0);

  if (maxWidth < natural.width) {
    return {
      width: maxWidth,
      height: maxWidth / natural.width * natural.height,
      isAutoFit: true
    };
  }

  return natural;
};

exports.default = _default;
//# sourceMappingURL=calcImageSize.js.map