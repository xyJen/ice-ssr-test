"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isStyleActive = isStyleActive;

var _lodash = require("lodash");

function isStyleActive(styleData, imageData) {
  const styleKeys = ['outline', 'radius', 'shadow'];
  const pickedStyleData = (0, _lodash.pick)(styleData, styleKeys);
  const pickedImageData = (0, _lodash.pick)(imageData, styleKeys);
  return (0, _lodash.isEqual)(pickedStyleData, pickedImageData);
}
//# sourceMappingURL=isStyleActive.js.map