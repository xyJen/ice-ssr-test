"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = insertBlockLinkCard;

var _buildLinkCardBlock = _interopRequireDefault(require("../utils/buildLinkCardBlock"));

var _fetchLinkData = require("../utils/fetchLinkData");

function insertBlockLinkCard(controller, data) {
  const {
    text,
    href,
    cardInfo,
    getLinkInfo
  } = data; // 将 text link 转换为 block link card

  const nodeText = text.length ? text : href; // 如果已经有 cardInfo，则不需要拉取链接信息

  const needFetchLinkInfo = !cardInfo; // 如果更新了卡片信息

  const card = (0, _buildLinkCardBlock.default)({
    text: nodeText,
    href,
    cardInfo: cardInfo || {}
  });
  controller.setOpRelatedId(card.key).command('insertCard', card);

  if (needFetchLinkInfo) {
    (0, _fetchLinkData.fetchLinkData)(card, controller, href, nodeText, getLinkInfo, card.key);
  }

  return controller;
}
//# sourceMappingURL=insertBlockLinkCard.js.map