"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isLinkCardSelected;

var _utils = require("../utils");

function isLinkCardSelected(controller, {
  node
}) {
  const {
    selection,
    document
  } = controller.value;
  const isSelected = document.isNodeInRange(node.key, selection);
  const isLinkCard = (0, _utils.isLinkCardNode)(node);
  return isLinkCard && isSelected && selection.isCollapsed && controller.value.isFocused;
}
//# sourceMappingURL=isLinkCardSelected.js.map