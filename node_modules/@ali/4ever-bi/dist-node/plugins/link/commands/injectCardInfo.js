"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = injectCardInfo;

var _everCangjie = require("@ali/4ever-cangjie");

function injectCardInfo(controller, node, cardInfo, range) {
  const {
    injections
  } = controller.value;

  const injection = _everCangjie.Injection.create({
    key: node.key,
    data: {
      cardInfo,
      range
    }
  });

  const excludeInjections = injections.filter(i => i.key !== node.key);
  controller.setInjections([...excludeInjections, injection]);
  return controller;
}
//# sourceMappingURL=injectCardInfo.js.map