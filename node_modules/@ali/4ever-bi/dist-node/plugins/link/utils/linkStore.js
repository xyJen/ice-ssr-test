"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isJustCreated = exports.setJustCreated = void 0;
const STORE = {};

const mergeStore = (key, data) => {
  STORE[key] = Object.assign({}, STORE[key], data);
};

const getStore = key => {
  return STORE[key] || {};
};

const setJustCreated = (key, isJustCreated) => {
  mergeStore(key, {
    isJustCreated
  });
};

exports.setJustCreated = setJustCreated;

const isJustCreated = key => {
  return getStore(key).isJustCreated;
};

exports.isJustCreated = isJustCreated;
//# sourceMappingURL=linkStore.js.map