"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getCardInfo;

var _isLinkCard = require("./isLinkCard");

function getCardInfo(node, injections) {
  if ((0, _isLinkCard.isLinkCardNode)(node)) {
    const {
      data
    } = node;
    const {
      cardInfo
    } = data;

    if (cardInfo) {
      return cardInfo;
    }
  }

  if (injections && injections.length > 0) {
    const injection = injections.find(i => i.key === node.key);

    if (injection) {
      return injection.data.cardInfo;
    }
  }

  return null;
}
//# sourceMappingURL=getCardInfo.js.map