"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getLinkDefaultSz;

var _everMo = require("@ali/4ever-mo");

var _everBamboo = require("@ali/4ever-bamboo");

const {
  DEFAULT: DEFAULT_FONT_SIZE
} = _everBamboo.theme.fontSize;
/**
 * 这里是为了获取默认的文本中默认的 font-size, 用于支持 placeholder 中 hover 背景跟随尺寸变化的 case
 * @param document
 * @param node
 */

function getLinkDefaultSz(document, node) {
  let headingNode = node;

  while (headingNode && !_everMo.Heading.isHeading(headingNode)) {
    headingNode = document.getClosestBlock(headingNode.key);
  }

  if (_everMo.Heading.isHeading(headingNode)) {
    return _everBamboo.SzUtil.fromHeadingType(headingNode.type);
  }

  return DEFAULT_FONT_SIZE;
}
//# sourceMappingURL=getLinkDefaultSz.js.map