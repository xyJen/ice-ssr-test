"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _utils = require("../utils");

const getParagraphColor = (controller, payload) => {
  const {
    node: currentBlock
  } = payload;
  const blocks = (0, _utils.getCurrentBlocks)(controller, currentBlock.key);
  let color = '';

  if (Array.isArray(blocks) && blocks.length > 0 && blocks[0] !== null) {
    const range = (0, _utils.getRangeFromNodes)(controller, blocks);
    color = controller.query('getColorAtRange', range);
  }

  return color;
};

var _default = getParagraphColor;
exports.default = _default;
//# sourceMappingURL=getParagraphColor.js.map