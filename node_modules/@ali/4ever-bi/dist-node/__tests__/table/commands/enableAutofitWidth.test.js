"use strict";

var _everBamboo = require("@ali/4ever-bamboo");

var _everMo = require("@ali/4ever-mo");

var _helpers = require("../helpers");

const {
  enableAutofitWidth
} = _everBamboo.TablePlugin.commands;
describe('bi/table/commands/enableAutofitWidth', () => {
  it('normal case', () => {
    const table = (0, _helpers.createTableInstance)();
    const controller = (0, _helpers.createControllerInstance)([table]);
    const originTable = (0, _helpers.getTableFromController)(controller);
    expect(_everMo.Table.isAutofitWidth(originTable)).toBe(false);
    enableAutofitWidth(controller, originTable, true);
    const autofitTable = (0, _helpers.getTableFromController)(controller);
    expect(_everMo.Table.isAutofitWidth(autofitTable)).toBe(true);
    enableAutofitWidth(controller, autofitTable, false);
    const notAutofitTable = (0, _helpers.getTableFromController)(controller);
    expect(_everMo.Table.isAutofitWidth(notAutofitTable)).toBe(false);
  });
});
//# sourceMappingURL=enableAutofitWidth.test.js.map