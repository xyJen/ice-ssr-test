import createRenderContent from "./createRenderContent";

function createLeftToolbarPlugin(config) {
  if (config === void 0) {
    config = {};
  }

  return {
    renderContent: createRenderContent(config),
    models: []
  };
}

export default createLeftToolbarPlugin;
//# sourceMappingURL=index.js.map