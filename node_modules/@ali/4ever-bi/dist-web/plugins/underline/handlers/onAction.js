import { UNDERLINE_TOGGLE } from "../actions";
import toggleUnderline from "../commands/toggleUnderlineMark";
export default function onAction(action, controller, next) {
  var type = action.type;

  if (type === UNDERLINE_TOGGLE) {
    return toggleUnderline(controller);
  }

  return next();
}
//# sourceMappingURL=onAction.js.map