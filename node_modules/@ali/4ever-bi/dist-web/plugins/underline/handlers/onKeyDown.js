import { isKeyHotkey } from 'is-hotkey';
import toggleUnderline from "../commands/toggleUnderlineMark";
var isUnderlineHotKey = isKeyHotkey('mod+u');
export default function onKeyDown(event, controller, next) {
  if (isUnderlineHotKey(event)) {
    return toggleUnderline(controller);
  }

  return next();
}
//# sourceMappingURL=onKeyDown.js.map