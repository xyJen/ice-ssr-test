import { Commands } from '@ali/4ever-cangjie';
export default function onTab(event, editor, next) {
  var value = editor.value;
  var selection = value.selection;
  if (selection.isExpanded) return next();
  var currentItem = value.startBlock;

  if (currentItem.type === 'hr') {
    event.preventDefault();
    return editor.command(Commands.insertBlock, 'paragraph').command(Commands.focus);
  }

  return next();
}
//# sourceMappingURL=onTab.js.map