import _extends from "@babel/runtime/helpers/extends";
import { Commands } from '@ali/4ever-cangjie';
import { merge } from 'lodash-es';
export default function updateDivider(editor, node, data) {
  var merged = {}; // merge 解决undefined的字段不应该覆盖原有值

  merge(merged, node.data, data);
  return editor.command(Commands.setNodeByKey, node.key, {
    data: _extends({}, merged)
  });
}
//# sourceMappingURL=updateHr.js.map