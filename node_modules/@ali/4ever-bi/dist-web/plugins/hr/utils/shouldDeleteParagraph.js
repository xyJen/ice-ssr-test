import { Text } from '@ali/4ever-cangjie';

var shouldDeleteParagraph = function shouldDeleteParagraph(block) {
  return block.type === 'paragraph' && (!block.data.ind || block.data.ind.left === 0) && !block.data.list && Text.isTextList(block.nodes) && block.text === '';
};

export default shouldDeleteParagraph;
//# sourceMappingURL=shouldDeleteParagraph.js.map