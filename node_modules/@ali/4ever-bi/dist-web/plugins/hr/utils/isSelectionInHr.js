import { isSelectedBlock } from '@ali/4ever-utils';
export default function isSelectionInHr(controller) {
  var tableSelection = controller.query('hasTableSelection');
  var selectTable = tableSelection;
  return !selectTable && isSelectedBlock(controller.value, 'hr');
}
//# sourceMappingURL=isSelectionInHr.js.map