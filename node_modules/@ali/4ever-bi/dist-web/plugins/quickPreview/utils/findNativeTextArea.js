import { constants } from '@ali/4ever-cangjie';
export default function findNativeTextArea(controller) {
  var key = controller.value.document.key;
  var textArea = window.document.querySelector("[" + constants.Selector.dockey + "=\"" + key + "\"]");
  return textArea;
}
//# sourceMappingURL=findNativeTextArea.js.map