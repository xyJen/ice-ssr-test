import findNativeTextArea from "../utils/findNativeTextArea";
export default function onCangjieFocus(controller, next) {
  var isPreviewing = controller.query('isQuickPreview');

  if (isPreviewing && controller.value.isBlurred) {
    // 额外调用一次 native blur 事件，避免键盘弹出、数据没有 focus 的情形
    var textArea = findNativeTextArea(controller);

    if (textArea != null && textArea.blur) {
      textArea.blur();
    }

    return controller;
  }

  return next();
}
//# sourceMappingURL=onCangjieFocus.js.map