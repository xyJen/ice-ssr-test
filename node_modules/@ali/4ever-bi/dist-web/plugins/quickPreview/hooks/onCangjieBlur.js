import Actions from "../actions";
export default function onCangjieBlur(controller, next) {
  var isPreviewing = controller.query('isQuickPreview');

  if (!isPreviewing) {
    controller.run('onAction', {
      type: Actions.ENTER
    });
  }

  return next();
}
//# sourceMappingURL=onCangjieBlur.js.map