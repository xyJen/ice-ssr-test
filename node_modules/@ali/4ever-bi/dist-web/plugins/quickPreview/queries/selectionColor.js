import { constants } from '@ali/4ever-cangjie';
export default function selectionColor(_c, _s, isLocal) {
  if (isLocal) {
    return constants.CANGJIE_SELECTION_COLOR;
  }

  return undefined;
}
//# sourceMappingURL=selectionColor.js.map