import { isEqual, merge } from 'lodash-es';
export var isChangeTagData = function isChangeTagData(oldData, newData) {
  var mergeData = merge({}, oldData, newData);
  return !isEqual(mergeData, oldData);
};
//# sourceMappingURL=utils.js.map