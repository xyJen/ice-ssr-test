import _extends from "@babel/runtime/helpers/extends";
import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { InlinePlaceholder, Placeholder } from '@ali/4ever-component';
import { removePlaceholder } from "./actions";
export default function createRenderNode(config) {
  var locale = config.locale || {};
  var renderText = config.renderText;
  return {
    ph: function ph(props, controller) {
      var node = props.node;
      return /*#__PURE__*/_createElement(Placeholder, _extends({}, props, {
        handleRemoveNode: function handleRemoveNode() {
          return controller.run('onAction', removePlaceholder(node));
        },
        editor: controller,
        renderText: renderText,
        locale: locale
      }));
    },
    iph: function iph(props, controller) {
      return /*#__PURE__*/_createElement(InlinePlaceholder, _extends({}, props, {
        editor: controller,
        renderText: renderText
      }));
    }
  };
}
//# sourceMappingURL=createRenderNode.js.map