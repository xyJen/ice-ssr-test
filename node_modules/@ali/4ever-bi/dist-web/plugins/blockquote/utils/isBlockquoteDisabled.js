;
export default function isBlockquoteDisabled(controller) {
  var selection = controller.value.selection;
  var isCollapsedAtEdge = selection.isCollapsedAtEdge;

  if (isCollapsedAtEdge) {
    return true;
  }

  if (controller.query('isSelectionInListSymbol')) {
    return true;
  }

  return false;
}
//# sourceMappingURL=isBlockquoteDisabled.js.map