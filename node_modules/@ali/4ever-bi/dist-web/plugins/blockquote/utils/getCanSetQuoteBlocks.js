import { getRangesFromEditor } from '@ali/4ever-utils';
var effectiveTypesList = ['paragraph', 'heading-1', 'heading-2', 'heading-3', 'heading-4', 'heading-5', 'heading-6'];

function getCanSetQuoteBlocks(controller) {
  var document = controller.value.document;
  var ranges = getRangesFromEditor(controller);
  var ret = [];
  ranges.forEach(function (selection) {
    var blocks = document.getLeafBlocksAtRange(selection);
    var filteredBlocks = blocks.filter(function (block) {
      return effectiveTypesList.includes(block.type);
    });
    ret.push.apply(ret, filteredBlocks);
  });
  return ret;
}

export default getCanSetQuoteBlocks;
//# sourceMappingURL=getCanSetQuoteBlocks.js.map