import { getRangesFromEditor } from '@ali/4ever-utils';

function getBlockquoteState(controller) {
  var ranges = getRangesFromEditor(controller);
  return ranges.some(function (selection) {
    var document = controller.value.document;
    var blocks = document.getLeafBlocksAtRange(selection);
    return blocks.some(function (block) {
      var _block$data;

      return block == null ? void 0 : (_block$data = block.data) == null ? void 0 : _block$data.blockquote;
    });
  });
}

export default getBlockquoteState;
//# sourceMappingURL=getBlockquoteState.js.map