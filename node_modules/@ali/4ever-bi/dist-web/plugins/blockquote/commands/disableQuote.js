import _extends from "@babel/runtime/helpers/extends";
import { Commands } from '@ali/4ever-cangjie';
import getCanSetQuoteBlocks from "../utils/getCanSetQuoteBlocks";
export default function disableQuote(editor, next) {
  getCanSetQuoteBlocks(editor).forEach(function (block) {
    editor.command(Commands.setNodeByKey, block.key, {
      type: block.type,
      data: _extends({}, block.data, {
        blockquote: false
      })
    });
  });

  if (next) {
    next();
  }

  return editor;
}
//# sourceMappingURL=disableQuote.js.map