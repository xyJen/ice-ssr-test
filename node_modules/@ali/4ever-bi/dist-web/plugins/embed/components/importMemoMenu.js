import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";
import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { MenuItem } from '@ali/we-design';
import { importMemo } from '@ali/4ever-factory';
export default function ImportMemoMenu(props) {
  var menuItem = props.menuItem,
      controller = props.controller;

  var title = menuItem.title,
      rest = _objectWithoutPropertiesLoose(menuItem, ["title"]);

  var value = controller.value;
  var isDisabled = controller.query('isSelectionInImage') || controller.query('isSelectionInLinkCard');

  var handleClick = function handleClick() {
    if (isDisabled) {
      return;
    }

    controller.run('onAction', importMemo());
  };

  return /*#__PURE__*/_createElement(MenuItem, _extends({}, rest, {
    onClick: handleClick,
    disabled: isDisabled
  }), title);
}
//# sourceMappingURL=importMemoMenu.js.map