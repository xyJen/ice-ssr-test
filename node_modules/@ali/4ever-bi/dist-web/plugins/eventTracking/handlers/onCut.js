import { eventTracking, TrackingType } from "../actions";
import { sample } from '@ali/4ever-bamboo';
var trackCut = sample(function (controller) {
  controller.run('onAction', eventTracking({
    type: TrackingType.cut
  }));
}, {
  sample: 5
});
export default function onCut(event, controller, next) {
  if (controller.readOnly && !controller.canCopy) {
    return next();
  }

  trackCut(controller);
  return next();
}
//# sourceMappingURL=onCut.js.map