import { sample } from '@ali/4ever-bamboo';
import { eventTracking, TrackingType } from "../actions";
var trackCopy = sample(function (controller) {
  controller.run('onAction', eventTracking({
    type: TrackingType.copy
  }));
}, {
  sample: 10
});
export default function onCopy(event, controller, next) {
  if (controller.readOnly && !controller.canCopy) {
    return next();
  }

  trackCopy(controller);
  return next();
}
//# sourceMappingURL=onCopy.js.map