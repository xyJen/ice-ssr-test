import { sample } from '@ali/4ever-bamboo';
import { eventTracking, TrackingType } from "../actions";
var trackPaste = sample(function (controller) {
  Promise.resolve().then(function () {
    controller.run('onAction', eventTracking({
      type: TrackingType.paste
    }));
  });
}, {
  sample: 10
});
export default function onPaste(event, controller, next) {
  trackPaste(controller);
  return next();
}
//# sourceMappingURL=onPaste.js.map