import { sample } from '@ali/4ever-bamboo';
import { eventTracking, TrackingType } from "../actions";
var trackFocusText = sample(function (controller) {
  controller.run('onAction', eventTracking({
    type: TrackingType.focusText
  }));
}, {
  sample: 50
});
export default function onMouseDown(event, controller, next) {
  if (!event.shiftKey) {
    trackFocusText(controller);
  }

  return next();
}
//# sourceMappingURL=onMouseDown.js.map