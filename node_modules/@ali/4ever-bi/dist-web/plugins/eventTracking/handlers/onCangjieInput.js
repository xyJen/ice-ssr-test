import { sample } from '@ali/4ever-bamboo';
import { eventTracking, TrackingType } from "../actions";
var trackIMEInputText = sample(function (controller) {
  controller.run('onAction', eventTracking({
    type: TrackingType.IMEInputText
  }));
}, {
  sample: 50
});
var trackInputText = sample(function (controller) {
  controller.run('onAction', eventTracking({
    type: TrackingType.inputText
  }));
}, {
  sample: 50
});
export default function onCangjieInput(event, controller, next) {
  if (event.detail.type === 'compositionStart') {
    trackIMEInputText(controller);
  } else if (event.detail.type === 'insertText') {
    trackInputText(controller);
  }

  return next();
}
//# sourceMappingURL=onCangjieInput.js.map