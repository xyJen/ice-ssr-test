import { Commands } from '@ali/4ever-cangjie';
export default function replace(editor, value) {
  var text = value;
  editor.command(Commands.insertText, text).command(Commands.focus);
  return editor;
}
//# sourceMappingURL=replace.js.map