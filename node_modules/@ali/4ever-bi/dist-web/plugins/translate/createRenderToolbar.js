import _extends from "@babel/runtime/helpers/extends";
import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { ItemKey, ItemSlot, SlotType } from '@ali/4ever-component';
import createTranslateButton from "./components/next/translateButton";
import NewTranslationButton from "./components/next/newTranslateButton";
export default function createRenderToolbar(config) {
  return function (props, controller) {
    var buttonName = props.buttonName,
        extraProps = props.extraProps;

    if (buttonName === ItemKey.translate) {
      if (!config.enableDocTranslation) {
        return createTranslateButton(_extends({
          controller: controller,
          config: config,
          locale: config.locale
        }, extraProps));
      }

      return /*#__PURE__*/_createElement(ItemSlot, {
        display: SlotType.iconMenu
      }, /*#__PURE__*/_createElement(NewTranslationButton, _extends({
        controller: controller,
        config: config
      }, extraProps)));
    }

    return null;
  };
}
//# sourceMappingURL=createRenderToolbar.js.map