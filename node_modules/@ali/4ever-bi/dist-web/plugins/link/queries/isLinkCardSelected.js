import { isLinkCardNode } from "../utils";
export default function isLinkCardSelected(controller, _ref) {
  var node = _ref.node;
  var _controller$value = controller.value,
      selection = _controller$value.selection,
      document = _controller$value.document;
  var isSelected = document.isNodeInRange(node.key, selection);
  var isLinkCard = isLinkCardNode(node);
  return isLinkCard && isSelected && selection.isCollapsed && controller.value.isFocused;
}
//# sourceMappingURL=isLinkCardSelected.js.map