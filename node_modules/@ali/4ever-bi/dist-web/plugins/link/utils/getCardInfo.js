import { isLinkCardNode } from "./isLinkCard";
export default function getCardInfo(node, injections) {
  if (isLinkCardNode(node)) {
    var data = node.data;
    var cardInfo = data.cardInfo;

    if (cardInfo) {
      return cardInfo;
    }
  }

  if (injections && injections.length > 0) {
    var injection = injections.find(function (i) {
      return i.key === node.key;
    });

    if (injection) {
      return injection.data.cardInfo;
    }
  }

  return null;
}
//# sourceMappingURL=getCardInfo.js.map