var STORE = {};

var mergeStore = function mergeStore(key, data) {
  STORE[key] = Object.assign({}, STORE[key], data);
};

var getStore = function getStore(key) {
  return STORE[key] || {};
};

export var setJustCreated = function setJustCreated(key, isJustCreated) {
  mergeStore(key, {
    isJustCreated: isJustCreated
  });
};
export var isJustCreated = function isJustCreated(key) {
  return getStore(key).isJustCreated;
};
//# sourceMappingURL=linkStore.js.map