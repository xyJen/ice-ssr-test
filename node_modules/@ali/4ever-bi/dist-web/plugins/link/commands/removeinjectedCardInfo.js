export default function removeinjectedCardInfo(controller, node) {
  var injections = controller.value.injections;
  var excludeInjections = injections.filter(function (i) {
    return i.key !== node.key;
  });
  controller.setInjections([].concat(excludeInjections));
  return controller;
}
//# sourceMappingURL=removeinjectedCardInfo.js.map