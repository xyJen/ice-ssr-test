import { Injection } from '@ali/4ever-cangjie';
export default function injectCardInfo(controller, node, cardInfo, range) {
  var injections = controller.value.injections;
  var injection = Injection.create({
    key: node.key,
    data: {
      cardInfo: cardInfo,
      range: range
    }
  });
  var excludeInjections = injections.filter(function (i) {
    return i.key !== node.key;
  });
  controller.setInjections([].concat(excludeInjections, [injection]));
  return controller;
}
//# sourceMappingURL=injectCardInfo.js.map