import { Commands } from '@ali/4ever-cangjie';
export default function removePureLink(controller, node, offset, href) {
  var selection = controller.value.selection;
  var range = selection.moveToStartOfNode(node, controller).moveForward(offset).moveFocusForward(href.length);
  return controller.command('deleteAtRange', range).command(Commands.focus);
}
//# sourceMappingURL=removePureLink.js.map