import { Commands } from '@ali/4ever-cangjie';
export default function unwrapLinkCard(controller, node) {
  var selection = controller.value.selection;
  var range = selection.moveAnchorToStartOfNode(node, controller).moveFocusToEndOfNode(node, controller);
  controller.command(Commands.select, range).command('unwrapLink');
  return controller;
}
//# sourceMappingURL=unwrapLinkCard.js.map