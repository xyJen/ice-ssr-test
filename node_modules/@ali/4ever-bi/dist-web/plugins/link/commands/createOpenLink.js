import { LinkPlugin } from '@ali/4ever-bamboo';
var normalizeLink = LinkPlugin.normalizeLink,
    openLink = LinkPlugin.openLink;
export default function createOpenLink(protocolAllowList, openLinkOfConfig) {
  return function (controller, href) {
    if (openLinkOfConfig) {
      var normalizedLink = normalizeLink(href, protocolAllowList);
      openLinkOfConfig(normalizedLink);
    } else {
      openLink(href, protocolAllowList);
    }

    return controller;
  };
}
//# sourceMappingURL=createOpenLink.js.map