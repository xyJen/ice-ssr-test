import { SetInlineHrefMenu } from "./components/sidebarMenu";

function sidebar(config) {
  var locale = config.locale;
  return {
    items: {
      setInlineHref: SetInlineHrefMenu
    },
    config: config,
    locale: locale
  };
}

export default sidebar;
//# sourceMappingURL=sidebar.js.map