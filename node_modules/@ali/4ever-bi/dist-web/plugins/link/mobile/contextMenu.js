import { PluginRoles } from '@ali/4ever-bamboo';
import { LINK_OPEN } from "../actions";
import locale from "../../../locales/zh_CN";
export default (function (configs) {
  return function contextMenu(controller, next) {
    var link = controller.query('getSelectedLinkUrl');

    if (!link) {
      return next();
    }

    var title = (configs == null ? void 0 : configs.locale.openLinkMenu) || locale.link.locale.openLinkMenu;
    var item = {
      name: title,
      action: {
        type: LINK_OPEN,
        payload: {
          href: link
        }
      },
      key: 'openLink',
      icon: 'https://gw.alicdn.com/imgextra/i3/O1CN01PvOSYX1Y9NJ9rGzkZ_!!6000000003016-55-tps-24-24.svg',
      options: {
        group: 100,
        mobile: true
      },
      role: PluginRoles.link
    }; // 非只读的预览模式，显示 open link 按钮

    if (controller.query('isQuickPreview')) {
      return [item];
    }

    return [item].concat(next());
  };
});
//# sourceMappingURL=contextMenu.js.map