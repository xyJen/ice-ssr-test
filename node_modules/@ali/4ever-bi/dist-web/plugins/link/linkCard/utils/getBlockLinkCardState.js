/**
 * 在编辑形态下，如果 title、desc、imgURL 都为 undefined，则说明请求失败，需要显示 error
 * 如果存在 injection 数据，说明是 loading 状态
 * @param title
 * @param desc
 * @param imgURL
 */
export var getBlockLinkCardState = function getBlockLinkCardState(node) {
  var isLoading = node.data.isLoading;
  var _ref = node.data.metadata,
      cardInfo = _ref.cardInfo; // injection 有状态，返回 loading

  if (isLoading) {
    return 'loading';
  }

  var _ref2 = cardInfo || {},
      title = _ref2.title,
      desc = _ref2.desc,
      imgURL = _ref2.imgURL; // 数据都为 undefined 返回 error


  if (title === undefined && desc === undefined && imgURL === undefined) {
    return 'error';
  }

  return 'normal';
};
//# sourceMappingURL=getBlockLinkCardState.js.map