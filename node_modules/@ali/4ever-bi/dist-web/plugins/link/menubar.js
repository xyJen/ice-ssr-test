import { menu, inlineMenu } from "./components/linkMenu";
export default function menubar(locale) {
  return {
    menu: menu,
    inlineMenu: inlineMenu,
    locale: locale
  };
}
//# sourceMappingURL=menubar.js.map