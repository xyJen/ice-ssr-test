import { LASER_POINTER_HIGHLIGHT, LASER_POINTER_REMOVE_HIGHLIGHT } from "./actions";
import { highlightByPath, removeHighlight } from "./utils";

var onAction = function onAction(action, controller, next) {
  var type = action.type,
      payload = action.payload;

  if (type === LASER_POINTER_HIGHLIGHT) {
    var _ref = payload,
        path = _ref.path;
    highlightByPath(controller, path);
  } else if (type === LASER_POINTER_REMOVE_HIGHLIGHT) {
    removeHighlight(controller);
  }

  return next();
};

export default onAction;
//# sourceMappingURL=onAction.js.map