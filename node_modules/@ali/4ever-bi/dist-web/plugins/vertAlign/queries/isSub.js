export default function isSub(controller) {
  var value = controller.value;
  return value.activeMarks.some(function (m) {
    return m.type === 'vertAlign' && m.data.value === 'subscript';
  });
}
//# sourceMappingURL=isSub.js.map