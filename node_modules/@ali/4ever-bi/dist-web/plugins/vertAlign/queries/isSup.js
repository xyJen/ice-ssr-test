export default function isSup(controller) {
  var value = controller.value;
  return value.activeMarks.some(function (m) {
    return m.type === 'vertAlign' && m.data.value === 'superscript';
  });
}
//# sourceMappingURL=isSup.js.map