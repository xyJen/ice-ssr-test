import { createSpaceHandler } from '@ali/4ever-utils';
export default function createOnCangjieInput() {
  var onSubSpace = createSpaceHandler({
    mark: {
      type: 'vertAlign',
      data: {
        value: 'subscript'
      }
    },
    delimiter: [{
      character: '~',
      size: 1
    }]
  });
  var onSupSpace = createSpaceHandler({
    mark: {
      type: 'vertAlign',
      data: {
        value: 'superscript'
      }
    },
    delimiter: [{
      character: '^',
      size: 1
    }]
  });
  return function onCangjieInput(event, controller, next) {
    if (event.detail.data !== ' ') {
      return next();
    }

    return onSubSpace(event, controller, function () {
      onSupSpace(event, controller, next);
    });
  };
}
//# sourceMappingURL=onCangjieInput.js.map