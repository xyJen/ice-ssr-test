import { environment } from '@ali/4ever-cangjie';

function hasVertAlign(value, type) {
  return value.activeMarks.some(function (m) {
    return m.type === 'vertAlign' && m.data.get('value') === type;
  });
}

export function hasSubMark(value) {
  return hasVertAlign(value, 'subscript');
}
export function hasSupMark(value) {
  return hasVertAlign(value, 'superscript');
}
var MOD = environment.IS_MAC ? '⌘' : 'Ctrl';
var supKey = environment.IS_WINDOWS ? "'" : '.';
var subKey = environment.IS_WINDOWS ? ';' : ',';
export var SHORTCUT = {
  sub: MOD + "+" + subKey,
  sup: MOD + "+" + supKey
};
export function getHotKeyTips(type, location) {
  var key = type.toLowerCase().concat('ButtonTitle');
  return location[key] + " " + SHORTCUT[type];
}
//# sourceMappingURL=utils.js.map