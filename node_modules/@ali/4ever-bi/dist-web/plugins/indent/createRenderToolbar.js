import _extends from "@babel/runtime/helpers/extends";
import createDecreaseIndentButton from "./components/next/createDecreaseIndentButton";
import createIncreaseIndentButton from "./components/next/createIncreaseIndentButton";
var createButtons = {
  'indent.decrease': createDecreaseIndentButton,
  'indent.increase': createIncreaseIndentButton
};
export default function createRenderToolbar(locale) {
  return function (props, controller) {
    var buttonName = props.buttonName,
        extraProps = props.extraProps;
    var createButton = createButtons[buttonName];

    if (createButton) {
      return createButton(_extends({
        controller: controller,
        locale: locale
      }, extraProps));
    }

    return null;
  };
}
//# sourceMappingURL=createRenderToolbar.js.map