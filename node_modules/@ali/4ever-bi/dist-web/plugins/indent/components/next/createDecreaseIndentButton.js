import _extends from "@babel/runtime/helpers/extends";
import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { IconReduceIndent } from '@ali/4ever-component';
import { PluginRoles } from '@ali/4ever-bamboo';
import { DECREASE_HOT_KEY_TIP } from "../../utils/hotKeyTips";
import { decreaseIndent } from "../../actions";
;

var _ref = /*#__PURE__*/_createElement(IconReduceIndent, null);

export default function createDecreaseIndentButton(props) {
  var controller = props.controller,
      locale = props.locale,
      icon = props.icon,
      title = props.title,
      tooltip = props.tooltip,
      badgeDot = props.badgeDot,
      disabled = props.disabled;

  var handleClick = function handleClick() {
    return controller.run('onAction', decreaseIndent());
  };

  var isCollapsedAtEdge = controller.value.selection.isCollapsedAtEdge;
  var isDisabled = disabled || isCollapsedAtEdge || controller.query('isSelectionInHr') || controller.query('isSelectionInListSymbol');
  var buttonConfig = {
    icon: icon || _ref,
    onClick: handleClick,
    title: title || (locale == null ? void 0 : locale.leftButtonTitle),
    tooltip: _extends({
      title: title || (locale == null ? void 0 : locale.leftButtonTitle),
      shortCut: DECREASE_HOT_KEY_TIP
    }, tooltip),
    badgeDot: badgeDot,
    disabled: isDisabled,
    role: PluginRoles.indentDecrease,
    testid: "bi-toolbar-decindent"
  };
  return buttonConfig;
}
//# sourceMappingURL=createDecreaseIndentButton.js.map