import { Sticker } from '@ali/4ever-mo';
import { getRangesFromEditor } from '@ali/4ever-utils';
/**
 * @description 获取选区内的 sticker 节点
 * @param {controller}
 * */

export default function getStickersAtRange(controller) {
  var value = controller.value;
  var ranges = getRangesFromEditor(controller);
  var inlines = ranges.reduce(function (pre, range) {
    var rangeInlines = value.document.getLeafInlinesAtRange(range);
    Array.prototype.push.apply(pre, rangeInlines);
    return pre;
  }, []);
  return inlines.filter(function (inline) {
    return inline.type === Sticker.TYPE;
  });
}
//# sourceMappingURL=getStickersAtRange.js.map