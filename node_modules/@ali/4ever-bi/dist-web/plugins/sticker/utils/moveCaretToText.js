import { moveCaret } from "../actions";
import { STICKER_NODE_TYPE } from "../constants";

/**
 * @description sticker 为选中态时，根据实际场景移动光标至节点左侧或右侧
 * @param offset 光标移动偏移量
 * */
export default function moveCaretToText(_ref) {
  var controller = _ref.controller,
      offset = _ref.offset,
      cb = _ref.cb;
  var _controller$value = controller.value,
      document = _controller$value.document,
      selection = _controller$value.selection;
  var anchor = selection.anchor;
  var parent = document.getParent(anchor.key);
  if ((parent == null ? void 0 : parent.type) !== STICKER_NODE_TYPE) return;

  if (cb) {
    cb();
  } else {
    controller.run('onAction', moveCaret({
      offset: offset
    }));
  }
}
//# sourceMappingURL=moveCaretToText.js.map