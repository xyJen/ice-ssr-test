import { Commands } from '@ali/4ever-cangjie';
export default function setSticker(controller, payload) {
  var code = payload.code,
      node = payload.node;
  return node && controller.command(Commands.setNodeByKey, node.key, {
    type: node.type,
    data: {
      code: code
    }
  }).command(Commands.moveToStartOfNextText).command(Commands.focus);
}
//# sourceMappingURL=setSticker.js.map