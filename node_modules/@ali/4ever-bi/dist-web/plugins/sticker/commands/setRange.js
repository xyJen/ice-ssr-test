import { Commands } from '@ali/4ever-cangjie';
import { MOVE_SET } from "../constants";
export default function setRange(controller, payload) {
  var document = controller.value.document;
  var range = payload.range,
      trigger = payload.trigger;
  var _MOVE_SET$trigger = MOVE_SET[trigger],
      getTargetMethod = _MOVE_SET$trigger.getTargetMethod,
      moveMethod = _MOVE_SET$trigger.moveMethod;
  var maybeSticker = document.getParent(range.anchor.key);
  var sideNode = document[getTargetMethod](maybeSticker.key);
  var newRange = range[moveMethod](sideNode, controller);
  return controller.command(Commands.select, newRange);
}
//# sourceMappingURL=setRange.js.map