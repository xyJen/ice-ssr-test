import _extends from "@babel/runtime/helpers/extends";
import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { ItemKey, ItemSlot, SlotType } from '@ali/4ever-component';
import FontButton from "./components/next/fontsButton";
var SMALL_WIDTH = 70;
var WIDE_WIDTH = 135;
var buttons = {
  'fonts': FontButton,
  'fonts.wide': FontButton
};
export default function createRenderToolbar(locale) {
  if (locale === void 0) {
    locale = {};
  }

  return function (props, controller) {
    var buttonName = props.buttonName,
        extraProps = props.extraProps;
    var Component = buttons[buttonName];

    if (Component) {
      var width = buttonName === ItemKey.fonts ? SMALL_WIDTH : WIDE_WIDTH;
      return /*#__PURE__*/_createElement(ItemSlot, {
        display: SlotType.textSelect,
        width: width
      }, /*#__PURE__*/_createElement(Component, _extends({
        controller: controller,
        locale: locale,
        width: width
      }, extraProps)));
    }

    return null;
  };
}
//# sourceMappingURL=createRenderToolbar.js.map