import getActiveFontsMark, { defaultFonts } from "../utils/getActiveFontsMark";
export default function getFonts(controller, inheritedFonts) {
  var symbolFont = controller.query('getSymbolStyleByKey', 'fonts');
  if (symbolFont) return symbolFont;
  var activeFontsMark = getActiveFontsMark(controller, inheritedFonts);

  if (activeFontsMark) {
    var _activeFontsMark$data = activeFontsMark.data,
        eastAsia = _activeFontsMark$data.eastAsia,
        ascii = _activeFontsMark$data.ascii;

    if (eastAsia && eastAsia !== defaultFonts) {
      // 优先显示中文字体
      return eastAsia;
    } else if (ascii && ascii !== defaultFonts) {
      return ascii;
    }

    return defaultFonts;
  } // 没有公共字体则返回空，以便区别默认


  return '';
}
//# sourceMappingURL=getFonts.js.map