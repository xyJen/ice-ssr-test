import { FONTS_SET } from "../actions";
import setFonts from "../commands/setFonts";
export default function onAction(action, controller, next) {
  var type = action.type,
      payload = action.payload;

  if (type === FONTS_SET) {
    var fonts = payload.fonts;
    setFonts(controller, fonts);
  }

  return next();
}
//# sourceMappingURL=onAction.js.map