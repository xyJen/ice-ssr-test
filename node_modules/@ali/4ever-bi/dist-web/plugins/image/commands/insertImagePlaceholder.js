import { Commands, Inline } from '@ali/4ever-cangjie';
export default function insertImagePlaceholder(controller) {
  var value = controller.value;
  var selection = value.selection,
      startInline = value.startInline;
  if (selection.isExpanded) return controller;

  if (startInline) {
    controller.command(Commands.moveToStartOfNextText);
  }

  var inline = Inline.create({
    type: 'image',
    data: {}
  });
  return controller.command(Commands.insertInline, inline);
}
//# sourceMappingURL=insertImagePlaceholder.js.map