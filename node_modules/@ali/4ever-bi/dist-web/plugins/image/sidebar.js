import Sidebar from "./components/Sidebar";
import { SizeAndStyle, RecommendStyle } from "./components/SidebarMenus";

function sidebar(config) {
  var _config$image = config.image,
      image = _config$image === void 0 ? {} : _config$image;
  var _image$locale = image.locale,
      locale = _image$locale === void 0 ? {} : _image$locale;
  return {
    imageOptions: Sidebar,
    items: {
      sizeAndStyle: SizeAndStyle,
      recommendStyle: RecommendStyle
    },
    config: image,
    locale: locale
  };
}

export default sidebar;
//# sourceMappingURL=sidebar.js.map