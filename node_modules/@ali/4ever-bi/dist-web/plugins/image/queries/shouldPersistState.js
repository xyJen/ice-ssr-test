import { Image } from '@ali/4ever-mo';
export default function shouldPersistState(_, _ref) {
  var node = _ref.node;

  if (Image.isImage(node)) {
    return true;
  }

  return undefined;
}
//# sourceMappingURL=shouldPersistState.js.map