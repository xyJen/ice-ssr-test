export default function getResizeCursor(angle, rotation) {
  var index = Math.floor(((angle + rotation + 22.5) % 360 + 360) % 360 / 45) % 4;
  var cursors = ['ns-resize', 'nesw-resize', 'ew-resize', 'nwse-resize'];
  return cursors[index];
}
//# sourceMappingURL=getResizeCursor.js.map