import { pick, isEqual } from 'lodash-es';
export function isStyleActive(styleData, imageData) {
  var styleKeys = ['outline', 'radius', 'shadow'];
  var pickedStyleData = pick(styleData, styleKeys);
  var pickedImageData = pick(imageData, styleKeys);
  return isEqual(pickedStyleData, pickedImageData);
}
//# sourceMappingURL=isStyleActive.js.map