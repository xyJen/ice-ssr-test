import calcImageSize from "./calcImageSize";

var getImageSize = function getImageSize(src, controller) {
  return new Promise(function (resolve, reject) {
    var img = new Image();

    img.onload = function () {
      return resolve(calcImageSize(img.width, img.height, controller));
    };

    img.onerror = function () {
      return reject();
    };

    img.src = src;
  });
};

export default getImageSize;
//# sourceMappingURL=getImageSize.js.map