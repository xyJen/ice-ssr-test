// 90度，180度等为断点，旋转到阈值内会自动吸附到断点度数
var THRESHOLD = 3;

var fitRotate = function fitRotate(rotate, threshold) {
  if (threshold === void 0) {
    threshold = THRESHOLD;
  }

  var noun = rotate % 90;
  var num = Math.floor(rotate / 90);

  if (noun >= 90 - threshold) {
    return 90 * (num + 1);
  }

  if (noun <= threshold) {
    return 90 * num;
  }

  return rotate;
};

export default fitRotate;
//# sourceMappingURL=fitRotate.js.map