export default function isSelectionInImage(value) {
  var selection = value.selection,
      document = value.document;
  return Boolean(selection && selection.isCollapsed && document.getClosest(selection.anchor.key, // @ts-ignore
  function (_ref) {
    var type = _ref.type;
    return type === 'image';
  }));
}
//# sourceMappingURL=inSelectionInImage.js.map