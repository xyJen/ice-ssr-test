import onSpace from "./onSpace";
export default function createOnCangjieInput(config, state) {
  return function onCangjieInput(event, controller, next) {
    if (event.detail.data !== ' ') return next();

    if (!config.disableMarkdown) {
      return onSpace(event, controller, next);
    }

    return next();
  };
}
//# sourceMappingURL=onCangjieInput.js.map