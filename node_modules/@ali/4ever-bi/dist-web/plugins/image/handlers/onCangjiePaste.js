import onInsertImage from "./onInsertImage";
export default function createOnCangjiePaste(config, state) {
  var imageConfig = config.image || {};
  return function onCangjiePaste(event, controller, next) {
    var clipboardData = controller.query('getClipboardData', event);
    var files = clipboardData == null ? void 0 : clipboardData.getImages();

    if (files && files.length >= 1) {
      return onInsertImage(controller, files, imageConfig, state);
    }

    return next();
  };
}
//# sourceMappingURL=onCangjiePaste.js.map