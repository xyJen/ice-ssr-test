import React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { ImagePlugin } from '@ali/4ever-bamboo';
import { isStyleActive } from "../../utils/isStyleActive";
import { updateImage } from "../../actions";
import { RecommendStyleMenu } from "../recommendStyleItems";
;
export var FocusImageRecommendStyleMenu = function FocusImageRecommendStyleMenu(props) {
  var controller = props.controller,
      size = props.size;
  var node = ImagePlugin.useFocusImage(controller);

  var handleClick = function handleClick(data) {
    if (isStyleActive(data, node.data)) {
      controller.run('onAction', updateImage(node, ImagePlugin.DEFAULT_STYLE));
    } else {
      controller.run('onAction', updateImage(node, data));
    }
  };

  return node && /*#__PURE__*/_createElement(RecommendStyleMenu, {
    data: node.data,
    size: size,
    onClick: handleClick
  });
};
export default RecommendStyleMenu;
//# sourceMappingURL=RecommendStyleMenu.js.map