import { checkBlockData, checkBlockContent } from "./isNewLine";
import { Heading } from '@ali/4ever-plugin-heading';
export default function isInsertBtnDisplay(controller, data) {
  var node = data.node,
      composing = data.composing;
  if (composing.length > 0 || !node) return false; // 使用 query 后不会再有 data.pi，直接比较 type

  if (node.type !== 'paragraph') return false;
  if (Heading.isCallout(node)) return false;
  if (!checkBlockData(controller, node)) return false;
  if (!checkBlockContent(node)) return false;
  if (controller.query('isHideDragButton', {
    node: node
  })) return false;
  return true;
}
//# sourceMappingURL=isInsertBtnDisplay.js.map