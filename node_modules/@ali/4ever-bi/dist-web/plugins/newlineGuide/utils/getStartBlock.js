/**
 * 根据 startBlock 获取光标所在的 block（兼容排版模式）
 * line 是排版中分割的行，需要再向上一层
 * @param controller
 */
export default function getStartBlock(controller) {
  var _controller$value = controller.value,
      startBlock = _controller$value.startBlock,
      document = _controller$value.document;
  if (!startBlock) return startBlock;
  var node = startBlock;

  if (node.type === 'line') {
    node = document.getClosestBlock(node.key);
  }

  return node;
}
//# sourceMappingURL=getStartBlock.js.map