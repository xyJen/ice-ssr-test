import _extends from "@babel/runtime/helpers/extends";
import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { environment } from '@ali/4ever-cangjie';
import NewlineGuide from "./components/newlineGuide";
export default function createRenderEditable(config) {
  return function (props, controller, next) {
    var _config$newlineGuide;

    // 移动端、配置未开启不显示
    var isNecessary = !environment.IS_MOBILE && !!((_config$newlineGuide = config.newlineGuide) != null && _config$newlineGuide.enabled);
    return /*#__PURE__*/_createElement(React.Fragment, null, next(), isNecessary && /*#__PURE__*/_createElement(NewlineGuide, _extends({
      placeholder: props.placeholder,
      zoom: props.zoom,
      controller: controller
    }, config.newlineGuide)));
  };
}
//# sourceMappingURL=createRenderEditable.js.map