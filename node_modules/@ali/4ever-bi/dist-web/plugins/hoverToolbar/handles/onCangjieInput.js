export default function createOnCanjigeInput(config, hoverCapture) {
  var isDisabled = config.isDisabled && (config == null ? void 0 : config.isDisabled());
  return function (event, controller, next) {
    if (!isDisabled) {
      hoverCapture.getCurrentNode() && hoverCapture.forceClose();
    }

    return next();
  };
}
//# sourceMappingURL=onCangjieInput.js.map