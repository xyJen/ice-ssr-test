export default function createOnQuery(config) {
  return function onQuery(event, controller, next) {
    var type = event.type,
        args = event.args;

    if (type === 'hasHoverToolbar') {
      var currentType = args[0];
      var isDisabled = config.isDisabled,
          layouts = config.layouts;
      var disabled = isDisabled && isDisabled();
      return !disabled && layouts && !!layouts[currentType];
    }

    return next();
  };
}
//# sourceMappingURL=createOnQuery.js.map