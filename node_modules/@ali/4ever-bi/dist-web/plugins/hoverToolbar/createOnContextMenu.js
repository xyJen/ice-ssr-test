export default function createOnContextMenu(hoverCapture, hoverToolbarConfig) {
  var isDisabled = hoverToolbarConfig.isDisabled;
  var disabled = isDisabled && isDisabled();
  return function (event, controller, next) {
    if (!disabled) {
      hoverCapture.forceClose();
    }

    return next();
  };
}
//# sourceMappingURL=createOnContextMenu.js.map