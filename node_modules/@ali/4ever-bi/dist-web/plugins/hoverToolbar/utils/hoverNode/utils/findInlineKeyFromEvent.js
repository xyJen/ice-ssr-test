/* eslint-disable @typescript-eslint/ban-ts-comment */
import { constants, Inline } from '@ali/4ever-cangjie';
/**
 * 根据鼠标移动事件查找文档 key
 * 策略：查找最近的 data-cangjie-key, 如果是 inline node 就直接返回
 * @param event
 * @param container
 */

export default function findInlineKeyFromEvent(event, document) {
  var eventTarget = event.target;

  if (!eventTarget) {
    return '';
  }

  var nodeDom = eventTarget.closest("[" + constants.Selector.key + "]");
  var key = nodeDom == null ? void 0 : nodeDom.getAttribute(constants.Selector.key);

  if (key) {
    var node = document.getNode(key);
    return Inline.isInline(node) ? key : '';
  }

  return '';
}
//# sourceMappingURL=findInlineKeyFromEvent.js.map