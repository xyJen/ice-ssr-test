import { Selection, Commands, Point } from '@ali/4ever-cangjie';
export default function selectNode(controller, node) {
  var anchor = Point.createFromParams(node.key, 'before');
  var focus = Point.createFromParams(node.key, 'after');
  var newSelection = Selection.create({
    anchor: anchor,
    focus: focus
  });
  controller.command(Commands.select, newSelection);
  return controller;
}
//# sourceMappingURL=selectNode.js.map