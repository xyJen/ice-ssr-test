import _extends from "@babel/runtime/helpers/extends";

function filterVoteCard(node) {
  if (node.isElement()) {
    var _node$data;

    var newChildren = node.nodes.map(filterVoteCard);

    if (((_node$data = node.data) == null ? void 0 : _node$data.cardType) === 'vote') {
      return node.set('data', _extends({}, node.data, {
        metadata: _extends({}, node.data.metadata, {
          voteId: undefined
        })
      }));
    }

    return node.set('nodes', newChildren);
  }

  return node;
}

export default function filterFragment(node) {
  var newNode = filterVoteCard(node);
  return newNode;
}
//# sourceMappingURL=filterFragment.js.map