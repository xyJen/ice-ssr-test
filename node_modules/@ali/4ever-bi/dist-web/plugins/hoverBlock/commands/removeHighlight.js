import { removeHighlightSingle } from "../utils";
export default function removeHighlight(controller, payload, isHighlightBorder) {
  if (isHighlightBorder) {
    var node = payload.node,
        type = payload.type,
        reason = payload.reason;
    var nodes = Array.isArray(node) ? node : [node];
    nodes.forEach(function (n) {
      removeHighlightSingle(n, type, reason);
    });
  }
}
//# sourceMappingURL=removeHighlight.js.map