import { updateHighlightSingle } from "../utils";
export default function updateHighlight(controller, payload, isHighlightBorder) {
  if (isHighlightBorder) {
    var node = payload.node,
        type = payload.type,
        reason = payload.reason;
    var nodes = Array.isArray(node) ? node : [node];
    nodes.forEach(function (node) {
      updateHighlightSingle(node, type, reason);
    });
  }
}
//# sourceMappingURL=updateHighlight.js.map