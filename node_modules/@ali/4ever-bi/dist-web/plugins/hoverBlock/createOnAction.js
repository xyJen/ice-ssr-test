import { UPDATE_HOVER_HIGHLIGHT, REMOVE_HOVER_HIGHLIGHT } from "./actions";
import updateHighlight from "./commands/updateHighlight";
import removeHighlight from "./commands/removeHighlight";
export default function createOnAction(config) {
  return function (action, controller, next) {
    var type = action.type,
        payload = action.payload;
    var _config$isHighlightBo = config.isHighlightBorder,
        isHighlightBorder = _config$isHighlightBo === void 0 ? false : _config$isHighlightBo;

    if (type === UPDATE_HOVER_HIGHLIGHT) {
      updateHighlight(controller, payload, isHighlightBorder);
      return controller;
    }

    if (type === REMOVE_HOVER_HIGHLIGHT) {
      removeHighlight(controller, payload, isHighlightBorder);
      return controller;
    }

    return next();
  };
}
//# sourceMappingURL=createOnAction.js.map