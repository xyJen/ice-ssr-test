import { Marks } from '@ali/4ever-mo';
export default function getLetterSpacing(controller) {
  var value = controller.value; // 不支持列表符号

  if (value.data.listSelections && value.data.listSelections.length > 0) return 0; // 公共字符间距

  var activeSpacingMark = controller.query('getActiveMark', Marks.Spacing.TYPE);
  if (!activeSpacingMark) return 0;
  return activeSpacingMark.data.value || 0;
}
//# sourceMappingURL=getLetterSpacing.js.map