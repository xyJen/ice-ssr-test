import { SET_LETTER_SPACING } from "../actions";
import setLetterSpacing from "../commands/setLetterSpacing";
export default function onAction(action, controller, next) {
  var type = action.type,
      payload = action.payload;

  if (type === SET_LETTER_SPACING) {
    return setLetterSpacing(controller, payload.spacing);
  }

  return next();
}
//# sourceMappingURL=onAction.js.map