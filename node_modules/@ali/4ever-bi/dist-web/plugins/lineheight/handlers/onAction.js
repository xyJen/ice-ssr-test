import { LINEHEIGHT_SET } from "../actions";
import setLineHeight from "../commands/setLineHeight";
export default function onAction(action, controller, next) {
  var type = action.type,
      payload = action.payload;

  if (type === LINEHEIGHT_SET) {
    var lineHeight = payload.lineHeight,
        lineRule = payload.lineRule;
    return setLineHeight(controller, lineHeight, lineRule);
  }

  return next();
}
//# sourceMappingURL=onAction.js.map