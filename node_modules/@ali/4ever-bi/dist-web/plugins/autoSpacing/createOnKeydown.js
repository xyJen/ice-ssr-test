import { hotkeys } from '@ali/4ever-cangjie';
import onDelete from "./handlers/onDelete";
export default function createOnKeydown(config) {
  return function (event, controller, next) {
    var _config$autoSpacing;

    if (!(config != null && (_config$autoSpacing = config.autoSpacing) != null && _config$autoSpacing.enable) || !config.autoSpacing.enable()) return next();
    var isDeleteBackward = hotkeys.isDeleteBackward(event) || hotkeys.isDeleteWordBackward(event);
    var isDeleteForward = hotkeys.isDeleteForward(event) || hotkeys.isDeleteWordForward(event);
    if (!isDeleteBackward && !isDeleteForward) return next();
    return onDelete(controller, next, isDeleteForward);
  };
}
//# sourceMappingURL=createOnKeydown.js.map