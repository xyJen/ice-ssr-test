import { isKeyHotkey } from 'is-hotkey';
import { clearStyles } from "../actions";
var isClearStyleHotkey = isKeyHotkey('mod+\\');
export default function onKeyDown(event, controller, next) {
  if (isClearStyleHotkey(event)) {
    event.preventDefault();
    return controller.run('onAction', clearStyles());
  }

  return next();
}
//# sourceMappingURL=onKeyDown.js.map