import { removeMarkAtRange } from '@ali/4ever-utils';
export default function createClearMarksAtRange(ignores) {
  if (ignores === void 0) {
    ignores = [];
  }

  return function clearMarksAtRange(controller, range) {
    var value = controller.value;
    var document = value.document;
    var marks = document.getMarksAtRange(range);

    if (marks.length) {
      marks.forEach(function (mark) {
        if (ignores.includes(mark.type)) {
          return;
        }

        removeMarkAtRange(controller, range, mark.type);
      });
    }

    return controller;
  };
}
//# sourceMappingURL=clearMarksAtRange.js.map