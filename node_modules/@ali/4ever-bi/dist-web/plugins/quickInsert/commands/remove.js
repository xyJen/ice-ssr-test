import { Commands } from '@ali/4ever-cangjie';
import isSelectionInQuickInsert from "../queries/isSelectionInQuickInsert";
export default function remove(controller) {
  if (isSelectionInQuickInsert(controller)) {
    return controller.setDecorations([]).command(Commands.focus);
  }

  return controller;
}
//# sourceMappingURL=remove.js.map