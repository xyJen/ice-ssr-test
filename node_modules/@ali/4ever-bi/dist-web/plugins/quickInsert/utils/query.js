/* eslint-disable import/prefer-default-export */
import { flatten } from 'lodash-es';
export function findItemByKey(groups, key) {
  return flatten(groups).find(function (item) {
    return item.key === key;
  });
}
//# sourceMappingURL=query.js.map