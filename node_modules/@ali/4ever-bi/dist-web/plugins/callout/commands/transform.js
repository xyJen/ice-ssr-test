;
import { convertKeysToContainer } from "./convertKeysToContainer";
export var transform = function transform(controller) {
  var blocks = controller.query('getTransformNodes');

  if (Array.isArray(blocks)) {
    controller.command(convertKeysToContainer, blocks.map(function (b) {
      return b.key;
    }));
  }

  return controller;
};
//# sourceMappingURL=transform.js.map