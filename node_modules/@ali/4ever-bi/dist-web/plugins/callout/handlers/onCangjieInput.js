export default function onCangjieInput(event, controller, next) {
  var _endBlock$data;

  var value = controller.value;
  var data = value.data,
      endBlock = value.endBlock; // 暂定：高亮块整体选中时禁止输入

  if (data.selectedCallout && endBlock != null && (_endBlock$data = endBlock.data) != null && _endBlock$data.calloutPr) {
    return controller;
  }

  return next();
}
//# sourceMappingURL=onCangjieInput.js.map