import { mobileBiFactory, OldBiActions as actions, mobileInsertMentionCommand as insertMention } from '@ali/4ever-plugin-mention';
var commands = {
  insertMention: insertMention
};

var createMentionPlugin = function createMentionPlugin(configs) {
  return mobileBiFactory(configs);
};

createMentionPlugin.actions = actions;
createMentionPlugin.commands = commands;
export default createMentionPlugin;
//# sourceMappingURL=mobile.js.map