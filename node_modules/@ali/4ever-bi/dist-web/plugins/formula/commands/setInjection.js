import { Injection } from '@ali/4ever-cangjie';
export function setInjection(controller, key, data) {
  var injections = controller.value.injections;
  var injection = Injection.create({
    key: key,
    data: data
  });
  return controller.setInjections([].concat(injections.filter(function (inj) {
    return inj.key !== key;
  }), [injection]));
}
//# sourceMappingURL=setInjection.js.map