import _extends from "@babel/runtime/helpers/extends";
import { omit, map as lodashMap, isEqual } from 'lodash-es'; // 头像及isInitialized不参与对比

var isEqualValue = function isEqualValue(value, otherValue) {
  var v1 = omit(_extends({}, value, {
    comment: value.comment || '',
    place: value.place || '',
    topReceivers: lodashMap(value.topReceivers, function (o) {
      return omit(o, 'avatarMediaId');
    })
  }), ['isInitialized', 'mode']);
  var v2 = omit(_extends({}, otherValue, {
    comment: otherValue.comment || '',
    place: otherValue.place || '',
    topReceivers: lodashMap(otherValue.topReceivers, function (o) {
      return omit(o, 'avatarMediaId');
    })
  }), ['isInitialized', 'mode']);
  return isEqual(v1, v2);
};

export default isEqualValue;
//# sourceMappingURL=isEqualValue.js.map