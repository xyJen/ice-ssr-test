import _extends from "@babel/runtime/helpers/extends";
import { cardFactory } from '@ali/4ever-factory';
import locale from "../../locales/zh_CN";
import createOnCangjiePaste from "./handlers/createOnCangjiePaste";
import createRenderNode from "./createRenderNode";
import createMenubar from "./handlers/createMenu";

var createPlugin = function createPlugin(bizConfig) {
  var calendarCardConfig = bizConfig.calendarCard || {
    locale: _extends({}, locale.calendarCard.locale)
  };
  var renderNode = createRenderNode(calendarCardConfig);
  return {
    cardType: 'calendar',
    cardStyle: {
      minWidth: 300
    },
    renderNode: renderNode,
    onCangjiePaste: createOnCangjiePaste(bizConfig),
    menubar: {
      menu: createMenubar(calendarCardConfig)
    }
  };
};

var createBiCalendarCardPlugin = cardFactory(createPlugin);
export default createBiCalendarCardPlugin;
//# sourceMappingURL=index.js.map