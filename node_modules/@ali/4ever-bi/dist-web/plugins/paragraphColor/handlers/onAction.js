import { PARAGRAPH_SET_COLOR, PARAGRAPH_SET_BG_COLOR, PARAGRAPH_SELECT_RANGE } from "../actions";
import { setParagraphBgColor, setParagraphColor, selectRange } from "../commands";

var onAction = function onAction(action, controller, next) {
  var type = action.type,
      payload = action.payload;

  switch (type) {
    case PARAGRAPH_SET_COLOR:
      return controller.command(setParagraphColor, payload);

    case PARAGRAPH_SET_BG_COLOR:
      return controller.command(setParagraphBgColor, payload);

    case PARAGRAPH_SELECT_RANGE:
      return controller.command(selectRange, payload);

    default:
      return next();
  }
};

export default onAction;
//# sourceMappingURL=onAction.js.map