import _extends from "@babel/runtime/helpers/extends";

// 把属性解构出来，便于 delete
var cloneData = function cloneData(data) {
  var newData = _extends({}, data);

  if (data.list) {
    newData = _extends({}, data, {
      list: _extends({}, data.list, {
        symbolStyle: _extends({}, data.list.symbolStyle)
      })
    });
  }

  return newData;
};

export default cloneData;
//# sourceMappingURL=cloneData.js.map