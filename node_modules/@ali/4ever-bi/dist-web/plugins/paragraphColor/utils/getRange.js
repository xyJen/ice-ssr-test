import { Selection, Queries } from '@ali/4ever-cangjie';

var getRangeFromNodes = function getRangeFromNodes(controller, nodes) {
  var startBlock = nodes[0];
  var endBlock = nodes[nodes.length - 1];
  var anchor = controller.query(Queries.pointAtStartOfNode, startBlock);
  var focus = controller.query(Queries.pointAtEndOfNode, endBlock);
  var selection = Selection.create({
    anchor: anchor,
    focus: focus
  });
  return selection;
};

export default getRangeFromNodes;
//# sourceMappingURL=getRange.js.map