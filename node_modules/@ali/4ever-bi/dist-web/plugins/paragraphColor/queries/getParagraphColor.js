import { getCurrentBlocks, getRangeFromNodes } from "../utils";

var getParagraphColor = function getParagraphColor(controller, payload) {
  var currentBlock = payload.node;
  var blocks = getCurrentBlocks(controller, currentBlock.key);
  var color = '';

  if (Array.isArray(blocks) && blocks.length > 0 && blocks[0] !== null) {
    var range = getRangeFromNodes(controller, blocks);
    color = controller.query('getColorAtRange', range);
  }

  return color;
};

export default getParagraphColor;
//# sourceMappingURL=getParagraphColor.js.map