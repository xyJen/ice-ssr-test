import { getCurrentBlocks } from "../utils";

var getParagraphBgColor = function getParagraphBgColor(controller, payload) {
  var currentBlock = payload.node;
  var blocks = getCurrentBlocks(controller, currentBlock.key);
  var color = '';

  if (Array.isArray(blocks) && blocks.length > 0) {
    color = controller.query('getCommonShd').fill;
  }

  return color;
};

export default getParagraphBgColor;
//# sourceMappingURL=getParagraphBgColor.js.map