import { SZ_ADJUST, SZ_SET } from "../actions";
import setSize from "../commands/setSize";
import adjustSz from "../commands/adjustSz";
export default function onAction(action, controller, next) {
  var type = action.type,
      payload = action.payload;

  if (type === SZ_SET) {
    var sz = payload.sz;
    setSize(controller, sz);
  }

  if (type === SZ_ADJUST) {
    var isIncreasingSz = payload.isIncreasingSz;
    adjustSz(controller, isIncreasingSz);
  }

  return next();
}
//# sourceMappingURL=onAction.js.map