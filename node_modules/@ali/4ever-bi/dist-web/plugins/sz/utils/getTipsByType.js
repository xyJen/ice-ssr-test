import { environment } from '@ali/4ever-cangjie';
var MOD = environment.IS_MAC ? '⌘' : 'Ctrl';
export var BIGGER_SZ_HOT_KEY_TIP = MOD + "+Shfit+>";
export var SMALLER_SZ_HOT_KEY_TIP = MOD + "+Shift+<";
export var SHORTCUT = {
  bigger: MOD + "+Shift+>",
  smaller: MOD + "+Shift+<"
};
export default function getTipsByType(type, locale) {
  var title = type === 'bigger' ? locale.increaseSzButtonTitle : locale.decreaseSzButtonTitle;
  return title + " " + SHORTCUT[type];
}
//# sourceMappingURL=getTipsByType.js.map