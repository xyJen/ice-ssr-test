import { INLINECODE_TOGGLE } from "../actions";
import toggleInlineCode from "../commands/toggleInlineCodeMark";
export default function onAction(action, controller, next) {
  var type = action.type;

  if (type === INLINECODE_TOGGLE) {
    return toggleInlineCode(controller);
  }

  return next();
}
//# sourceMappingURL=onAction.js.map