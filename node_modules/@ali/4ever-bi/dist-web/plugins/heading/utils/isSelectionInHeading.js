import { getClosestParagraph } from '@ali/4ever-utils';
export default function isSelectionInHeading(value, type) {
  if (!value) return false;
  var startBlock = value.startBlock;
  if (!startBlock) return false;
  var block = getClosestParagraph(value);
  return !!block && block.type === type;
}
export var isSelectionInOnlyHeading = function isSelectionInOnlyHeading(value, type) {
  if (!value) return false;
  var startBlock = value.startBlock;
  if (!startBlock) return false;
  var block = getClosestParagraph(value);
  if (!block) return false;
  if (block.data.list || block.data.blockquote || block.data.calloutPr) return false;
  if (block.type !== type) return false;
  return true;
};
//# sourceMappingURL=isSelectionInHeading.js.map