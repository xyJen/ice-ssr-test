function filterSameHeading(headings) {
  var keyMap = {};
  return headings.filter(function (heading) {
    if (keyMap[heading]) {
      return false;
    }

    keyMap[heading] = true;
    return true;
  });
}

export default function getHeadingAtRanges(controller, ranges) {
  var document = controller.value.document;
  var headings = [];
  ranges.forEach(function (range) {
    headings = headings.concat(document.getLeafBlocksAtRange(range).map(function (block) {
      return block.type;
    }));
  });
  return filterSameHeading(headings);
}
//# sourceMappingURL=getHeadingAtRanges.js.map