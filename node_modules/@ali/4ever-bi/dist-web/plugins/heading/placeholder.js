import { Heading } from '@ali/4ever-mo';
var Heading1 = Heading.Heading1,
    Heading2 = Heading.Heading2,
    Heading3 = Heading.Heading3,
    Heading4 = Heading.Heading4;
export default function placeholder(config) {
  var locale = config.locale;
  return [{
    text: locale == null ? void 0 : locale.heading1Placeholder,
    when: function when(controller, node) {
      return Heading1.isHeading1(node) && Heading.isEmpty(node);
    }
  }, {
    text: locale == null ? void 0 : locale.heading2Placeholder,
    when: function when(controller, node) {
      return Heading2.isHeading2(node) && Heading.isEmpty(node);
    }
  }, {
    text: locale == null ? void 0 : locale.heading3Placeholder,
    when: function when(controller, node) {
      return Heading3.isHeading3(node) && Heading.isEmpty(node);
    }
  }, {
    text: locale == null ? void 0 : locale.heading4Placeholder,
    when: function when(controller, node) {
      return Heading4.isHeading4(node) && Heading.isEmpty(node);
    }
  }];
}
//# sourceMappingURL=placeholder.js.map