import { HEADING_SET, HEADING_TRANSFORM } from "../actions";
import setHeading from "../commands/setHeading";
import transform from "../commands/transform";
export default function onAction(action, controller, next) {
  var type = action.type,
      payload = action.payload;

  if (type === HEADING_SET) {
    var heading = payload.heading;
    setHeading(controller, heading);
  }

  if (type === HEADING_TRANSFORM) {
    var _heading = payload.heading;
    controller.command(transform, _heading);
  }

  return next();
}
//# sourceMappingURL=onAction.js.map