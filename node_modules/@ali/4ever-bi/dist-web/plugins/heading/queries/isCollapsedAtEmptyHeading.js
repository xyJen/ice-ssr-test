import { Text } from '@ali/4ever-cangjie';
import { Heading } from '@ali/4ever-mo';
export default function isCollapsedAtEmptyHeading(controller, range) {
  var isCollapsed = range.isCollapsed,
      isCollapsedAtEdge = range.isCollapsedAtEdge; // 边缘不算

  if (!isCollapsed || isCollapsedAtEdge) return false;
  var document = controller.value.document;
  var block = document.getClosestBlock(range.focus.key); // 段落和标题以外的块级元素不算

  if (!Heading.isHeading(block)) return false;
  return (block == null ? void 0 : block.text) === '' && Text.isTextList(block.nodes);
}
//# sourceMappingURL=isCollapsedAtEmptyHeading.js.map