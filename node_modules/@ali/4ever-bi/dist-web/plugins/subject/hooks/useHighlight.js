import * as React from 'react';
import { HIGHLIGHT_SUBJECT_KEY } from "../constants";
import { highlightSubject } from "../actions";

function useHighlight(controller, subjectId) {
  var highlight = !!subjectId && subjectId === controller.value.data[HIGHLIGHT_SUBJECT_KEY];
  var setHighlight = React.useCallback(function (value) {
    controller.run('onAction', highlightSubject({
      subjectId: subjectId,
      highlight: value
    }));
  }, [controller, subjectId]);
  return [highlight, setHighlight];
}

export default useHighlight;
//# sourceMappingURL=useHighlight.js.map