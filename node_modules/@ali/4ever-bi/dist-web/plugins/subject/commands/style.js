import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";
import { Commands } from '@ali/4ever-cangjie';
/**
 * 清楚议题格式
 * @param controller
 */

export function clearSubjectStyle(controller) {
  var _controller$value = controller.value,
      selection = _controller$value.selection,
      document = _controller$value.document;
  var blocks = document.getRootBlocksAtRange(selection);
  blocks.forEach(function (block) {
    if (block.data.subjectPr) {
      var _block$data = block.data,
          subjectPr = _block$data.subjectPr,
          rest = _objectWithoutPropertiesLoose(_block$data, ["subjectPr"]);

      controller.command(Commands.setNodeByKey, block.key, {
        data: rest
      });
    }
  });
  return controller;
}
//# sourceMappingURL=style.js.map