import _extends from "@babel/runtime/helpers/extends";
import { HIGHLIGHT_SUBJECT_KEY } from "../constants";
export function highlightSubject(controller, subjectId, highlight) {
  var data = controller.value.data;

  if (highlight) {
    var _extends2;

    return controller.setData(_extends({}, data, (_extends2 = {}, _extends2[HIGHLIGHT_SUBJECT_KEY] = subjectId, _extends2)));
  }

  if (data[HIGHLIGHT_SUBJECT_KEY]) {
    var newData = _extends({}, data);

    delete newData[HIGHLIGHT_SUBJECT_KEY];
    return controller.setData(newData);
  }

  return controller;
}
//# sourceMappingURL=highlight.js.map