import _extends from "@babel/runtime/helpers/extends";
import { Commands } from '@ali/4ever-cangjie';
export function toggleFinished(controller, node, finished) {
  var subjectPr = _extends({}, node.data.subjectPr, {
    finished: finished
  });

  if (!finished) {
    delete subjectPr.finished;
  }

  controller.command(Commands.setNodeByKey, node.key, {
    data: _extends({}, node.data, {
      subjectPr: subjectPr
    })
  });
  return controller;
}
export function toggleFinishedByKey(controller, key, finished) {
  var node = controller.value.document.assertElement(key);
  return toggleFinished(controller, node, finished);
}
//# sourceMappingURL=finished.js.map