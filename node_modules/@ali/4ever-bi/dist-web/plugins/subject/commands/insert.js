import { Commands } from '@ali/4ever-cangjie';
import { Heading } from '@ali/4ever-mo';
import { findNextSubjectIndex, findPrevSubjectIndex } from "../queries";
var Heading1 = Heading.Heading1;
export function insertSubject(controller, properties, from, direction) {
  var index = direction === 'before' ? controller.query(findPrevSubjectIndex, from) : controller.query(findNextSubjectIndex, from);
  var subject = Heading1.create({
    data: {
      subjectPr: properties
    }
  }); // 插入议题节点

  return controller.command(Commands.insertNodeByPath, [], index, subject).command(Commands.moveToEndOfNode, subject);
}
//# sourceMappingURL=insert.js.map