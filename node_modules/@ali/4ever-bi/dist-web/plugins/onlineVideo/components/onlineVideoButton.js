import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { Button, VedioNormal } from '@ali/we-design';
;
import { insertOnlineVideo } from "../actions";

var _ref = /*#__PURE__*/_createElement(VedioNormal, null);

export default function OnlineVideoButton(props) {
  var controller = props.controller,
      locale = props.locale;
  var isDisabled = controller.query('isSelectionInTable', {
    excludeColumns: true
  }) || controller.query('isSelectionInImage') || controller.query('isSelectionInLinkCard');

  var handleClick = function handleClick() {
    if (isDisabled) {
      return;
    }

    controller.run('onAction', insertOnlineVideo());
  };

  return /*#__PURE__*/_createElement(Button, {
    title: locale.buttonTitle,
    bordered: false,
    testid: "bi-toolbar-onlinevideo",
    icon: _ref,
    onClick: handleClick
  });
}
//# sourceMappingURL=onlineVideoButton.js.map