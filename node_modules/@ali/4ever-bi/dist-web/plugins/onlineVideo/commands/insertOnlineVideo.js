import { Commands } from '@ali/4ever-cangjie';
import uuid from 'uuid/v4';
export default (function (state) {
  return function (editor) {
    if (editor.query('isCollapsedInBlockVoid')) {
      editor.command(Commands.insertBlock, 'paragraph');
    }

    if (editor.query('isCollapsedInInlineVoid')) {
      editor.command(Commands.moveToStartOfNextText);
    }

    var id = uuid();
    editor.command(Commands.focus).command(Commands.insertBlock, {
      type: 'onlineVideo',
      data: {
        id: id
      }
    }).command(Commands.insertBlock, 'paragraph').command(Commands.blur);
    state[id] = true;
    return editor;
  };
});
//# sourceMappingURL=insertOnlineVideo.js.map