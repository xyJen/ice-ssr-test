import VideoButton from "./components/videoButton";
import { isSelectedBlock } from '@ali/4ever-utils';
export default function toolbar(config) {
  var videoConfig = config.video || {};
  var videoLocaleConfig = videoConfig.locale || {};
  return {
    buttons: {
      "default": VideoButton
    },
    locale: videoLocaleConfig,
    getTarget: function getTarget(controller) {
      if (isSelectedBlock(controller.value, 'embed')) {
        return 'embed';
      }

      return undefined;
    }
  };
}
//# sourceMappingURL=toolbar.js.map