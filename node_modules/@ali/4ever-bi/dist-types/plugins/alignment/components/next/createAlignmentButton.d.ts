import * as React from 'react';
import { Controller } from '@ali/4ever-cangjie';
import { IToolbarButtonConfig } from '@ali/4ever-component';
export interface AlignmentButtonProps extends IToolbarButtonConfig {
    controller: Controller;
    locale?: Record<string, string>;
}
export default function createAlignmentButton(type: 'left' | 'right' | 'center' | 'justify', titleKey?: string, tooltipKey?: string): React.FC<AlignmentButtonProps>;
