import { Command, Query } from '@ali/4ever-cangjie';
import { BiPlugin } from '../../../types';
declare function createAlignmentPlugin(config: any): BiPlugin;
declare namespace createAlignmentPlugin {
    var actions: {
        ALIGNMENT_SET: symbol;
        setAlignment: typeof import("../actions").setAlignment;
    };
    var commands: Record<string, Command>;
    var queries: Record<string, Query>;
}
export default createAlignmentPlugin;
