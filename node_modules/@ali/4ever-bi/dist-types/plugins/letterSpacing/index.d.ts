import { Command, Query } from '@ali/4ever-cangjie';
import LetterSpacingMenu from './components/letterSpacingMenu';
import * as actions from './actions';
import onAction from './handlers/onAction';
declare function createLetterSpacingPlugin(): {
    menubar: {
        menu: typeof LetterSpacingMenu;
    };
    commands: Record<string, Command>;
    queries: Record<string, Query>;
    actions: typeof actions;
    onAction: typeof onAction;
    models: {
        klass: string;
        type: string;
    };
};
declare namespace createLetterSpacingPlugin {
    var actions: typeof import("./actions");
    var commands: Record<string, Command>;
    var queries: Record<string, Query>;
}
export default createLetterSpacingPlugin;
