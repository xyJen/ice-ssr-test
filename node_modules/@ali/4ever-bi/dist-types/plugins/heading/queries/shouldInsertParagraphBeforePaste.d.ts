import { Controller, Document } from '@ali/4ever-cangjie';
/**
 * 判断是否需要在 insert fragment 前插入空段落
 * 目前只有 heading 有特殊产品需求，因此移到 heading 的 query 中
 */
export default function shouldInsertParagraphBeforePaste(controller: Controller, fragment: Document): boolean;
