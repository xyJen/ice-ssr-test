import { Controller } from '@ali/4ever-cangjie';
export interface HeadingButtonProps {
    controller: Controller;
}
export default function mobileHeadingButton(heading: any): (props: HeadingButtonProps) => JSX.Element;
