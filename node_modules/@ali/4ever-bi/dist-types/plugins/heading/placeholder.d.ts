import { HeadingPluginConfig } from './types';
export default function placeholder(config: HeadingPluginConfig): {
    text: string | undefined;
    when: (controller: any, node: any) => boolean;
}[];
