import ImageMenu, { ImageSetting } from './components/imageMenu';
import { ImagePluginConfig } from './types';
export default function menubar(config: ImagePluginConfig): {
    menu: typeof ImageMenu;
    download: (props: any) => JSX.Element;
    options: typeof ImageSetting;
    locale: Record<string, any>;
};
