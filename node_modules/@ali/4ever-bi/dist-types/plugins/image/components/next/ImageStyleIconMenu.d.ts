import React from 'react';
import { IImageStyleOverlayProps } from '../imageStyleOverlay';
export interface IImageStyleIconMenu extends Omit<IImageStyleOverlayProps, 'onClick'> {
    onOverlayVisibleChange: (visible: boolean) => void;
    testid?: string;
}
export declare const ImageStyleIconMenu: React.FC<IImageStyleIconMenu>;
