import { Controller } from '@ali/4ever-cangjie';
export interface ImageProperties {
    src: string;
    width?: number;
    height?: number;
    extraData?: Record<string, any>;
}
export default function insertImage(controller: Controller, maybeImages: ImageProperties | ImageProperties[]): Controller;
