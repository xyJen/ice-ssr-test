import { Controller, Inline } from '@ali/4ever-cangjie';
import { ImagePlugin } from '@ali/4ever-bamboo';
import { IImageData, ImageState } from '../types';
export declare type TransferImageData = Partial<IImageData> & {
    isError?: boolean;
};
export default function transferImages(controller: Controller, images: Inline[], transfer: ImagePlugin.ITransferImageURL, state: ImageState): Promise<{
    image: Inline;
    data: TransferImageData;
}>[];
