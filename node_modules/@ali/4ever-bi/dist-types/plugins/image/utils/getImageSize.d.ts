import { Controller } from '@ali/4ever-cangjie';
import calcImageSize from './calcImageSize';
declare type IGetImageSizeType = (src: string, controller: Controller) => Promise<ReturnType<typeof calcImageSize>>;
declare const getImageSize: IGetImageSizeType;
export default getImageSize;
