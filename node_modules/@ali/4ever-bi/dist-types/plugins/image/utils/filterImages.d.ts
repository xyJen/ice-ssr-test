import { Document, Inline } from '@ali/4ever-cangjie';
import { ImagePluginConfig } from '../types';
/**
 * 删除某些特定 URL 的 img，这个问题主要用来处理 Word 拷贝图文的情况，图片的链接是本地文件，无法读取
 * @param fragment
 * @param dropImage
 */
export declare function dropImages(fragment: Document, shouldSkipImageURL: (src: string) => boolean): Document;
/**
 * 过滤出可以 transfer 的图片
 * @param fragment
 * @param filterTransfer
 */
export declare function filterTransferableImages(fragment: Document, shouldTransferImageURL: ImagePluginConfig['shouldTransferImageURL']): Inline[];
