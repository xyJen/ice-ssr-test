import { MobileImage, MobileBigImage } from '../components/mobileImageButton';
export default function toolbar(imageConfig: any): {
    buttons: {
        mobile: typeof MobileImage;
        mobileBig: typeof MobileBigImage;
    };
    locale: any;
};
