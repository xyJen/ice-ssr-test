import { Controller, RenderEditableProps } from '@ali/4ever-cangjie';
import { BiPluginConfig } from '../../types';
export default function createRenderEditable(config: BiPluginConfig): (props: RenderEditableProps, controller: Controller, next: any) => JSX.Element;
