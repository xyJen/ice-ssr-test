import { Block, Controller } from '@ali/4ever-cangjie';
export declare function checkBlockContent(block: Block): boolean;
export declare function checkBlockData(controller: Controller, block: Block): boolean;
export declare function checkBlockCount(controller: Controller): boolean;
export interface IsNewLineProps {
    node: Block | null;
    composing: string;
    placeholder?: string;
}
export default function isNewline(controller: Controller, { node, composing, placeholder, }: IsNewLineProps): boolean;
