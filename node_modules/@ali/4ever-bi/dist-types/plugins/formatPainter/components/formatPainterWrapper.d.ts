import * as React from 'react';
import { Controller, DocStyle } from '@ali/4ever-cangjie';
export interface FormatPainterWrapperProps {
    controller: Controller;
    children: React.ReactNode;
    onDocStyleChange?: (style?: DocStyle) => void;
}
declare const FormatPainterWrapper: {
    (props: FormatPainterWrapperProps): React.ReactNode;
    displayName: string;
};
export default FormatPainterWrapper;
