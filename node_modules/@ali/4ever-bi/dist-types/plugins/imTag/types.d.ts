export interface IMTagPluginConfig {
    locale?: {
        imFrom: string;
    };
    jumpToConversition?: (cid: string) => void;
    getConversitionNameFromCid?: (cid: string) => Promise<{
        success: boolean;
        imTitle: string;
    }>;
}
