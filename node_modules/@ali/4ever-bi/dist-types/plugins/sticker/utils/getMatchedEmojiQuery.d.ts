interface QueryInfo {
    query: string;
    offsetWithDelimiter: {
        start: number;
        end: number;
    };
}
export default function getMatchedEmojiQuery(text: string, shouldTrailDelimiter: boolean): QueryInfo | null;
export {};
