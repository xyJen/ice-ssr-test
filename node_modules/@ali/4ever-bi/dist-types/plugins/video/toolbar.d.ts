import VideoButton from './components/videoButton';
export default function toolbar(config: any): {
    buttons: {
        default: typeof VideoButton;
    };
    locale: any;
    getTarget: (controller: any) => "embed" | undefined;
};
