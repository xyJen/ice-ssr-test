import { Controller } from '@ali/4ever-cangjie';
import { QuickPreviewConfigs } from '../types';
export default function createRenderContent(configs?: QuickPreviewConfigs): (props: any, controller: Controller, next: any) => JSX.Element;
