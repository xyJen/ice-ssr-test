import { Action, Controller } from '@ali/4ever-cangjie';
import { QuickPreviewConfigs } from '../types';
export default function createOnAction(configs?: QuickPreviewConfigs): (action: Action, controller: Controller, next: any) => any;
