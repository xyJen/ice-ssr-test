import { Controller, RenderContentProps } from '@ali/4ever-cangjie';
import { BiPluginConfig } from '../../types';
export default function createRenderContent(config: BiPluginConfig): (props: RenderContentProps, controller: Controller, next: any) => JSX.Element;
