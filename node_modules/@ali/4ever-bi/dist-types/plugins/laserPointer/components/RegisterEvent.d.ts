import { Controller, Node } from '@ali/4ever-cangjie';
import { LaserPointerRole } from '../types';
export interface Props {
    getRole: () => LaserPointerRole;
    controller: Controller;
    getHighlightType: (node: Node, controller: Controller) => string | null;
}
export default function RegisterEvent({ controller, getRole, getHighlightType }: Props): null;
