import { Controller, RenderEditableProps } from '@ali/4ever-cangjie';
import { LaserPointerPluginConfig } from './types';
export default function createRenderEditable(config?: LaserPointerPluginConfig): (props: RenderEditableProps, controller: Controller, next: any) => any;
