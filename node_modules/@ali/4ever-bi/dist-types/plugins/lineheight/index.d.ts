import { Query } from '@ali/4ever-cangjie';
import { BiPlugin } from '../../types';
interface ILineHeightProps extends BiPlugin {
    toolbar: any;
    menubar: any;
}
declare function createLineHeightPlugin(config: any): ILineHeightProps;
declare namespace createLineHeightPlugin {
    var actions: typeof import("./actions");
    var queries: Record<string, Query>;
}
export default createLineHeightPlugin;
