import { Controller, Block } from '@ali/4ever-cangjie';
import { CalendarCardMetadataType, CalendarCardDataType, CalendarCardPluginConfig, RefreshReponse, CalendarInjectionType, CalendarType } from '../types';
interface IProps {
    isInitialized?: boolean;
    isPlaceholder?: boolean;
    containerWidthInPC?: number;
    isLoading: boolean;
    isFirstLoaded?: boolean;
    isMobile?: boolean;
    calendarId: string;
    setLoading: (isLoading: boolean) => void;
    refresh: CalendarCardPluginConfig['refresh'];
    controller: Controller;
    node: Block;
    finalMetadata: CalendarCardMetadataType;
    finalData: CalendarCardDataType;
    injectToData: (mergeData: CalendarInjectionType) => void;
    setRefreshResult: (res: RefreshReponse) => void;
    onInitData?: () => void;
    mode?: CalendarType;
}
export default function useInit(props: IProps): void;
export {};
