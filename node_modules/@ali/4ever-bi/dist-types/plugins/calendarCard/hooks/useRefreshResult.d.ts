import { RefreshReponse } from '../types';
declare const useRefreshResult: () => {
    refreshTipsNode: JSX.Element | null;
    setRefreshResult: (res: RefreshReponse) => void;
};
export default useRefreshResult;
