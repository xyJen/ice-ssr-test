import { CalendarCardMetadataType, IReceiver } from '../types';
/**
 * 计算应该返回的Receivers数量，标签以中文为准
 * 规则：只取前两行，其中最后一行显示为 等{count}人参会
 * @param receivers 参会人
 * @param organizer 组织者
 * @param containerWidth 容器宽度
 */
export declare function getCalculatedCalendarReceivers(metadata: CalendarCardMetadataType, containerWidth: number, totalCount: number): IReceiver[];
export declare function calculatedCalendarSize(metadata: CalendarCardMetadataType, containerWidth: number): Promise<number>;
