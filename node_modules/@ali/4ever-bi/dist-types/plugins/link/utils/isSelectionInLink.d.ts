import { Value } from '@ali/4ever-cangjie';
import { LinkNode } from '../types';
export default function isSelectionInLink(value: Value): boolean;
export declare function tryGetSelectedLinkPlaceholder(value: Value, placeholder: string): LinkNode | null;
