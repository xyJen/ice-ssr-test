import { Node, Document } from '@ali/4ever-cangjie';
/**
 * 这里是为了获取默认的文本中默认的 font-size, 用于支持 placeholder 中 hover 背景跟随尺寸变化的 case
 * @param document
 * @param node
 */
export default function getLinkDefaultSz(document: Document, node: Node): number;
