import { Injection, Node } from '@ali/4ever-cangjie';
import { CardState } from '../types';
/**
 * 在编辑形态下，如果 title、desc、imgURL 都为 undefined，则说明请求失败，需要显示 error
 * 如果存在 injection 数据，说明是 loading 状态
 * @param readonly
 * @param title
 * @param desc
 * @param imgURL
 */
export declare const getLinkCardState: (readonly: boolean, node: Node, injections?: Injection[]) => CardState;
