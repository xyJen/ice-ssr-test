import { Controller, Node } from '@ali/4ever-cangjie';
export default function updatePureLink(controller: Controller, node: Node, offset: number, text: string, href: string, srcHref: string): Controller;
