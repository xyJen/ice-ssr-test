import { Controller, Mo as Serializer } from '@ali/4ever-cangjie';
import { BlockLinkCard } from '../../types';
export default function copyLink(controller: Controller, node: BlockLinkCard, serializer: Serializer): Controller;
