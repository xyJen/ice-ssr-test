import { Controller, Node } from '@ali/4ever-cangjie';
import { ILinkCardInfo, LinkPluginConfig } from '../../types';
export default function convertPureLinkToBlockLinkCard(controller: Controller, node: Node, data: {
    offset: number;
    text: string;
    href: string;
    srcHref: string;
    cardInfo: ILinkCardInfo;
    needFetchLinkInfo?: boolean;
    getLinkInfo?: LinkPluginConfig['getLinkInfo'];
}): Controller;
