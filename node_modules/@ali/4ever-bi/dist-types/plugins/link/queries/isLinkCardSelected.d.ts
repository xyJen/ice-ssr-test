import { Controller, Inline } from '@ali/4ever-cangjie';
export interface IsLinkCardSelectedProps {
    node: Inline;
}
export default function isLinkCardSelected(controller: Controller, { node }: IsLinkCardSelectedProps): boolean;
