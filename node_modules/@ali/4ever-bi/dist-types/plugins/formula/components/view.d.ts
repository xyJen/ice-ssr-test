import '../katex.css';
interface Props {
    formula: string;
    isEditing: boolean;
    isSelected?: boolean;
    readOnly?: boolean;
}
declare const _default: (props: Props) => JSX.Element;
export default _default;
