import { Controller, Block } from '@ali/4ever-cangjie';
/**
 * block 为高亮块中最后一个段落
 * 高亮块整体选中时，会选中高亮块最后一个段落后方，并视觉上隐藏光标
 * 同时将 controller.value.data.selectedCallout 置为 true
 */
export declare function setCalloutSelected(controller: Controller, block: Block): Controller;
