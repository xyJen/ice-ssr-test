import { Controller } from '@ali/4ever-cangjie';
import { BiPluginConfig } from '../../types';
export default function createOnKeydown(config: BiPluginConfig): (event: React.KeyboardEvent, controller: Controller, next: Function) => any;
