import { Block, Controller, Document, Value } from '@ali/4ever-cangjie';
/**
 * 判断节点是否禁止 newLineGuide
 * @param node
 * @param document
 * @returns
 */
export declare function isDisableNewLineGuide(node: Block, document: Document): boolean;
/**
 * 查找上一个可插入议题的坐标
 * @param value
 * @param from 查询开始节点，若不指定，则默认为光标 focus 节点
 * @returns
 */
export declare function findPrevSubjectIndex(value: Value, from?: Block): number;
/**
 * 查找下一个可插入议题的坐标
 * @param value 文档数据
 * @param from 查询开始节点，若不指定，则默认为光标 focus 节点
 * @returns
 */
export declare function findNextSubjectIndex(value: Value, from?: Block): number;
/**
 * 获取议题颜色 (Designed By @狗毛)
 * @param name 议题名称
 * @returns
 */
export declare function getSubjectBorderColor(name: string): string;
/**
 * 是否有议题被选中
 * @param controller
 * @returns
 */
export declare function hasSubjectBeenSelected(controller: Controller): boolean;
