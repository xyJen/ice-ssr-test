import { EmbedCommonPluginConfig } from '@ali/4ever-factory';
/**
 * @packageDocumentation
 * @module Plugin-Embed
 */
export declare type EmbedPluginConfig = EmbedCommonPluginConfig & {
    /**
     * 文档预览 URL 转换。如果是文档类型，这里可以进行鉴权等。这里允许异步返回结果。
     */
    transformDocURL?: (url: string, file?: {
        name: string;
        type: string;
        size: string;
    }) => Promise<{
        previewUrl: string;
    }>;
    /**
     * 支持预览的附件类型（word, excel, ppt, pdf等，上传成功后，是否自动切换到预览态，默认为 true
     */
    autoPreview?: boolean;
};
