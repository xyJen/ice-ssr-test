import * as React from 'react';
import { Value } from '@ali/4ever-cangjie';
import { InsertGroup } from '../types';
interface MenuProps {
    groups: InsertGroup[];
    value: string;
    onChange: (plugin: string) => void;
    editorValue: Value;
}
declare const Menu: React.FunctionComponent<MenuProps>;
export default Menu;
