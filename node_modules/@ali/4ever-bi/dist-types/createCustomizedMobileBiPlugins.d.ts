/**
 * @packageDocumentation
 * @module Editor(Bi)
 */
import { Plugin } from '@ali/4ever-cangjie';
import { BiPluginConfig, PluginFactory } from './types';
export interface BiPlugins {
    array: Plugin[];
    hash: Record<string, Plugin>;
    lang?: string | Record<string, any>;
}
export default function createCustomizedBiPlugins(plugins: Record<string, PluginFactory>): (configs?: BiPluginConfig) => BiPlugins;
