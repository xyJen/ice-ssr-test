import _extends from "@babel/runtime/helpers/extends";
import _inheritsLoose from "@babel/runtime/helpers/inheritsLoose";
import { Inline } from '@ali/4ever-cangjie';
import { ELEMENT_TYPE } from "../../utils/meta";

var Image = /*#__PURE__*/function (_Inline) {
  _inheritsLoose(Image, _Inline);

  function Image() {
    return _Inline.apply(this, arguments) || this;
  }

  Image.create = function create(properties) {
    if (typeof properties === 'string') {
      throw new Error('`Image.create` does not accept string');
    }

    if (Image.isImage(properties)) {
      return properties;
    }

    return _Inline.create.call(this, _extends({}, properties, {
      type: Image.TYPE
    }));
  };

  Image.isImage = function isImage(maybeImage) {
    return Inline.isInline(maybeImage) && maybeImage.type === Image.TYPE;
  };

  return Image;
}(Inline);

Image.TYPE = ELEMENT_TYPE;
export { Image as default };
//# sourceMappingURL=index.js.map