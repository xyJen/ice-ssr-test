import Image from "../models";

var valueToText = function valueToText(locale) {
  return {
    name: 'image',
    match: function match(node) {
      return Image.isImage(node);
    },
    convert: function convert() {
      var title = locale == null ? void 0 : locale.imageTitle;
      return title ? "[" + title + "]" : '';
    }
  };
};

export default valueToText;
//# sourceMappingURL=valueToText.js.map