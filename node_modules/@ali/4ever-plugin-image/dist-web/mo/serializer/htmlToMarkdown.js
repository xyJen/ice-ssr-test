export default {
  name: 'image',
  filter: function filter(node) {
    return node.tagName === 'IMG';
  },
  replacement: function replacement(content, node) {
    var _map = ['name', 'src'].map(function (attr) {
      return node == null ? void 0 : node.getAttribute(attr);
    }),
        name = _map[0],
        src = _map[1];

    return "![" + (name || 'image') + "](" + (src || '') + ")";
  }
};
//# sourceMappingURL=htmlToMarkdown.js.map