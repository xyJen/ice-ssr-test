import React from 'react';
import Image from "../../mo/models";
export default function useFocusImage(controller) {
  var value = controller.value;
  var selection = value.selection,
      document = value.document;
  var node = React.useMemo(function () {
    var isCollapsed = selection.isCollapsed,
        focus = selection.focus;

    if (isCollapsed) {
      var _node = document.getClosestVoid(focus.key, controller);

      return Image.isImage(_node) ? _node : null;
    }

    return null;
  }, [controller, document, selection]);
  return node;
}
//# sourceMappingURL=useFocusImage.js.map