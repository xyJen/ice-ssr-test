import isValidNumber from "./isValidNumber";
export var isComplexImg = function isComplexImg(data) {
  var width = data.width,
      height = data.height,
      rotation = data.rotation,
      _data$rectClip = data.rectClip,
      rectClip = _data$rectClip === void 0 ? {} : _data$rectClip;
  var _rectClip$left = rectClip.left,
      left = _rectClip$left === void 0 ? 0 : _rectClip$left,
      _rectClip$right = rectClip.right,
      right = _rectClip$right === void 0 ? 0 : _rectClip$right,
      _rectClip$bottom = rectClip.bottom,
      bottom = _rectClip$bottom === void 0 ? 0 : _rectClip$bottom,
      _rectClip$top = rectClip.top,
      top = _rectClip$top === void 0 ? 0 : _rectClip$top;
  var isClip = left || right || bottom || top;
  var isRotated = isValidNumber(rotation) && rotation !== 0;
  return typeof width === 'number' && typeof height === 'number' && (isRotated || isClip);
};
export function isValidSize(width, height) {
  return isValidNumber(width) && isValidNumber(height) && width !== 0 && height !== 0;
}
//# sourceMappingURL=aslService.js.map