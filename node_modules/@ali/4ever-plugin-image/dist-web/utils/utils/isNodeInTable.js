import { Block } from '@ali/4ever-cangjie';

var isTableCell = function isTableCell(tc) {
  return Block.isBlock(tc) && tc.type === 'table-cell';
};

export function isNodeInTable(node, document) {
  if (!node) return false;
  return !!document.getClosest(node.key, isTableCell);
}
//# sourceMappingURL=isNodeInTable.js.map