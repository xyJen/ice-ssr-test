export default function isParentClickable(controller, node) {
  var document = controller.value.document;
  var parent = document.getParent(node.key);
  return (parent == null ? void 0 : parent.isElement()) && parent.type === 'link';
}
//# sourceMappingURL=isParentClickable.js.map