import { Block } from '@ali/4ever-cangjie';

var isTaskNode = function isTaskNode(node) {
  var _node$data, _node$data$list;

  return Block.isBlock(node) && node.type === 'paragraph' && (node == null ? void 0 : (_node$data = node.data) == null ? void 0 : (_node$data$list = _node$data.list) == null ? void 0 : _node$data$list.isTaskList);
};

export function isNodeInTask(node, document) {
  if (!node) return false;
  return !!document.getClosest(node.key, isTaskNode);
}
//# sourceMappingURL=isNodeInTask.js.map