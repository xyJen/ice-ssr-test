export default function getReloadSrc(src) {
  try {
    var url = new URL(src);
    var searchParams = url.searchParams;
    searchParams.set('t', Math.random().toString());
    url.search = searchParams.toString();
    return url.toString();
  } catch (e) {
    return src;
  }
}
//# sourceMappingURL=getReloadSrc.js.map