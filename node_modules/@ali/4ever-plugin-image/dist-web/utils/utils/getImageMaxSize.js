import { isValidSize } from "./aslService";
export var getImageMaxSize = function getImageMaxSize(width, height, maxWidth) {
  if (isValidSize(width, height) && width > maxWidth) {
    return {
      width: maxWidth,
      height: maxWidth / width * height
    };
  }

  return {
    width: width,
    height: height
  };
};
//# sourceMappingURL=getImageMaxSize.js.map