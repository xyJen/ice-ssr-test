import _extends from "@babel/runtime/helpers/extends";
export var getZoomImageData = function getZoomImageData(data, zoom) {
  var _ref = data,
      width = _ref.width,
      height = _ref.height,
      rectClip = _ref.rectClip;

  var zoomRectClip = _extends({}, rectClip);

  if (typeof zoomRectClip === 'object' && zoomRectClip) {
    var top = zoomRectClip.top,
        right = zoomRectClip.right,
        bottom = zoomRectClip.bottom,
        left = zoomRectClip.left;
    zoomRectClip.top = top ? top * zoom : top;
    zoomRectClip.right = right ? right * zoom : right;
    zoomRectClip.bottom = bottom ? bottom * zoom : bottom;
    zoomRectClip.left = left ? left * zoom : left;
  }

  return _extends({}, data, {
    width: width * zoom,
    height: height * zoom,
    rectClip: zoomRectClip
  });
};
//# sourceMappingURL=getZoomImageData.js.map