export default (function (outline) {
  if (!(outline && outline.width) || outline && outline.type === 'none') {
    return undefined;
  }

  var width = Math.round(outline.width);
  var type = outline.type;
  var color = outline.color || '#000000';

  if (type !== 'dotted' && type !== 'dashed') {
    type = 'solid';
  }

  return width + "px " + type + " " + color;
});
//# sourceMappingURL=getBorder.js.map