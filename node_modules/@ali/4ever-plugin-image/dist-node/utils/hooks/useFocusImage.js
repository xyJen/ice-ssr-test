"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = useFocusImage;

var _react = _interopRequireDefault(require("react"));

var _models = _interopRequireDefault(require("../../mo/models"));

function useFocusImage(controller) {
  const {
    value
  } = controller;
  const {
    selection,
    document
  } = value;

  const node = _react.default.useMemo(() => {
    const {
      isCollapsed,
      focus
    } = selection;

    if (isCollapsed) {
      const node = document.getClosestVoid(focus.key, controller);
      return _models.default.isImage(node) ? node : null;
    }

    return null;
  }, [controller, document, selection]);

  return node;
}
//# sourceMappingURL=useFocusImage.js.map