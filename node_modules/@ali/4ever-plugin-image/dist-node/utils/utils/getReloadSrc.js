"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getReloadSrc;

function getReloadSrc(src) {
  try {
    const url = new URL(src);
    const {
      searchParams
    } = url;
    searchParams.set('t', Math.random().toString());
    url.search = searchParams.toString();
    return url.toString();
  } catch (e) {
    return src;
  }
}
//# sourceMappingURL=getReloadSrc.js.map