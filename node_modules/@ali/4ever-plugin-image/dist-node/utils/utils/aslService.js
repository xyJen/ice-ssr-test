"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isValidSize = isValidSize;
exports.isComplexImg = void 0;

var _isValidNumber = _interopRequireDefault(require("./isValidNumber"));

const isComplexImg = data => {
  const {
    width,
    height,
    rotation,
    rectClip = {}
  } = data;
  const {
    left = 0,
    right = 0,
    bottom = 0,
    top = 0
  } = rectClip;
  const isClip = left || right || bottom || top;
  const isRotated = (0, _isValidNumber.default)(rotation) && rotation !== 0;
  return typeof width === 'number' && typeof height === 'number' && (isRotated || isClip);
};

exports.isComplexImg = isComplexImg;

function isValidSize(width, height) {
  return (0, _isValidNumber.default)(width) && (0, _isValidNumber.default)(height) && width !== 0 && height !== 0;
}
//# sourceMappingURL=aslService.js.map