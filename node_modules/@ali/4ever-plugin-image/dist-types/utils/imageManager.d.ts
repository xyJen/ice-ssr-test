import type { Controller, Inline } from '@ali/4ever-cangjie';
import type { ImagePluginConfig } from './types';
import type { ImageData } from '../mo/models';
export default class ImageManager {
    private imageTransformMap;
    private readonly transformImageURL;
    private readonly controller;
    constructor(transformImageURL: ImagePluginConfig['transformImageURL'], controller?: Controller | null);
    updateImageData: (image: Inline, data: ImageData) => void;
    register: (src: string, image: Inline) => Promise<any>;
    getTransformedURL(src: string): string | undefined;
    isImgSrcRegistered(src: string | undefined): boolean;
}
