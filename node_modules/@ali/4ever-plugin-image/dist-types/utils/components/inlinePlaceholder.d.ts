import React, { PropsWithChildren } from 'react';
interface IProps {
    onClick?: (e: React.MouseEvent) => void;
}
export default function InlinePlaceholder({ children, onClick }: PropsWithChildren<IProps>): JSX.Element;
export {};
