import { environment } from '@ali/4ever-cangjie';
import { Highlight } from '@ali/4ever-plugin-marks';
import { OldBambooPlugin } from '@ali/4ever-plugin-list';
export function getHighlight(controller) {
  var symbolBgColor = OldBambooPlugin.getSymbolStyleByKey(controller, 'shd');
  if (symbolBgColor) return symbolBgColor;
  var highlightMark = controller.query('getActiveMark', Highlight.TYPE);
  var highlight = highlightMark && highlightMark.data.value; // 为空字符串, 则无颜色

  if (highlight === '') {
    // 安卓需要使用 transparent 代表无颜色，且安卓设置无颜色实际会写入 transparent
    // https://aone.alibaba-inc.com/v2/project/897543/bug/39048505
    return environment.IS_ANDROID ? 'transparent' : '';
  }

  return highlight;
}
//# sourceMappingURL=getHighlight.js.map