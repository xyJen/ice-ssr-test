import { isHighlight } from "../actions";
import { setHighlight } from "../commands/setHighlightMark";
export default function onAction(action, controller, next) {
  if (isHighlight(action)) {
    var highlight = action.payload.highlight;
    setHighlight(controller, highlight);
  }

  return next();
}
//# sourceMappingURL=onAction.js.map