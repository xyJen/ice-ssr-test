"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isSelectionInFoldArea = isSelectionInFoldArea;

/**
 * 判断选区是否在折叠的区域内
 * @param controller
 * @param node 折叠标题节点
 * @returns
 */
function isSelectionInFoldArea(controller, node) {
  const {
    document,
    selection
  } = controller.value;
  const end = selection.getEnd(document);
  const endBlock = document.getFurthsestBlock(end.key);

  if (endBlock === node) {
    return false;
  }

  return true;
}
//# sourceMappingURL=helpers.js.map