import { isFoldInGroup, isUnfoldInGroup } from '@ali/4ever-plugin-heading-group';

function createShouldComponentUpdate() {
  return function shouldNodeComponentUpdate(prevProps, nextProps, controller, next) {
    var prevNode = prevProps.node,
        prevDocument = prevProps.document;
    var nextNode = nextProps.node,
        nextDocument = nextProps.document;

    if (isFoldInGroup(prevDocument, prevNode, controller) && isUnfoldInGroup(nextDocument, nextNode, controller)) {
      return true;
    }

    return next();
  };
}

export default createShouldComponentUpdate;
//# sourceMappingURL=createShouldComponentUpdate.js.map