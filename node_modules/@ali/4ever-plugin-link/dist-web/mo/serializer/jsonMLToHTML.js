import * as React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { getTagName, getAttributes, getChildren, sanitizeUrl } from '@ali/4ever-utils';
export default {
  name: 'a',
  match: function match(node) {
    return getTagName(node) === 'a';
  },
  convert: function convert(node, path, _convert) {
    // @ts-ignore
    var _getAttributes = getAttributes(node),
        href = _getAttributes.href;

    var children = getChildren(node).map(function (child, index) {
      return _convert(child, [].concat(path, [index]));
    });
    var validUrl = sanitizeUrl(href);
    return /*#__PURE__*/_createElement("a", {
      href: validUrl,
      "data-link-href-cangjie": validUrl,
      target: "_blank",
      rel: "noopener noreferrer",
      key: path.toString()
    }, children);
  }
};
//# sourceMappingURL=jsonMLToHTML.js.map