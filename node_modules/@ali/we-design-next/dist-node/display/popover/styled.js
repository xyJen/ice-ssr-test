"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.S = exports.ContentWrap = void 0;
var _styledComponents = _interopRequireWildcard(require("styled-components"));
function _getRequireWildcardCache(nodeInterop) { if (typeof WeakMap !== "function") return null; var cacheBabelInterop = new WeakMap(); var cacheNodeInterop = new WeakMap(); return (_getRequireWildcardCache = function (nodeInterop) { return nodeInterop ? cacheNodeInterop : cacheBabelInterop; })(nodeInterop); }
function _interopRequireWildcard(obj, nodeInterop) { if (!nodeInterop && obj && obj.__esModule) { return obj; } if (obj === null || typeof obj !== "object" && typeof obj !== "function") { return { default: obj }; } var cache = _getRequireWildcardCache(nodeInterop); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }
const KEYFRAMES = (name, x, y) => `
  @keyframes wds-popover-animation-${name} {
    0% {
      transform: scale(0.8);
      opacity: 0;
      transform-origin: ${x}% ${y}%;
    }
    100% {
      transform: scale(1);
      opacity: 1;
      transform-origin: ${x}% ${y}%;
    }
  }
`;
const ANIMATION = name => `
  &.wds-popover-animation-${name} {
    animation: wds-popover-animation-${name} 100ms cubic-bezier(0.39, 0.575, 0.565, 1) forwards;
    animation-iteration-count: 1;
  }
`;
const CSS_KEYFRAMES = `
  ${KEYFRAMES('topLeft', 0, 100)}
  ${KEYFRAMES('top', 50, 100)}
  ${KEYFRAMES('topRight', 100, 100)}
  ${KEYFRAMES('rightTop', 0, 0)}
  ${KEYFRAMES('right', 0, 50)}
  ${KEYFRAMES('rightBottom', 0, 100)}
  ${KEYFRAMES('bottomRight', 100, 0)}
  ${KEYFRAMES('bottom', 50, 0)}
  ${KEYFRAMES('bottomLeft', 0, 0)}
  ${KEYFRAMES('leftBottom', 100, 100)}
  ${KEYFRAMES('left', 100, 50)}
  ${KEYFRAMES('leftTop', 100, 0)}
`;
const CSS_ANIMATIONS = `
  ${ANIMATION('topLeft')}
  ${ANIMATION('top')}
  ${ANIMATION('topRight')}
  ${ANIMATION('rightTop')}
  ${ANIMATION('right')}
  ${ANIMATION('rightBottom')}
  ${ANIMATION('bottomRight')}
  ${ANIMATION('bottom')}
  ${ANIMATION('bottomLeft')}
  ${ANIMATION('leftBottom')}
  ${ANIMATION('left')}
  ${ANIMATION('leftTop')}
`;
const ContentWrap = /*#__PURE__*/_styledComponents.default.div(["position:absolute;left:-9999px;top:-9999px;z-index:999;", " ", " box-shadow:0px 0px 1px rgba(0,0,0,0.24),0px 8px 24px rgba(0,0,0,0.16);background-color:rgba(255,255,255,1);border-radius:4px;width:max-content;", " cursor:default;"], CSS_ANIMATIONS, CSS_KEYFRAMES, ({
  hidden
}) => hidden ? `display: none;` : null);
exports.ContentWrap = ContentWrap;
const S = /*#__PURE__*/(0, _styledComponents.createGlobalStyle)(["", " ", " ", " ", " ", " ", ""], KEYFRAMES('topLeft', 0, 100), KEYFRAMES('top', 50, 100), KEYFRAMES('topRight', 100, 100), KEYFRAMES('rightTop', 0, 0), KEYFRAMES('right', 0, 50), KEYFRAMES('rightBottom', 0, 100));
exports.S = S;
//# sourceMappingURL=styled.js.map