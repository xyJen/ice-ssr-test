"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _utils = require("../utils/utils");
var _domUtils = require("../utils/dom-utils");
/**
 * 获取元素的 { width , height , 距离文档顶部的 top, left 等信息}
 */
function getRegion(node) {
  let offset;
  let w;
  let h;
  if (!_utils.utils.isWindow(node) && !_utils.utils.isDocument(node)) {
    // 非window、Document Node
    offset = _utils.utils.offset(node);
    w = _domUtils.domUtils.outerWidth(node);
    h = _domUtils.domUtils.outerHeight(node);
  } else {
    const win = _utils.utils.getWindow(node);
    offset = {
      left: _utils.utils.getWindowScrollLeft(win),
      top: _utils.utils.getWindowScrollTop(win)
    };
    w = _domUtils.domUtils.viewportWidth(win);
    h = _domUtils.domUtils.viewportHeight(win);
  }
  return {
    ...offset,
    width: w,
    height: h
  };
}
var _default = getRegion;
exports.default = _default;
//# sourceMappingURL=getRegion.js.map