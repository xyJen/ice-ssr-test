import React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import SketchPicker from "./sketch/index.js";
var toString = function toString(color) {
  if (typeof color.rgb.a === 'number' && color.rgb.a < 1) {
    var _color$rgb = color.rgb,
      r = _color$rgb.r,
      g = _color$rgb.g,
      b = _color$rgb.b,
      a = _color$rgb.a;
    return "rgba(" + r + ", " + g + ", " + b + ", " + a + ")";
  } else {
    return color.hex;
  }
};
export var ColorPickerSketch = /*#__PURE__*/React.memo(function (props) {
  var color = props.color,
    onChange = props.onChange,
    onChangeComplete = props.onChangeComplete;
  return _createElement(SketchPicker, {
    color: color,
    onChange: onChange ? function (e) {
      return onChange(toString(e));
    } : undefined,
    onChangeComplete: onChangeComplete ? function (e) {
      return onChangeComplete(toString(e));
    } : undefined
  });
});
//# sourceMappingURL=ColorPickerSketch.js.map