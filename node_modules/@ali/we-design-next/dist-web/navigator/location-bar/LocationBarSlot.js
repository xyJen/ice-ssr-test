export var Priority = {
  min: function min() {
    return 1;
  },
  max: function max() {
    return 5;
  },
  isMin: function isMin(p) {
    return p === Priority.min();
  },
  isMax: function isMax(p) {
    return p === Priority.max();
  },
  next: function next(p) {
    return Priority.isMax(p) ? p : p + 1;
  },
  prev: function prev(p) {
    return Priority.isMin(p) ? p : p - 1;
  }
};
export var LocationBarSlot = function LocationBarSlot() {
  return null;
};
//# sourceMappingURL=LocationBarSlot.js.map