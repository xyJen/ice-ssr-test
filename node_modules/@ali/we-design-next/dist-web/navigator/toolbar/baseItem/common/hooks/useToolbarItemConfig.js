import { useContext } from 'react';
import { ToolbarContext, DoubleLineRowContext } from "../../../layout/contexts";
export var useToolbarItemConfig = function useToolbarItemConfig() {
  var _useContext = useContext(ToolbarContext),
    mode = _useContext.mode;
  var inDbRow = useContext(DoubleLineRowContext);
  var itemSize;
  if (mode === 'singleLine') {
    itemSize = 'normal';
  } else if (inDbRow) {
    itemSize = 'small';
  } else {
    itemSize = 'large';
  }
  return {
    itemSize: itemSize
  };
};
//# sourceMappingURL=useToolbarItemConfig.js.map