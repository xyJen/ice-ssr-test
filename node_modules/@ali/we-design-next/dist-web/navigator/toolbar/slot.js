import React, { useContext } from 'react';
import { ToolbarContext } from "./layout/contexts";
var fixedItem = new Set(['iconMenu', 'iconButton']);
export var Slot = function Slot(props) {
  var width = props.width;
  var _useContext = useContext(ToolbarContext),
    mode = _useContext.mode,
    toolbarWidth = _useContext.toolbarWidth;
  if (fixedItem.has(props.display)) {
    return props.children;
  }
  var renderWidth = typeof width === 'function' ? width({
    mode: mode,
    toolbarWidth: toolbarWidth || 0
  }) : width;
  return renderWidth ? /*#__PURE__*/React.cloneElement(props.children, {
    width: renderWidth
  }) : props.children;
};
Slot.displayName = "toolbarSlot";
//# sourceMappingURL=slot.js.map