import _extends from "@babel/runtime/helpers/extends";
import { mergeProps } from "./mergeProps";
export var mergeRestProps = function mergeRestProps(restProps, className, style, events) {
  return mergeProps(restProps, _extends({
    className: className,
    style: style
  }, events));
};
export var proxyRestProps = function proxyRestProps(restProps, to) {
  var res = {};
  if (typeof restProps['data-testid'] === 'string') {
    res['data-testid'] = restProps['data-testid'] + '__' + to;
  }
  return res;
};
export var spreadRestProps = function spreadRestProps(restProps, key) {
  var res = {};
  if (typeof restProps['data-testid'] === 'string') {
    res['data-testid'] = restProps['data-testid'] + '_' + key;
  }
  return res;
};
//# sourceMappingURL=mergeRestProps.js.map