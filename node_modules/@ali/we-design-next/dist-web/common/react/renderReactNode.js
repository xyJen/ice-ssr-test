import { mergeReactNode } from "./mergeReactNode";
export var renderReactNode = function renderReactNode(render, sourceNode, source, depth) {
  if (depth === void 0) {
    depth = 2;
  }
  if (typeof render === 'function') {
    var res = render(source);
    if (typeof res === 'function') {
      return res(sourceNode);
    } else {
      return mergeReactNode(res, sourceNode, depth);
    }
  } else {
    return mergeReactNode(render, sourceNode, depth);
  }
};
//# sourceMappingURL=renderReactNode.js.map