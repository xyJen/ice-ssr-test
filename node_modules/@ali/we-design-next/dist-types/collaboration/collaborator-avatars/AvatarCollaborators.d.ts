import React from 'react';
import { MergePropsType } from '../../common';
import { IAvatarCollaborator } from './AvatarWithPopover';
export declare type IAvatarCollaboratorsProps = MergePropsType<{
    /**
     * 显示区最多显示多少个,  默认为 8
     */
    max?: number;
    /**
     *  点击头像组中头像（或 dropdown 列表中选项）的回调函数
     *  idx 为头像组中点击的头像的序号 (0-based)
     */
    onClick?: (key: string) => void;
    /**
     * 用户列表
     */
    collaborators?: IAvatarCollaborator[];
    /**
     * 协作者头像面板中的菜单
     */
    renderCollaboratorMenu?: (c: IAvatarCollaborator) => React.ReactElement;
    /**
     * 更多用户按钮出现时，其是否高亮
     */
    highlight?: boolean;
    /**
     * 菜单顶部
     */
    renderMenu?: () => React.ReactNode;
}, React.HTMLAttributes<HTMLDivElement>>;
export declare const AvatarCollaborators: React.FC<IAvatarCollaboratorsProps>;
