/**
 * 20200126 引入 uuid，保证多实例 hook，避免多个hook同时对dom操作造成的判断失效
 * 保证每次调用能拿到同一个值
 */
export declare class FontDetector {
    private css;
    private timer;
    private readonly uuid;
    private readonly styleUuid;
    private readonly iframeUuid;
    private detection;
    fonts: string[];
    constructor(fonts: string[]);
    private inject;
    private remove;
    /**
     * 增加500ms的delay避免hooks快速触发导致的回掉失效
     * @param callback
     */
    private ready;
    destroy(): void;
    private getResult;
    detect: () => Promise<boolean[]>;
}
