import React, { DOMAttributes } from 'react';
import { IRestProps } from '../../common/react/mergeRestProps';
import { IAlign } from './styled';
export interface INavBarItemProps extends IRestProps, DOMAttributes<HTMLDivElement> {
    /**
     * 子节点
     */
    children?: React.ReactNode;
    /**
     * 对齐方式
     */
    align: IAlign;
}
/**
 * 导航栏元素，控制元素大小
 */
export declare const NavigationBarMobileItem: React.MemoExoticComponent<(props: INavBarItemProps) => JSX.Element>;
