import React from 'react';
export interface Route {
    path: string;
    name: string;
}
export interface BreadcrumbLayoutProps {
    routes: Route[];
    maxWidth?: number;
    width?: number;
    renderFilename?: (route: Route, options: {
        maxWidth: number;
    }) => React.ReactNode;
    onClick?: (path: string) => void;
}
export declare const BreadcrumbLayout: React.MemoExoticComponent<(props: BreadcrumbLayoutProps) => JSX.Element>;
