import * as React from 'react';
export interface Route {
    path: string;
    name: string;
    children?: Array<Omit<Route, 'children'>>;
}
export interface BreadcrumbProps {
    routes?: Route[];
    separator?: React.ReactNode;
    renderItem?: (route: Route, routes: Route[], options?: Record<string, any>) => React.ReactNode;
    renderLastItem?: (route: Route, options: {
        maxWidth: number;
    }) => React.ReactNode;
    children?: React.ReactNode;
    getOrCreateRef?: (id: number, ref: React.MutableRefObject<HTMLLIElement | null>) => void;
    onBreadcrumbItemClick?: (path: string) => void;
}
export declare const Breadcrumb: React.FC<BreadcrumbProps>;
