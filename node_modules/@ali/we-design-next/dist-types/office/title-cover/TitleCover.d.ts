import React, { DOMAttributes } from 'react';
import { IRestProps } from '../../common/react/mergeRestProps';
import { Cover } from './components/cover';
export declare type Cover = {
    /**
     * 封面图片地址
     */
    src: string;
    /**
     * 封面缩略图地址
     */
    thumbSrc?: string;
    /**
     * 偏移量（目前仅支持垂直方向）
     * 相对垂直方向位置，参考 CSS 属性 object-position
     * 默认居中（0.5），取值范围 [0, 1]
     */
    transform?: {
        offsetY?: number;
    };
};
declare type IChangeOption = {
    isReposition: boolean;
    position: number;
};
export declare type TitleCoverProps = {
    cover: Cover;
    /**
     * 图片加载失败占位
     */
    renderError?: () => React.ReactNode;
} & IRestProps & DOMAttributes<HTMLDivElement>;
declare type TitleCoverHandlers = {
    onChange: (fn: (option: IChangeOption) => void) => () => void;
    startReposition: () => void;
    endReposition: () => void;
    setRenderPosition: (p: number) => void;
    getImageRef: () => React.RefObject<HTMLImageElement>;
    getCover: () => Cover;
};
export declare const TitleCover: React.MemoExoticComponent<React.ForwardRefExoticComponent<{
    cover: Cover;
    /**
     * 图片加载失败占位
     */
    renderError?: (() => React.ReactNode) | undefined;
} & IRestProps & React.DOMAttributes<HTMLDivElement> & React.RefAttributes<TitleCoverHandlers>>>;
export {};
