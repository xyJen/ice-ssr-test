import type { IFullRegion, IOffsets } from '../interface';
declare function flipOffset(offset: IOffsets, index: number): IOffsets;
declare function normalizeOffset(offset: IOffsets, el: IFullRegion): void;
export { flipOffset, normalizeOffset };
