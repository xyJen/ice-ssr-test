import type { IAlign } from '../interface';
declare type ITargetPoint = {
    pageX: number;
    pageY: number;
} | {
    clientX: number;
    clientY: number;
};
/**
 * `tgtPoint`: { pageX, pageY } or { clientX, clientY }.
 * If client position provided, will internal convert to page position.
 */
declare function alignPoint(el: HTMLElement, tgtPoint: ITargetPoint, align: IAlign): import("../interface").IRect;
export default alignPoint;
