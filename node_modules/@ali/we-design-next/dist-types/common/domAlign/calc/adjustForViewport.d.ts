import type { IPos, IFullRegion, IAlign, IVisibleRect } from '../interface';
declare function adjustForViewport(elFuturePos: IPos, elRegion: IFullRegion, tgtRegion: IFullRegion, visibleRect: IVisibleRect, align: IAlign): IFullRegion;
export default adjustForViewport;
