import React from "react";
import { IRestProps, IRenderReactNode } from "../../common";
declare type RawMode = 'date' | 'datetime';
export declare type IDatePickerProps = {
    /**
     * 日期值
     */
    value?: Date | null;
    /**
     * 默认的日期值
     */
    defaultValue?: Date | null;
    /**
     * 值发生变化
     */
    onChange?: (value: Date | null, options: {
        showTime: boolean;
    }) => void;
    /**
     * 展示面板类型，默认是date; auto时展示时间开关，开关默认关闭
     */
    mode?: RawMode | 'time' | 'auto' | ['auto', {
        default: RawMode;
    }];
    /**
     * 时间面板展示类型，默认HM（只展示时分面板），只在有时间面板时生效
     */
    timeFormat?: 'hm' | 'hms';
    /**
     * 是否可清除，为true显示清除按钮，默认false
     */
    clearable?: boolean;
    /**
     * 是否只有点确定才生效，为true显示确定按钮，默认true
     */
    confirmable?: boolean;
    /**
     * 不可选择的日期
     */
    disabledDate?: (date: Date) => boolean;
    /**
     * 自定义底部
     */
    footer?: IRenderReactNode<void>;
    /**
     * 是否显示今天或现在, 默认true
     */
    showNow?: boolean;
} & IRestProps;
export declare const DatePicker: React.FC<IDatePickerProps>;
export {};
