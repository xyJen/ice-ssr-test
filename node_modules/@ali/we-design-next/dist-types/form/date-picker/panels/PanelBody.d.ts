import React from "react";
import { Dayjs } from "dayjs";
import { PanelMode } from "../date-util";
interface IPanelBodyProps {
    headerCells?: React.ReactNode;
    rowNum: number;
    colNum: number;
    mode: PanelMode;
    viewDate: Dayjs;
    baseDate: Dayjs;
    disabledDate?: (date: Dayjs) => boolean;
    onSelect: (value: Dayjs) => void;
    getCellDate: (date: Dayjs, offset: number) => Dayjs;
    getCellText: (date: Dayjs) => React.ReactNode;
    getCellClassName: (date: Dayjs) => (string | undefined)[];
}
export declare const PanelBody: React.FC<IPanelBodyProps>;
export {};
