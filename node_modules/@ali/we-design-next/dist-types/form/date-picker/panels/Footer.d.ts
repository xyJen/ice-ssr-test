import React, { ReactNode } from "react";
declare type IDatePickerFooter = {
    extra?: ReactNode;
    timeSwitch?: ReactNode;
    clear?: ReactNode;
    confirm?: ReactNode;
};
export declare const DatePickerFooter: React.MemoExoticComponent<(props: IDatePickerFooter) => JSX.Element>;
export {};
