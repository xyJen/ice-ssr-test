import React, { ReactElement, DOMAttributes } from 'react';
import { IRestProps } from '../../common';
export declare type IColorPickerProps = {
    /**
     * 当前选中的颜色
     */
    color?: string | null;
    /**
     * 初始颜色值
     */
    defaultColor?: string;
    /**
     * 颜色选中后的回调函数，intermediate 表示是否处在中间过渡态，即在高级的 Sketch 风格面板中按下鼠标拖拽过程产生的事件
     */
    onColorChange?: (color: string | null, options: {
        intermediate: boolean;
        fromSketch: boolean;
    }) => void;
    /**
     * 色卡
     */
    presets?: Array<{
        key: string;
        title: string;
        colors: Array<Array<string> | ReactElement>;
    }>;
    /**
     * 当前色卡
     */
    currentPreset?: string;
    /**
     * 当前色卡
     */
    onCurrentPresetChange?: (v: string) => void;
    /**
     * 自动颜色，不传显示为「无颜色」
     */
    autoColor?: string;
    /**
     * 最近使用的颜色
     */
    recentColors?: string[];
    /**
     * 显示高级选择器
     */
    advance?: boolean;
    /**
     * 颜色块在悬浮时显示为文本或颜色块
     */
    hoverMode?: 'square' | 'text';
} & IRestProps & DOMAttributes<HTMLDivElement>;
export declare const ColorPicker: React.MemoExoticComponent<(props: IColorPickerProps) => JSX.Element>;
