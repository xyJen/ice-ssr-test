import { ReactElement } from 'react';
import { IRestProps } from '../../common';
/**
 * Item 唯一标识
 */
declare type SelectedVal = string | number;
declare type IGalleryItem<T> = {
    key: SelectedVal;
    value: T;
};
declare type RenderItemHandler<T> = ({ data, index }: {
    data: IGalleryItem<T>;
    index: number;
}) => ReactElement;
export interface GalleryProps<T> {
    /**
     * 需要渲染的列表
     */
    items: Array<IGalleryItem<any>>;
    /**
     * 每行显示数量
     * @default 3
     */
    columnCount?: number;
    /**
     * 渲染单个元素
     */
    RenderItem?: RenderItemHandler<T>;
    /**
     * 受控 value
     */
    value?: SelectedVal;
    /**
     * 初始值
     */
    defaultValue?: SelectedVal;
    /**
     * 选中回调
     */
    onChange?: (value: SelectedVal) => void;
}
export declare const Gallery: <T extends Record<string, any>>(props: IRestProps & GalleryProps<T>) => ReactElement;
export {};
