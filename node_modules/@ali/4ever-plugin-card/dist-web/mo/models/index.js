import _extends from "@babel/runtime/helpers/extends";
import _inheritsLoose from "@babel/runtime/helpers/inheritsLoose";
import { Block } from '@ali/4ever-cangjie';
import { ELEMENT_TYPE } from "../../utils/meta";

var Card = /*#__PURE__*/function (_Block) {
  _inheritsLoose(Card, _Block);

  function Card() {
    return _Block.apply(this, arguments) || this;
  }

  Card.create = function create(properties) {
    if (typeof properties === 'string') {
      throw new Error('`Card.create` does not accept string');
    }

    if (Card.isCard(properties)) {
      return properties;
    }

    return _Block.create.call(this, _extends({}, properties, {
      type: Card.TYPE
    }));
  };

  Card.isCard = function isCard(maybeCard) {
    return Block.isBlock(maybeCard) && maybeCard.type === Card.TYPE;
  };

  return Card;
}(Block);

Card.TYPE = ELEMENT_TYPE;
export { Card as default };
//# sourceMappingURL=index.js.map