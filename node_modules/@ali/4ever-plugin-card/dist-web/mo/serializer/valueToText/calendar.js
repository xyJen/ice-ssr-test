export default function calendar(data) {
  var _ref = data.metadata || {},
      _ref$time = _ref.time,
      time = _ref$time === void 0 ? '' : _ref$time,
      _ref$place = _ref.place,
      place = _ref$place === void 0 ? '' : _ref$place,
      _ref$comment = _ref.comment,
      comment = _ref$comment === void 0 ? '' : _ref$comment,
      _ref$subject = _ref.subject,
      subject = _ref$subject === void 0 ? '' : _ref$subject,
      _ref$topReceivers = _ref.topReceivers,
      topReceivers = _ref$topReceivers === void 0 ? [] : _ref$topReceivers;

  var texts = [subject];
  if (time) texts.push(time);
  if (place) texts.push(place);
  texts.push(topReceivers.map(function (rec) {
    return rec.name || '';
  }).join(' '));
  if (comment) texts.push(comment);
  return texts.join('\n');
}
//# sourceMappingURL=calendar.js.map