import { Block, Controller } from '@ali/4ever-cangjie';
/**
 * 统一插入 Block 逻辑
 *
 * * 插入后内容前无空行
 * * 仅当在段尾(包括空段落)或文末的 void 元素上插入内容后有空行
 *
 * @param {Controller} controller
 * @param {Block} newBlock 待插入的 Block
 * @returns
 */
export default function insertBlockWithoutExtraBlank(controller: Controller, newBlock: Block): Controller;
