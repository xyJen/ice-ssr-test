import { constants } from '@ali/4ever-cangjie';
import { createValidateLastNodeAction } from "../actions";
var Selector = constants.Selector;
export default function onMouseUp(event, controller, next) {
  var target = event.target;
  var editableNode = target instanceof HTMLElement && target.querySelector("[" + Selector.editable + "]");

  if (editableNode) {
    var clientY = event.clientY;

    var _editableNode$getBoun = editableNode.getBoundingClientRect(),
        bottom = _editableNode$getBoun.bottom;

    if (clientY >= bottom) {
      controller.run('onAction', createValidateLastNodeAction());
    }
  }

  return next();
}
//# sourceMappingURL=onMouseUp.js.map