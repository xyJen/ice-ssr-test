import { Commands, Text } from '@ali/4ever-cangjie';
import { Paragraph } from '@ali/4ever-plugin-paragraph';
export default function validateLastParagraph(controller) {
  var document = controller.value.document;
  var lastNode = document.nodes[document.nodes.length - 1];

  if (Paragraph.isParagraph(lastNode)) {
    return controller;
  }

  var newParagraph = Paragraph.create({
    nodes: [Text.create('')]
  });
  return controller.command(Commands.insertNodeByKey, document.key, document.nodes.length, newParagraph).command(Commands.moveToStartOfNode, newParagraph).command(Commands.focus);
}
//# sourceMappingURL=validateLastParagraph.js.map