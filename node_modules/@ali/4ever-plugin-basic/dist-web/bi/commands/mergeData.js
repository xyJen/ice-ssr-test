import _extends from "@babel/runtime/helpers/extends";

function merge(target, source) {
  return Object.keys(source).reduce(function (tgt, key) {
    var _extends2;

    if (key === 'history') {
      return tgt;
    }

    return _extends({}, tgt, (_extends2 = {}, _extends2[key] = source[key], _extends2));
  }, target);
}

export default function mergeData(controller, data) {
  var value = controller.value;
  return controller.setData(merge(value.data, data));
}
//# sourceMappingURL=mergeData.js.map