import { NoopAppender, APIParams, Level } from '@ali/we-logger-main';
export default class DebugAppender extends NoopAppender {
    private _ignoreFunction;
    private _baseInfo;
    private debug;
    constructor(name: string);
    setName(name: string): void;
    setupIgnore(filterFunction: Function): void;
    ignore(action: string, params?: Record<string, any>): boolean;
    setBaseInfo(params?: Record<string, any>): void;
    sum(key: string): void;
    avg(key: string, value?: number): void;
    log(level: Level, category: string, text: string | Error | Record<string, any>): void;
    api(params: APIParams): void;
    private _generateBaseInfoText;
}
