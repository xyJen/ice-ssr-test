/**
 * 上报页面加载性能到魔兔 请求参数定义
 * @apiName biz.resource.reportPerf
 */
export interface IBizResourceReportPerfParams {
    /** URL，不包含query、hash */
    url: string;
    /** DOMContentLoaded耗时，单位ms */
    load_time: number;
    /** js资源加载耗时，单位ms */
    resource_time: number;
    /** 离线包资源状态，enable、disable、prepare、unknown */
    resource_status: string;
}
/**
 * 上报页面加载性能到魔兔 返回结果定义
 * @apiName biz.resource.reportPerf
 */
export interface IBizResourceReportPerfResult {
}
/**
 * 上报页面加载性能到魔兔
 * @apiName biz.resource.reportPerf
 * @supportVersion ios: 6.5.20 android: 6.5.20
 * @author Android 零封 iOS 犹树
 */
export declare function reportPerf$(params: IBizResourceReportPerfParams): Promise<IBizResourceReportPerfResult>;
export default reportPerf$;
