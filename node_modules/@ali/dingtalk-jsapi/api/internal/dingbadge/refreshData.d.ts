/**
 * 钉工牌 刷新（同步）数据 请求参数定义
 * @apiName internal.dingbadge.refreshData
 */
export interface IInternalDingbadgeRefreshDataParams {
    /** 工牌数据json串 */
    data: string;
    /** 是不是更新当前工牌，默认false */
    refreshCurrentBadge?: boolean;
}
/**
 * 钉工牌 刷新（同步）数据 返回结果定义
 * @apiName internal.dingbadge.refreshData
 */
export interface IInternalDingbadgeRefreshDataResult {
}
/**
 * 钉工牌 刷新（同步）数据
 * @apiName internal.dingbadge.refreshData
 * @supportVersion ios: 6.5.5 android: 6.3.35
 * @author Android：峰砺 iOS： 边枫
 */
export declare function refreshData$(params: IInternalDingbadgeRefreshDataParams): Promise<IInternalDingbadgeRefreshDataResult>;
export default refreshData$;
