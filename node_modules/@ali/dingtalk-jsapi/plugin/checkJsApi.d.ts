/**
 * 获取当前容器的所有 JSAPI（仅支持移动端）
 * @returns
 */
export declare const getAllJsApi: () => Promise<string[]>;
/**
 * 检查 JSAPI 是否存在与客户端（仅支持移动端）
 * @param {{ jsApiList: string[]}} params
 * @returns
 */
export declare const checkJsApi: (params: {
    jsApiList: string[];
}) => Promise<{
    [api: string]: boolean;
}>;
