export interface FormulaConfig {
    /**
     * 是否渲染为公式的html
     * prettyHTML为false输出公式输入值
     * prettyHTML为true时，需要引入公式样式文件
     * 参考：https://cdn.jsdelivr.net/npm/katex@0.12.0/dist/katex.min.css
     */
    prettyHTML: boolean;
}
declare module '@ali/4ever-cangjie' {
    interface MoPluginConfig {
        formula?: FormulaConfig;
    }
}
