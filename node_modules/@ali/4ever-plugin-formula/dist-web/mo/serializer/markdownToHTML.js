var rule = {
  uses: [{
    plugin: function plugin() {
      return import('markdown-it-texmath');
    },
    data: [{
      engine: {
        renderToString: function renderToString(text, options) {
          if (options.displayMode) {
            return "<span type=\"formula\" value=\"" + text.trim() + "\" mode=\"block\"></span>";
          } else {
            return "<span type=\"formula\" value=\"" + text.trim() + "\" mode=\"inline\"></span>";
          }
        }
      }
    }]
  }]
};
export default rule;
//# sourceMappingURL=markdownToHTML.js.map