import React from 'react';
import { ButtonSize } from "../button";
export interface IFillColorPickerProps {
    /**
     * SplitButton的默认激活状态
     */
    defaultActive?: boolean;
    /**
     * 按钮激活状态，受控模式
     */
    active?: boolean;
    /**
     * 激活状态 变化时的回调
     */
    onActiveChange?: (active: boolean) => void;
    /**
     * 默认的初始颜色值
     */
    defaultColor?: string;
    /**
     * 当前选中的颜色值
     */
    color?: string;
    /**
     * 默认颜色块的颜色值
     */
    automaticColor: string;
    /**
     * 要显示清楚线的颜色块的颜色值
     */
    clearColor?: string;
    /**
     * 是否显示 colorPicker 组件, 即：选择“更多”颜色的组件
     */
    picker?: boolean;
    /**
     * 颜色选中后的回调函数
     */
    onColorPicker: (color: string) => void;
    /**
     * SplitButton的大小
     */
    size?: ButtonSize;
    /**
     * 颜色选择按钮的组件
     */
    style?: React.CSSProperties;
    /**
     * 本地语言类型，如zh-CN
     */
    locale?: string;
    /**
     * tooltip提示
     */
    title?: string | React.ReactNode;
    /**
     * 是否显示自动
     */
    showAutomatic?: boolean;
}
interface IFillColorPickerState {
    currentColor: string;
    currentActive: boolean;
}
export declare class FillColorPicker extends React.PureComponent<IFillColorPickerProps, IFillColorPickerState> {
    constructor(props: IFillColorPickerProps);
    handleColorPick: (color: string) => void;
    handleCustomColorPick: (color: string) => void;
    handlePaletteClick: (e: any) => void;
    handleClickLeft: () => void;
    handleActive: (active: boolean) => void;
    getColorPalette: () => React.ReactElement;
    render(): JSX.Element;
}
export {};
