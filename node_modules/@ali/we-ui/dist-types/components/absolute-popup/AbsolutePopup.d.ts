import React, { Component } from 'react';
import { IAbsolutePopupProps, IAbsolutePopupInstance } from './interface';
declare class AbsolutePopup<P extends IAbsolutePopupProps = IAbsolutePopupProps> extends Component<P, P> {
    static defaultProps: {
        visible: boolean;
        offsetX: number;
        offsetY: number;
        overlayStyle: {};
    };
    state: Readonly<P> & Readonly<{
        children?: React.ReactNode;
    }>;
    /**
     * 创建实例
     */
    static create: <P_1 extends IAbsolutePopupProps = IAbsolutePopupProps>(props: P_1) => IAbsolutePopupInstance<P_1>;
    /**
     * 暴露给外面的 ref 进行状态修改
     *
     * @param {Partial<IAbsolutePopupProps>} state
     */
    handleChangeState(state: Pick<P, keyof P>): void;
    /**
     * Dropdown 显示/隐藏处理
     *
     * @param {boolean} e
     * @return {void}
     */
    handleVisibleChange: (e: boolean) => void;
    render(): JSX.Element;
}
export { AbsolutePopup };
