import React, { Component } from 'react';
import ResizeObserver from 'resize-observer-polyfill';
import { ToolBoxArray } from './ToolBox';
import { ToolLineArray } from './ToolLine';
export interface ISingleLineToolbarProps {
    /**
     * 是否启用自动折叠功能
     */
    autoFolding?: boolean;
    /**
     * 包裹的组件
     */
    children?: ToolLineArray;
    /**
     * toolbar wrapper 的行内样式
     */
    style?: React.CSSProperties;
    /**
     * 层级
     */
    zIndex?: number;
    /**
     * 对齐方式
     */
    alignType?: 'left' | 'center' | 'right';
    /**
     * 类名
     */
    className?: string;
    /**
     * 更多面板点击后自动关闭
     */
    autoCloseMorePanel?: boolean;
}
interface LineWidth {
    totalWidth: number;
    buttonWidths: number[];
}
interface IToolbarState {
    hiddenPivot: number;
    hiddenBtnPivot: number;
    groups: ToolLineArray;
    hiddenGroups: ToolLineArray;
    prevChildren: ToolLineArray | null;
    needCalculating: boolean;
    morePanelVisible: boolean;
}
export declare class SingleLineToolbar extends Component<ISingleLineToolbarProps, IToolbarState> {
    static defaultProps: {
        autoFolding: boolean;
        alignType: string;
    };
    resizeObserver: ResizeObserver;
    slotsWidthList: LineWidth[];
    refToolbar: React.RefObject<HTMLElement>;
    refToolContent: React.RefObject<HTMLElement>;
    refShow: React.RefObject<HTMLDivElement>;
    refMore: React.RefObject<HTMLElement>;
    recomputeLayout: {
        (...args: any[]): any;
        cancel(): void;
    };
    constructor(props: ISingleLineToolbarProps);
    static isNeedCalculating: (params: {
        prevGroups: ToolLineArray;
        prevHiddenGroups: ToolLineArray;
        groups: ToolLineArray;
        hiddenGroups: ToolLineArray;
    }) => boolean;
    static getDerivedStateFromProps: (props: ISingleLineToolbarProps, state: IToolbarState) => {
        groups: ToolLineArray;
        hiddenGroups: ToolLineArray;
        prevChildren: ToolLineArray | undefined;
        hiddenPivot: number;
        hiddenBtnPivot: number;
        needCalculating: boolean;
    } | {
        groups: ToolLineArray;
        hiddenGroups: ToolLineArray;
        prevChildren: ToolLineArray | undefined;
        needCalculating: boolean;
        hiddenPivot?: undefined;
        hiddenBtnPivot?: undefined;
    } | null;
    componentDidMount(): void;
    componentDidUpdate(prevProps: ISingleLineToolbarProps): void;
    componentWillUnmount(): void;
    mouseDownAtShow: (e: MouseEvent) => void;
    getLineWidth: (toolLine: HTMLElement) => LineWidth;
    /**
     * 重新计算宽度来布局
     */
    calculateWidth: (isFirst?: boolean) => void;
    getResizeContainer: () => HTMLElement;
    /**
   * resize回调事件
   */
    handleResize: () => void;
    handleClickMorePanel: (e: React.MouseEvent) => void;
    handleMorePanelVisibleChange: (e: boolean) => void;
    renderMore(splitHiddenBoxs: ToolBoxArray, hiddenSlots: React.ReactNodeArray): React.ReactNode;
    render(): JSX.Element;
}
export {};
