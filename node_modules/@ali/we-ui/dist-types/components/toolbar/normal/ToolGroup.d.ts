import React from 'react';
export interface IToolGroupProps {
    /**
    * 包裹的组件
    */
    children?: any;
    /**
     * 复杂布局，应用于单双行按钮混排的情况，默认为false
     */
    isComplexLayout?: boolean;
    /**
     * 工具栏的高度，isComplexLayout为true时需要设置高度
     */
    height?: number;
}
interface IState {
    groupWidth: number;
}
export declare class ToolGroup extends React.Component<IToolGroupProps, IState> {
    refToolGroup: React.RefObject<HTMLDivElement>;
    constructor(props: IToolGroupProps);
    componentDidMount(): void;
    componentDidUpdate(prevProps: IToolGroupProps, prevState: IState): void;
    calculateWidth: () => number;
    render(): JSX.Element;
}
export declare type ToolGroupArray = Array<React.ReactElement<IToolGroupProps>>;
export declare class HiddenToolGroup extends ToolGroup {
}
export {};
