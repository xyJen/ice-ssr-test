import React from 'react';
import { SingleLineToolbar } from "../toolbar";
interface IRibbonItem {
    key: string;
    name: string;
    toolbar: SingleLineToolbar;
}
export interface IRibbonProps {
    /**
     * 左侧面板,如文档标题
     */
    leftPanel?: JSX.Element;
    /**
     * 右侧面板,如编辑状态，协同人员
     */
    rightPanel?: JSX.Element;
    /**
     * 激活项的key，默认：undefined
     */
    activeKey?: string;
    /**
     * 默认激活项的key，默认：undefined，如果有activeKey，则忽略
     */
    defaultActiveKey?: string;
    /**
     * ribbon 项
     */
    ribbonItems: IRibbonItem[];
    /**
     * activeKey发生变化时的回调函数
     */
    onChange?: (activeKey: string) => void;
}
interface IRibbonState {
    innerActiveKey: string | undefined;
}
export declare class Ribbon extends React.PureComponent<IRibbonProps, IRibbonState> {
    constructor(props: IRibbonProps);
    handleChange: (nextActiveKey: string) => void;
    render(): JSX.Element;
}
export {};
