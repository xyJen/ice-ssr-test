import { useState, useCallback } from 'react';

var useControlledValue = function useControlledValue(propsValue, propsDefaultValue) {
  // 存进state中
  var _useState = useState(propsValue === undefined ? propsDefaultValue : propsValue),
      value = _useState[0],
      setValue = _useState[1]; // 如果是value不是undefined，说明受控


  var noop = useCallback(function () {}, []);

  if (propsValue !== undefined) {
    return [propsValue, noop];
  }

  return [value, setValue];
};

export default useControlledValue;