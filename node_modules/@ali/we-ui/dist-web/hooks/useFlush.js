import { useState, useCallback } from 'react';
export default (function () {
  var _useState = useState(-2),
      setRandom = _useState[1];

  var handleUpdate = useCallback(function () {
    setRandom(Math.random());
  }, []);
  return handleUpdate;
});