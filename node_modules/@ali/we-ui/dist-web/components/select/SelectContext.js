import React from 'react';
var executed = false;
var context; // 为了传人VT，需要写成函数的形式，但是应该是单例

export var createSelectContext = function createSelectContext() {
  if (!executed) {
    context = /*#__PURE__*/React.createContext({});
    executed = true;
  }

  return context;
};