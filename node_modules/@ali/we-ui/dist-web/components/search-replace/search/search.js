import React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import { Container, Cancel } from "./styled";
import { useTranslate } from "../../../locale/locales";
import { InputComponent } from "../input";
export var Search = function Search(props) {
  var text = props.text,
      search = props.search,
      cancel = props.cancel,
      visible = props.visible,
      className = props.className; // 国际化

  var t = useTranslate(props.local); // 是否显示

  var style = {
    display: visible ? 'flex' : 'none'
  };
  return /*#__PURE__*/_createElement(Container, {
    style: style,
    className: className
  }, /*#__PURE__*/_createElement(InputComponent, {
    text: text,
    onChange: search,
    placeholder: t('we_ui_find_and_replace_find')
  }), /*#__PURE__*/_createElement(Cancel, {
    onClick: cancel
  }, t('we_ui_cancel_text')));
};