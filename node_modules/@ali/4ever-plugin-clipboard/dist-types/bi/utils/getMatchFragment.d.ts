import { Controller, Block, Document } from '@ali/4ever-cangjie';
export declare const getExtendBlock: (doc: Document, block: Block | null) => Block | null;
/**
 * @description 继承粘贴时光标所在位置 startBlock的style及type
 * @param {Document} fragment - 粘贴要插入的内存模型对象
 * */
export default function getMatchFragment(controller: Controller, fragment: Document): Document;
