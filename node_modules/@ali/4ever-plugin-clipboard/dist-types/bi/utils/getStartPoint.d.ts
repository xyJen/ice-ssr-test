import { Controller } from '@ali/4ever-cangjie';
import { Point } from '../../utils/types';
/**
 * @description 获取切换粘贴模式前光标所在的point
 * */
export default function getStartPoint(controller: Controller): Point | undefined;
