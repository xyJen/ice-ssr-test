import { Document } from '@ali/4ever-cangjie';
export declare function isSection(block: any): boolean;
/**
 * 检测 fragment 是否仅包含一个 普通段落或标题
 * section可以看作paragraph、heading的超集
 * @param fragment
 */
export declare function isSingleSection(fragment: Document): boolean;
/**
 * 从 fragment 中把 single paragraph 解构出来
 * @param fragment
 */
export declare function unwrapSingleSection(fragment: Document): Document;
