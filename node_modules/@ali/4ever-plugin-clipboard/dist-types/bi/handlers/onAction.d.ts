import { Controller, Action } from '@ali/4ever-cangjie';
import { ClipboardPluginConfig } from '../../utils/types';
export default function createOnAction(config?: ClipboardPluginConfig): (action: Action, controller: Controller, next: Function) => any;
