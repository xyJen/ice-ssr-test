import { Document, Schema } from '@ali/4ever-cangjie';
import { ClipboardFilter, ModelsMap } from './types';
interface IFilterAndNormalizeProps {
    fragment: Document;
    schema: Schema;
    rules?: ClipboardFilter;
    models?: ModelsMap;
}
export default function filterAndNormalize(props: IFilterAndNormalizeProps): Document;
export {};
