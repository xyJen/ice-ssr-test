import { constants } from '@ali/4ever-cangjie';
var CUSTOM_MIME_TYPES = constants.CUSTOM_MIME_TYPES;
var customTypes = new Set(Object.values(CUSTOM_MIME_TYPES));
/**
 * @description 获取剪切板上自定义的 mime type 数据
 */

export default function getAllCustomClipboardData(dataTransfer) {
  if (!(dataTransfer != null && dataTransfer.types)) {
    return [];
  }

  var mimeTypes = dataTransfer.types;
  return mimeTypes.filter(function (t) {
    return customTypes.has(t);
  }).map(function (type) {
    return [type, dataTransfer == null ? void 0 : dataTransfer.getData(type)];
  });
}
//# sourceMappingURL=getAllCustomClipboardData.js.map