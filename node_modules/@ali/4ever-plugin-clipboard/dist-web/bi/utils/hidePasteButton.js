import _extends from "@babel/runtime/helpers/extends";
import { PASTE_DECORATION } from "../../utils/constants";
export default function hidePasteButton(controller) {
  var _controller$value = controller.value,
      data = _controller$value.data,
      decorations = _controller$value.decorations;
  var _clipboardPanel = data._clipboardPanel;
  var newDecorations = decorations.filter(function (item) {
    return item.mark.type !== PASTE_DECORATION;
  });

  if (_clipboardPanel) {
    controller.setData(_extends({}, data, {
      _clipboardPanel: ''
    })).setDecorations(newDecorations);
  }
}
//# sourceMappingURL=hidePasteButton.js.map