import { isSelectionAtSameBlock, isAtStartOfParagraph, isAtEndOfParagraph } from '@ali/4ever-utils';
import { isSingleSection, unwrapSingleSection } from "./isParagraph";
export function dealWithFragmentOnCopy(props) {
  var controller = props.controller,
      fragment = props.fragment;

  if (isSelectionAtSameBlock(controller) && isSingleSection(fragment) && !(isAtStartOfParagraph(controller) && isAtEndOfParagraph(controller)) && !controller.value.data.selectedCallout) {
    /**
     * 当选区在同一段落，但未选中整个段落时，筛除掉多余的数据。
     * 如：选中部分列表，复制出来只是选中部分的文字，选中整个列表，复制出来是个列表项
     * */
    return unwrapSingleSection(fragment);
  }

  return fragment;
}
//# sourceMappingURL=dealWithFragmentOnCopy.js.map