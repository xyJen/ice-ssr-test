import { ClipboardErrorEnum } from "../../utils/constants";
export default function alarmTimeoutMonitor(monitor, execFn, timeout) {
  if (timeout === void 0) {
    timeout = 10000;
  }

  var startTime = new Date().getTime();
  var info = execFn();
  var endTime = new Date().getTime();

  if (endTime - startTime > timeout && monitor) {
    var error = new Error(); // @ts-ignore

    error.type = ClipboardErrorEnum.PASTE_TIME_OUT;
    error.message = info;
    monitor(error);
  }
}
//# sourceMappingURL=alarmTimeoutMonitor.js.map