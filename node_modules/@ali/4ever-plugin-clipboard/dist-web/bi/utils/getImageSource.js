import { Text } from '@ali/4ever-cangjie';
import { Image } from '@ali/4ever-plugin-image';
export default function getImageSource(fragment) {
  var _fragment$nodes;

  if ((fragment == null ? void 0 : (_fragment$nodes = fragment.nodes) == null ? void 0 : _fragment$nodes.length) !== 1) {
    return null;
  }

  var block = fragment.nodes[0];

  if (block.nodes.length === 3 && Text.isEmptyText(block.getFirstNode()) && Text.isEmptyText(block.getLastNode()) && Image.isImage(block.nodes[1])) {
    return block.nodes[1].data.src;
  }

  if (block.nodes.length === 1 && Image.isImage(block.nodes[0])) {
    return block.nodes[0].data.src;
  }

  return null;
}
//# sourceMappingURL=getImageSource.js.map