import { Text } from '@ali/4ever-cangjie';
import { isEmpty } from 'lodash-es';
export default function isEmptyBlock(block) {
  return isEmpty(block.data) && block.nodes.every(function (node) {
    return Text.isText(node) && !node.text;
  });
}
//# sourceMappingURL=isEmptyBlock.js.map