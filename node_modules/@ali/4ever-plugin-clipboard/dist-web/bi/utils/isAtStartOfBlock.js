import { Block } from '@ali/4ever-cangjie';
export default function isAtStartOfBlock(anchor, controller) {
  var document = controller.value.document;
  var parent = document.getParent(anchor.key);
  return Block.isBlock(parent) && !parent.data.list && anchor.isAtStartOfNode(parent);
}
//# sourceMappingURL=isAtStartOfBlock.js.map