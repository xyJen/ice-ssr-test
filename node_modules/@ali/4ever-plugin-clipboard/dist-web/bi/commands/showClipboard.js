import _extends from "@babel/runtime/helpers/extends";
import { Decoration, Mark } from '@ali/4ever-cangjie';
import { setDecorations } from "../actions";
import { PASTE_DECORATION } from "../../utils/constants";
export function showClipboard(controller, selection) {
  var _ref = selection || controller.value.selection,
      anchor = _ref.anchor,
      focus = _ref.focus;

  if (anchor.isTextPoint() && focus.isTextPoint()) {
    controller.setData(_extends({}, controller.value.data, {
      _clipboardPanel: "show-" + new Date().getTime()
    })).run('onAction', setDecorations(Decoration.create({
      start: anchor,
      end: focus,
      mark: Mark.fromJSON({
        type: PASTE_DECORATION
      })
    })));
  }

  return controller;
}
//# sourceMappingURL=showClipboard.js.map