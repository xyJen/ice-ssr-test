import { PASTE_DECORATION } from "../../utils/constants";
export default function setDecorations(controller, dec) {
  var decorations = controller.value.decorations.filter(function (item) {
    return item.mark.type !== PASTE_DECORATION;
  });
  decorations.push(dec);
  return controller.setDecorations(decorations);
}
//# sourceMappingURL=setDecorations.js.map