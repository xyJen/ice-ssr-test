import hideCustomPasteButton from "../utils/hideCustomPasteButton";
import { isKeyHotkey } from 'is-hotkey';

var isMoveHotKey = function isMoveHotKey(event) {
  return ['up', 'down', 'enter'].map(function (key) {
    return isKeyHotkey(key);
  }).some(function (isEvent) {
    return isEvent(event);
  });
};

export default function onKeyDown(event, controller, next) {
  var customPanel = controller.value.data.customPanel;

  if (customPanel && isMoveHotKey(event)) {
    return event.preventDefault();
  }

  hideCustomPasteButton(controller);
  return next();
}
//# sourceMappingURL=onKeyDown.js.map