import { message } from '@ali/we-design';
import { environment } from '@ali/4ever-cangjie';
var MOD = environment.IS_MAC ? '⌘' : 'Ctrl';
export default function onCangjiePasteFail(locale) {
  if (locale != null && locale.notSupport && locale.shortcutKey) {
    var pasteKey = " " + MOD + "+V ";
    message.info(locale.notSupport + pasteKey + locale.shortcutKey);
  }
}
//# sourceMappingURL=onCangjiePasteFail.js.map