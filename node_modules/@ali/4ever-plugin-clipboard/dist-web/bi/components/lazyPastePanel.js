import React from 'react';
var _createElement = /*#__PURE__*/React.createElement;
import Loadable from 'react-loadable';
import usePreviewCopy from "../../common/hooks/usePreviewCopy";
import { hideCustomPastePanel } from "../actions";
var PastePanel = Loadable({
  loader: function loader() {
    return import(
    /* webpackChunkName: "clipboard-pastepanel" */
    "./pastePanel");
  },
  loading: function loading() {
    return null;
  }
});
export default function LazyPastePanel(props) {
  var controller = props.controller,
      config = props.config;

  var _controller$value$dat = controller.value.data._clipboardPanel,
      _clipboardPanel = _controller$value$dat === void 0 ? '' : _controller$value$dat;

  usePreviewCopy(controller);
  React.useEffect(function () {
    if (!(config != null && config.pastePanel) && _clipboardPanel) {
      controller.run('onAction', hideCustomPastePanel());
    }
  });

  if (!(config != null && config.pastePanel) || !_clipboardPanel) {
    return null;
  }

  return /*#__PURE__*/_createElement(PastePanel, props);
}
//# sourceMappingURL=lazyPastePanel.js.map