import { default as copyNodesUtils } from "../../utils/copyNodes";
export function copyNodes(controller, maybeNodes) {
  if (maybeNodes === void 0) {
    maybeNodes = [];
  }

  return copyNodesUtils(maybeNodes, controller.serializer);
}
//# sourceMappingURL=copyNodes.js.map