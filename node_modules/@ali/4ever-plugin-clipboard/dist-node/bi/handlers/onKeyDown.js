"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = onKeyDown;

var _hideCustomPasteButton = _interopRequireDefault(require("../utils/hideCustomPasteButton"));

var _isHotkey = require("is-hotkey");

const isMoveHotKey = event => ['up', 'down', 'enter'].map(key => (0, _isHotkey.isKeyHotkey)(key)).some(isEvent => isEvent(event));

function onKeyDown(event, controller, next) {
  const {
    customPanel
  } = controller.value.data;

  if (customPanel && isMoveHotKey(event)) {
    return event.preventDefault();
  }

  (0, _hideCustomPasteButton.default)(controller);
  return next();
}
//# sourceMappingURL=onKeyDown.js.map