"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.showClipboard = showClipboard;

var _everCangjie = require("@ali/4ever-cangjie");

var _actions = require("../actions");

var _constants = require("../../utils/constants");

function showClipboard(controller, selection) {
  const {
    anchor,
    focus
  } = selection || controller.value.selection;

  if (anchor.isTextPoint() && focus.isTextPoint()) {
    controller.setData({ ...controller.value.data,
      _clipboardPanel: `show-${new Date().getTime()}`
    }).run('onAction', (0, _actions.setDecorations)(_everCangjie.Decoration.create({
      start: anchor,
      end: focus,
      mark: _everCangjie.Mark.fromJSON({
        type: _constants.PASTE_DECORATION
      })
    })));
  }

  return controller;
}
//# sourceMappingURL=showClipboard.js.map