"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getAllCustomClipboardData;

var _everCangjie = require("@ali/4ever-cangjie");

const {
  CUSTOM_MIME_TYPES
} = _everCangjie.constants;
const customTypes = new Set(Object.values(CUSTOM_MIME_TYPES));
/**
 * @description 获取剪切板上自定义的 mime type 数据
 */

function getAllCustomClipboardData(dataTransfer) {
  if (!dataTransfer?.types) {
    return [];
  }

  const mimeTypes = dataTransfer.types;
  return mimeTypes.filter(t => customTypes.has(t)).map(type => {
    return [type, dataTransfer?.getData(type)];
  });
}
//# sourceMappingURL=getAllCustomClipboardData.js.map