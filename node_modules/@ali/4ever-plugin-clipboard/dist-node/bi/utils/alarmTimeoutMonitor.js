"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = alarmTimeoutMonitor;

var _constants = require("../../utils/constants");

function alarmTimeoutMonitor(monitor, execFn, timeout = 10000) {
  const startTime = new Date().getTime();
  const info = execFn();
  const endTime = new Date().getTime();

  if (endTime - startTime > timeout && monitor) {
    const error = new Error(); // @ts-ignore

    error.type = _constants.ClipboardErrorEnum.PASTE_TIME_OUT;
    error.message = info;
    monitor(error);
  }
}
//# sourceMappingURL=alarmTimeoutMonitor.js.map