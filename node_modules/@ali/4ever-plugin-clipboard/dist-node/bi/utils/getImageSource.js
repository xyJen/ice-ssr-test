"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getImageSource;

var _everCangjie = require("@ali/4ever-cangjie");

var _everPluginImage = require("@ali/4ever-plugin-image");

function getImageSource(fragment) {
  if (fragment?.nodes?.length !== 1) {
    return null;
  }

  const block = fragment.nodes[0];

  if (block.nodes.length === 3 && _everCangjie.Text.isEmptyText(block.getFirstNode()) && _everCangjie.Text.isEmptyText(block.getLastNode()) && _everPluginImage.Image.isImage(block.nodes[1])) {
    return block.nodes[1].data.src;
  }

  if (block.nodes.length === 1 && _everPluginImage.Image.isImage(block.nodes[0])) {
    return block.nodes[0].data.src;
  }

  return null;
}
//# sourceMappingURL=getImageSource.js.map