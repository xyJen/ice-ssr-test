"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = hidePasteButton;

var _constants = require("../../utils/constants");

function hidePasteButton(controller) {
  const {
    data,
    decorations
  } = controller.value;
  const {
    _clipboardPanel
  } = data;
  const newDecorations = decorations.filter(item => item.mark.type !== _constants.PASTE_DECORATION);

  if (_clipboardPanel) {
    controller.setData({ ...data,
      _clipboardPanel: ''
    }).setDecorations(newDecorations);
  }
}
//# sourceMappingURL=hidePasteButton.js.map