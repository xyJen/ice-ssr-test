"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = hideCustomPasteButton;

var _constants = require("../../utils/constants");

function hideCustomPasteButton(controller) {
  const {
    data,
    decorations
  } = controller.value;
  const {
    _clipboardPanel,
    customPanel
  } = data;
  const newDecorations = decorations.filter(item => item.mark.type !== _constants.PASTE_DECORATION);

  if (customPanel || _clipboardPanel) {
    controller.setData({ ...data,
      _clipboardPanel: '',
      customPanel: null
    }).setDecorations(newDecorations);
  }
}
//# sourceMappingURL=hideCustomPasteButton.js.map