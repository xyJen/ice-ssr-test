import { Block, Value, Node } from '@ali/4ever-cangjie';
import { RefBlockRole } from './roles';
import RefBlock from '../mo/models';
/**
 * 获得引用块拖拽项目
 * @param options
 * @returns
 */
export declare function getRefBlockDragMenuItems(options: {
    block: Block;
    locale: Record<string, string>;
    value: Value;
    disableNodeInRefBlock: (node: Node) => boolean;
    /** 复制为引用块 */
    onCopyAsRefBlock: () => void;
    /** 复制引用块 */
    onCopyRefBlock: (block: RefBlock) => void;
}): {
    onClick: () => void;
    type: symbol;
    title: string;
    icon: JSX.Element;
    role: typeof RefBlockRole;
    menuExtraKey: string;
}[];
export declare type RefBlockDraggableConfig = Record<string, any>;
