import { Value, Node, Element } from '@ali/4ever-cangjie';
import RefBlock from '../mo/models';
/**
 * 当前选区是否位于引用块内
 * @param controller
 * @returns
 */
export declare function isSelectionInRefBlock(value: Value): boolean;
/**
 * 获得距离选区最近的引用块
 * @param value
 * @returns
 */
export declare function getClosestRefBlockInSelection(value: Value, container?: Element): Element<import("@ali/4ever-cangjie/src/interfaces/data").default, Node[]> | null;
/**
 * 判断 refblock 是否只读
 * @param controller
 * @param refblock
 */
export declare const isReadOnly: (value: Value, refblock: RefBlock) => boolean;
/**
 * 判断节点是否存在于只读的引用块
 * @param controller
 * @param node
 */
export declare function isInReadOnlyRefBlock(value: Value, node: Node): boolean;
/**
 * 判断节点是否在引用块中
 * @param value
 * @param node
 * @returns
 */
export declare function isInRefBlock(value: Value, node: Node): boolean;
/**
 * 是否是河图同步节点
 * @param node
 * @returns
 */
export declare function isSyncHetuNode(node: Node): boolean;
