import { Document, Value } from '@ali/4ever-cangjie';
export declare type RefBlockMap = Record<string, Array<{
    nodeKey: string;
    uuid?: string;
}>>;
/**
 * 获得引用块的 key 映射表 { docKey: [{ nodeKey, uuid }] }
 *
 * ## Example
 *
 * ```tsx
 * const document = (
 *   <document>
 *     <paragraph key="0">Introduction</paragraph>
 *     <refblock docKey="doc-a" uuid="a-0" key="1">
 *       <paragraph>Cangjie Editor</paragraph>
 *     </refblock>
 *     <refblock docKey="doc-a" uuid="a-1" key="2">
 *       <paragraph>Cangjie Editor</paragraph>
 *     </refblock>
 *     <refblock docKey="doc-b" uuid="b-0" key="3">
 *       <paragraph>Commands</paragraph>
 *     </refblock>
 *   </document>
 * );
 *
 * const refblockMap = getRefBlockMap(document);
 *
 * // refblock map is:
 * // {
 * //   'doc-a': [{nodeKey: '1', uuid: 'a-0'}, {nodeKey: '2', uuid: 'a-1'}],
 * //   'doc-b': [{nodeKey: '3', uuid: 'b-0'}]
 * // }
 * ```
 * @param document
 * @returns
 */
export declare function getRefBlockMap(document: Document): RefBlockMap;
/**
 * 根据 node key 刷新引用块
 *
 * ## Example
 *
 * ```tsx
 * const value = (
 *   <value>
 *     <document>
 *       <paragraph key="0">Introduction</paragraph>
 *       <refblock docKey="doc-a" key="1"/>
 *     </document>
 *   </value>
 * );
 *
 * const docA = (
 *   <document>
 *     <paragraph>JavaScript</paragraph>
 *     <code lang="javascript">const a = 1;</code>
 *   </document>
 * );
 *
 * const newValue = refreshRefBlockByKey(document, '1', docA);
 * // newValue is:
 * // (
 * //   <value>
 * //     <document>
 * //       <paragraph key="0">Introduction</paragraph>
 * //       <refblock docKey="doc-a" key="1">
 * //         <paragraph>JavaScript</paragraph>
 * //         <code lang="javascript">const a = 1;</code>
 * //       </refblock>
 * //     </document>
 * //   </value>
 * // )
 * ```
 * @param document
 * @param nodeKey
 * @param refblockDocument
 */
export declare function refreshRefBlockByKey(value: Value, nodeKey: string, refblockDocument: Document, uuidKey?: string): Value;
/**
 * 根据 doc key 刷新引用块
 *
 * ## Example
 *
 * ```tsx
 * const value = (
 *   <value>
 *     <document>
 *       <paragraph key="0">Introduction</paragraph>
 *       <refblock docKey="doc-a"/>
 *       <refblock docKey="doc-a"/>
 *     </document>
 *   </value>
 * );
 *
 * const docA = (
 *   <document>
 *     <paragraph>JavaScript</paragraph>
 *     <code lang="javascript">const a = 1;</code>
 *   </document>
 * );
 *
 * const newValue = refreshRefBlockByDocKey(document, 'doc-a', docA);
 * // newValue is:
 * // (
 * //   <value>
 * //     <document>
 * //       <paragraph key="0">Introduction</paragraph>
 * //       <refblock docKey="doc-a">
 * //         <paragraph>JavaScript</paragraph>
 * //         <code lang="javascript">const a = 1;</code>
 * //       </refblock>
 * //       <refblock docKey="doc-a">
 * //         <paragraph>JavaScript</paragraph>
 * //         <code lang="javascript">const a = 1;</code>
 * //       </refblock>
 * //     </document>
 * //   </value>
 * // )
 * ```
 * @param document
 * @param nodeKey
 * @param refblockDocument
 */
export declare function refreshRefBlockByDocKey(value: Value, docKey: string, refblockDocument: Document): Value;
