import { Controller, Action, Mo as Serializer } from '@ali/4ever-cangjie';
import type { BiConfig } from '../../utils/types';
export default function createOnAction(config: BiConfig, serializer?: Serializer): (action: Action, controller: Controller, next: () => void) => void | Controller;
