import { Controller, RenderNodeProps } from '@ali/4ever-cangjie';
import type { BiConfig } from '../../utils/types';
export default function createRenderNode(config: BiConfig): {
    [x: string]: (props: RenderNodeProps, controller: Controller) => JSX.Element;
};
