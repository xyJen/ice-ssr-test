import { Block, Controller } from '@ali/4ever-cangjie';
import { RefBlockData } from '../../mo/models';
/**
 * 将选中的 blocks 升级为 refblock
 * @param controller
 * @param blocks
 * @param docKey
 * @returns
 */
export declare function upgradeToRefBlock(controller: Controller, blocks: Block[], data: RefBlockData): Controller;
