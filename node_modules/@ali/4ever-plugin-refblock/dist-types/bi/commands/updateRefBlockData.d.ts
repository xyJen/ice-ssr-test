import { Controller } from '@ali/4ever-cangjie';
import RefBlock, { RefBlockData } from '../../mo/models';
/**
 * 更新引用块数据
 * @param controller
 * @param docKey
 * @returns
 */
export declare function updateRefBlockData(controller: Controller, node: RefBlock, data: Partial<RefBlockData>): Controller;
