import { Controller } from '@ali/4ever-cangjie';
import { RefBlockData } from '../../mo/models';
/**
 * 插入引用块
 * @param controller
 * @param data
 * @returns
 */
export declare function insertRefBlock(controller: Controller, data: RefBlockData): Controller;
