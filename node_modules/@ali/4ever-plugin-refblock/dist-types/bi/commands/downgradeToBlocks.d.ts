import { Controller } from '@ali/4ever-cangjie';
/**
 * 降级为普通块
 * @param controller
 * @param docKey
 */
export declare function downgradeToBlocksByDocKey(controller: Controller, docKey: string): Controller;
/**
 * 降级对应节点 key 的块为普通块
 * @param controller
 * @param key
 */
export declare function downgradeToBlocksByKey(controller: Controller, key: string): Controller;
