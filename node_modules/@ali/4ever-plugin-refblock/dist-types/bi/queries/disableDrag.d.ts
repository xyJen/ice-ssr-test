import { Controller, Node } from '@ali/4ever-cangjie';
/**
 * 如果块存在于一个只读引用块中，则不允许拖动
 * @param controller
 * @param node
 * @returns
 */
export declare function disableDrag(controller: Controller, node: Node): boolean;
