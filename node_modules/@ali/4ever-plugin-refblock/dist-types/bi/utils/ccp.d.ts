import { Document, Mo as Serializer } from '@ali/4ever-cangjie';
import RefBlock from '../../mo/models';
/**
 * 将引用块数据写入剪贴板
 * @param refblock
 * @param serializer
 */
export declare function writeRefBlockToClipboard(refblock: RefBlock, cacheHetuWhenCopy: (fragment: Document) => string, getRefBlockURL: (uuid: string) => string, serializer?: Serializer): void;
/**
 * 裁剪 Fragment 中的引用块内容：
 *
 * - uuid: 保证粘贴时生成新的引用关系
 * - children: 裁剪掉引用块的内容，保证引用块内容只由协同服务供给
 * @param fragment
 * @returns
 */
export declare function cropRefBlockInFragment(fragment: Document): Document;
/**
 * 剪贴板包含引用块时：
 * 1. 若引用块被设置标志位，代表用户明确意图复制引用块，需要保留引用块
 * 2. 否则，只保留引用块内内容
 * @param fragment
 */
export declare function unwrapRefBlock(fragment: Document): Document;
