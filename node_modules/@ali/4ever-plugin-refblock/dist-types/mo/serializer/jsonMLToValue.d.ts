import { Block, JsonML, Node } from '@ali/4ever-cangjie';
declare const _default: {
    name: string;
    match(node: JsonML): boolean;
    convert(node: JsonML): Block;
    decorate(node: Node): Node;
};
export default _default;
