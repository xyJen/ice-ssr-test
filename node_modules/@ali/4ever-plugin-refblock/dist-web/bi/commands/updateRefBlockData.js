import _extends from "@babel/runtime/helpers/extends";
import { Commands } from '@ali/4ever-cangjie';

/**
 * 更新引用块数据
 * @param controller
 * @param docKey
 * @returns
 */
export function updateRefBlockData(controller, node, data) {
  return controller.command(Commands.setNodeByKey, node.key, {
    data: _extends({}, node.data, data)
  });
}
//# sourceMappingURL=updateRefBlockData.js.map