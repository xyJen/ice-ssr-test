import RefBlock from "../../mo/models";
import { isSyncHetuNode } from "../../utils/logic";
export default function createDisableNodeInRefBlock(disableNestedInRefBlock) {
  /**
   * 禁止某种节点存在于引用块
   * @param controller
   * @param node
   * @returns
   */
  return function disableNodeInRefBlock(controller, node) {
    return RefBlock.isRefBlock(node) || isSyncHetuNode(node) || disableNestedInRefBlock(node, controller);
  };
}
//# sourceMappingURL=disableNodeInRefBlock.js.map