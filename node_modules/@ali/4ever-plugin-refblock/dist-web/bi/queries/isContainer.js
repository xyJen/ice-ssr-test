import { Document } from '@ali/4ever-cangjie';
import RefBlock from "../../mo/models";
export var isContainer = function isContainer(controller, node) {
  // 避免拷贝只含一个 void child 的引用块，粘贴时引用块丢失，只留下 void child
  if (Document.isDocument(node) && node.nodes.length === 1 && RefBlock.isRefBlock(node.nodes[0])) {
    return true;
  }

  return undefined;
};
//# sourceMappingURL=isContainer.js.map