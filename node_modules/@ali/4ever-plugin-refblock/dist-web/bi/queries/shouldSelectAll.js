import { getClosestRefBlockInSelection } from "../../utils/logic";
export function shouldSelectAll(controller, container) {
  var refblock = getClosestRefBlockInSelection(controller.value, container);
  return refblock ? true : undefined;
}
//# sourceMappingURL=shouldSelectAll.js.map